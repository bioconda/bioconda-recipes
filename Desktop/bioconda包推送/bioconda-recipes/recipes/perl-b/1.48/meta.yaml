{% set name = "perl-b" %}
{% set version = "1.48" %}
{% set sha256 = "" %}

package:
  name: {{ name }}
  version: {{ version }}

build:
  noarch: generic
  number: 2

requirements:
  build:
    - perl

  run:
    - perl

about:
  home: http://metacpan.org/pod/B
  license: perl_5
  summary: 'The Perl Compiler Backend'
