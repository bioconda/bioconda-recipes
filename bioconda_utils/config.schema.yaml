type: object
properties:
    docker_url:
        type: string
    env_matrix:
        type: string
    blacklists:
        type: array
    docker_image:
        type: string
    upload_channel:
        type: string
    channels:
        type: array
