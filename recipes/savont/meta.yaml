{% set version = "0.5.1" %}

package:
  name: savont
  version: {{ version }}

source:
  url: https://github.com/bluenote-1577/savont/archive/v{{ version }}.tar.gz
  sha256: 98b91cf712dfeed2d9d5691a7f63d06f16b2585551a067a269dbcca0ec00d9a2

build:
  number: 0
  run_exports:
    - {{ pin_subpackage('savont', max_pin="x.x") }}

requirements:
  build:
    - {{ compiler("c") }}
    - {{ stdlib("c") }}
    - {{ compiler("cxx") }}
    - {{ compiler("rust") }}
    - make
    - cmake >=3.12
    - cargo-bundle-licenses
    - pkg-config

test:
  commands:
    - savont --help

about:
  home: https://github.com/bluenote-1577/savont
  license: MIT
  license_family: MIT
  summary: "Amplicon sequencing variants (ASVs) and taxonomic profiling from modern long-read (nanopore + PacBio) amplicon sequencing with > 98% accuracy."
  license_file: LICENSE
  dev_url: https://github.com/bluenote-1577/savont

extra:
  additional-platforms:
    - linux-aarch64
    - osx-arm64
  recipe-maintainers:
    - bluenote-1577
