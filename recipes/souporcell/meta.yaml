{% set name = "souporcell" %}
{% set version = "2.5" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/wheaton5/souporcell/archive/refs/tags/v2.5.tar.gz
  sha256: 1404b87e44c4452b2f329abb81ce4a8383ed5b7e94f38ff2815c5bed62ec2c7f

build:
  number: 0
  run_exports:
    - {{ pin_subpackage('souporcell', max_pin="x.x.x") }}

requirements:
  build:
    - rust
    - cmake
    - make
    - autoconf
    - {{ compiler('cxx') }}
    - pkg-config
  host:
    - python =3.9
    - pip
  run:
    - python =3.9
    - pysam 
    - pystan <3
    - pyvcf 
    - numpy 
    - pyfaidx 
    - setuptools <58
    - scipy 
    - bcftools >=1.9
    - bedtools >=2.28
    - freebayes >=1.3
    - htslib >=1.9
    - hisat2 
    - minimap2 >=2.26
    - samtools >=1.9
    #- vatrix 
    
test:
  commands:
    - souporcell_pipeline.py -h

extra:
  skip-lints:
    - uses_setuptools  # uses pkg_resoures during run time
    
about:
  home: https://github.com/wheaton5/souporcell
  license: MIT
  summary: Clustering scRNAseq by genotypes.
