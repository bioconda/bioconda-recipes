diff --git a/src/Makefile b/src/Makefile
index aed70a3..2918392 100644
--- a/src/Makefile
+++ b/src/Makefile
@@ -1,4 +1,4 @@
-CC=             gcc
+CC=             $(CC)
 CFLAGS=         -O3 -fomit-frame-pointer -static
 #CFLAGS=         -O3 -g -D_DEBUG
 DFLAGS= 
@@ -32,11 +32,11 @@ endif
 INCLUDES=	-Iinc
 SUBDIRS=        . 
 LIBPATH=	
-LIBS=      	-pthread -lm -lrt -lbam -lz -L./inc
+LIBS=      	-pthread -lm -lrt -lbam -lz
 EXTRA_FLAGS=
 
 BIT_ERR = 0
-ifeq (,$(findstring $(shell uname -m), x86_64 ppc64 ia64))
+ifeq (,$(findstring $(shell uname -m), x86_64 ppc64 ia64 aarch64 arm64))
 BIT_ERR = 1
 endif
 
