# sdsl-lite doesn't build with Clang-20
cxx_compiler_version:
  - 18  # [osx]
c_compiler_version:
  - 18  # [osx]
# metagraph requires std::filesystem, which needs macOS >= 10.15
c_stdlib_version:  # [osx and x86_64]
  - "10.15"        # [osx and x86_64]
