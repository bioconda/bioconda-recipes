--- a/metagraph/CMakeLists.txt
+++ b/metagraph/CMakeLists.txt
@@ -341,6 +341,9 @@ IF(APPLE)
     set(CMAKE_USE_PTHREADS_INIT 1)
     set(THREADS_PREFER_PTHREAD_FLAG ON)
 ENDIF()
+# Add zlib include directory before sshash is added so it can find zlib.h
+# Use conda's zlib package headers via CMAKE_PREFIX_PATH
+include_directories($ENV{PREFIX}/include)
 add_subdirectory(external-libraries/spdlog)
 add_subdirectory(external-libraries/sshash SYSTEM)
 add_subdirectory(external-libraries/DYNAMIC)
