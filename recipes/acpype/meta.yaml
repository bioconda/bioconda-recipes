package:
  name: acpype
  version: "0.1.0"

source:
  fn: acpype-0.1.0.tar.gz
  url: https://pypi.python.org/packages/3e/ad/080b41fffc989d5eb77aab59491db9a3f968b0a5a878f09ed2b094ce24bb/acpype-0.1.0.tar.gz
  md5: f46194b5c588b04bfa22131413bd3ec2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # noarch: python
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - acpype = acpype:main
    #
    # Would create an entry point called acpype that calls acpype.main()

    - acpype = acpype.scripts.acpype:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - acpype

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - acpype --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/llazzaro/acpype
  license: MIT License
  summary: 'Python Boilerplate contains all the boilerplate you need to create a Python package.'
  license_family: MIT

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
