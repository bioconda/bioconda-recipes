{% set version = "3.0.7" %}

package:
  name: variant_tools
  version: {{ version }}

source:
  url: https://pypi.python.org/packages/source/v/variant_tools/variant_tools-{{ version }}.tar.gz
  sha256: 6ded750e88c3e27c52c5bf23ebbfd3c40fc9d89b381824b26204827cf1861e48

build:
  number: 0
  script: {{ PYTHON }} -m pip install . --ignore-installed --no-deps -vv

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
  host:
    - python
    - cython
    - numpy
    - boost-cpp
    - gsl
  run:
    - python
    - numpy
    - scipy
    - pyzmq
    - pytables
    - pycurl
    - gsl
    - boost-cpp

test:
  # Python imports
  imports:
    - variant_tools

  commands:
    - vtools -h
    - vtools_report -h


about:
  home: https://github.com/vatlab/varianttools
  license: GNU General Public License (GPL)
  summary: 'Integrated annotation and analysis of next gen sequencing data'
