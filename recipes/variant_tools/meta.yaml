package:
  name: variant_tools
  version: !!str 2.7.2

source:
  fn: variant_tools-2.7.2.tar.gz
  url: https://pypi.python.org/packages/source/v/variant_tools/variant_tools-2.7.2.tar.gz

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simupop = simupop:main


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - variant_tools

  commands:
    - vtools

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/vatlab/varianttools
  license: GNU General Public License (GPL)
  summary: 'Integrated annotation and analysis of next gen sequencing data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
