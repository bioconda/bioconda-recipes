# This file created by conda-build 2.0.7
# meta.yaml template originally from:
# /opt/recipe, last modified Sun Oct 30 01:06:11 2016
# ------------------------------------------------

package:
    name: bioconductor-heatplus
    version: 2.21.0
source:
    fn: Heatplus_2.21.0.tar.gz
    url: https://bioarchive.galaxyproject.org/Heatplus_2.21.0.tar.gz


build:
    number: '0'
    rpaths:
        - lib/R/lib/
        - lib/
requirements:
    build:
        - htslib
        - r
        - r-essentials
        - gcc
        - bioconductor-rhtslib
        - bioconductor-iranges
        - bioconductor-genomicranges
        - bioconductor-summarizedexperiment
        - bioconductor-biostrings
        - bioconductor-variantannotation
        - r-vgam

    run:
        - htslib
        - r
        - r-essentials
        - bioconductor-rhtslib
        - bioconductor-iranges
        - bioconductor-genomicranges
        - bioconductor-summarizedexperiment
        - bioconductor-biostrings
        - bioconductor-variantannotation
        - r-vgam
test:
    commands:
        - $R -e "library('Heatplus')"
about:
    home: http://bioconductor.org/packages/release/bioc/html/Heatplus.html
    license: GPL (>= 2)
    summary: "Display a rectangular heatmap (intensity plot) of a data matrix. By default, both samples (columns) and features (row) of the matrix are sorted according to a hierarchical clustering, and the corresponding dendrogram is plotted. Optionally, panels with additional information about samples and features can be added to the plot."
