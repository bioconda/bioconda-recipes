{% set name = "rilseq" %}
{% set version = "0.70" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: "https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/RILseq-{{ version }}.tar.gz"
  sha256: d926844b47df23e1e614c53a059c9291e95a1f56ff640ef95b8053afd31ed472

build:
  number: 0
  noarch: python
  script: "{{ PYTHON }} -m pip install . -vv"

requirements:
  host:
    - biopython
    - numpy
    - pip
    - pysam >=0.14.1
    - python
    - scipy
    - bwa>=0.7.12
    - samtools>=1.2
  run:
    - biopython
    - numpy
    - pysam >=0.14.1
    - python
    - scipy
    - bwa>=0.7.12
    - samtools>=1.2

test:
  imports:
    - RILseq

about:
  home: "http://github.com/asafpr/RILseq"
  license: MIT
  license_family: MIT
  license_file: 
  summary: "Processing RILSeq experiments results"
  doc_url: 
  dev_url: 

extra:
  recipe-maintainers:
    - asafpr
