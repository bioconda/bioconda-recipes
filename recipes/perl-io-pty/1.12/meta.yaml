{% set name = "perl-io-pty" %}
{% set version = "1.12" %}
{% set sha256 = "a2ef8770d3309178203f8c8ac25e623e63cf76e97830fd3be280ade1a555290d" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  fn: IO-Tty-1.12.tar.gz
  url: https://cpan.metacpan.org/authors/id/T/TO/TODDR/IO-Tty-1.12.tar.gz
  sha256: {{ sha256 }}

# If this is a new build for the same version, increment the build
# number. If you do not include this key, it defaults to 0.
build:
  number: 0

requirements:
  build:
    - perl

  run:
    - perl

# test:
  # Perl 'use' tests
  # imports:

  # You can also put a file called run_test.pl (or run_test.py) in the recipe
  # that will be run at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://metacpan.org/pod/IO::Pty
  license: perl_5
  summary: 'Pseudo TTY object class'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
