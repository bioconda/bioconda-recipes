{% set name = "cgat-apps" %}
{% set version = "0.7.8" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/c/cgat/cgat-{{ version }}.tar.gz
  sha256: d87b7d43112eaed5e87b3ce0e83692d611ebaa4fd0ed3ce340bb6b770cbf9c82
  
build:
  number: 0
  run_exports:
    - {{ pin_subpackage('cgat-apps', max_pin="x.x") }}
  entry_points:
    - cgat = cgat.cgat:main

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
  host:
    - python >=3.10
    - pip
    - cython >=0.29.35
    - numpy
    - pysam
    - htslib
  run:
    - python >=3.10
    - cgatcore
    - alignlib-lite
    - biopython
    - matplotlib-base
    - numpy
    - pandas
    - pybedtools
    - pybigwig
    - pysam
    - htslib
    - pyyaml
    - quicksect
    - scipy
    - scikit-learn
    - sortedcontainers
    # Misc dependencies
    - bedtools
    - ucsc-bedgraphtobigwig
    - ucsc-wigtobigwig
    - coreutils
    - grep

test:
  imports:
    - cgat
  commands:
    - cgat --help
    - cgat --help Conversion
    - cgat gtf2table -h
    - find ${PREFIX} -iname "*bamtools.cpython-*-x86_64-linux-gnu.so*" -exec ldd {} \;
    - cgat bam2bed -h

about:
  home: "https://github.com/cgat-developers/cgat-apps"
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: "Computational Genomics Analysis Toolkit."
  dev_url: "https://github.com/cgat-developers/cgat-apps"
  doc_url: "https://cgat-apps.readthedocs.io/en/latest"
