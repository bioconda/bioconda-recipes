recipe_sha: 6e5ea74b249965b1ae392524e0f4354930e01d8765c35fda5df863a9a3aaa4f6  # The hash of the recipe's meta.yaml at which this recipe failed to build.
skiplist: false # Set to true to skiplist this recipe so that it will be ignored as long as its latest commit is the one given above.
log: |2-
         numpy 1.22.4, which can be installed;
       python >=3.8,<3.9.0a0 *, which can be installed;
     pybigwig 0.3.18 would require
       numpy >=1.20.0,<2.0a0 * with the potential options
         numpy [1.23.0|1.23.1|1.23.2|1.23.3|1.23.4], which can be installed;
         numpy [1.23.5|1.24.0|...|1.26.3], which can be installed;
         numpy 1.26.4, which can be installed;
         numpy 1.26.4, which can be installed (as previously explained);
         numpy 1.26.4, which can be installed (as previously explained);
         numpy [1.20.0|1.20.1|...|1.21.5], which can be installed;
         numpy [1.21.6|1.22.0|1.22.1|1.22.2|1.22.3], which can be installed;
         numpy 1.22.4, which can be installed;
       python >=3.8,<3.9.0a0 *, which can be installed;
     pybigwig [0.3.18|0.3.22] would require
       numpy >=1.21.6,<2.0a0 * with the potential options
         numpy [1.23.0|1.23.1|1.23.2|1.23.3|1.23.4], which can be installed;
         numpy [1.23.5|1.24.0|...|1.26.3], which can be installed;
         numpy 1.26.4, which can be installed;
         numpy 1.26.4, which can be installed (as previously explained);
         numpy 1.26.4, which can be installed (as previously explained);
         numpy [1.21.6|1.22.0|1.22.1|1.22.2|1.22.3], which can be installed;
         numpy 1.22.4, which can be installed;
       python >=3.8,<3.9.0a0 *, which can be installed;
     pybigwig [0.3.18|0.3.22] would require
       numpy >=1.19.5,<2.0a0 * with the potential options
         numpy [1.23.0|1.23.1|1.23.2|1.23.3|1.23.4], which can be installed;
         numpy [1.23.5|1.24.0|...|1.26.3], which can be installed;
         numpy 1.26.4, which can be installed;
         numpy 1.26.4, which can be installed (as previously explained);
         numpy 1.26.4, which can be installed (as previously explained);
         numpy 1.19.5, which can be installed;
         numpy [1.20.0|1.20.1|...|1.21.5], which can be installed;
         numpy [1.21.6|1.22.0|1.22.1|1.22.2|1.22.3], which can be installed;
         numpy 1.22.4, which can be installed;
       python >=3.9,<3.10.0a0 *, which can be installed;
     pybigwig [0.3.18|0.3.22] would require
       numpy >=1.21.6,<2.0a0 * with the potential options
         numpy [1.23.0|1.23.1|1.23.2|1.23.3|1.23.4], which can be installed;
         numpy [1.23.5|1.24.0|...|1.26.3], which can be installed;
         numpy 1.26.4, which can be installed;
         numpy 1.26.4, which can be installed (as previously explained);
         numpy 1.26.4, which can be installed (as previously explained);
         numpy [1.21.6|1.22.0|1.22.1|1.22.2|1.22.3], which can be installed;
         numpy 1.22.4, which can be installed;
       python >=3.9,<3.10.0a0 *, which can be installed;
     pybigwig 0.3.22 would require
       numpy >=1.22.4,<2.0a0 * with the potential options
         numpy [1.23.0|1.23.1|1.23.2|1.23.3|1.23.4], which can be installed;
         numpy [1.23.5|1.24.0|...|1.26.3], which can be installed;
         numpy 1.26.4, which can be installed;
         numpy 1.26.4, which can be installed (as previously explained);
         numpy 1.26.4, which can be installed (as previously explained);
         numpy 1.22.4, which can be installed;
       python >=3.10,<3.11.0a0 *, which can be installed;
     pybigwig 0.3.22 would require
       numpy >=1.23.5,<2.0a0 * with the potential options
         numpy [1.23.5|1.24.0|...|1.26.3], which can be installed;
         numpy 1.26.4, which can be installed;
         numpy 1.26.4, which can be installed (as previously explained);
         numpy 1.26.4, which can be installed (as previously explained);
       python >=3.11,<3.12.0a0 *, which can be installed;
     pybigwig 0.3.22 would require
       numpy >=1.26.4,<2.0a0 * with the potential options
         numpy 1.26.4, which can be installed;
         numpy 1.26.4, which can be installed (as previously explained);
         numpy 1.26.4, which can be installed (as previously explained);
       python_abi =3.12 *_cp312, which can be installed (as previously explained);
     pybigwig 0.3.22 would require
       numpy >=1.22.4,<2.0a0 * with the potential options
         numpy [1.23.0|1.23.1|1.23.2|1.23.3|1.23.4], which can be installed;
         numpy [1.23.5|1.24.0|...|1.26.3], which can be installed;
         numpy 1.26.4, which can be installed;
         numpy 1.26.4, which can be installed (as previously explained);
         numpy 1.26.4, which can be installed (as previously explained);
         numpy 1.22.4, which can be installed;
       python >=3.8,<3.9.0a0 *, which can be installed;
     pybigwig 0.3.22 would require
       numpy >=1.22.4,<2.0a0 * with the potential options
         numpy [1.23.0|1.23.1|1.23.2|1.23.3|1.23.4], which can be installed;
         numpy [1.23.5|1.24.0|...|1.26.3], which can be installed;
         numpy 1.26.4, which can be installed;
         numpy 1.26.4, which can be installed (as previously explained);
         numpy 1.26.4, which can be installed (as previously explained);
         numpy 1.22.4, which can be installed;
       python >=3.9,<3.10.0a0 *, which can be installed;
     pybigwig [0.3.3|0.3.4] would require
       numpy =1.10 *, which conflicts with any installable versions previously reported;
       python =2.7 *, which can be installed;
     pybigwig [0.3.3|0.3.4] would require
       numpy =1.10 *, which conflicts with any installable versions previously reported;
       python =3.4 *, which can be installed;
     pybigwig [0.3.3|0.3.4] would require
        numpy =1.10 *, which conflicts with any installable versions previously reported;
        python =3.5 *, which can be installed;
   python >=3.13,<3.14.0a0 * is not installable because it conflicts with any installable versions previously reported;
   python_abi =3.13 *_cp313 is not installable because there are no viable options
      python_abi 3.13 would require
        python =3.13 *_cpython, which can be installed;
      python_abi 3.13 would require
         python =3.13 *_cp313, which can be installed.
# Last 100 lines of the build log.
