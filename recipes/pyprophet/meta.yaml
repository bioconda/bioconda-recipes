{% set name = "pyprophet" %}
{% set version = "3.0.10" %}
{% set sha256 = "ae2bebc60df31a9f1ec64b0dddc6086e4ae0c4b91acbc5ec876fbd9c84323b33" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/pyprophet-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  skip: True  # [arm64 and (py < 310)]
  entry_points:
    - pyprophet = pyprophet.main:cli
  script: {{ PYTHON }} -m pip install . --no-deps --no-build-isolation --no-cache-dir -vvv
  run_exports:
    - {{ pin_subpackage(name, max_pin="x") }}

requirements:
  build:
    - {{ compiler('c') }}
  host:
    - python >=3.9,<3.14
    - pip
    - setuptools
    - cython
    - numpy >=1.26.4
    - pypdf
    - libgomp  # [linux]  
    - llvm-openmp  # [osx]
  run:
    - python >=3.9,<3.14
    - libgomp  # [linux]  
    - llvm-openmp  # [osx]
    - click
    # - duckdb-extensions
    # - duckdb-extension-sqlite-scanner
    - loguru
    - lxml
    - matplotlib-base
    - numpy >=1.26.4
    - numexpr ==2.10.2
    - packaging
    - pandas >=2.0
    - polars >=1.28.1
    - psutil
    - pyarrow
    - pyopenms
    - pypdf
    - python-duckdb
    - scikit-learn >=1.5
    - scipy
    - seaborn-base
    - statsmodels >=0.8.0
    - tabulate
    - typing-extensions
    - xgboost

test:
  imports:
    - pyprophet
  commands:
    - pyprophet --help

about:
  home: "https://github.com/PyProphet/pyprophet"
  license: "BSD-3-Clause"
  license_family: BSD
  license_file: LICENSE
  summary: "PyProphet: Semi-supervised learning and scoring of OpenSWATH results."
  dev_url: "https://github.com/PyProphet/pyprophet"
  doc_url: "https://openswath.org/en/latest"

extra:
  additional-platforms:
    - linux-aarch64
    - osx-arm64
  identifiers:
    - doi:10.1038/nmeth.1584
