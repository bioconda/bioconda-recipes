{% set name = "pyprophet" %}
{% set version = "2.3.4" %}
{% set hash_type = "sha256" %}
{% set hash_value = "c755eb397778a91a85375db01942830e976f1e83ea046ace05cc9ef00e0b59aa" %}

package:
  name: '{{ name|lower }}'
  version: '{{ version }}'

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  '{{ hash_type }}': '{{ hash_value }}'

build:
  number: 0
  run_exports:
    - {{ pin_subpackage(name, max_pin="x") }}
  entry_points:
    - pyprophet = pyprophet.main:cli
  script: {{ PYTHON }} -m pip install --no-deps --no-build-isolation --no-cache-dir . -vvv

requirements:
  build:
    - {{ compiler('c') }}
  host:
    - python
    - pip
    - cython
    - numpy
    - pypdf
    - libgomp  # [linux]  
    - llvm-openmp  # [osx]
  run:
    - python
    - libgomp  # [linux]  
    - llvm-openmp  # [osx]
    - {{ pin_compatible("numpy") }}
    - scipy
    - python-duckdb
    #- duckdb-extensions
    #- duckdb-extension-sqlite-scanner
    - pandas >=2.0
    - polars
    - numexpr >=2.10.1
    - scikit-learn >=1.5
    - matplotlib-base
    - tabulate
    - pyarrow
    - xgboost
    - hyperopt
    - statsmodels >=0.8.0
    - click
    - python-duckdb
    - pypdf
    - typing-extensions

test:
  imports:
    - pyprophet
  commands:
    - pyprophet --help

about:
  home: https://github.com/PyProphet/pyprophet
  license: "BSD-3-Clause"
  license_family: BSD
  license_file: LICENSE
  summary: "PyProphet: Semi-supervised learning and scoring of OpenSWATH results."
  dev_url: https://github.com/PyProphet/pyprophet
  doc_url: https://openswath.org/en/latest

extra:
  additional-platforms:
    - linux-aarch64
  identifiers:
    - doi:10.1038/nmeth.1584
