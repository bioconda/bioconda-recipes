{% set version = "0.30.21" %}
{% set sha256hash = "ec941c4247b26a5dcaa672173fe48690214e5a8fee39393029ac49a4b4f74743" %}

package:
  name: cyvcf2
  version: {{ version }}

source:
  url: https://github.com/brentp/cyvcf2/archive/v{{ version }}.tar.gz
  sha256: {{ sha256hash }}
#  patches:
#    # Link to htslib shipped by conda
#    - patches/setup.py.patch

build:
  number: 0

requirements:
  build:
    - {{ compiler('c') }}
  host:
    - python
    - setuptools
    - cython
    - numpy
    - zlib
    - libcurl
    # statically links to htslib, sadly
    - htslib
    - openssl
    - bzip2
    - xz
  run:
    - python
    - numpy
    - zlib
    - coloredlogs
    - click
    - libcurl
    - openssl
    - pathlib2  # [py<34]
    - bzip2
    - xz

test:
  imports:
    - cyvcf2

about:
  home: https://github.com/brentp/cyvcf2
  summary: A cython wrapper around htslib built for fast parsing of Variant Call Format (VCF) files
  license: MIT
