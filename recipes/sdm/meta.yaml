{% set name = "sdm" %}
{% set version = "3.29" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://github.com/hildebra/{{ name }}/archive/v{{ version }}.tar.gz
  sha256: c61cfe8b08006bcff07c53200a5cbeb475389409461bee51f5bbbb701d98e99c

build:
  number: 0
  run_exports:
    - {{ pin_subpackage(name, max_pin="x") }}

requirements:
  build:
    - {{ compiler('cxx') }}
    - make
    - pkg-config
  host:
    - zlib

test:
  commands:
    - sdm

about:
  home: "https://github.com/hildebra/sdm"
  license: "GPL-3.0-or-later"
  license_family: GPL3
  license_file: LICENSE
  summary: "sdm - simple demultiplex tool for FASTQ demultiplexing and dereplication."
  dev_url: "https://github.com/hildebra/sdm"

extra:
  additional-platforms:
    - linux-aarch64
    - osx-arm64
  skip-lints:
    - compiler_needs_stdlib_c
