package:
  name: optbuild
  version: "0.1.11"

source:
  fn: optbuild-0.1.11.tar.gz
  url: https://files.pythonhosted.org/packages/b0/5e/b88949a1e557724fa61a81dfdd54a3067441050624ace665a5ed605b6fd3/optbuild-0.1.11.tar.gz
  md5: 7c3aa06d509bff214e715e670db7b615
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - optbuild = optbuild:main
    #
    # Would create an entry point called optbuild that calls optbuild.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - autolog

  run:
    - python
    - autolog

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://noble.gs.washington.edu/~mmh1/software/optbuild/
  license: GNU GPLv2
  summary: 'build command lines for external programs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
