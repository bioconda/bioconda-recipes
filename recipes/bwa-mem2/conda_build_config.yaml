# current version does not build with newer compilers on x86 (rdtsc redefinition)
cxx_compiler_version:
  - 10  # [linux and x86_64]
  - 16  # [osx]
c_compiler_version:
  - 10  # [linux and x86_64]
  - 16  # [osx]
