{% set version = "1.5.0" %}
{% set archive_base = "https://github.com/marbl/parsnp/archive" %}
{% set sha256sourcearchive = "82ef93a082736bf14fd3557a85a84df67d4df88754433fa9f3100ad695fcebbe" %}

package:
  name: parsnp
  version: '{{version}}'

source:
  url: "{{archive_base}}/v{{version}}.tar.gz"  
  sha256: "{{sha256sourcearchive}}"

build:
  number: 0
  skip: True # [linux]

requirements:
  build:
    - {{ compiler('cxx') }}
    - automake 1.15
    - autoconf
    - libtool
    - make
    - openmp
  host:
    - zlib
    - openmp
  run:
    - openmp
    - harvesttools
    - fasttree
    - phipack
    - numpy
    - zlib
    - phipack
    - raxml
    - fasttree
    - fastani
    - mash
    - harvesttools

about:
  home: https://github.com/marbl/parsnp
  license: "custom; see https://raw.githubusercontent.com/marbl/parsnp/master/LICENSE"
  summary: Parsnp is a command-line-tool for efficient microbial core genome alignment and SNP detection.

extra:
  skip-lints:
    - should_be_noarch_generic
    - should_not_be_noarch_source
