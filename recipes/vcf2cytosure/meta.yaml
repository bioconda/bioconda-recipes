{% set name = "vcf2cytosure" %}
{% set version = "0.9.3" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: "https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz"
  sha256: 365a57e372711c9507c218d6165f756f9fec5eb453aeb0bd33b38dadfe5379a2

build:
  noarch: python
  number: 0
  entry_points:
    - vcf2cytosure=vcf2cytosure.vcf2cytosure:main
  script: "{{ PYTHON }} -m pip install . -vv"
  run_exports:
    - {{ pin_subpackage("vcf2cytosure", max_pin="x.x") }}

requirements:
  host:
    - pip
    - python
  run:
    - cyvcf2
    - lxml
    - pandas
    - python

test:
  imports:
    - tests
    - vcf2cytosure
    - vcf2cytosure.constants
  commands:
    - vcf2cytosure --help

about:
  home: "https://github.com/NBISweden/vcf2cytosure"
  license: MIT
  license_family: MIT
  summary: "Convert VCF with structural variations to CytoSure format"

