{% set name = "kfb2zarr" %}
{% set version = "0.1.3" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://github.com/camlloyd/kfb2zarr/archive/refs/tags/v{{ version }}.tar.gz
  sha256: 1c3d15cb99037c1e0620c57583c8c3d5482943a55895f605801ad166a1fd45bb

build:
  number: 0
  run_exports:
    - {{ pin_subpackage(name, max_pin="x.x") }}

requirements:
  build:
    - {{ stdlib('c') }}
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - {{ compiler('rust') }}
    - cargo-bundle-licenses
    - cmake
    - make
    - pkg-config

test:
  commands:
    - kfb2zarr --help

about:
  home: https://github.com/camlloyd/kfb2zarr
  license: MIT
  license_family: MIT
  license_file:
    - LICENSE-MIT
    - THIRDPARTY.yml
  summary: Convert KFBio whole slide images (.kfb, .kfbf) to OME-Zarr

extra:
  additional-platforms:
    - linux-aarch64
    - osx-arm64
  recipe-maintainers:
    - camlloyd
