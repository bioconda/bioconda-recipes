package:
  name: schema
  version: "0.4.0"

source:
  fn: schema-0.4.0.tar.gz
  url: https://pypi.python.org/packages/b0/41/68972daad372fff3a2381e0416ff704faf524b2974e01d1c4fc997b4fb39/schema-0.4.0.tar.gz
  md5: cfabca802f206176e87888f716ee6baf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - schema = schema:main
    #
    # Would create an entry point called schema that calls schema.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/keleshev/schema
  license: MIT License
  summary: 'Simple data validation library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
