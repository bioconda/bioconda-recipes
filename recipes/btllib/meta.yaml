{% set name = "btllib" %}
{% set version = "1.7.8" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/BirolLab/btllib/releases/download/v{{ version }}/{{ name|lower }}-{{ version }}.tar.gz
  sha256: cd213d20a971ae3441551dd61b0e46a08559e3d9da19cb59ee8dc3397807f121
  patches:
    - sdsl-louds-tree-fix.patch

build:
  number: 0
  run_exports:
    - {{ pin_subpackage('btllib', max_pin="x") }}

requirements:
  build:
    - {{ compiler('cxx') }}
    - {{ stdlib('c') }}
    - python
    - cmake <4
    - meson
    - ninja
    - pkg-config
    - setuptools
  host:
    - python
    - pip
    - llvm-openmp  # [osx]
    - libgomp      # [linux]
    - setuptools
    - samtools
    - swig
    - doxygen
    - pigz
    - gzip  # alternative to pigz
    - tar
    - bzip2
    - xz
    - lrzip  # [linux]
    - zip
    - wget
  run:
    - python
    - samtools
    - llvm-openmp  # [osx]
    - libgomp      # [linux]

test:
  commands:
    - indexlr --help
  imports:
    - btllib

about:
  home: "https://github.com/BirolLab/btllib"
  license: "GPL-3.0-or-later"
  license_family: GPL3
  license_file: LICENSE
  summary: "Bioinformatics common code library in C++ with Python wrappers, from Bioinformatics Technology Lab."
  doc_url: "https://birollab.github.io/btllib/"
  dev_url: "https://github.com/BirolLab/btllib"

extra:
  additional-platforms:
    - linux-aarch64
    - osx-arm64
  identifiers:
    - doi:10.21105/joss.04720
    - doi:10.1093/bioadv/vbad162
