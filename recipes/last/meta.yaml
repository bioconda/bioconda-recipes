package:
  name: last
  version: "852"

source:
  fn: last-852.zip
  url: http://last.cbrc.jp/last-852.zip
  md5: 96b8fdadab2b0ed77f8e55ce4c91687f

build:
  number: 0
  skip: True # [py3k]

requirements:
  build:
     - gcc # [linux]
     - llvm # [osx]
     - zlib
  run:
     - python
     - libgcc # [linux]
     - zlib

test:
  commands:
     - lastdb --help
     - lastal --help
     - maf-convert --help

about:
  home: http://last.cbrc.jp/
  license: GNU GPL (>= 3)
  summary: LAST finds similar regions between sequences, and aligns them.
