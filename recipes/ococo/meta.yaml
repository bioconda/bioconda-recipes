package:
  name: ococo
  version: "0.1.2"

build:
  number: 0

source:
  fn: 0.1.2.tar.gz
  url: https://github.com/karel-brinda/ococo/archive/0.1.2.tar.gz

requirements:
  build:
    - gcc   # [not osx]
    - llvm  # [osx]
    - zlib ==1.2.8
    - libgcc
  run:
    - zlib ==1.2.8
    - libgcc

test:
  commands:
    - ococo -v

about:
  home: http://github.com/karel-brinda/ococo
  license: MIT
  summary: Ococo, the first online consensus caller.
