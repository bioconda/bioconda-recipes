{% set version = "v0.1.1" %}
{% set sha256 = "f634aaae01d723953e5d6d9051f049ead3b3022b6c57aa42883d1f1f33336abb" %}

package:
  name: mcroni
  version: {{ version }}

source:
  url: https://github.com/liampshaw/mcroni/archive/{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  noarch: python
  entry_points:
    - mcroni = mcroni.mcroni:main
  script: "{{ PYTHON }} -m pip install . --no-deps --ignore-installed --no-cache-dir -vvv"

requirements:
  host:
    - python >=3.8
    - pip

  run:
    - python >=3.8
    - pandas >=1.2
    - biopython >=1.78
    - abricate >=1.0.1
    - minimap2 >=2.17

test:
  imports:
    - mcroni
  commands:
    - mcroni --help

about:
  home: https://github.com/liampshaw/mcroni
  summary: 'mcr-1 analysis'
  license: MIT
