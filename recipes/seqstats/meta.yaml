{% set version = "1.0.0" %}

package:
        name: seqstats
        version: {{ version }}

source:
        url: https://github.com/vinisalazar/seqstats/archive/v{{ version }}.tar.gz
        sha256: aab92bd82fc9dee64945483db49d938836b99053ab2eb17f8c7d36bc9cc499d1

build:
        number: 0
        skip: True  # [win]

requirements:
        build:
                - {{ compiler('c') }}
        run:

test:
        commands:
                - seqstats

about: 
        home: https://github.com/clwgg/seqstats
        license: MIT
        summary: Quick summary statistics on fasta/fastq(.gz) files
