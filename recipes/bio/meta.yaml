{% set name = "bio" %}
{% set version = "1.8.4" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/bio-{{ version }}.tar.gz
  sha256: c4fd59d535b1591d3a35e3fc190004dfe86565699dfd71e9e565b7df41a86fe1

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vvv --no-deps --no-build-isolation --no-cache-dir
  run_exports:
    - {{ pin_subpackage('bio', max_pin="x") }}

requirements:
  host:
    - python >=3.10,<3.15
    - hatchling
    - pip
  run:
    - python >=3.10,<3.15
    - biopython >=1.80
    - requests
    - tqdm
    - mygene
    - pandas
    - pooch
    - gprofiler-official

test:
  imports:
    - biorun
  commands:
    - pip check
    - bio 2>&1 | grep '^bio'

about:
  home: https://github.com/ialbert/bio
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: "Command-line utilities to make bioinformatics explorations more enjoyable."
  dev_url: https://github.com/ialbert/bio
