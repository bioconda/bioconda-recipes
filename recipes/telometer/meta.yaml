{% set name = "telometer" %}
{% set version = "0.95" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: "https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/telometer-{{ version }}.tar.gz"
  sha256: b1cd74685ef7a04ccc5f8e6f4506ef95a73488dd2ea76de83329b5840f3f736b

build:
  number: 0
  noarch: python
  entry_points:
    - telometer=telometer:calculate_telomere_length
  script: "{{ PYTHON }} -m pip install . --no-deps --no-build-isolation --no-cache-dir -vvv"
  run_exports:
    - {{ pin_subpackage(name, max_pin='x') }}

requirements:
  host:
    - pip
    - python >=3.7
  run:
    - python >=3.7
    - pysam
    - pandas
    - regex

test:
  imports:
    - telometer
  commands:
    - telometer --help

about:
  home: https://github.com/santiago-es/Telometer
  license: MIT
  license_family: MIT
  license_file: LICENSE.txt
  summary: "A simple regular expression based method for measuring individual, chromosome-specific telomere lengths from long-read sequencing data."
  dev_url: https://github.com/santiago-es/Telometer




extra:
  recipe-maintainers:
    - santiago-es
