package:
  name: requestsexceptions
  version: 1.3.0

source:
  fn: requestsexceptions-1.3.0.tar.gz
  url: https://github.com/openstack-infra/requestsexceptions/archive/1.3.0.tar.gz 

build:
  skip: True # [not py27]

requirements:
  build:
    - python
    - setuptools
    - six
    - pbr

  run:
    - python
    - setuptools
    - six
    - pbr

# XXX: Have to clarify what this lib does :/
#test:
#  imports:
#    - urllib3

about:
  home: https://pypi.org/project/requestsexceptions/
  license: Apache 2.0
  summary: Import exceptions from potentially bundled packages in requests. 
