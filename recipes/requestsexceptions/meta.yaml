package:
  name: requestsexceptions
  version: 1.2.0

source:
  fn: requestsexceptions-1.2.0.tar.gz
  url: https://files.pythonhosted.org/packages/64/61/61907e515f08cd550bac200696d66ee518fac0d7d7aa7fcfe855fe5de546/requestsexceptions-1.2.0.tar.gz

build:
  skip: True # [not py27]

requirements:
  build:
    - python
    - setuptools
    - six
    - pbr

  run:
    - python
    - setuptools
    - six
    - pbr

# XXX: Have to clarify what this lib does :/
#test:
#  imports:
#    - urllib3

about:
  home: https://pypi.org/project/requestsexceptions/
  license: Apache 2.0
  summary: Import exceptions from potentially bundled packages in requests. 
