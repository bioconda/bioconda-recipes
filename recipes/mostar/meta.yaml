{% set version = "1.0.3" %}

package:
  name: mostar
  version: "1.0.3"

source:
  url: https://pypi.io/packages/source/m/mostar/mostar-{{ version }}.tar.gz
  sha256: e920829c8fb051ba969e3a75c359e25713da23136b945bd58a2f1ca929c1752d

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . --no-deps --no-build-isolation -vv
  run_exports:
    - {{ pin_subpackage("mostar", max_pin="x") }}

requirements:
  host:
    - python >=3.11
    - pip
    - setuptools >=61

  run:
    - python >=3.11
    # Core Assembly & Polishing
    - flye
    - medaka
    - filtlong
    - polypolish

    # Pre-processing & Alignment
    - fastp
    - bwa
    - samtools
    - bcftools
    - minimap2
    - circlator

    # Annotation & Profiling
    - bakta =1.11.4
    - pyhmmer <0.11
    - hmmer =3.4
    - alive-progress =3.0.1
    - ncbi-amrfinderplus
    - blast
    - kraken2

    # ICE & Conjugation Detection
    - macsyfinder

    # Plasticity & Mobile Elements
    - genomad

    # Python libraries
    - matplotlib-base
    - numpy
    - pandas
    - biopython
    - osfclient

    # Utilities
    - wget
    - curl

test:
  commands:
    - mostar --help

about:
  home: https://github.com/nermze/MOSTAR
  license: MIT
  license_file: LICENSE
  summary: "Modular ONT-Short-read Taxonomic Assembly and Resistome-Evolution pipeline"

extra:
  recipe-maintainers:
    - nermze
