{% set name = "scatac-fragment-tools" %}
{% set version = "0.1.5" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/scatac_fragment_tools-{{ version }}.tar.gz
  sha256: 60fdd64549e3f718c9588b51ada766dce56598ceb0140cdc908efb24d984e4dc

build:
  number: 0
  script: "{{ PYTHON }} -m pip install . --no-deps -vv"
  run_exports: {{ pin_compatible("scatac-fragment-tools", max_pin="x.x.x") }}

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('rust') }}
    - cargo-bundle-licenses
    - clangdev
  host:
    - python >=3.10
    - pip
    - maturin >=1,<2
  run:
    - python >=3.10
    - numpy
    - pybigwig
    - pybigtools >=0.2.4
    - polars >=1.32.0
    - pyarrow
    - numba
    - rich-argparse
    - joblib

test:
  imports:
    - scatac_fragment_tools
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/aertslab/scatac_fragment_tools/
  summary: 'Tools for working with scATAC-seq fragment files.'
  license: MIT
  doc_url: https://aertslab.github.io/scatac_fragment_tools/

extra:
  recipe-maintainers:
    - nilchia
