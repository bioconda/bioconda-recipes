{% set name = "simba_pbg" %}
{% set version = "1.3" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: "https://github.com/adamlerer/{{ name }}/archive/refs/tags/v{{ version }}.zip"
  sha256: 04ef38c1986fbccdb14355186a2b13f4c2eb209beca320069012f3d72c6cd5d4

build:
  number: 0
  skip: True  # [py2k]
  script: "{{ PYTHON }} -m pip install . --ignore-installed -vv"
  
requirements:
  build:
    - {{ compiler('c') }}
    - pytorch >=1.7.1
  host:
    - python
    - pip
  run:
    - python
    # - numpy >=1.12
    # - attrs >=18.2
    # - h5py >=2.8
    # - tqdm
    # - torchvision

test:
  imports:
    - torchbiggraph

about:
  home: https://github.com/adamlerer/simba_pbg
  dev_url: https://github.com/adamlerer/simba_pbg
  license: BSD-3
  license_family: BSD
  summary: A customized PyTorch-BigGraph (PBG) package for `simba`
