{% set name = "wub" %}
{% set version = "0.2.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

build:
  number: 1

source:
  fn: {{ name|lower }}_{{ version }}.tar.gz
  url: https://github.com/nanoporetech/wub/archive/v0.2.0.tar.gz
  md5: 6ccc37bec61a0def1cdb964d7092f8cb
  patches: 
  - wub_setup.patch

requirements:
  build:
  - python
  - setuptools
  - pytest
#  - pycmd
  - biopython
  - numpy
  - matplotlib
  - seaborn
  - editdistance
  - pandas
  - pysam
  - tqdm
  - h5py
  - statsmodels
  - htslib {{ CONDA_HTSLIB }}*

  run:
  - python
  - pytest
#  - pycmd
  - biopython
  - numpy
  - matplotlib
  - seaborn
  - editdistance
  - pandas
  - pysam
  - tqdm
  - h5py
  - statsmodels
  - htslib {{ CONDA_HTSLIB }}*

test:
  imports:
    - wub

  commands:
    - add_errors.py -h 
    - compare_genomes_lastal.py -h
    - reads_stats.py -h 
    - sequence_filter.py -h
    - simulate_genome.py -h

about:
  home: https://github.com/nanoporetech/wub
  license: MPL-2.0
  license_file: LICENSE.md
  summary: 'Tools and software library developed by the ONT Applications group'
