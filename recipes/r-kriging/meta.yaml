{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-kriging
  # Note that conda versions cannot contain -, so any -'s in the version have
  # been replaced with _'s.
  version: "1.1"

source:
  fn: kriging_1.1.tar.gz
  url:
    - http://cran.r-project.org/src/contrib/kriging_1.1.tar.gz
    - http://cran.r-project.org/src/contrib/Archive/kriging/kriging_1.1.tar.gz


  # You can add a hash for the file here, like md5 or sha1
  # md5: 49448ba4863157652311cc5ea4fea3ea
  # sha1: 3bcfbee008276084cbb37a2b453963c61176a322
  # patches:
   # List any patch files here
   # - fix.patch

build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: maps
requirements:
  build:
    - r-base
    - posix               # [win]
    - {{native}}toolchain # [win]
    - gcc                 # [not win]

  run:
    - r-base

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('kriging')" # [not win]
    - "\"%R%\" -e \"library('kriging')\"" # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  #home:
  license: GPL-2
  summary: Simple and highly optimized ordinary kriging algorithm to plot geographical data
  license_family: GPL2

# The original CRAN metadata for this package was:

# Package: kriging
# Version: 1.1
# License: GPL-2
# Description: Simple and highly optimized ordinary kriging algorithm to plot geographical data
# Title: Ordinary Kriging
# Author: Omar E. Olmedo
# Maintainer: Omar E. Olmedo <omareolmedo@gmail.com>
# Suggests: maps
# Packaged: 2014-12-01 15:38:27 UTC; oolmedo
# NeedsCompilation: yes
# Repository: CRAN
# Date/Publication: 2014-12-01 17:55:32

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
