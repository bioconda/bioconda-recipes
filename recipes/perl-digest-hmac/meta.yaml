package:
  name: perl-digest-hmac
  version: "1.05"

source:
  url: https://cpan.metacpan.org/authors/id/A/AR/ARODLAND/Digest-HMAC-1.05.tar.gz
  sha256: 215cb59cba610745cfb2d4b3f8ef756d590e57e3ad7986a992e87c4969fcdc7a

build:
  number: 0
  noarch: generic
  run_exports:
    - {{ pin_subpackage('perl-digest-hmac', max_pin="x") }}

requirements:
  build:
    - make
  host:
    - perl
  run:
    - perl

test:
  # Perl 'use' tests
  imports:
    - Digest::HMAC

about:
  home: "https://metacpan.org/pod/Digest-HMAC"
  license: "Perl_5"
  summary: "Keyed-Hashing for Message Authentication."
  doc_url: "https://metacpan.org/pod/Digest-HMAC"
