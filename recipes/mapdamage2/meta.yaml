package:
  name: mapdamage2
  version: "2.0.6"

build:
  number: 0
  skip: True # [osx]

source:
  fn: 2.0.6.tar.gz
  url: https://github.com/ginolhac/mapDamage/archive/2.0.6.tar.gz
  md5: f14e1ca52d68fd9c7e42e1f4e6f40f8c

requirements:
  build:
    - python >=2.7,<3
    - zlib
  run:
    - python >=2.7,<3
    - pysam
    
# Skip test for now due to error
# Error: Could not import required module 'pysam':
# 	- /lib64/libc.so.6: version `GLIBC_2.7' not found (required by /anaconda/envs/_build/lib/python2.7/site-packages/pysam/../../../libhts.so.1)
#test:
#  requires:
#    - pysam
#  commands:
#    - mapDamage 2>&1 | grep Usage > /dev/null
#    - mapDamage -h 2>&1 | grep 'Options for the statistical estimation' > /dev/null
    
about:
  home: https://github.com/ginolhac/mapDamage
  license: MIT
  summary: "mapDamage: tracking and quantifying damage patterns in ancient DNA sequences http://geogenetics.ku.dk/all_literature/mapdamage/"
