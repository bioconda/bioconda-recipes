{% set name = "TransComb" %}
{% set version = "1.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://sourceforge.net/projects/transcriptomeassembly/files/TransComb/TransComb_v.{{ version }}.tar.gz
  sha256: 7e00fd35f9473354412bee73f44780e6a253f3ad10b25cb43783e7c600582dff
  patches:
    - CMakeLists.txt.patch
    - 0002-unordered_map.patch  # [osx]

build:
  number: 2
  run_exports:
    - {{ pin_subpackage(name|lower, max_pin="x") }}

requirements:
  build:
    - make
    - {{ compiler('cxx') }}
    - cmake <4
    - pkg-config
  host:
    - bamtools
    - boost-cpp
    - samtools 0.1.19
    - zlib
  run:
    - bamtools
    - boost-cpp
    - samtools 0.1.19

test:
  commands:
    - TransComb -h | grep "TransComb -b file.bam -s unstranded"
    - CorrectName -h | grep "CorrectName fq reads_1.fq reads_2.fq"

about:
  home: "https://github.com/yechengxi/SparseAssembler"
  summary: "A sparse k-mer graph based, memory-efficient genome assembler."
  license: Unknown

extra:
  additional-platforms:
    - linux-aarch64
    - osx-arm64
