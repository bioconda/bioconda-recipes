--- histoneHMM/src/densities.cpp.orig
+++ histoneHMM/src/densities.cpp
@@ -505,7 +505,7 @@
   char transa = 'N', transb = 'N';
   int m = nrowA, n = ncolB, k = ncolA;
   double one = 1.0, zero = 0.0;
-  F77_CALL(dgemm)(&transa, &transb, &m, &n, &k, &one, a, &nrowA, b, &nrowB, &zero, c, &m);
+  F77_CALL(dgemm)(&transa, &transb, &m, &n, &k, &one, a, &nrowA, b, &nrowB, &zero, c, &m, 1, 1);
 }
