package:
  name: optplus
  version: "0.1.1"

source:
  fn: optplus-0.1.1.tar.gz
  url: https://files.pythonhosted.org/packages/1e/93/0e68324166920d816ff434e1cf5f098570029110eaa636a452c00966759c/optplus-0.1.1.tar.gz
  md5: 6dcd292165e21888837705f6369e3b1b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - optplus = optplus:main
    #
    # Would create an entry point called optplus that calls optplus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - optplus

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://noble.gs.washington.edu/~mmh1/software/optplus/
  license: UNKNOWN
  summary: 'additional options for optparse'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
