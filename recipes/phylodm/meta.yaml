{% set name = "phylodm" %}
{% set version = "1.3.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 9dd70cc517317ff613b48489790457ffafb08f517af2eb0097820d2e25f9fe65

build:
  number: 0
  skip: True  # [py2k]
  script: "{{ PYTHON }} -m pip install . --no-deps -vv"

requirements:
  build:
    - {{ compiler('c') }}
  host:
    - python
    - pip
    - cython
    - numpy
    - setuptools
  run:
    - python
    - numpy
    - dendropy
    - h5py

test:
  imports:
    - phylodm
    - phylodm.pdm
    - phylodm.symmat
  commands:
    - phylodm -h

about:
  home: https://github.com/aaronmussig/PhyloDM
  license: GPL-3
  license_family: GPL
  license_file: LICENSE
  summary: 'Efficient calculation of phylogenetic distance matrices.'

extra:
  recipe-maintainers:
    - aaronmussig
