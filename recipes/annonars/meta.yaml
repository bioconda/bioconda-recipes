{% set version = "0.12.0" %}

package:
  name: annonars
  version: {{ version }}

build:
  number: 0
  skip: True # [osx]

source:
  url: https://github.com/bihealth/annona-rs/archive/refs/tags/v{{ version }}.tar.gz
  sha256: 7dfd680af42514f056dfa23bec3ad532f06af2cefaabb3b6f6fd03af95dd86a5

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - rust >=1.30
    - clangdev
    - pkg-config
    - protobuf
    - make
    - cmake
  host:
    - clangdev
    - openssl
    - sqlite
    - zlib
  run:
    - sqlite

test:
  commands:
    - annonars --help

about:
  home: https://github.com/bihealth/annona-rs
  license: Apache-2.0
  summary: |
     Genome annotation based on Rust and RocksDB.

extra:
  recipe-maintainers:
    - holtgrewe
