{% set version = "0.1.6" %}

package:
  name: mozaiko
  version: {{ version }}

source:
  url: https://github.com/CIBIO-BU/mozaiko/archive/refs/tags/{{ version }}.tar.gz
  sha256: a7c339b304c710ce87efdd00c5cf98c3f8a38a3cf7bf762cf53fc2fd56bf0105

build:
  noarch: python
  number: 0
  script: "{{ PYTHON }} -m pip install . --no-deps --no-build-isolation -vv"
  run_exports:
    - {{ pin_subpackage('mozaiko', max_pin="x.x") }}

requirements:
  host:
    - python >=3.9
    - pip
    - setuptools >=61
    - wheel

  run:
    - python >=3.9
    - biopython >=1.80
    - numpy
    - pandas
    - matplotlib-base
    - tqdm
    - vsearch ==2.16
    - cutadapt ==4.9
    - catnip-seq
    - crabs ==0.1.7

test:
  imports:
    - mozaiko
  commands:
    - mozaiko --version
    - crabs --version
  requires:
    - pip
    - python

about:
  home: https://github.com/CIBIO-BU/mozaiko
  summary: Piecing Together Complete Genetic Coverage for Biomonitoring
  license: GPL-3.0-or-later
  license_file: LICENSE

extra:
  recipe-maintainers:
    - camilababo
