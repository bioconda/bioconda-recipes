{% set name = "perl-extutils-constant" %}
{% set version = "0.24" %}
{% set hash = "aa9e8c0b564576195a7508374f4a80b97fe3cf4a4c13efe4c949ece3e76b217d" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: ExtUtils-Constant-{{ version }}.tar.gz
  url: https://cpan.metacpan.org/authors/id/N/NW/NWCLARK/ExtUtils-Constant-{{ version }}.tar.gz
  sha256: {{ hash }}

build:
  number: 0

requirements:
  build:
    - perl
    - perl-extutils-makemaker

  run:
    - perl

test:
  # Perl 'use' tests
  imports:
    - ExtUtils::Constant
    # testing fails due to version mismatches
    #- ExtUtils::Constant::Base
    #- ExtUtils::Constant::Utils
    #- ExtUtils::Constant::XS

about:
  home: http://metacpan.org/pod/ExtUtils-Constant
  license: perl_5
  summary: "A wrapper for perl's configuration"
