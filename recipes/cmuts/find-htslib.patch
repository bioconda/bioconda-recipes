--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -32,13 +32,19 @@
 #


-find_package(PkgConfig REQUIRED)
 if (DEFINED MPI_BUILD AND MPI_BUILD)
     find_package(MPI REQUIRED)
     set(HDF5_PREFER_PARALLEL TRUE)
 endif()
 find_package(HDF5 REQUIRED)
 find_package(ZLIB REQUIRED)
-pkg_check_modules(HTSLIB REQUIRED htslib)
+find_path(HTSLIB_INCLUDE_DIRS htslib/hts.h)
+find_library(HTSLIB_LIBRARIES hts)
+if(NOT HTSLIB_INCLUDE_DIRS OR NOT HTSLIB_LIBRARIES)
+    message(FATAL_ERROR "htslib not found. Set CMAKE_PREFIX_PATH to the htslib install prefix.")
+endif()
+get_filename_component(HTSLIB_LIBRARY_DIRS "${HTSLIB_LIBRARIES}" DIRECTORY)
+message(STATUS "Found htslib: ${HTSLIB_LIBRARIES}")
 find_package(OpenMP)

 include(FetchContent)
