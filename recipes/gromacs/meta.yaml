about:
  home: http://www.gromacs.org/
  license: GNU Lesser General Public License (LGPL)
  summary: GROMACS is a versatile package to perform molecular dynamics.

package:
  name: gromacs
  version: 2016.4

build:
  number: 0

# See http://manual.gromacs.org/documentation/
# for latest release. Note that version scheme changed from 
# 5.1 to 2016.1.
source:
  fn: gromacs-2016.4.tar.gz
  url: ftp://ftp.gromacs.org/pub/gromacs/gromacs-2016.4.tar.gz
  md5: 19c8b5c85f3ec62df79d2249a3c272f8

requirements:
  build:
  # Compare with Debian package
  ## tinyxml # openmpi
  # but Not cuda 9 yet! 
  #hwloc
    - gcc  # [linux] ## NOTE: cuda might require specific gcc version
    - llvm  # [osx]  ##openmp needed on osx
    - cmake ## fftw ## clib
    - perl 
    - python >=2.7,<3
  run:
    - libgcc  # [linux]
    - libhwloc

test:
  commands:
    - mdrun -version
