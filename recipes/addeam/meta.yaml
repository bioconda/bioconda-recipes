{% set name = "addeam" %}
{% set version = "1.0.0" %}
{% set sha256 = "8e6174c4df55326a3a67a77d497cd52f9f1f3675a42907ce1ce77b5eef11d368" %}



package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://github.com/LouisPwr/AdDeam/releases/download/{{ version }}/AdDeam-v{{ version }}.tar.gz
  sha256: {{ sha256 }}
  

build:
  number: 0
  skip: True  # [osx]
  run_exports:
    - {{ pin_subpackage("addeam", max_pin="x") }}  

requirements:
  build:
    - make
    - {{ compiler('cxx') }}
    - gxx >=12.0
  host:
    - cython
    - zlib
    - xz
    - curl
    - bzip2
    - gsl
    - htslib
    - samtools
  run:
    - python
    - numpy >=2
    - pandas
    - matplotlib-base
    - scikit-learn
    - scipy
    - opencv
    - pypdf2
    - bottleneck
    - htslib
    - samtools
    - zlib
    - xz
    - curl
    - bzip2
    - gsl
    - libcurl
    - openblas


test:
  commands:
    - addeam-bam2prof.py --help | grep "Python wrapper for bam2prof"
    - addeam-cluster.py --help | grep "Cluster and plot damage profiles."


about:
  home: "https://github.com/LouisPwr/AdDeam"
  license: GPL-3.0-or-later
  license_file: LICENSE
  summary: "A Fast and Scalable Tool for Estimating and Clustering Reference-Level Damage Profiles."

extra:
  identifiers:
    - biotools:addeam
  recipe-maintainers:
    - gabriel-renaud
    - louis-kraft
