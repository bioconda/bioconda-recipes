package:
  name: bwa
  version: "0.6.2"

build:
  number: 0

source:
  fn: bwa-0.6.2.tar.bz2
  url: http://downloads.sourceforge.net/project/bio-bwa/bwa-0.6.2.tar.bz2?r=https%3A%2F%2Fsourceforge.net%2Fprojects%2Fbio-bwa%2Ffiles%2F&ts=1478640973&use_mirror=iweb

requirements:
  build:
    - gcc   # [not osx]
    - llvm  # [osx]
    - zlib
  run:
    - zlib

test:
  commands:
    - bwa 2>&1 | grep 'index sequences in the'

about:
  home: http://bio-bwa.sourceforge.net
  license: MIT
  summary: The BWA read mapper.
