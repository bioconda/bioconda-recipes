{% set name = "scitrack" %}
{% set version = "2026.8.5" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/scitrack-{{ version }}.tar.gz
  sha256: 1eb6fba03107021d43cb8890bc13f4be1a7e342cdc695c9f5e1dca95b1e4f27f

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . --no-deps --no-build-isolation --no-cache-dir -vvv
  number: 0
  run_exports:
    - {{ pin_subpackage('scitrack', max_pin=None) }}

requirements:
  host:
    - python >=3.9
    - pip
    - flit-core
    - hatchling
  run:
    - python >=3.9

test:
  imports:
    - scitrack

about:
  home: https://github.com/HuttleyLab/scitrack
  summary: "SciTrack provides basic logging capabilities to track scientific computations."
  license: BSD-3-Clause
  license_family: BSD
  license_file: LICENSE
  dev_url: https://github.com/HuttleyLab/scitrack
