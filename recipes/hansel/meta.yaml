package:
  name: hansel
  version: "0.5.0"

## Currently waiting on REPO to move the PHAC NML repository so the information for
## Source below will change. In the mean time, it will have Peter's repo.
source:
  fn:  heidelberg_subtyping-0.5.0.tar.gz
  url: https://github.com/peterk87/heidelberg_subtyping/archive/0.5.0.tar.gz
  md5: c81a6b4193dbffc253ea554d69337bcc
#The first build number should always be 0 if it's a new recipe.
build:
  number: 0

requirements:
  build:
    - python >=3.5
    - pip >=9.0.1

  run:
    - python >=3.5
    - blast >=2.6
    - jellyfish >=2.0

test:
  commands:
    - which heidelberg_subtyping
    - heidelberg_subtyping -h

about:
  home: https://github.com/peterk87/heidelberg_subtyping
  license: Copyright 2017 Public Health Agency of Canada, distributed under the GNU Public License version 3.0.
  summary: 'Subtype Salmonella enterica subsp. enterica serovar Heidelberg genomes using an in-silico 33 bp k-mer subtyping method developed by Genevieve Labbe et al.'
  license_family: GPL3