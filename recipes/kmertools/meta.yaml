{% set version = "0.2.2" %}
{% set name = "kmertools" %}
{% set sha256 = "0260d69aa48ee88d90d0e6f9f404dd5ed64cc0ef823dbb9b3674cf79699cd5f0" %}

package:
  name: {{ name }}
  version: {{ version }}

build:
  number: 0
  skip: true  # [py < 310]
  run_exports:
    - {{ pin_subpackage('kmertools', max_pin="x.x") }}

source:
  url: https://github.com/anuradhawick/{{ name }}/archive/v{{ version }}.tar.gz
  sha256: {{ sha256 }}

requirements:
  build:
    - {{ compiler("rust") }}
    - {{ stdlib("c") }}
    - cargo-bundle-licenses
    - python
  host:
    - maturin >=1.7,<2.0
    - python
    - pip
  run:
    - python

test:
  imports:
    - pykmertools
  commands:
    - {{ name }} --help

about:
  home: https://github.com/anuradhawick/kmertools
  license: GPL-3.0-only
  license_family: GPL3
  license_file:
    - LICENSE
    - THIRDPARTY.yml
  summary: "kmertools: DNA Vectorisation Tool"
  description: "kmertools is a k-mer based feature extraction tool designed to support metagenomics and other bioinformatics analytics."
  doc_url: https://github.com/anuradhawick/kmertools/wiki
  dev_url: https://github.com/anuradhawick/kmertools

extra:
  additional-platforms:
    - linux-aarch64
    - osx-arm64
  recipe-maintainers:
    - anuradhawick
    - Vini2
