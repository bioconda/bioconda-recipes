{% set package = "ucsc-bed-tools" %}
{% set version = "454" %}
{% set sha256 = "dd547be640684982b27e419fa891d359d0693fbcf2f36d57b4a916939d31f1a7" %}

package:
  name: {{ package }}
  version: {{ version }}

source:
  url: "http://hgdownload.cse.ucsc.edu/admin/exe/userApps.archive/userApps.v{{ version }}.src.tgz"
  sha256: {{ sha256 }}

build:
  number: 0
  skip: True  # [osx]
  binary_relocation: False
  run_exports:
    - {{ pin_subpackage("ucsc-bed-tools", max_pin="x") }}
  #ignore_run_exports:
    #- libpng
    #- libuuid

requirements:
  build:
    - rsync
    #- make
    #- {{ compiler('c') }}
    #- {{ compiler('cxx') }}
    #- binutils # [linux]
  host:
    #- libpng
    #- libuuid
    #- mysql-connector-c
    #- openssl
    #- libopenssl-static
    #- zlib
  run:
    #- libpng
    #- libuuid
    #- mysql-connector-c
    #- openssl

test:
  commands:
    - which bedClip
    - which bedCommonRegions
    - which bedCoverage
    - which bedExtendRanges
    - which bedGeneParts
    - which bedGraphPack
    - which bedGraphToBigWig
    - which bedGraphToBigWig
    - which bedIntersect
    - which bedItemOverlapCount
    - which bedJoinTabOffset
    - which bedJoinTabOffset.py
    - which bedMergeAdjacent
    - which bedPartition
    - which bedPileUps
    - which bedRemoveOverlap
    - which bedRestrictToPositions
    - which bedSort
    - which bedToBigBed
    - which bedToBigBed
    - which bedToExons
    - which bedToGenePred
    - which bedToPsl
    - which bedWeedOverlapping
    - which bigBedInfo
    - which bigBedNamedItems
    - which bigBedSummary
    - which bigBedToBed
    - which bigWigAverageOverBed
    - which bigWigAverageOverBed
    - which bigWigCat
    - which bigWigCluster
    - which bigWigCorrelate
    - which bigWigInfo
    - which bigWigMerge
    - which bigWigSummary
    - which bigWigToBedGraph
    - which bigWigToBedGraph
    - which bigWigToWig
    - which clusterMatrixToBarChartBed
    - which crTreeIndexBed
    - which crTreeSearchBed
    - which expMatrixToBarchartBed
    - which fixStepToBedGraph.pl
    - which genePredToBed
    - which hgLoadBed
    - which hgLoadWiggle
    - which hgWiggle
    - which mafToSnpBed
    - which matrixToBarChartBed
    - which netToBed
    - which pslToBed
    - which qacToWig
    - which varStepToBedGraph.pl
    - which vcfToBed
    - which wigCorrelate
    - which wigEncode
    - which wigToBigWig
    - which wigToBigWig

about:
  home: "http://hgdownload.cse.ucsc.edu/admin/exe/"
  license: "varies; see http://genome.ucsc.edu/license"
  summary: "All of the ucsc kent tools"

extra:
  skip-lints:
    # repackaged binary
    - should_be_noarch_generic
