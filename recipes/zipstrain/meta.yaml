{% set name = "zipstrain" %}
{% set version = "0.10.2" %}   

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 304c23e469815289fd1954a02211d235a71dd006ad57370807c2ceeb254e0113

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . --no-deps --ignore-installed -vv
  run_exports:
    - {{ pin_subpackage('zipstrain', max_pin="x.x") }}

requirements:
  host:
    - python >=3.12
    - poetry-core   
    - pip
    
  run:
    - python >=3.12
    - samtools 
    - click
    - polars
    - plotly
    - numpy
    - seaborn
    - scipy
    - pyarrow
    - aiofiles
    - pydantic >=2,<3
    - rich
    - psutil
    - duckdb
    - pandas
    - matplotlib-base
    - h5py
    - pytorch
test:
  imports:
    - zipstrain
  commands:
    - zipstrain --help
    - samtools --version
    - zipstrain compare --help
    - zipstrain utilities --help

about:
  home: https://github.com/OlmLab/ZipStrain  
  summary: Fast strain-level metagenomics
  license: MIT     
  license_family: MIT
  dev_url: https://github.com/OlmLab/ZipStrain

