{% set name = "MARS" %}
{% set version = "1.0" %}

package:  
  name: {{ name|lower }}
  version: {{ version }}

build:
  noarch: python
  number: 0
  script: python setup.py install --single-version-externally-managed --record=record.txt

source:
  url: https://github.com/maiziex/{{ name }}/archive/v{{ version }}.tar.gz
  sha256: a4ba7660a68ccc6fffeaa9bc2c64c1bd5a46837e93fce7e3bc0a5710e8deb36c

requirements:
  host:
    - python >=3
    - setuptools

  run:
    - python >=3
    - biopython
    - samtools
    - pysam
    - minimap2
    - openpyxl
    - pandas

test:
  commands:
    - MARS_step1 --help
    - MARS_step2 --help


about:
  home: https://github.com/maiziex/MARS
  license: MIT
  summary: Multiple Alignment-based Refinement of SVs(MARS)

