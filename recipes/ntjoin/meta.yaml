{% set version = "1.1.6" %}

package:
  name: ntjoin
  version: {{ version }}

source:
  url: https://github.com/BirolLab/ntJoin/releases/download/v{{ version }}/ntJoin-{{ version }}.tar.gz 
  sha256: c0d5d8e1ae0174a689b3912d5a8a01db00e519fd30018854d4c715b68799eaab 

build:
  number: 0
  noarch: generic
  run_exports: 
    - {{ pin_subpackage("ntjoin", max_pin="x") }}

requirements:
  host:
    - make
    - python <=3.12
    - pip
    - zlib
    - btllib >=1.7.1
  run:
    - python-igraph
    - pybedtools
    - pymannkendall
    - bedtools >=2.21.0
    - pysam >=0.16.0
    - samtools >=1.10
    - python <=3.12
    - zlib
    - make
    - btllib >=1.7.1
    - packaging

test:
  commands:
    - ntJoin help 
    - ntJoin check_install
    - samtools --help

about:
  home: https://github.com/BirolLab/ntJoin
  license: GPL-3.0-or-later
  license_file: LICENSE
  summary: 'Genome assembly scaffolder using minimizer graphs'
