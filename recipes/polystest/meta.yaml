{% set name = "PolySTest" %}
{% set version = "0.99" %}

package:
  name: polystest
  version: {{ version }}

build:
  number: 0
  noarch: generic


source:
  url: https://bitbucket.org/veitveit/polystest/get/release-0.99.tar.gz
  sha256: 2faa41c947a1e9a630fe9211eb5a24b66538c99739b1670cf73500e8c7d5f54d

requirements:
  host:
    - r-base
    - r-matrixstats
    - r-fdrtool
    - bioconductor-qvalue
    - r-circlize
    - r-dt
    - r-upsetr
    - r-heatmaply
    - r-gplots
    - r-shiny
    - r-shinybs
    - r-yaml
    - r-knitr
    - r-shinydashboard
    - bioconductor-limma

  run:
    - r-base
    - r-matrixstats
    - r-fdrtool
    - bioconductor-qvalue
    - r-circlize
    - r-dt
    - r-upsetr
    - r-heatmaply
    - r-gplots
    - r-shiny
    - r-shinybs
    - r-yaml
    - r-knitr
    - r-shinydashboard
    - bioconductor-limma

test:
  commands:
#    - R -e "setTimeLimit(elapse=10, trans=T);shiny::runApp()"
     - cd $PREFIX/bin; ls ;  runPolySTestCLI.R polystest.yml; cd -

about:
  home: https://bitbucket.org/veitveit/polystest/src/master/
  license: 'GPL (>=2)'
  license_family: GPL
  summary: Interactive tool for statistical testing, data browsing and interactive visualization of quantitative omics data
  description: |
    PolySTest is a web service (shiny app) and command-line tool for statistical testing, data browsing and interactive visualization quantitative omics data. It contains multiple statistical tests and a new method to incorporate missing values. 


extra:
  notes: |
    SearchGUI is Java program that comes with a custom wrapper shell script.
    This shell wrapper is called "opsin" and is on $PATH by default. By default
    "-Xms512m -Xmx1g" is set in the wrapper. If you want to overwrite it you can
    specify these values directly after your binaries. If you have _JAVA_OPTIONS
    set globally this will take precedence.
    For example run it with "searchgui -Xms512m -Xmx1g"
  identifiers:
    - biotools:polystest
    - doi:10.1074/mcp.RA119.001777

