{% set version="0.93.2" %}

package:
  name: rtk
  version: {{ version }}

source:
  url: https://github.com/hildebra/Rarefaction/archive/v{{ version }}.tar.gz
  sha256: 58bda0c96ece79026aae1682a255e8ed5ba36edd0a2ed9509f6b5c4f5e992b72

build:
  number: 0

requirements:
  build:
    - {{ compiler('cxx') }}
  host:
    - zlib
  run:
    - zlib

test:
  files:
    - table.tsv
  commands:
    # (see run_test.sh for remainder of tests)

about:
  home: https://github.com/hildebra/Rarefaction/
  license: 'GPLv2'
  license_file: LICENSE
  summary: 'rtk - rarefaction toolkit for OTU tables'
