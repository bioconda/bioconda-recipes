#!/bin/bash
mkdir -p $PREFIX/bin
mv *.pl $PREFIX/bin
mv Quick* $PREFIX/bin
mv plot* $PREFIX/bin
mv make_HTMLs.sh $PREFIX/bin
mv prepare_small* $PREFIX/bin
mv run.config.template $PREFIX/bin
mv database $PREFIX/bin
