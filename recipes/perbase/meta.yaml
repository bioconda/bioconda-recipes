{% set version = "0.3.2" %}
{% set sha256 = "ded0690ee052274b98ede17e4c4d734483af3e2740d6bd770713b5a429d8df82" %}

package:
  name: perbase
  version: {{ version }}

source:
  url: https://github.com/sstadick/perbase/archive/v{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0

requirements:
  build:
    - rust >=1.42
    - clangdev
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - pkg-config
  host:
    - gsl
    - libcblas
    - curl
    - openssl
    - zlib
    - bzip2
    - xz
    - clangdev
    - blis
  run:
    - gsl
    - curl
    - openssl
    - zlib
    - bzip2
    - xz
    - blis

test:
  commands:
    - perbase --help

about:
  home: https://github.com/sstadick/perbase
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: Per-base metrics on BAM/CRAM files.
