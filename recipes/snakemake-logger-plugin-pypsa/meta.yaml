{% set name = "snakemake-logger-plugin-pypsa" %}
{% set version = "0.2.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/snakemake_logger_plugin_pypsa-{{ version }}.tar.gz
  sha256: 3a3a3350984598c903115b3f6224bf39d818a9bddb0f239d64c879e122b07bc8

build:
  number: 0
  run_exports:
    - {{ pin_subpackage(name, max_pin="x.x") }}
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python >=3.11
    - setuptools >=61.0
    - wheel
    - pip
  run:
    - python >=3.11
    - snakemake-interface-logger-plugins >=2,<3

test:
  # We do not test the import as the plugin needs snakemake for importing, but adding
  # it would create a circular dependency.
  commands:
    - pip check
  requires:
    - pip
    - python >=3.11

about:
  home: https://github.com/PyPSA/snakemake-logger-plugin-pypsa
  summary: A PyPSA-Eur logger plugin for Snakemake
  license: MIT
  license_file: 
    - LICENSES/MIT.txt

extra:
  recipe-maintainers:
    - willu47
    - lkstrp
