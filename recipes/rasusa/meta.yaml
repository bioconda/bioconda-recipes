{% set version = "2.2.1" %}
{% set name = "rasusa" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://github.com/mbhall88/{{ name }}/archive/{{ version }}.tar.gz
  sha256: 09eacadcfd6cfad492716aa5045955623665dd254fdff47fadd1bf81e49e402e

build:
  number: 0
  run_exports:
    - {{ pin_subpackage('rasusa', max_pin="x.x") }}
  script:
    - cargo-bundle-licenses --format yaml --output THIRDPARTY.yml
    - cargo install -v --locked --no-track --root $PREFIX --path .

requirements:
  build:
    - {{ compiler('rust') }}
    - {{ compiler('c') }}              # [not win]
    - cargo-bundle-licenses
    - clangdev

test:
  commands:
    - rasusa --help
    - rasusa reads --help
    - rasusa aln --help
    - rasusa cite

about:
  home: https://github.com/mbhall88/rasusa
  license: MIT
  summary: Randomly subsample sequencing reads or alignments
  license_file:
    - LICENSE
    - THIRDPARTY.yml

extra:
  additional-platforms:
    - linux-aarch64
    - osx-arm64
  recipe-maintainers:
    - mbhall88
