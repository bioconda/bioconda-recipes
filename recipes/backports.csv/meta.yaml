package:
  name: backports.csv
  version: "1.0.1"

source:
  fn: backports.csv-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/backports.csv/backports.csv-1.0.1.tar.gz
  md5: 68349405f94164612371a536badfd2d6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  skip: False
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ryanhiebert/backports.csv
  license: UNKNOWN
  summary: 'Backport of Python 3 csv module'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
