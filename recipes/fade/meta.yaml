{% set version = "0.3.4" %}

package:
  name: fade
  version: '{{ version }}'

source:
  url: https://github.com/blachlylab/fade/archive/refs/tags/v{{ version }}.tar.gz
  sha256: 6054a64ce83e81e5e6527bef76eda4d31fd7cc78c3bb616263f7567fc7610e58


build:
  number: 0
  # binary_relocation: False # [linux]

requirements:
  build:
    - {{ compiler('c') }}
  host:
    - htslib
  run:
    - htslib

test:
  commands:
    - fade
  
about:
  home: https://github.com/blachlylab/fade
  license: MIT
  summary: "fade is a D program that provides fast identification and removal of enymatic fragmentation artifacts."

extra:
  identifiers:
    - doi:10.1093/nargab/lqaa070


