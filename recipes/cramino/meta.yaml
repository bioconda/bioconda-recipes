{% set version = "1.2.0" %}

package:
  name: cramino
  version: {{ version }}

source:
  url: https://github.com/wdecoster/cramino/archive/v{{ version }}.tar.gz
  sha256: a4e4992da306bb78fda422ead45f52e87e65facd91f31bd82a818d18b0979c0d

build:
  number: 0
  run_exports:
    - {{ pin_subpackage('cramino', max_pin="x") }}

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('rust') }}
    - {{ compiler('cxx') }}
    - clangdev
    - llvmdev
    - cmake
    - make
    - pkg-config
  host:
    - openssl

test:
  commands:
    - cramino --help

about:
  home: "https://github.com/wdecoster/cramino"
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: "A tool for very fast quality assessment of long read cram/bam files."
  dev_url: "https://github.com/wdecoster/cramino"

extra:
  additional-platforms:
    - linux-aarch64
    - osx-arm64
