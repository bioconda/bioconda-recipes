{% set version = "2.0.0" %}

package:
  name: cramino
  version: {{ version }}

source:
  url: https://github.com/wdecoster/cramino/archive/v{{ version }}.tar.gz
  sha256: ca5f773704fbb926de2ef7b85f93b1e3b65a1f072f1a67238aa1ab7f3b5bcd9d

build:
  number: 0
  run_exports:
    - {{ pin_subpackage('cramino', max_pin="x") }}

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('rust') }}
    - {{ compiler('cxx') }}
    - clangdev
    - llvmdev
    - cmake
    - make
    - pkg-config
  host:
    - openssl

test:
  commands:
    - cramino --help

about:
  home: "https://github.com/wdecoster/cramino"
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: "A tool for very fast quality assessment of long read cram/bam files."
  dev_url: "https://github.com/wdecoster/cramino"

extra:
  additional-platforms:
    - linux-aarch64
    - osx-arm64
