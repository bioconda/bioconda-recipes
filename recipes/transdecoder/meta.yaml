package:
  name: transdecoder
  version: "2.1.0"

build:
  number: 2
  skip: True # [osx]

source:
  fn: transdecoder-2.1.0.tar.gz
  url: https://github.com/TransDecoder/TransDecoder/archive/v2.1.0.tar.gz
  md5: f86c688e13ced3cc51fed3d508a2ea05

requirements:
  build:
    #- perl-carp
    - perl-db-file
    #- perl-data-dumper
    - perl-uri
    - gcc
  run:
    #- perl-carp
    - perl-db-file
    #- perl-data-dumper
    - perl-uri
    - libgcc >=4.9.1 # GOMP_4.0 requirement

about:
  home: https://transdecoder.github.io/
  license: Broad Institute 
  summary: TransDecoder identifies candidate coding regions within transcript 
           sequences, such as those generated by de novo RNA-Seq transcript 
           assembly using Trinity, or constructed based on RNA-Seq alignments 
           to the genome using Tophat and Cufflinks.

test:
  commands:
    - 'TransDecoder.Predict -h 2>&1 | grep USAGE' 
    #- 'TransDecoder.Predict -h'
    - 'TransDecoder.LongOrfs -h 2>&1 | grep USAGE'
    #- 'TransDecoder.LongOrfs -h'
    - 'perl -e "use DB_File"'
