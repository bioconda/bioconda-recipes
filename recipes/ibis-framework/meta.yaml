package:
  name: ibis-framework
  version: "0.7.1"

source:
  fn: ibis-framework-0.7.1.tar.gz
  url: https://pypi.python.org/packages/e7/ef/bfe99f259c037d49fcaf56b1906328d562645dec7d1c9c6770a16e23d01a/ibis-framework-0.7.1.tar.gz
  md5: 2b57e8a68cab182554fc7be7d0774f32
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ibis-framework = ibis-framework:main
    #
    # Would create an entry point called ibis-framework that calls ibis-framework.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  number: 1
  skip: False

requirements:
  build:
    - python
    - setuptools
    - pytest
    - numpy >=1.7.0
    - pandas >=0.12.0
    - impyla >=0.13.6
    - hdfs >=2.0.0
    - sqlalchemy >=1.0.0
    - six
    - mock

  run:
    - python
    - pytest
    - numpy >=1.7.0
    - pandas >=0.12.0
    - impyla >=0.13.6
    - hdfs >=2.0.0
    - sqlalchemy >=1.0.0
    - six
    - mock

test:
  # Python imports
  imports:
    - ibis
    - ibis.expr
    - ibis.expr.tests
    - ibis.hive
    - ibis.hive.tests
    - ibis.impala
    - ibis.impala.tests
    - ibis.spark
    - ibis.spark.tests
    - ibis.sql
    - ibis.sql.presto
    - ibis.sql.presto.tests
    - ibis.sql.redshift
    - ibis.sql.redshift.tests
    - ibis.sql.sqlite
    - ibis.sql.sqlite.tests
    - ibis.sql.tests
    - ibis.sql.vertica
    - ibis.sql.vertica.tests
    - ibis.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache License, Version 2.0
  summary: 'Productivity-centric Python Big Data Framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
