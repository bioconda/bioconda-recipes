# current version does not build with newer compilers
cxx_compiler_version:
  - 10  # [linux and x86_64]
  - 14  # [osx and x86_64]
c_compiler_version:
  - 10  # [linux and x86_64]
  - 14  # [osx and x86_64]
