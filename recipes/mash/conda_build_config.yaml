# current version does not build with newer compilers
cxx_compiler_version:
  - 11  # [linux]   # last version to define uint32_t etc without include
  - 16  # [osx]
c_compiler_version:
  - 11  # [linux]
  - 16  # [osx]
