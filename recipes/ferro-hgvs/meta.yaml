{% set name = "ferro-hgvs" %}
{% set version = "0.9.0" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://github.com/fulcrumgenomics/{{ name }}/archive/v{{ version }}.tar.gz
  sha256: e4541f95d362fa26fa9e32103a701bb2f81e39e66dcfc702ea59f2650e1a5d58

build:
  number: 0
  run_exports:
    - {{ pin_subpackage('ferro-hgvs', max_pin="x.x") }}

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('rust') }}
    - {{ stdlib('c') }}
    - git
    - pkg-config
    - cargo-bundle-licenses
  host:
    - openssl
    - libsqlite

test:
  commands:
    - ferro --help
    - ferro-benchmark --help

about:
  home: "https://github.com/fulcrumgenomics/{{ name }}"
  license: MIT
  license_family: MIT
  license_file:
    - LICENSE
    - THIRDPARTY.yml
  summary: "HGVS variant parser and normalizer."
  dev_url: "https://github.com/fulcrumgenomics/{{ name }}"
  doc_url: "https://github.com/fulcrumgenomics/{{ name }}/blob/v{{ version }}/README.md"

extra:
  additional-platforms:
    - linux-aarch64
    - osx-arm64
  recipe-maintainers:
    - nh13
    - tfenne
