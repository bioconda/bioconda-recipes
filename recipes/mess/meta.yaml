{% set version = "0.2.2" %}
{% set sha256 = "e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855" %}

package:
  name: mess
  version: {{ version }}

source:
  url: https://github.com/metagenlab/MeSS/releases/tag/v{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  entry_points:
    - mess=mess.mess:cli
  noarch: python
  script: "python -m pip install --no-deps --ignore-installed ."

requirements:
  host:
    - python>3.6,<3.9
    - pip

  run:
    - python>3.6,<3.9
    - snakemake-minimal=6.2.1
    - pandas >1
    - click>=7

test:
    imports:
        - mess
    commands:
        - mess run -h
        - mess --version

about:
  home: https://github.com/metagenlab/MeSS
  license: GPL-3.0
  license_file: LICENSE
  summary: The Metagenomic Sequence Simulator (MeSS) is a snakemake workflow used for simulating metagenomic mock communities.

extra:
  recipe-maintainers:
    - idfarbanecha

container:
  extended-base: true
