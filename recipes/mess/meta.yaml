{% set version = "0.8.0" %}
{% set sha256 = "3fc888a31f6c9acd71552b26f9e42f963a34382eeccb2215afcc872ba0f54c3e" %}

package:
  name: mess
  version: {{ version }}

source:
  url: https://github.com/metagenlab/MeSS/archive/refs/tags/v{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  entry_points:
    - mess=mess.mess:cli
  noarch: python
  script: "python -m pip install --no-deps --ignore-installed ."

requirements:
  host:
    - python>3.6,<3.9
    - pip

  run:
    - python>3.6,<3.9
    - snakemake-minimal=6.2.1
    - pandas >1
    - click>=7

test:
    imports:
        - mess
    commands:
        - mess run -h
        - mess --version

about:
  home: https://github.com/metagenlab/MeSS
  license: GPL-3.0
  license_file: LICENSE
  summary: The Metagenomic Sequence Simulator (MeSS) is a snakemake workflow used for simulating metagenomic mock communities.

extra:
  recipe-maintainers:
    - idfarbanecha

container:
  extended-base: true
