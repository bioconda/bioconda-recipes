package:
  name: popdel
  version: "1.0.3"

source:
  url: https://github.com/kehrlab/PopDel/archive/v1.0.3.tar.gz
  sha256: ec25d48354a20869095c5b5658de3b6b6ca2acf05670d4bf3e67d9a66a72368d
  patches:
    - Makefile.patch

build:
  number: 0

requirements:
  build:
    - {{ compiler('cxx') }}

  host:
    - zlib

  run:
    - zlib

test:
    commands:
        - popdel -h

about:
  home: https://github.com/kehrlab/PopDel
  license: GPL-3.0
  license_file: LICENSE
  summary: Fast structural deletion calling on population-scale short read paired-end data.
