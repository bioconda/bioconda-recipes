package:
  name: gsl 
  version: "2.2"

build:
  number: 1
  string: boost{{CONDA_BOOST}}_{{PKG_BUILDNUM}}

source:
  fn: gsl-2.2.tar.gz
  url: http://gnumirror.nkn.in/gsl/gsl-2.2.tar.gz

requirements:
  build:
  - gcc # [not osx]
  - llvm # [osx]

  run:
  - libgcc # [not osx]

test:
  commands:
    - ABYSS --version

about:
  home: https://www.gnu.org/software/gsl/ 
  license: GNU General Public License
  summary: The GNU Scientific Library (GSL) is a numerical library for C and C++ programmers.

