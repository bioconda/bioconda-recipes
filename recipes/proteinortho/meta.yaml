{% set version = "6.0b" %}
{% set sha256 = "06fcb6b96fc46a1fca681d75165d4ead40ebeeddaa7ca2001e55d1fd93908dfb" %}

package:
  name: proteinortho
  version: {{ version }}

source:
  url: https://gitlab.com/paulklemm_PHD/proteinortho/-/archive/v{{ version }}/proteinortho-v{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 1

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - make
    - cmake
  host:    
    - perl
    - python
    - diamond
    - openmp
    - lapack
    - libgcc
  run:
    - perl
    - python
    - diamond
    - openmp
    - lapack
    - libgcc

test:
  commands:
    - proteinortho --version
    - proteinortho_clustering -test

about:
  home: https://gitlab.com/paulklemm_PHD/proteinortho/
  license: GNU-3
  summary: "Proteinortho is a tool to detect orthologous genes within different species."
