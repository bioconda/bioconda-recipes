{% set version = "3.0.0" %}
{% set name = "kractor" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://github.com/sam-sims/{{ name }}/archive/{{ name }}-{{ version }}.tar.gz
  sha256: 225ff58ce67497f30be62eaf75e03907669d4164370bdafc4a3ae91d82c89d84

build:
  number: 0
  run_exports:
    - {{ pin_subpackage('kractor', max_pin="x.x") }}

requirements:
  build:
    - {{ compiler('rust') }}
    - cargo-bundle-licenses

test:
  commands:
    - kractor --version

about:
  home: https://github.com/Sam-Sims/kractor
  license: MIT
  license_file:
    - LICENSE
    - THIRDPARTY.yml
  summary: Rapidly extract reads from a FASTQ file based on taxonomic classification via Kraken2. 

extra:
  additional-platforms:
    - linux-aarch64
    - osx-arm64
  recipe-maintainers:
    - sam-sims