{% set name = "TwInsPEctor" %}
{% set version = "0.2.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

build:
  noarch: python
  number: 0
  script: "{{ PYTHON }} -m pip install . --no-deps --no-build-isolation --no-cache-dir -vvv"
  run_exports:
    - {{ pin_subpackage(name|lower, max_pin="x.x") }}

source:
  url: https://github.com/clementlab/TwInsPEctor/archive/v{{ version }}.tar.gz
  sha256: 1ab8c56864f1573517c524927fb82155d3716cea31b4df1afdc35701af161776

requirements:
  host:
    - python
    - pip
    - setuptools
    - wheel
  run:
    - python >=3.8
    - numpy
    - crispresso2
    - pandas
    - matplotlib-base
    - seaborn

test:
  imports:
    - TwInsPEctor
  commands:
    - TwInsPEctor -h

about:
  home: https://github.com/clementlab/TwInsPEctor
  license: MIT
  license_file: LICENSE
  summary: "TwInsPEctor: A tool for twin prime editing analysis."
