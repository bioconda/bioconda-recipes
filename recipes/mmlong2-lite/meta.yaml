{% set name = "mmlong2-lite" %}
{% set version = "1.2.0" %}
{% set sha256 = "7c10ce6403a7086bf1673f5ac4f81d85552e1b0a664337089b51b76dc252be0e" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/Serka-M/{{ name }}/archive/refs/tags/v{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  noarch: generic
  run_exports:
    - {{ pin_subpackage('mmlong2-lite', max_pin="x.x") }}

requirements:
  run:
    - snakemake 9.9.0
    - singularity 3.8.6
    - zenodo_get 
    - pv 
    - pigz 
    - tar 
    - rsync 

test:
  commands:
    - which mmlong2-lite

about:
  home: "https://github.com/Serka-M/mmlong2-lite"
  license: GPL-3.0-only
  license_file: LICENSE
  summary: "Lightweight workflow for microbial genome recovery using either Nanopore or PacBio HiFi reads"

extra:
  identifiers:
    - doi:10.5281/zenodo.15782630
  recipe-maintainers:
    - Mantas Sereika
