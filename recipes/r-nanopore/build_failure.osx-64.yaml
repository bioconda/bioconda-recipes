recipe_sha: ec276c9e38223109a41027d95a01dc84ad12c8f53e4b3cbe676264cea48fbc79  # The hash of the recipe's meta.yaml at which this recipe failed to build.
skiplist: true # Set to true to skiplist this recipe so that it will be ignored as long as its latest commit is the one given above.
log: |2-
     bioconductor-s4vectors 0.44.0 would require
       bioconductor-biocgenerics >=0.52.0,<0.53.0 , which requires
          r-base >=4.4,<4.5.0a0 , which can be installed;
     bioconductor-s4vectors [0.10.3|0.12.0|0.8.11|0.9.0] would require
       r 3.3.1* , which does not exist (perhaps a missing channel);
     bioconductor-s4vectors 0.12.2 would require
       r-base 3.3.1* , which does not exist (perhaps a missing channel);
     bioconductor-s4vectors [0.12.2|0.14.7|...|0.8.7] conflicts with any installable versions previously reported;
     bioconductor-s4vectors 0.18.3 conflicts with any installable versions previously reported;
     bioconductor-s4vectors 0.20.1 conflicts with any installable versions previously reported;
     bioconductor-s4vectors 0.22.0 conflicts with any installable versions previously reported;
     bioconductor-s4vectors 0.24.0 conflicts with any installable versions previously reported;
     bioconductor-s4vectors 0.26.0 conflicts with any installable versions previously reported;
     bioconductor-s4vectors [0.28.0|0.28.1] conflicts with any installable versions previously reported;
     bioconductor-s4vectors 0.30.0 conflicts with any installable versions previously reported;
     bioconductor-s4vectors [0.32.0|0.32.3|0.32.4] conflicts with any installable versions previously reported;
     bioconductor-s4vectors 0.36.0 conflicts with any installable versions previously reported;
     bioconductor-s4vectors 0.38.1 conflicts with any installable versions previously reported;
     bioconductor-s4vectors 0.40.2 conflicts with any installable versions previously reported;
   bioconductor-variantannotation is not installable because there are no viable options
     bioconductor-variantannotation 1.16.4 would require
       bioconductor-bsgenome >=1.37.6  but there are no viable options
          bioconductor-bsgenome 1.38.0 would require
            bioconductor-rsamtools, which cannot be installed (as previously explained);
          bioconductor-bsgenome [1.40.1|1.42.0] would require
            r 3.3.1* , which does not exist (perhaps a missing channel);
          bioconductor-bsgenome 1.42.0 would require
            bioconductor-genomicranges >=1.23.15 , which cannot be installed (as previously explained);
          bioconductor-bsgenome [1.44.2|1.46.0] would require
            bioconductor-genomeinfodb [>=1.11.4 |>=1.13.1 ], which cannot be installed (as previously explained);
          bioconductor-bsgenome 1.48.0 would require
            bioconductor-s4vectors >=0.18.3,<0.20.0 , which conflicts with any installable versions previously reported;
          bioconductor-bsgenome 1.50.0 would require
            bioconductor-s4vectors >=0.20.0,<0.21.0 , which conflicts with any installable versions previously reported;
          bioconductor-bsgenome 1.52.0 would require
            bioconductor-s4vectors >=0.22.0,<0.23.0 , which conflicts with any installable versions previously reported;
          bioconductor-bsgenome 1.54.0 would require
            bioconductor-s4vectors >=0.24.0,<0.25.0 , which conflicts with any installable versions previously reported;
          bioconductor-bsgenome 1.56.0 would require
            bioconductor-s4vectors >=0.26.0,<0.27.0 , which conflicts with any installable versions previously reported;
          bioconductor-bsgenome 1.58.0 would require
            bioconductor-s4vectors >=0.28.0,<0.29.0 , which conflicts with any installable versions previously reported;
          bioconductor-bsgenome 1.60.0 would require
            bioconductor-s4vectors >=0.30.0,<0.31.0 , which conflicts with any installable versions previously reported;
          bioconductor-bsgenome 1.62.0 would require
            bioconductor-s4vectors >=0.32.0,<0.33.0 , which conflicts with any installable versions previously reported;
          bioconductor-bsgenome [1.66.1|1.66.3] would require
            bioconductor-s4vectors >=0.36.0,<0.37.0 , which conflicts with any installable versions previously reported;
          bioconductor-bsgenome 1.68.0 would require
            bioconductor-s4vectors >=0.38.0,<0.39.0 , which conflicts with any installable versions previously reported;
          bioconductor-bsgenome 1.70.1 would require
             bioconductor-s4vectors >=0.40.0,<0.41.0 , which conflicts with any installable versions previously reported;
     bioconductor-variantannotation [1.18.7|1.20.3] would require
       r 3.3.1* , which does not exist (perhaps a missing channel);
     bioconductor-variantannotation 1.20.3 would require
       bioconductor-genomeinfodb >=1.7.1  but there are no viable options
          bioconductor-genomeinfodb 1.10.3 would require
            r-base 3.3.1* , which does not exist (perhaps a missing channel);
          bioconductor-genomeinfodb 1.10.3 would require
            r-base 3.3.2* , which conflicts with any installable versions previously reported;
          bioconductor-genomeinfodb [1.10.3|1.12.3|1.14.0], which cannot be installed (as previously explained);
          bioconductor-genomeinfodb 1.16.0, which cannot be installed (as previously explained);
          bioconductor-genomeinfodb 1.16.0, which cannot be installed (as previously explained);
          bioconductor-genomeinfodb 1.8.7 would require
            r 3.3.1* , which does not exist (perhaps a missing channel);
          bioconductor-genomeinfodb 1.18.1, which cannot be installed (as previously explained);
          bioconductor-genomeinfodb 1.20.0, which cannot be installed (as previously explained);
          bioconductor-genomeinfodb 1.22.0, which cannot be installed (as previously explained);
          bioconductor-genomeinfodb 1.24.0, which cannot be installed (as previously explained);
          bioconductor-genomeinfodb [1.26.0|1.26.4], which cannot be installed (as previously explained);
          bioconductor-genomeinfodb 1.28.0, which cannot be installed (as previously explained);
          bioconductor-genomeinfodb [1.30.0|1.30.1], which cannot be installed (as previously explained);
          bioconductor-genomeinfodb [1.34.1|1.34.8|1.34.9], which cannot be installed (as previously explained);
          bioconductor-genomeinfodb 1.36.1, which cannot be installed (as previously explained);
          bioconductor-genomeinfodb 1.38.1, which cannot be installed (as previously explained);
     bioconductor-variantannotation [1.22.3|1.24.1] would require
       bioconductor-genomeinfodb >=1.11.4 , which cannot be installed (as previously explained);
     bioconductor-variantannotation 1.26.1 would require
       bioconductor-s4vectors >=0.18.3,<0.20.0 , which conflicts with any installable versions previously reported;
     bioconductor-variantannotation 1.28.3 would require
       bioconductor-s4vectors >=0.20.0,<0.21.0 , which conflicts with any installable versions previously reported;
     bioconductor-variantannotation [1.30.0|1.30.1] would require
       bioconductor-s4vectors >=0.22.0,<0.23.0 , which conflicts with any installable versions previously reported;
     bioconductor-variantannotation 1.32.0 would require
       bioconductor-s4vectors >=0.24.0,<0.25.0 , which conflicts with any installable versions previously reported;
     bioconductor-variantannotation 1.34.0 would require
       bioconductor-s4vectors >=0.26.0,<0.27.0 , which conflicts with any installable versions previously reported;
     bioconductor-variantannotation 1.36.0 would require
       bioconductor-s4vectors >=0.28.0,<0.29.0 , which conflicts with any installable versions previously reported;
     bioconductor-variantannotation 1.38.0 would require
       bioconductor-s4vectors >=0.30.0,<0.31.0 , which conflicts with any installable versions previously reported;
     bioconductor-variantannotation 1.40.0 would require
       bioconductor-s4vectors >=0.32.0,<0.33.0 , which conflicts with any installable versions previously reported;
     bioconductor-variantannotation 1.44.0 would require
       bioconductor-s4vectors >=0.36.0,<0.37.0 , which conflicts with any installable versions previously reported;
     bioconductor-variantannotation 1.46.0 would require
       bioconductor-s4vectors >=0.38.0,<0.39.0 , which conflicts with any installable versions previously reported;
     bioconductor-variantannotation 1.48.1 would require
        bioconductor-s4vectors >=0.40.0,<0.41.0 , which conflicts with any installable versions previously reported;
   r-base 4.4.*  is requested and can be installed.
# Last 100 lines of the build log.
