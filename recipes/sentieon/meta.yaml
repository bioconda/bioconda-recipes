package:
  name: sentieon
  version: '201603.02'
source:
  fn: sentieon-genomics-201603.02.tar.gz
  url: https://sentieon.sharepoint.com/Release/_layouts/15/guestaccess.aspx?guestaccesstoken=utIi6Vs0CTW5Y7UNVRqIwhoEjlVt5WY7rz4eQeAOR6E%3d&docid=0d49d0040113b4607b1c685d4ea138007 # [linux]
  patches:
    # Fix driver script so it resolves location including symlinks
    - sentieon_symlinks.patch

build:
  number: 1
  skip: True # [not linux]

requirements:
  build:
  run:
    - libgfortran
    - libgcc
    - zlib
test:
  commands:
    # Pre-built binaries require recent glibc (2.7) so won't work on CentOS 5 tests
    #- 'sentieon driver'
    #- 'sentieon-licsrvr'
about:
  home: http://sentieon.com
  license: Commercial (requires license for use)
  summary: Accelerated performance bioinformatics tools for mapping and variant calling
