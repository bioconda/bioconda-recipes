package:
  name: prosit-cpu
  version: "1.0"

source:
  git_url: https://github.com/kusterlab/prosit.git
  git_rev: ce93f3aaed49aab4628223b9025dcaf0512ea767

build:
  noarch: python
  number: 0
  ## script: {{ PYTHON }} -m pip install . --ignore-installed --no-deps -vv

requirements:
  host:
    - python >=3.5
    - pip
    - numpy
    - flask
    - keras
    - pandas
    - pyteomics
    - tensorflow
  run:
    - python >=3.5
    - numpy
    - flask
    - keras
    - pandas
    - pyteomics
    - tensorflow

test:
  commands:
    - echo "Hello JJ"
  ## imports:
  ##   - prosit

about:
  home: https://github.com/kusterlab/prosit
  license: GPLv3
  summary: |
    Prosit is a deep neural network to predict iRT values
    and MS2 spectra for given peptide sequences. 

extra:
  skip-lints:
    - uses_vcs_url
    - missing_hash
