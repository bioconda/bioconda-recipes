{% set name = "snakemake-logger-plugin-snkmt" %}
{% set version = "0.1.6" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/snakemake_logger_plugin_snkmt-{{ version }}.tar.gz
  sha256: deea560361829b5ce4782e6237a76ebcfd54549c1c51d8702c2bd269fcc571ab

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vvv --no-deps --no-build-isolation --no-cache-dir
  number: 0
  run_exports:
    - {{ pin_subpackage(name, max_pin='x') }}

requirements:
  host:
    - python >=3.11,<4.0
    - hatchling
    - pip
  run:
    - snakemake-interface-logger-plugins >=2.0.0,<3.0.0
    - sqlalchemy >=2.0.38,<3.0.0
    - snkmt >=0.2.3,<1.0.0

test:
  imports:
    - snakemake_logger_plugin_snkmt

about:
  home: "https://github.com/cademirch/snakemake-logger-plugin-snkmt"
  summary: "Snakemake logger plugin that writes logs to SQLite database."
  license: MIT
  license_family: MIT
  license_file: LICENSE
  dev_url: "https://github.com/cademirch/snakemake-logger-plugin-snkmt"

extra:
  recipe-maintainers:
    - fgvieira
