{% set name="graphanalyzer" %}
{% set version="1.2.2" %}

package:
  name: {{ name }}
  version: {{ version }}

build:
  noarch: generic 
  number: 0

source:
  url: https://github.com/lazzarigioele/{{ name }}/archive/v{{ version }}.tar.gz
  sha256: 50977cbb17c7760c326d2884df389d5b3c4bf4e0b4561e0098d59bff4963d055

requirements:
  run:
    - python >=3,<3.9
    - pandas >=1.0
    - networkx
    - hvplot
    - holoviews
    - pygraphviz >=1.6,<=1.7
    - openpyxl

test:
  commands:
    - graphanalyzer.py --version | grep {{ version }}

about:
  home: https://github.com/lazzarigioele/graphanalyzer
  license: 'GPLv3'
  summary: 'A tool to automatically interpret the outputs generated by vConTACT2 when using the INPHARED database'

extra:
  recipe-maintainers:
    - telatin
