{% set name = "alignoth" %}
{% set version = "1.9.0" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://github.com/alignoth/alignoth/archive/v{{ version }}.tar.gz
  sha256: be45d3dfbaa501242967acf33f001ffb3b99c30c1aeb74d5c4a629deb280efa9

build:
  number: 0
  run_exports:
    - {{ pin_subpackage('alignoth', max_pin="x.x") }}

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler("cxx") }}
    - {{ compiler('rust') }}
    - {{ stdlib('c') }}
    - cargo-bundle-licenses
    - clangdev <22
    - pkg-config
    - make
    - cmake
  host:
    - libcblas
    - libcurl
    - openssl

test:
  commands:
    - alignoth --help

about:
  home: "https://alignoth.github.io"
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: "A tool for creating alignment plots from bam files."
  dev_url: "https://github.com/alignoth/alignoth"
  doc_url: "https://github.com/alignoth/alignoth/blob/v{{ version }}/README.md"

extra:
  recipe-maintainers:
    - fxwiegand
    - johanneskoester
  additional-platforms:
    - linux-aarch64
    - osx-arm64
