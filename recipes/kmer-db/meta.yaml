{% set name = "kmer-db" %}
{% set version = "2.0.4" %}
{% set sha256 = "1989fa2afd9928976f7821d0fd7377eac963cf665ae44929d5aa1d3a0bb61d8e" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://github.com/refresh-bio/kmer-db/archive/v{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  run_exports:
    - {{ pin_subpackage('kmer-db', max_pin="x") }}

requirements:
  build:
    - make
    - {{ compiler('cxx') }}
  host:
    - zlib

test:
  commands:
    - "kmer-db > /dev/null"

about:
  home: https://github.com/refresh-bio/kmer-db
  summary: "Kmer-db is a fast and memory-efficient tool for estimating evolutionary distances."
  license: "GPL-3.0-or-later"
  license_family: GPL3
  license_file: LICENSE
  dev_url: https://github.com/refresh-bio/kmer-db
