--- t/https_proxy.t	2018-08-08 09:48:17.030816864 +0200
+++ t/https_proxy.t	2018-08-08 09:48:38.494596410 +0200
@@ -38,7 +38,7 @@
 	LocalPort => 0, # let system pick port
 	Listen => 10
     ) or die "failed to create INET listener";
-    my $saddr = $server->sockhost.':'.$server->sockport;
+    my $saddr = '127.0.0.1:'.$server->sockport;
     $server[$i] = $server;
     $saddr[$i]  = $saddr;
 }
