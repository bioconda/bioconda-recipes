{% set name = "seqLogo" %}
{% set version = "0.0.5" %}
{% set sha256 = "28a16d1531ba9736f64b63d9ed6323e91904a0064c74c6d86212c824032df5b0" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  skip: True # [win32]
  skip: True # [win64]
  skip: True # [py<34]
  number: 0
  script: "{{ PYTHON }} -m pip install . --no-deps --ignore-installed -vv "

requirements:
  build:
    - numpy
    - pandas
    - pip
    - python
    - weblogo
  run:
    - numpy
    - pandas
    - python
    - weblogo

test:
  imports:
    - seqLogo

about:
  home: https://github.com/betteridiot/seqLogo
  license: BSD
  license_family: BSD
  license_file: 
  summary: Python port of the R Bioconductor `seqLogo` package

extra:
  recipe-maintainers:
      - betteridiot
