{% set name = "ipyrad" %}
{% set version = "0.9.34" %}
{% set sha256 = "a40c018f157db35550fad98aa052ee569b37ceff876e55b07b0dd1772dd9c730" %}

package:
  name: {{name}}
  version: {{version}}

source:
  url: https://github.com/dereneaton/{{name}}/archive/{{version}}.tar.gz
  sha256: {{sha256}}

build:
  number: 0
  script: "touch iprad/plotting/__init__.py && {{ PYTHON }} -m pip install . --no-deps --ignore-installed -vv "
  noarch: python  
  entry_points:
    - ipyrad = ipyrad.__main__:main

requirements:
  host:
    - pip
    - python
  run:
    - python
    - pandas
    - scipy
    - numpy
    - numba >=0.37
    - future
    - h5py
    - ipyparallel >=6.0.2
    - requests
    - cutadapt
    - pysam >=0.15
    - mpi4py >=3.0 # [ py >= 37 ]
    - notebook
    - bedtools
    - muscle
    - vsearch >=2.13
    - bwa
    - samtools

test:
  imports:
    - ipyrad
    - ipyrad.analysis
  commands:
    - ipyrad -h

about:
  home: http://github.com/dereneaton/ipyrad
  license: GPL3
  license_file: LICENSE.txt
  summary: Interactive assembly and analysis of RAD-seq data sets.
