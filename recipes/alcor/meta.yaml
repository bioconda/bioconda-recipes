# This file created by conda-build 3.23.3
# meta.yaml template
# ------------------------------------------------
{% set version = "1.0" %}

package:
  name: alcor
  version: {{ version }}

source:
  url: https://github.com/cobilab/alcor/archive/v{{ version }}.tar.gz
  sha256: 1748cb8064acc739e9ab8e6f1b5253557b74b6d8552ae4b1dd59c6b82ae01736

build:
  number: 0
  noarch: generic

rrequirements:
   build:
     - cmake
   host:
     - gxx_linux-64:
         platform: linux
     - clang:
         platform: osx
   run:
     - gxx_linux-64:
         platform: linux
     - clang:
         platform: osx

test:
  commands:
    - alcor --help  # Test that alcor can be run with the --help flag

about:
  home: https://cobilab.github.io/alcor/
  license: GPL v3 License
  license_file: LICENSE
  license_family: GPL
  summary: Software for alignment-free simulation, mapping, and visualization of low-complexity regions in FASTA data.