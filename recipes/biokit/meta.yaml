{% set version = "0.4.3" %}

package:
  name: biokit
  version: {{ version }}

source:
  fn: biokit-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/b/biokit/biokit-{{ version }}.tar.gz
  sha256: f91eaa7b4eb6ebb0be93a5e8c1a03ec113ff195d86e311a942365c9e0a3b68e8

build:
  skip: True # [py34]
  number: 0

# Set xmltodict >= 0.10.2 to use conda-forge version instead of bioconda
# Same with easydev
requirements:
  host:
    - python
    - setuptools
    - easydev >=0.9.34
    - pandas
    - colormap
    - scipy
    - xmltodict
    - numpydoc
    - bioservices
    - mesalib
    - biopython
    - pyyaml
    - colorlog

  run:
    - python
    - easydev >=0.9.34
    - pandas
    - bioservices >=1.4.16
    - colormap
    - scipy
    - xmltodict
    - numpydoc
    - bioservices
    - matplotlib
    - libgcc
    - mesalib
    #- xz
    #- libxcb
    - biopython
    - pyyaml
    - colorlog

# Could not manage to import it without an libXext issue from conda-forge ? and
# matplotlib v1.5.3 or v2.0.0. Hopefully, this will be resolved in the future
test:
  imports:
    - biokit

about:
  home: http://pypi.python.org/pypi/biokit
  license: BSD
  summary: 'Set of visualisation and analysis tools for biological data sets'

extra:
  container:
    # needs libGL.so.1
    extended-base: true
