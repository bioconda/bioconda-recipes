package:
  name: manormfast
  version: "0.1.0"

source:
  fn: MAnormFast-0.1.0.tar.gz
  url: https://pypi.python.org/packages/8f/7b/40de2b7e72ee943ede8f7f911988ae37155c39e81ae308ceaef855b40863/MAnormFast-0.1.0.tar.gz
  md5: 295cb8ee9c8967f481f6d5f176e8cfb6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - manormfast = manormfast:main
    #
    # Would create an entry point called manormfast that calls manormfast.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - MAnormFast

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/semal
  license: GNU General Public License
  summary: 'MAnorm version, Fast but more memory'
  license_family: GPL

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
