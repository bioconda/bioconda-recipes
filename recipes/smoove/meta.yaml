{% set version = "0.2.8" %}
{% set sha256 = "4e0624097f8b2b9a152caa51947659d370ef8311621582abd23a77b733ba3b3b" %}

package:
  name: smoove
  version: {{ version }}

source:
  url: https://github.com/brentp/smoove/releases/download/v{{ version }}/smoove
  sha256: {{ sha256 }}
  patches:
    - 0001-smoove.patch

build:
  number: 2
  run_exports:
    - {{ pin_subpackage('smoove', max_pin="x.x") }}

requirements:
  build:
    - {{ compiler('go') }}
    - go-licenses
  run:
    - bcftools
    - duphold  # [linux and x86_64]
    - gsort  # [linux and x86_64]
    - htslib
    - lumpy-sv >=0.3
    - mosdepth
    - samtools
    - svtools
    - svtyper

test:
  commands:
    - smoove -h 2>&1 | grep smoove\ version
    - smoove call -h

about:
  home: "https://github.com/brentp/smoove"
  license: "Apache-2.0"
  license_family: APACHE
  license_file: LICENSE
  summary: "Structural variant calling and genotyping with existing tools, but, smoothly."
  dev_url: "https://github.com/brentp/smoove"
  doc_url: "https://github.com/brentp/smoove/blob/v{{ version }}/README.md"

extra:
  additional-platforms:
    - linux-aarch64
    - osx-arm64
