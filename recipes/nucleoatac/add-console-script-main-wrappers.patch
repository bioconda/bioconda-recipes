--- a/nucleoatac/cli.py
+++ b/nucleoatac/cli.py
@@ -262,5 +262,16 @@ def add_run_parser( subparsers):
     group3.add_argument('--write_all', action="store_true", default = False,
                     help="write all tracks")
     return
+
+
 
 
+def main():
+    """Console-script entry point."""
+    parser = nucleoatac_parser()
+    args = parser.parse_args()
+    if getattr(args, "call", None) is None:
+        parser.print_help()
+        return 1
+    nucleoatac_main(args)
+    return 0
--- a/pyatac/cli.py
+++ b/pyatac/cli.py
@@ -354,7 +354,18 @@ def add_cov_parser( subparsers):
                          help = "scaling value.  default is 10, corresponding to signal corresponding to # of fragment centers  per 10 bp. Use 1 for fragments per 1 bp.")
     group3.add_argument('--not_atac', action="store_false", dest = "atac", default = True, help = "Don't use atac offsets")
     return
+
+
 
 
 
 
+def main():
+    """Console-script entry point."""
+    parser = pyatac_parser()
+    args = parser.parse_args()
+    if getattr(args, "call", None) is None:
+        parser.print_help()
+        return 1
+    pyatac_main(args)
+    return 0
