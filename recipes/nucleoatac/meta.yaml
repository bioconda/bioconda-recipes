package:
  name: nucleoatac
  version: '0.3.1'

source:
  fn: v0.3.1.tar.gz
  url: https://github.com/GreenleafLab/NucleoATAC/archive/v0.3.1.tar.gz

build:
  number: 0

requirements:
  build:
    - python
    - setuptools
    - cython
    - numpy >=1.8.0
    - scipy >=0.15.0
    - matplotlib >=1.4.0
    - pysam >=0.8.2
  run:
    - python
    - pybigwig >=0.2.3
    - numpy >=1.8.0
    - scipy >=0.15.0
    - matplotlib >=1.4.0
    - pysam >=0.8.2
    - cython
