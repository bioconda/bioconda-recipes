---
package:
  name: pedesigner
  version: "0.2.0"

source:
  git_rev: v0.2.0
  git_url: https://github.com/VeredKunik/pedesigner.git

build:
  noarch: python
  number: 0

requirements:
  build:
    - python >=3.7

  run:
    - python >=3.7
    - cas-offinder

test:
  imports:
    - pedesigner
  commands:
    - pedesigner --help

about:
  home: https://github.com/VeredKunik/pedesigner
  license: GNU General Public License v3 (GPLv3)
  license_file: LICENSE.md
  summary: "A tool for prime-editing guideRNA (pegRNA) design"
