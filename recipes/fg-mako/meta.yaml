{% set name = "fg-mako" %}
{% set version = "0.1.7" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://github.com/fg-labs/mako/archive/v{{ version }}.tar.gz
  sha256: 1e41d115708d8bc43b6e938719f86668398ce0f0a98aa03446278c92984e7a4c

build:
  number: 0
  run_exports:
    - {{ pin_subpackage(name, max_pin="x.x") }}
  ignore_run_exports_from:
    - libdeflate
    - libgit2
    - zlib

requirements:
  build:
    - {{ compiler('c') }}
    - {{ stdlib('c') }}
    - {{ compiler('rust') }}
    - cargo-bundle-licenses
    - pkg-config
    - make
  host:
    - libdeflate
    - libgit2
    - zlib

test:
  commands:
    - mako --version

about:
  home: https://github.com/fg-labs/mako
  license: MIT
  license_family: MIT
  license_file:
    - LICENSE
    - THIRDPARTY.yml
  summary: Fast SAM/BAM sorter (installs the `mako` binary).
  dev_url: https://github.com/fg-labs/mako
  doc_url: https://docs.rs/fg-mako

extra:
  additional-platforms:
    - linux-aarch64
    - osx-arm64
  recipe-maintainers:
    - nh13
