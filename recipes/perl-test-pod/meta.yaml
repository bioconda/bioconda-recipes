package:
  name: perl-test-pod
  version: "1.51"

source:
  fn: Test-Pod-1.51.tar.gz
  url: https://cpan.metacpan.org/authors/id/E/ET/ETHER/Test-Pod-1.51.tar.gz
  md5: f806aa84de2f0c0fba48b3a5a8a4aecf

build:
  number: 0

requirements:
  build:
    - perl-threaded

  run:
    - perl-threaded

test:
  # Perl 'use' tests
  imports:
    - Test::Pod

about:
  home: http://search.cpan.org/dist/Test-Pod/
  license: perl_5
  summary: 'check for POD errors in files'
