{% set name = "perl-io-zlib" %}
{% set version = "1.13" %}
{% set sha256 = "3e8a6623bef7c498f9192bb2aad3e85a8f141cb9e5a86a65a631ac90989669a5" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://cpan.metacpan.org/authors/id/T/TO/TOMHUGHES/IO-Zlib-1.13.tar.gz
  sha256: {{ sha256 }}

build:
  noarch: generic
  number: 0

requirements:
  build:
    - make
    - perl

  run:
    - perl

test:
  # Perl 'use' tests
  imports:
    - IO::Zlib

about:
  home: http://metacpan.org/pod/IO::Zlib
  license: unknown
  summary: 'IO:: style interface to Compress::Zlib'
