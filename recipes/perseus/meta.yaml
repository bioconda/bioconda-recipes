{% set name = "Perseus" %}
{% set version = "1.1.3" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: https://github.com/matnguyen/Perseus/archive/refs/tags/v{{ version }}.tar.gz
  sha256: d113ab67c9d53fa3e7c0e4329403124c219cc51d06abe39546f0517a4e3fdb1f

build:
  number: 0
  noarch: python
  run_exports:
    - {{ pin_subpackage(name|lower, max_pin="x") }}
  script: |
    {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python >=3.10,<3.13
    - pip
    - setuptools
    - wheel
  run:
    - python >=3.10,<3.13
    - numpy
    - pandas
    - pytorch
    - scikit-learn
    - ete3
    - pyarrow
    - alive-progress

test:
  commands:
    - perseus -h

about:
  home: https://github.com/matnguyen/Perseus
  summary: "Perseus: Lineage-aware Kraken2 refinement for long-read metagenomics"
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - matnguyen