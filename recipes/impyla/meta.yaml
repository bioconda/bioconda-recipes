package:
  name: impyla
  version: "0.13.7"

source:
  fn: impyla-0.13.7.tar.gz
  url: https://pypi.python.org/packages/d3/23/c64eeade2f7997161e490565baa7ccfc37ae314cf6207e3585fe5c9c72e7/impyla-0.13.7.tar.gz
  md5: 331f581a45d48b0482b62d5c67005f8e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # noarch_python: True
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - impyla = impyla:main
    #
    # Would create an entry point called impyla that calls impyla.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  number: 1
  skip: False

requirements:
  build:
    - python
    - setuptools
    - six
    - bitarray
    - thriftpy >=0.3.5

  run:
    - python
    - setuptools
    - six
    - bitarray
    - thriftpy >=0.3.5

test:
  # Python imports
  imports:
    - impala
    - impala.tests
    - impala.thrift

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cloudera/impyla
  license: Apache License, Version 2.0
  summary: 'Python client for the Impala distributed query engine'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
