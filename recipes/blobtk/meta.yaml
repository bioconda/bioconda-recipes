{% set name = "blobtk" %}
{% set version = "0.8.1" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://github.com/genomehubs/blobtk/archive/{{ version }}.tar.gz
  sha256: 9732f3e47d5a6d51cc4f9cdaabea533cd915fbec7cf8c63a94adf7aadb34c068

build:
  number: 0
  run_exports:
    - {{ pin_subpackage('blobtk', max_pin="x.x") }}

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - {{ compiler('rust') }}
    - {{ stdlib('c') }}
    - cmake
    - make
    - clangdev
    - llvmlite
    - pkg-config
  host:
    - python
    - pip
    - maturin
    - curl
    - wget
    - openssl
    - zlib
  run:
    - python

test:
  imports:
    - blobtk
  commands:
    - blobtk --help

about:
  home: "https://github.com/genomehubs/blobtk"
  summary: "Core functionality shared across BlobToolKit tools."
  license: MIT
  license_family: MIT
  license_file: LICENSE
  dev_url: "https://github.com/genomehubs/blobtk"
  doc_url: "https://github.com/genomehubs/blobtk/wiki"

extra:
  additional-platforms:
    - linux-aarch64
    - osx-arm64
