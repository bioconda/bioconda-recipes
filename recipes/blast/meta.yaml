{% set version = "2.10.0" %}
{% set sha256 = "3acdd9cec01c4f43e56aeaf89049cb8f8013d60b9c1705eced10166967f1d926" %}

package:
  name: blast
  version: {{ version }}

source:
  url: https://ftp.ncbi.nlm.nih.gov/blast/executables/blast+/{{ version }}/ncbi-blast-{{ version }}+-src.tar.gz
  sha256: {{ sha256 }}

build:
  number: 1

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
  host:
    - boost
    - zlib
    - bzip2
    - gnutls
    - gmp
    - nettle 3.4
    - pcre
  run:
    - boost
    - zlib
    - bzip2
    - gnutls
    - gmp
    - nettle 3.4
    - pcre
    - perl
    - perl-list-moreutils
    - perl-archive-tar
    - perl-json

test:
  files:
    - test.fa
    - testdatabase.fa
  commands:
    # (see run_tests.sh for remainder of tests)
    - blastn -help
    - blastp -help
    - makeblastdb -help
    - update_blastdb.pl --help

about:
  home: http://blast.ncbi.nlm.nih.gov/Blast.cgi?PAGE_TYPE=BlastDocs
  license: Public Domain
  summary: BLAST+ is a new suite of BLAST tools that utilizes the NCBI C++ Toolkit.

extra:
  identifiers:
    - biotools:blast
    - doi:10.1016/S0022-2836(05)80360-2
