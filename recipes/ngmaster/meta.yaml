{% set name = "ngmaster" %}
{% set version = "2.0.0" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: "https://github.com/MDU-PHL/ngmaster/archive/refs/tags/v{{ version }}.tar.gz"
  sha256: 1215140b5ea060b6274d40b87ae3e38cbf608174067c762a3bfba383f1687487

build:
  number: 0
  noarch: python
  entry_points:
    - ngmaster=ngmaster.run_ngmaster:main
  script: "{{ PYTHON }} -m pip install . -vvv --no-deps --no-build-isolation --no-cache-dir"
  run_exports:
    - {{ pin_subpackage('ngmaster', max_pin="x") }}

requirements:
  host:
    - python >=3.12
    - pip
    - setuptools
  run:
    - python >=3.12
    - biopython
    - mlst
    - requests
    - bs4
    - mlstdb ==1.0.0

test:
  imports:
    - ngmaster
  commands:
    - ngmaster --help
    - ngmaster --test

about:
  home: "https://github.com/MDU-PHL/ngmaster"
  license: "GPL-3.0-only"
  license_family: "GPL3"
  license_file: "LICENSE"
  summary: "In silico multi-antigen sequence typing for N. gonorrhoeae (NG-MAST and NG-STAR)."
  doc_url: "https://github.com/MDU-PHL/ngmaster/blob/master/README.md"
  dev_url: "https://github.com/MDU-PHL/ngmaster"

extra:
  recipe-maintainers:
    - stroehleina
    - kristyhoran
