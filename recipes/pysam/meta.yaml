package:
  name: pysam
  version: 0.9.0

source:
  fn: v0.9.0.tar.gz
  url: https://github.com/pysam-developers/pysam/archive/v0.9.0.tar.gz

build:
  number: 0

requirements:
  build:
    - python
    - setuptools
    - cython

  run:
    - python

test:
  imports:
    - pysam

about:
  home: https://github.com/pysam-developers/pysam
  license: MIT
  summary: Pysam is a python module for reading and manipulating Samfiles. It's a lightweight wrapper of the samtools C-API. Pysam also includes an interface for tabix.
