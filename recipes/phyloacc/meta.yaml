{% set name = "PhyloAcc" %}
{% set version = "2.1.0" %}
{% set sha256 = "e4ed79c6984b878807fb2f7b0498af179b55e5b1d95b5f8cdb11e46ecb46f0c4" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source: 
  url: https://github.com/phyloacc/PhyloAcc/archive/refs/tags/v{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  skip: True # [py < 38]

requirements:
  build:
    - {{ compiler('cxx') }}
    - make
    - libgomp  # [linux]
    - llvm-openmp  # [osx]
  host:
    - gsl
    - openblas
    - blis
    - liblapack
    - armadillo
    - python
  run:
    - python
    - blis
    - matplotlib-base >=3.5
    - biopython >=1.79
    - numpy >=1.22
    - snakemake-minimal >=7.3

test:
  commands:
    - phyloacc.py --version
    - phyloacc.py --depcheck
    - phyloacc_post.py -h
# Test commands to run after building and installing

about:
  home: https://phyloacc.github.io/
  license: GNU GPLv3 
  license_family: GPL
  license_file: LICENSE.md
  summary: 'Bayesian estimation of substitution rate shifts in non-coding regions'
  description: 'Bayesian estimation of substitution rate shifts in non-coding regions'
  doc_url: https://phyloacc.github.io/
  dev_url: https://github.com/phyloacc/PhyloAcc
# Package meta info
# URLs just point to the github page until we update the docs

extra:
  recipe-maintainers:
    - gwct
# Extra info
