{% set name = "snipe" %}
{% set version = "0.1.2" %}

package:
  name: {{ name|lower }}
  version: "{{ version }}"

source:
  url: https://github.com/snipe-bio/snipe/archive/refs/tags/v{{ version }}.tar.gz
  sha256: f2db889bf477920b837b63297e0e7390cec913314ee32578a672361a8523e7e1


entry_points:
  - snipe = snipe.cli.main:cli

build:
  number: 0
  script: "{{ PYTHON }} -m pip install . --no-deps -vv"

requirements:
  host:
    - python <3.12
    - pip
    - hatchling
  run:
    - python >=3.7,<3.12
    - requests >=2.25.1
    - click >=8.0.0
    - pytest
    - numpy >=1.22,<2.0
    - pathos
    - pandas
    - tqdm
    - lzstring
    - rapidfuzz
    - zlib
    - pyfastx >=2.1.0,<3.0a0
    - sourmash >=4.8.11

test:
  imports:
    - snipe

about:
  home: "https://github.com/snipe-bio/snipe"
  summary: "SRA-Scale sequence QC and analysis"
  license: "AGPL-3.0-or-later"
  license_family: "AGPL"
  license_file: "LICENSE.txt"

extra:
  recipe-maintainers:
    - mr-eyes
  authors:
    - mr-eyes
    - drtamermanosur
  channels:
    - conda-forge
    - bioconda
    - defaults
