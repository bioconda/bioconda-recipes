{% set name = "genotopep" %}
{% set version = "0.2.0.1" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 81a35218f21a6285d4304d6ec7e4ea76a16d220e122fab8de1971e711dd609ed

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . --no-deps --no-build-isolation --no-cache-dir -vvv
  run_exports:
    - {{ pin_subpackage(name, max_pin="x.x") }}

requirements:
  host:
    - python >=3.10
    - pip
    - hatchling >=1.25
  run:
    - python >=3.10
    - pyrodigal >=3.6,<4
    - pyyaml >=6,<7

test:
  imports:
    - genotopep
  commands:
    - genotopep --version
    - genotopep --help

about:
  home: https://github.com/ljunwon1114/GenoToPep
  license: GPL-3.0-or-later
  license_family: GPL3
  license_file: LICENSE
  summary: Reproducible peptide candidate generation from genome and metagenome assemblies or protein FASTA files.
  dev_url: https://github.com/ljunwon1114/GenoToPep

extra:
  recipe-maintainers:
    - ljunwon1114
