package:
  name: perl-algorithm-cluster
  version: "1.52"

source:
  fn: Algorithm-Cluster-1.52.tar.gz
  url: http://cpan.metacpan.org/authors/id/M/MD/MDEHOON/Algorithm-Cluster-1.52.tar.gz
  md5: 8a80e644e705c829e02a9339025c4225
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - perl
    - perl-extutils-makemaker

  run:
    - perl

test:
  # Perl 'use' tests
  imports:
    - Algorithm::Cluster
    - Algorithm::Cluster::Record

  # You can also put a file called run_test.pl (or run_test.py) in the recipe
  # that will be run at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://metacpan.org/pod/Algorithm::Cluster
  license: unknown
  summary: 'Perl interface to the C Clustering Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
