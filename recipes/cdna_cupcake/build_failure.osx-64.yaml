recipe_sha: c83817f5ddeb0f0845dce7a77cf737b84cfbc02821b844f69920dcb5ab0ac851  # The commit at which this recipe failed to build.
skiplist: true # Set to true to skiplist this recipe so that it will be ignored as long as its latest commit is the one given above.
log: |-
  positional arguments:
    filename              Input fasta/fastq filename, can be .gz

  optional arguments:
    -h, --help            show this help message and exit
    -b BINWIDTH, --binwidth BINWIDTH
                          Bin width, in bp (default: 1000 bp)
   revcomp.py -h
  d-
   fa2fq.py -h
  usage: Convert fasta to fastq [-h] fasta_filename

  positional arguments:
    fasta_filename  input fasta (must end with .fasta or .fa)

  optional arguments:
    -h, --help      show this help message and exit
   fq2fa.py -h
  usage: Convert fastq to fasta [-h] fastq_filename

  positional arguments:
    fastq_filename  input fastq (must end with .fastq or .fq)

  optional arguments:
    -h, --help      show this help message and exit
   get_seqs_from_list.py -h
  usage: Get sequences from a fasta/fastq file from a list [-h] [--partial]
                                                           [--exclude]
                                                           fasta_filename
                                                           list_filename

  positional arguments:
    fasta_filename  Input fasta/fastq filename to extract sequences from
    list_filename   List of sequence IDs to extract

  optional arguments:
    -h, --help      show this help message and exit
    --partial       OK if seq IDs only match the beginning
    --exclude       Output sequences NOT in the list, default OFF
   err_correct_w_genome.py -h
  usage: Generate sequences using genome bases and SAM alignment file
         [-h] genome_file sam_file output_file

  positional arguments:
    genome_file  Genome Fasta File
    sam_file     GMAP SAM File
    output_file  Output Fasta File

  optional arguments:
    -h, --help   show this help message and exit
   sam_to_bam.py -h
   grep 'Only accepts'
  Only accepts files ending in .sam. Abort!
   sam_to_gff3.py -h
  usage: Convert SAM to GFF3 format using BCBio GFF [-h] [-i INPUT_FASTA] -s
                                                    SOURCE
                                                    sam_filename

  positional arguments:
    sam_filename

  optional arguments:
    -h, --help            show this help message and exit
    -i INPUT_FASTA, --input_fasta INPUT_FASTA
                          (Optional) input fasta. If given, coverage will be
                          calculated.
    -s SOURCE, --source SOURCE
                          source name (ex: hg38, mm10)
   simulate.py -h
  usage: Simple error simulation [-h] [--copy COPY] [--ins INS] [--dele DELE]
                                 [--sub SUB]
                                 fasta_filename output_prefix

  positional arguments:
    fasta_filename
    output_prefix

  optional arguments:
    -h, --help            show this help message and exit
    --copy COPY           Number of copies to simulate per input sequence
                          (default: 1)
    --ins INS, -i INS     Insert error rate [0-1] (default: 0)
    --dele DELE, -d DELE  Deletion error rate [0-1] (default: 0)
    --sub SUB, -s SUB     Substitution error rate [0-1] (default: 0)
   collapse_isoforms_by_sam.py -h
  Traceback (most recent call last):
    File "/opt/mambaforge/envs/bioconda/conda-bld/cdna_cupcake_1685530075556/_test_env_placehold_placehold_placehold_placehold_placehold_placehold_placehold_placehold_placehold_placehold_placehold_placehold_placehold_placehold_placehold_placehold_placehold_placeh/bin/collapse_isoforms_by_sam.py", line 31, in <module>
      from cupcake.tofu.branch import branch_simple2
    File "/opt/mambaforge/envs/bioconda/conda-bld/cdna_cupcake_1685530075556/_test_env_placehold_placehold_placehold_placehold_placehold_placehold_placehold_placehold_placehold_placehold_placehold_placehold_placehold_placehold_placehold_placehold_placehold_placeh/lib/python3.9/site-packages/cupcake/tofu/branch/branch_simple2.py", line 6, in <module>
      import cupcake.tofu.branch.c_branch as c_branch
    File "cupcake/tofu/branch/c_branch.pyx", line 6, in init cupcake.tofu.branch.c_branch
    File "/opt/mambaforge/envs/bioconda/conda-bld/cdna_cupcake_1685530075556/_test_env_placehold_placehold_placehold_placehold_placehold_placehold_placehold_placehold_placehold_placehold_placehold_placehold_placehold_placehold_placehold_placehold_placehold_placeh/lib/python3.9/site-packages/numpy/__init__.py", line 305, in __getattr__
      raise AttributeError(__former_attrs__[attr])
  AttributeError: module 'numpy' has no attribute 'int'.
  np.int was a deprecated alias for the builtin int. To avoid this error in existing code, use int by itself. Doing this will not modify any behavior and is safe. When replacing np.int, you may wish to use e.g. np.int64 or np.int32 to specify the precision. If you wish to review your current use, check the release note link for additional information.
  The aliases was originally deprecated in NumPy 1.20; for more details and guidance see the original release note at:
      https://numpy.org/devdocs/release/1.20.0-notes.html#deprecations
  Tests failed for cdna_cupcake-29.0.0-py39he55f33c_1.tar.bz2 - moving package to /opt/mambaforge/envs/bioconda/conda-bld/broken
  WARNING:conda_build.build:Tests failed for cdna_cupcake-29.0.0-py39he55f33c_1.tar.bz2 - moving package to /opt/mambaforge/envs/bioconda/conda-bld/broken
  TESTS FAILED: cdna_cupcake-29.0.0-py39he55f33c_1.tar.bz2
# Last 100 lines of the build log.
