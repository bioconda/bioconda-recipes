recipe_sha: 0d557f8183f470193798b8b3599fc854446ee148c258bb008e95d6e5314354fb  # The hash of the recipe's meta.yaml at which this recipe failed to build.
skiplist: true # Set to true to skiplist this recipe so that it will be ignored as long as its latest commit is the one given above.
log: |2-
         numpy [2.0.0|2.0.1|...|2.4.3], which can be installed;
         numpy [2.3.2|2.3.3|...|2.4.3], which can be installed (as previously explained);
         numpy [2.3.2|2.3.3|...|2.4.3], which can be installed (as previously explained);
       python_abi =3.11 *_cp311, which conflicts with any installable versions previously reported;
     matplotlib-base 3.8.4 would require
       numpy >=1.21,<2 *, which can be installed;
       python_abi =3.12 *_cp312, which conflicts with any installable versions previously reported;
     matplotlib-base 3.8.4 would require
       numpy >=1.21 * with the potential options
         numpy [1.23.4|1.23.5|...|1.25.2], which can be installed;
         numpy [1.26.0|1.26.2|1.26.3], which can be installed;
         numpy 1.26.4, which can be installed;
         numpy [1.21.4|1.21.5], which can be installed;
         numpy [1.21.6|1.22.0|1.22.1|1.22.2|1.22.3], which can be installed;
         numpy 1.22.4, which can be installed;
         numpy [1.23.0|1.23.1|1.23.2|1.23.3], which can be installed;
         numpy [1.21.0|1.21.1|1.21.2|1.21.3], which can be installed;
         numpy [2.0.0|2.0.1|...|2.4.3], which can be installed;
         numpy [2.3.2|2.3.3|...|2.4.3], which can be installed (as previously explained);
         numpy [2.3.2|2.3.3|...|2.4.3], which can be installed (as previously explained);
       python_abi =3.12 *_cp312, which conflicts with any installable versions previously reported;
     matplotlib-base 3.8.4 would require
       numpy >=1.21,<2 *, which can be installed;
       python_abi =3.9 *_cp39, which conflicts with any installable versions previously reported;
     matplotlib-base 3.8.4 would require
       numpy >=1.21 * with the potential options
         numpy [1.23.4|1.23.5|...|1.25.2], which can be installed;
         numpy [1.26.0|1.26.2|1.26.3], which can be installed;
         numpy 1.26.4, which can be installed;
         numpy [1.21.4|1.21.5], which can be installed;
         numpy [1.21.6|1.22.0|1.22.1|1.22.2|1.22.3], which can be installed;
         numpy 1.22.4, which can be installed;
         numpy [1.23.0|1.23.1|1.23.2|1.23.3], which can be installed;
         numpy [1.21.0|1.21.1|1.21.2|1.21.3], which can be installed;
         numpy [2.0.0|2.0.1|...|2.4.3], which can be installed;
         numpy [2.3.2|2.3.3|...|2.4.3], which can be installed (as previously explained);
         numpy [2.3.2|2.3.3|...|2.4.3], which can be installed (as previously explained);
       python_abi =3.9 *_cp39, which conflicts with any installable versions previously reported;
     matplotlib-base [3.9.1|3.9.2|3.9.3|3.9.4] would require
       numpy >=1.23 * with the potential options
         numpy [1.23.4|1.23.5|...|1.25.2], which can be installed;
         numpy [1.26.0|1.26.2|1.26.3], which can be installed;
         numpy 1.26.4, which can be installed;
         numpy [1.23.0|1.23.1|1.23.2|1.23.3], which can be installed;
         numpy [2.0.0|2.0.1|...|2.4.3], which can be installed;
         numpy [2.3.2|2.3.3|...|2.4.3], which can be installed (as previously explained);
         numpy [2.3.2|2.3.3|...|2.4.3], which can be installed (as previously explained);
       python_abi =3.9 *_cp39, which conflicts with any installable versions previously reported;
     matplotlib-base [2.1.2|2.2.3|...|3.2.0] would require
       python_abi =* *_cp36m, which can be installed;
     matplotlib-base [2.1.2|2.2.3|...|3.2.0] would require
       python_abi =* *_cp37m, which can be installed;
     matplotlib-base [2.2.4|3.1.1|3.1.2|3.1.3|3.2.0] would require
       python_abi =* *_cp38, which can be installed;
     matplotlib-base 2.2.5 would require
       python >=2.7,<2.8.0a0 *, which can be installed;
     matplotlib-base [2.2.5|3.2.1|...|3.3.4] would require
       python >=3.6,<3.7.0a0 *, which can be installed;
     matplotlib-base [2.2.5|3.2.1|...|3.5.3] would require
       python >=3.7,<3.8.0a0 *, which can be installed;
     matplotlib-base [2.2.5|3.2.1|...|3.5.3] would require
       python >=3.8,<3.9.0a0 *, which can be installed;
     matplotlib-base [2.2.5|3.3.2|...|3.5.3] would require
       python >=3.9,<3.10.0a0 *, which can be installed;
     matplotlib-base [3.2.2|3.3.0|...|3.3.4] would require
       python_abi ==3.6 *_pypy36_pp73, which requires
          python =3.6 *_73_pypy, which can be installed;
     matplotlib-base [3.3.3|3.3.4|...|3.5.2] would require
       python_abi ==3.7 *_pypy37_pp73, which requires
          python =3.7 *_73_pypy, which can be installed;
     matplotlib-base [3.4.3|3.5.0|3.5.1] would require
       numpy >=1.21.4,<2.0a0 *, which can be installed;
       python_abi =3.10 *_cp310, which conflicts with any installable versions previously reported;
     matplotlib-base [3.5.2|3.5.3] would require
       python_abi ==3.8 *_pypy38_pp73, which cannot be installed (as previously explained);
     matplotlib-base [3.5.2|3.5.3] would require
       python_abi ==3.9 *_pypy39_pp73, which cannot be installed (as previously explained);
     matplotlib-base [3.6.1|3.6.2] would require
        numpy >=1.23.4,<2.0a0 *, which can be installed;
        python_abi =3.11 *_cp311, which conflicts with any installable versions previously reported;
   numpy >=1.7,<1.20 * is installable with the potential options
     numpy [1.14.6|1.16.5|1.17.3|1.18.1] would require
       python_abi =* *_cp38, which can be installed;
     numpy [1.10.4|1.11.0|...|1.9.3] would require
       python_abi =* *_cp27m, which can be installed;
     numpy [1.10.4|1.11.0|...|1.9.3] would require
       python_abi =* *_cp34m, which can be installed;
     numpy [1.10.4|1.11.0|...|1.9.3] would require
       python_abi =* *_cp35m, which can be installed;
     numpy [1.10.4|1.11.3|...|1.9.3] would require
       python_abi =* *_cp36m, which can be installed;
     numpy [1.11.3|1.14.5|...|1.9.3] would require
       python_abi =* *_cp37m, which can be installed;
     numpy 1.14.5 would require
       libgfortran-ng >=3.0 *, which does not exist (perhaps a missing channel);
     numpy [1.16.5|1.16.6|...|1.18.5] conflicts with any installable versions previously reported;
     numpy [1.19.0|1.19.1|1.19.2|1.19.4|1.19.5] conflicts with any installable versions previously reported;
     numpy 1.19.5, which cannot be installed (as previously explained);
   python =3.13 *_cp313 is not installable because it requires
      python_abi =3.13 *_cp313, which conflicts with any installable versions previously reported.
# Last 100 lines of the build log.
