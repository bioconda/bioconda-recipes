{% set name = "perl-crypt-openssl-rsa" %}
{% set version = "0.39" %}
{% set sha256 = "2589eb4a46f9d30c2a4757969702b22d53cb7f7190d73603fb10eba7870b62f3" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://cpan.metacpan.org/authors/id/T/TI/TIMLEGGE/Crypt-OpenSSL-RSA-0.39.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  run_exports:
    - {{ pin_subpackage(name, max_pin="x.x") }}

requirements:
  build:
    - make
    - {{ compiler('c') }}
    - {{ stdlib('c') }}
  host:
    - perl
    - perl-crypt-openssl-bignum
    - perl-crypt-openssl-guess
    - perl-crypt-openssl-random
    - perl-test-more
    - openssl
  run:
    - perl

test:
  imports:
    - Crypt::OpenSSL::RSA

about:
  home: "https://github.com/cpan-authors/Crypt-OpenSSL-RSA"
  license: "Perl_5"
  summary: "RSA encoding and decoding, using the openSSL libraries."
  dev_url: "https://github.com/cpan-authors/Crypt-OpenSSL-RSA"
  doc_url: "https://metacpan.org/pod/Crypt::OpenSSL::RSA"

extra:
  additional-platforms:
    - linux-aarch64
    - osx-arm64
