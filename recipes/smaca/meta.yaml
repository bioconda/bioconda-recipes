{% set name = "smaca" %}
{% set version = "1.2.4rc9" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 9dcf432dc5518ca3a21533900442fa7059be827d9d34973724880d3c471aee72

build:
  number: 0
  run_exports:
    - {{ pin_subpackage(name, max_pin="x") }}
  skip: true   # [osx]
  entry_points:
    - smaca = smaca.cli:main
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
  host:
    - pip
    - python
    - cython
    - numpy
    - pysam
  run:
    - click
    - joblib
    - pysam
    - python
    - {{ pin_compatible('numpy') }}

test:
  imports:
    - smaca
  commands:
    - smaca --help

about:
  home: https://github.com/babelomics/SMAca
  license: GPL-3.0
  license_file: LICENSE.txt
  summary: smaca is a python tool to detect putative SMA carriers and estimate the absolute SMN1 copy-number in a population.

extra:
  identifiers:
    - doi:https://zenodo.org/badge/latestdoi/250259934
  container:
    extended-base: True
  recipe-maintainers:
    - loucerac
