package:
  name: perl-perl4-corelibs
  version: "0.003"

source:
  fn: Perl4-CoreLibs-0.003.tar.gz
  url: https://cpan.metacpan.org/authors/id/Z/ZE/ZEFRAM/Perl4-CoreLibs-0.003.tar.gz
  md5: f74747c234470a087871f564a5f5db15
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - perl
    - perl-module-build
    - perl-test-simple
      # - perl-text-parsewords
      #- perl-io
      #- perl-socket
      #- perl-getopt-long
      #- perl-time-local

  run:
    - perl
      #- perl-socket
      #- perl-getopt-long
      #- perl-text-parsewords
      #- perl-time-local

#test:
  # Perl 'use' tests
  #  commands:
  #  - perl -e 'use FindBin qw($Bin); require "getopts.pl";'

  # You can also put a file called run_test.pl (or run_test.py) in the recipe
  # that will be run at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://metacpan.org/pod/Perl4::CoreLibs
  license: perl_5
  summary: 'libraries historically supplied with Perl 4'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
