{% set name = "busco" %}
{% set version = "4.0.beta1" %}
{% set sha256 = "2df411b944baf7b71f68eda5242a7346605d9f272e9624893aa88b5b1a05eb63" %}
{% set repo = "busco_beta" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

build:
  number: 0
  noarch: python


source:
  url: https://gitlab.com/ezlab/{{ repo }}/-/archive/{{ version }}/busco-{{ version }}.tar.gz
  sha256: {{ sha256 }}

requirements:
  host:
    - python >=3
    - pip
  run:
    - python >=3
    - findutils
    - blast >=2.2,<2.4
    - hmmer >=3.1b2 # sepp needs this version
    - augustus >=3.3,<3.3.2
    - r-base
    - r-ggplot2 >=2.2.1
    - prodigal
    - sepp >=4.3.10

test:
  requires:
    - wget
  commands:
    - run_BUSCO.py -h
    - run_busco -h
    - generate_plot.py -h
    - generate_plot -h
    - mkdir findtest ; cd findtest ; touch file ; find . -delete ; cd ..

    # Message reported by busco when the config file exists but no input provided
    - run_busco | grep "Please add it in the config file"

about:
  home: http://busco.ezlab.org/
  license: GPL
  summary: BUSCO provides measures for quantitative assessment of genome
           assembly, gene set, and transcriptome completeness based on
           evolutionarily informed expectations of gene content from
           near-universal single-copy orthologs selected from OrthoDB.

extra:
  identifiers:
    - biotools:busco
    - doi:10.1093/bioinformatics/btv351
  recipe-maintainers:
    - silask
    - matsepp
