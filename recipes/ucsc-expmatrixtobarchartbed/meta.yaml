{% set version = "357" %}

package:
  name: ucsc-expmatrixtobarchartbed
  version: '{{version}}'

source:
  sha256: 8591f13a19ee8b01e56c1206a475dc062d452c04b92dc04cb4a6f5545b87bb22  # [osx]
  url: http://hgdownload.cse.ucsc.edu/admin/exe/macOSX.x86_64/expMatrixToBarchartBed  # [osx]
  url: http://hgdownload.cse.ucsc.edu/admin/exe/userApps.v{{version}}.src.tgz                # [linux]
  sha256: 0cec445f47c319e407d2cf866fb2f6ec0c9b72a2b10a0f75142ebc01ca4aca87                                                      # [linux]
  patches:                                                                                   # [linux]
    - include.patch                                                                          # [linux]
    - 2to3.patch                                                                             # [linux]

build:
  number: 2

requirements:
  build:
    - '{{ compiler("c") }}'
  host:
    - libpng                                                    # [linux]
    - libuuid                                                   # [linux]
    - mysql-connector-c                                         # [linux]
    - openssl                                                   # [linux]
    - zlib
  run:
    - libpng                                                    # [linux]
    - libuuid                                                   # [linux]
    - mysql-connector-c                                         # [linux]
    - openssl                                                   # [linux]
    - python                                                    # [linux]
    - zlib

test:
  commands:
    # just check for existence, because the individual packages have no unified behavior
    - which expMatrixToBarchartBed

about:
  home: http://hgdownload.cse.ucsc.edu/admin/exe/
  license: varies; see http://genome.ucsc.edu/license
  summary: ' Generate a barChart bed6+5 file from a matrix, meta data, and coordinates. '
