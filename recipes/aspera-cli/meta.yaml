{% set name = "aspera-cli" %}
{% set version = "4.18.0" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  - url: https://github.com/IBM/{{ name }}/archive/refs/tags/v{{ version }}.tar.gz
    sha256: 936da30629fa55786038370729ada8de38ebb893c3cdb003e44afc14c7dc8baf

build:
  noarch: generic
  number: 0
  run_exports:
    - {{ pin_subpackage('aspera-cli', max_pin="x") }}

requirements:
  build:
    - ruby >=3
  host:
    - ruby >=3
  run:
    - ruby >=3

test:
  commands:
    - ascli -h
    - ascp -h

about:
  home: https://github.com/IBM/aspera-cli
  license: Apache-2.0
  license_file: 
    - LICENSE
    - aspera-license
  summary: "Command Line Interface for IBM Aspera products"
  dev_url: https://github.com/IBM/aspera-cli

extra:
  recipe-maintainers:
    - farchaab
