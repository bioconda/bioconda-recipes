{% set name = "pyranges1" %}
{% set version = "1.3.4" %}
{% set sha256 = "ab5a17a5d01cf08ff01e9dc3aba7890bb29c32fb32e006d7e679a2c6609932ef" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  entry_points:
    - pyranger = pyranges1.cli:main
  run_exports:
    - {{ pin_subpackage('pyranges1', max_pin='x.x') }}

requirements:
  host:
    - python >=3.12
    - pip
    - setuptools >=61
    - wheel
    - setuptools-scm >=6.2
  run:
    - python >=3.12
    - pyrle >=0.0.41
    - bamread
    - pybigwig
    - pandas
    - tabulate
    - natsort
    - ruranges >=0.1.1

test:
  imports:
    - pyranges1
  commands:
    - python -c "import pyranges1 as pr; print(pr.__version__)"
  requires:
    - python >=3.12

about:
  home: http://github.com/pyranges/pyranges_1.x
  summary: GenomicRanges for Python.
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - endrebak
