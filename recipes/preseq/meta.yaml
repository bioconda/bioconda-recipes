{% set version = "3.2.0" %}

package:
  name: preseq
  version: {{ version }}

source:
  url: https://github.com/smithlabcode/preseq/releases/download/v{{ version }}/preseq-{{ version }}.tar.gz
  sha256: 4a9e2bb65d9add17a0fa6ed81970cd3bb4290404c1e8ed71fe9794fe64aac5f9

build:
  number: 0

requirements:
  build:
    - make
    - {{ compiler('cxx') }}
    - {{ compiler('c') }}
  host:
    - zlib
    - gsl
    - openblas
    - htslib
  run:
    - zlib
    - gsl
    - openblas
    - htslib

test:
  commands:
    - preseq
    - to-mr

about:
  home: https://github.com/smithlabcode/preseq
  license: GNU GENERAL PUBLIC LICENSE
  summary: 'Software for predicting library complexity and genome coverage in high-throughput sequencing'
