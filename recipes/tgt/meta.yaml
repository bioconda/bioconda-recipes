package:
  name: tgt
  version: "1.4.2"

source:
  fn: tgt-1.4.2.tar.gz
  url: https://pypi.python.org/packages/bc/90/f1a4c6a1d12ea21db92dc5c2a7b600b276541ba77c5e6cc4f3d7e05fc01f/tgt-1.4.2.tar.gz
  md5: b396d66ef78edb9e2d51c098b97cd14d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tgt = tgt:main
    #
    # Would create an entry point called tgt that calls tgt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python
    - numpy

test:
  # Python imports
  imports:
    - tgt

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'TextGridTools -- Read, write, and manipulate Praat TextGrid files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
