package:
  name: test
  version: 1.0

build:
  noarch: generic
  number: 0

test:
  commands:
    - cmd_does_not_exist
    - cmd_does_not_exist || echo "TEST"
about:
  home: https://github.com/bioconda
  license: GPL-3.0
  summary: just a test
