{% set version = "0.1.1" %}

package:
  name: scrapp
  version: {{ version }}

build:
  number: 0
  skip: True  # [py3k]

source:
  url: https://github.com/Pbdas/scrapp/archive/v{{ version }}.tar.gz
  sha256: a4b6b09f924d4b25d3bfd4a9604ff1fc34b571b6e27593d5cb724347d5df4a76

requirements:
  build:
    - {{ compiler('cxx') }}
    - autoconf
    - automake
    - flex
    - bison
    - m4
    - cmake
    - libtools
    - llvm-openmp # [osx]
    - gmp
  host:
    - {{ mpi }}
    - zlib
    - unzip
    - wget
    - python
    - numpy
    - mpi4py
  run:
    - {{ mpi }}
    - python
    - numpy
    - mpi4py
    - zlib
    - llvm-openmp # [osx]

test:
  commands:
    - scrapp.py -h | grep "Pipeline wrapper script that calculates species counts for each branch" > /dev/null

about:
  home: https://github.com/Pbdas/scrapp
  license: GNU General Public License v3.
  license_file: LICENSE
  summary: Species Counting on Reference trees viA Phylogenetic Placements

extra:
   identifiers:
     - doi:10.1101/2020.02.28.969980
