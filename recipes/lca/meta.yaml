{% set name = "LCA" %}
{% set version = "0.30" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/hildebra/{{ name }}/archive/refs/tags/v{{ version }}.tar.gz
  sha256: 748896646fc637ddc6d645d4db57bf7218f4714e1576a170daf2884f9ef11fb6

build:
  number: 0
  run_exports:
    - {{ pin_subpackage(name|lower, max_pin="x.x") }}

requirements:
  build:
    - {{ compiler('cxx') }}
    - make
    - pkg-config
  host:
    - zlib

test:
  commands:
    - "LCA -h"

about:
  home: "https://github.com/hildebra/LCA"
  license: "GPL-3.0-only"
  license_family: GPL3
  license_file: LICENSE
  summary: "LCA - Lowest Common Ancestor calculation tool."
  dev_url: "https://github.com/hildebra/LCA"

extra:
  additional-platforms:
    - linux-aarch64
    - osx-arm64
  skip-lints:
    - compiler_needs_stdlib_c
