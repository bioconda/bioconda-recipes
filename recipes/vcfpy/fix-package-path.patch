--- a/pyproject.toml
+++ b/pyproject.toml
@@ -65,10 +65,10 @@ pythonVersion = "3.10"
 pythonPlatform = "Linux"

 [tool.hatch.build.targets.wheel]
-packages = ["src/vcfpy"]
+packages = ["vcfpy"]

 [tool.hatch.build.targets.sdist]
-packages = ["src/vcfpy"]
+packages = ["vcfpy"]
 exclude = [
   "/.editorconfig",
   "/Makefile",
