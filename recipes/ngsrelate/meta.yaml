{% set name = "NgsRelate" %}
{% set version = "2.1.0" %}
{% set sha256 = "7196b1ebc08ca1e9f980c658f65eb60f40025356a027dec402bd9aff518900e2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/ANGSD/{{ name }}/archive/v{{ version }}.tar.gz
  sha256: {{ sha256 }}
  patches:
    - makefile.patch

build:
  number: 0
  # OSX requires older G++ version
  skip: True  # [osx]
  run_exports:
    - {{ pin_subpackage(name|lower, max_pin='x') }}

requirements:
  build:
    - make
    - {{ compiler('cxx') }}
  host:
    - zlib
    - bzip2
    - xz
    - htslib
    - libdeflate
  run:
    - zlib
    - bzip2
    - xz
    - htslib
    - libdeflate
  
test:
  commands:
    - ngsRelate

about:
  home: https://github.com/ANGSD/NgsRelate
  license: GPL-2.0-only
  license_family: GPL
  license_file: LICENSE
  summary: "Software tool for estimating pairwise relatedness from next-generation sequencing data."
  dev_url: https://github.com/ANGSD/NgsRelate

extra:
  identifiers:
    - doi:10.1093/bioinformatics/btv509
    - doi:10.1093/gigascience/giz034
