{% set name = "NgsRelate" %}
{% set version = "2.2.0" %}
{% set sha256 = "32e137c382f2ef52022437396b78eb940e35105888bd6c3f6a01ec9a968b2138" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/ANGSD/{{ name }}/archive/v{{ version }}.tar.gz
  sha256: {{ sha256 }}
  patches:
    - makefile.patch

build:
  number: 0
  # OSX requires older G++ version
  skip: True  # [osx]
  run_exports:
    - {{ pin_subpackage(name|lower, max_pin='x') }}

requirements:
  build:
    - make
    - {{ compiler('cxx') }}
  host:
    - zlib
    - bzip2
    - xz
    - htslib
    - libdeflate
  run:
    - zlib
    - bzip2
    - xz
    - htslib
    - libdeflate
  
test:
  commands:
    - ngsRelate

about:
  home: https://github.com/ANGSD/NgsRelate
  license: GPL-2.0-only
  license_family: GPL
  license_file: LICENSE
  summary: "Software tool for estimating pairwise relatedness from next-generation sequencing data."
  dev_url: https://github.com/ANGSD/NgsRelate

extra:
  identifiers:
    - doi:10.1093/bioinformatics/btv509
    - doi:10.1093/gigascience/giz034
