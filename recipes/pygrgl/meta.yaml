{% set name = "pygrgl" %}
{% set version = "2.6" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: "https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz"
  sha256: f025f4f06f355f89e7d066cf6f370c59a8df2d2827d1f5142f8df93d4f0f2cc7

build:
  number: 0
  entry_points:
    - grg=pygrgl.cli:main
  script: "{{ PYTHON }} -m pip install . -vv"
  run_exports:
    - {{ pin_subpackage('pygrgl', max_pin="x.x") }}

requirements:
  build:
    - {{ compiler('cxx') }}
    - cmake
    - make
  host:
    - python
    - zlib
    - pip
    - tqdm
  run:
    - python
    - tqdm

test:
  imports:
    - pygrgl
  commands:
    - grg --help

about:
  home: "https://aprilweilab.github.io/"
  license: GPL-3.0-only
  license_family: GPL3
  license_file: LICENSE
  summary: "Genotype Representation Graph Library for efficient storage/use of huge genetic datasets"
  doc_url: "https://grgl.readthedocs.io/en/stable/"
  dev_url: "https://github.com/aprilweilab/grgl"

extra:
  recipe-maintainers:
    - dcdehaas
