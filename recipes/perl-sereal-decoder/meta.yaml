{% set name = "perl-sereal-decoder" %}
{% set version = "5.010" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://cpan.metacpan.org/authors/id/Y/YV/YVES/Sereal-Decoder-{{ version }}.tar.gz
  sha256: b0e419b53eaabe38e6188c8afbb3b8fa86d9dd922e0f36ea53fd626973d9cb25

build:
  number: 0
  run_exports:
    - {{ pin_subpackage(name, max_pin="x") }}

requirements:
  build:
    - make
    - {{ compiler('c') }}
    - {{ stdlib("c") }}
  host:
    - perl
    - perl-test-warn
    - perl-test-simple
    - perl-extutils-makemaker
    - perl-test-longstring
    - perl-scalar-list-utils
    - perl-pathtools
    - perl-extutils-parsexs
    - perl-devel-checklib >=1.16
    - perl-xsloader
  run:
    - perl
    - perl-xsloader

test:
  imports:
    - Sereal::Decoder  # [osx]
    - Sereal::Decoder::Constants  # [osx]
    - Sereal::Performance

about:
  home: "https://metacpan.org/pod/Sereal::Decoder"
  license: "perl_5"
  summary: 'Fast, compact, powerful binary deserialization.'
  doc_url: "https://metacpan.org/pod/Sereal::Decoder"

extra:
  additional-platforms:
    - linux-aarch64
    - osx-arm64
