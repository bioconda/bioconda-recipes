{% set version = '0.71.0' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-gkmsvm
  version: {{ version|replace("-", "_") }}

source:
  fn: gkmSVM_{{ version }}.tar.gz
  url:
    - https://cran.r-project.org/src/contrib/gkmSVM_{{ version }}.tar.gz
    - https://cran.r-project.org/src/contrib/Archive/gkmSVM/gkmSVM_{{ version }}.tar.gz


  sha256: 60825f7e91fae0ab49ce21731dda819da3dcdd34fa02607991a559e16a7ffdfe
  # patches:
   # List any patch files here
   # - fix.patch

build:
  # If this is a new build for the same version, increment the build number.
  number: 0

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: BSgenome.Hsapiens.UCSC.hg18.masked, BSgenome.Hsapiens.UCSC.hg19.masked
requirements:
  build:
    - r-base
    - bioconductor-biocgenerics
    - bioconductor-biostrings
    - bioconductor-genomeinfodb
    - bioconductor-genomicranges
    - bioconductor-iranges
    - r-rocr
    - r-rcpp
    - bioconductor-s4vectors
    - r-kernlab
    - bioconductor-rtracklayer
    - r-seqinr
    - gcc                  # [not win]

  run:
    - r-base
    - bioconductor-biocgenerics
    - bioconductor-biostrings
    - bioconductor-genomeinfodb
    - bioconductor-genomicranges
    - bioconductor-iranges
    - r-rocr
    - r-rcpp
    - bioconductor-s4vectors
    - r-kernlab
    - bioconductor-rtracklayer
    - r-seqinr
    - libgcc  # [not win]

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('gkmSVM')"  # [not win]
    - "\"%R%\" -e \"library('gkmSVM')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: https://CRAN.R-project.org/package=gkmSVM
  license: GPL (>= 2)
  summary: 'Imports the ''gkmSVM'' v2.0 functionalities into R (www.beerlab.org/gkmsvm). It also
    uses the ''kernlab'' library (separate R package by different authors) for various
    SVM algorithms.   '

  license_family: GPL3

# The original CRAN metadata for this package was:

# Package: gkmSVM
# Type: Package
# Title: Gapped-Kmer Support Vector Machine
# Version: 0.71.0
# Date: 2015-12-05
# Author: Mahmoud Ghandi
# Maintainer: Mahmoud Ghandi <mghandi@gmail.com>
# Description: Imports the 'gkmSVM' v2.0 functionalities into R (www.beerlab.org/gkmsvm). It also uses the 'kernlab' library (separate R package by different authors) for various SVM algorithms.
# License: GPL (>= 2)
# Imports: Rcpp, kernlab, seqinr, utils, ROCR, grDevices, graphics, stats,BiocGenerics,Biostrings,GenomeInfoDb,GenomicRanges,IRanges,S4Vectors,rtracklayer
# Suggests: BSgenome.Hsapiens.UCSC.hg18.masked, BSgenome.Hsapiens.UCSC.hg19.masked
# LinkingTo: Rcpp
# SystemRequirements: C++11
# Packaged: 2016-07-18 21:41:00 UTC; mghandi
# NeedsCompilation: yes
# Repository: CRAN
# Date/Publication: 2016-07-19 10:38:07

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
