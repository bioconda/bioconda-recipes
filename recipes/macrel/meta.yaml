{% set name = "macrel" %}
{% set version = "1.6.1" %}
{% set sha256 = "897de93abb9857b85a8e3989feba1de30e558818088b42dbc90efd5a6d7d349e" %}

package:
  name: {{ name }}
  version: {{ version }}
  
source:
 url: https://github.com/BigDataBiology/macrel/archive/v{{ version }}.tar.gz
 sha256: {{ sha256 }}

build:
  noarch: python
  number: 0
  entry_points:
    - macrel= macrel.main:main
  script: python -m pip install --disable-pip-version-check --no-cache-dir --ignore-installed --no-deps -vv .
  run_exports:
    - {{ pin_subpackage("macrel", max_pin="x.x") }}

requirements:
  host:
    - python
    - pip
    - setuptools
  run:
    - python
    - atomicwrites
    - ngless
    - joblib
    - megahit
    - paladin
    - pandas
    - scikit-learn
    - requests
    - onnxruntime <1.26
    - tzlocal
    - pyrodigal >=0.7.3
  
test:
  imports:
    - macrel
  commands:
    - macrel --version

about:
  home: https://github.com/BigDataBiology/macrel
  license: MIT
  summary: "A pipeline for AMP (antimicrobial peptide) prediction"
  description: |
    Used for the prediction of AMPs in (meta)genomes.
  dev_url: https://github.com/BigDataBiology/macrel
  
extra:
  recipe-maintainers:
    - celiosantosjr
    - luispedro
  identifiers:
    - "doi:10.7717/peerj.10555" # Paper
