{% set name = "GOTTCHA2" %}
{% set version = "2.4.3" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install -vvv --no-deps --no-build-isolation --no-cache-dir .
  entry_points:
    - gottcha2 = gottcha.gottcha2:cli
  run_exports:
    - {{ pin_subpackage('gottcha2', max_pin='x') }}

source:
  url: https://github.com/poeli-lanl/GOTTCHA2/archive/refs/tags/{{ version }}.tar.gz
  sha256: 7dc8ca116f572a0d30eb752e6b4093c954c8271fb6f6e25201aa27f26a60b244

requirements:
  host:
    - python >=3.9
    - pip
    - setuptools
    - pandas
  run:
    - python >=3.9
    - minimap2 >=2.17
    - biom-format >=2.1.7
    - numpy
    - pandas
    - requests
    - tqdm
    - pysam >=0.22.0
    - samtools
    - sylph

test:
  commands:
    - minimap2 -h
    - python -c "import gottcha.gottcha2 as g; assert g.__version__ == '{{ version }}'"
    - gottcha2 version

about:
  home: https://github.com/poeli-lanl/GOTTCHA2
  license: BSD-3
  license_family: BSD
  license_file: LICENSE
  summary: 'Genomic Origin Through Taxonomic CHAllenge (GOTTCHA) v2.'
  dev_url: https://github.com/poeli-lanl/GOTTCHA2
  doc_url: https://github.com/poeli-lanl/GOTTCHA2/wiki

extra:
  identifiers:
    - biotools:gottcha2
