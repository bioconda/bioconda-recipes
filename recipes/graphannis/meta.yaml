{% set version = "0.21.0" %}
{% set sha256 = "1fd518d568f11d37ef41216981e33fe4ef807d6443c145a503641f4f09ea4513" %}

package:
  name: graphannis
  version: '{{ version }}'

build:
  number: 0

source:
  url: https://github.com/korpling/graphANNIS/archive/v{{ version }}.tar.gz
  sha256: '{{ sha256 }}'
  patches:
    - graphannis.patch

requirements:
  build:
    - rust
  host:
    - rust

test:
  commands:
    - annis --help


about:
  home: https://github.com/korpling/graphANNIS
  license: Apache-2.0
  license_file: LICENSE
  summary: This is a new backend implementation of the ANNIS linguistic search and visualization system.
