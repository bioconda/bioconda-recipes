{% set version = "1.1.50" %}

package:
  name: "treeswift"
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/t/treeswift/treeswift-{{ version }}.tar.gz
  sha256: af8614b6e44cf26c33ad1f4c36ce944cc04458ad1fe28cab02b5482f19ff6257

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . --no-deps --no-build-isolation --no-cache-dir -vvv
  run_exports:
    - {{ pin_subpackage('treeswift', max_pin="x") }}

requirements:
  host:
    - python
    - pip
    - setuptools
  run:
    - python

test:
  imports:
    - treeswift

about:
  home: "https://github.com/niemasd/TreeSwift"
  license: "GPL-3.0-or-later"
  license_family: GPL3
  license_file: LICENSE
  summary: "TreeSwift: Fast tree module for Python 2 and 3"
  dev_url: "https://github.com/niemasd/TreeSwift"
