{% set name = "portcullis" %}
{% set version = "1.1.1" %}
{% set sha256 = "c5499407011e26160f584aac0b272ee58bc675dd3f5f2ca0710cb29d4f4c2f68" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
   fn: portcullis-{{version}}.tar.gz
   url: https://github.com/maplesond/portcullis/archive/Hotfix-5.tar.gz
   sha256: {{ sha256 }}

about:
  home: https://github.com/maplesond/portcullis
  license: GPL3
  license_file: COPYING
  summary: 'Splice junction analysis and filtering from BAM files'

build:
  number: 1
  skip: True # [osx or py27]

requirements:
  build:
      # - gcc will wait on new conda gcc for this
      - llvm  # [osx]
      - libgcc  # [not osx]
      - python
      - zlib {{CONDA_ZLIB}}*
      - autoconf
      - automake
      - libtool
      - perl
      - setuptools
      - tabulate
      - numpy
      - pandas
      - samtools
  run:
      - python
      - libgcc  # [not osx]
      - zlib {{CONDA_ZLIB}}*
      - tabulate
      - numpy
      - pandas
      - samtools

test:
  commands:
    - portcullis --version
