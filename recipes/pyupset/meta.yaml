package:
  name: pyupset
  version: "0.1.1.post7"

source:
  fn: PyUpSet-0.1.1.post7.tar.gz
  url: https://pypi.python.org/packages/70/4e/ed9ef0952561292b6138c9c1a6eb8362ad13df227174ddc3057c69db5ed7/PyUpSet-0.1.1.post7.tar.gz
  md5: 474b1ea07fea4c9566b2aea7e0b0b7c2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyupset = pyupset:main
    #
    # Would create an entry point called pyupset that calls pyupset.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pandas
    - matplotlib
    - numpy

  run:
    - python
    - pandas
    - matplotlib
    - numpy

test:
  # Python imports
  imports:
    - pyupset

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ImSoErgodic/py-upset
  license: MIT
  summary: 'Python implementation of the UpSet visualisation suite by Lex et al.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
