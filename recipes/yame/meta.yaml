{% set name = "yame" %}
{% set version = "1.0.3" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://github.com/zhou-lab/YAME/archive/refs/tags/v{{ version }}.tar.gz
  sha256: a8f884c3dca5999448cbff5d0015592bab2807b2a274079f87b5d1c9fa530e10 

build:
  number: 0
  run_exports:
    - {{ pin_subpackage(name, max_pin="x") }}

requirements:
  build:
    - {{ compiler('c') }}
    - make
    - zlib
  host:
    - zlib
    - ncurses
  run:
    - libgcc-ng  # or libcxx if it's C++
    - ncurses

test:
  commands:
    - yame --help

about:
  home: https://github.com/zhou-lab/YAME
  license: MIT
  license_file: LICENSE
  summary: "YAME: Yet Another Methylation Extractor."

extra:
  recipe-maintainers:
    - Hongxiang2023


