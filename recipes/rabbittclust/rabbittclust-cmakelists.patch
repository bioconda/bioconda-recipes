--- CMakeLists.txt	2023-05-22 09:13:52.000000000 -0500
+++ CMakeLists.txt	2024-02-27 11:22:31.002336540 -0600
@@ -2,7 +2,6 @@
 
 project(rabbitTClust)
 
-set(CMAKE_INSTALL_PREFIX ..)
 option(USE_RABBITFX "parse input fasta file with RabbitFX" OFF)
 option(USE_DEBUG "print the debug information" ON)
 option(USE_Timer "print the time information" ON)
@@ -14,10 +13,9 @@
 if(OPENMP_FOUND)
 #message("openmp found")
 
-set(CMAKE_CXX_COMPILER "/usr/bin/g++")
 set(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} ${OpenMP_C_FLAGS}")
-set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} ${OpenMP_CXX_FLAGS}")
-set(CMAKE_EXE_LINKER_FLAGS "${CMAKE_EXE_LINKER_FLAGS} ${OpenMP_EXE_LINKER_FLAGS}")
+set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} ${OpenMP_CXX_FLAGS} $ENV{CXXFLAGS}")
+set(CMAKE_EXE_LINKER_FLAGS "${CMAKE_EXE_LINKER_FLAGS} ${OpenMP_EXE_LINKER_FLAGS} $ENV{LDFLAGS}")
 set(EXECUTABLE_OUTPUT_PATH .)
 endif()
 
@@ -25,7 +23,7 @@
 set(CMAKE_CXX_STANDARD_REQUIRED ON)
 #set(CMAKE_CXX_FLAGS "-g -O3 -D THREADPOOL_MINHASH -D DEBUG -D Timer ${CMAKE_CXX_FLAGS}")
 
-set(CMAKE_CXX_FLAGS "-g -O3 -Wno-format -Wno-unused-result${CMAKE_CXX_FLAGS}")
+set(CMAKE_CXX_FLAGS "-g -O3 -Wno-format -Wno-unused-result ${CMAKE_CXX_FLAGS}")
 
 if(USE_DEBUG)
 	set(CMAKE_CXX_FLAGS "-D DEBUG ${CMAKE_CXX_FLAGS}")
@@ -60,17 +58,17 @@
 if(USE_GREEDY)
 add_executable(clust-greedy ${DIR_SRCS})
 if(USE_RABBITFX)
-	target_link_libraries(clust-greedy rabbitsketch_static rabbitfx z)
+	target_link_libraries(clust-greedy rabbitsketch rabbitfx z)
 else()
-	target_link_libraries(clust-greedy rabbitsketch_static z)
+	target_link_libraries(clust-greedy rabbitsketch z)
 endif()
 
 else()
 add_executable(clust-mst ${DIR_SRCS})
 if(USE_RABBITFX)
-	target_link_libraries(clust-mst rabbitsketch_static rabbitfx z)
+	target_link_libraries(clust-mst rabbitsketch rabbitfx z)
 else()
-	target_link_libraries(clust-mst rabbitsketch_static z)
+	target_link_libraries(clust-mst rabbitsketch z)
 endif()
 endif()
 
