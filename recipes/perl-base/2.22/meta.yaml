{% set name = "perl-base" %}
{% set version = "2.22" %}
{% set sha256 = "" %}

package:
  name: {{ name }}
  version: {{ version }}

build:
  number: 1

requirements:
  build:
    - perl

  run:
    - perl

test:
  imports:
    - base

about:
  home: http://metacpan.org/pod/base
  license: unknown
  summary: 'compile-time class fields'
