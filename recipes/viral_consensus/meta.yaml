{% set version = "0.0.4" %}

package:
  name: viral_consensus
  version: {{ version }}

build:
  number: 0
  run_exports:
    - {{ pin_subpackage('viral_consensus', max_pin="x.x.x") }}

source:
  url: https://github.com/niemasd/ViralConsensus/archive/refs/tags/{{ version }}.tar.gz
  sha256: 59c832d9bba9e3f414dd5c183f06464a5fa0f72873c1413cb1ab31a8631aba8f # curl -sL URL | openssl sha256

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - bzip2
    - htslib
    - libcurl
    - make
    - ncurses
    - zlib
  host:
    - bzip2
    - htslib
    - libcurl
    - ncurses
    - zlib
  run:
    - bzip2
    - htslib
    - libcurl
    - ncurses
    - zlib

about:
  home: https://github.com/niemasd/ViralConsensus
  license: GNU General Public License v3 (GPLv3)
  license_file: LICENSE
  summary: Fast viral consensus genome reconstruction

test:
  commands:
    - viral_consensus --version
