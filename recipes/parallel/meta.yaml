# GNU Parallel
# updated to latest from https://github.com/ostrokach/conda-recipes-extra/tree/master/parallel

package:
  name: parallel
  version: 20160622

source:
  url: http://ftpmirror.gnu.org/parallel/parallel-20160622.tar.bz2
  fn: parallel-20160622.tar.bz2

build:
  number: 0
  skip: True # [osx]

requirements:
  build:
  run:

test:
  commands:
    - parallel --version

about:
  home: http://www.gnu.org/software/parallel/
  license: GNU
  summary: GNU parallel is a shell tool for executing jobs in parallel using one or more computers.
