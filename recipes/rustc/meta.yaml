package:
  name: rustc
  version: "1.6.0"

build:
  number: 0

source:
  fn: rustc-1.6.0-src.tar.gz
  url: https://static.rust-lang.org/dist/rustc-1.6.0-src.tar.gz

requirements:
  build:
    - llvm
    - python 2.7.*
    - curl
   
test:
  commands:
    - rustc --help
 
about:
  home: https://www.rust-lang.org
  license: MIT
  summary: Rust is a systems programming language that runs blazingly fast, prevents segfaults, and guarantees thread safety.
