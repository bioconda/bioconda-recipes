{% set version = "0.1.3" %}

package:
  name: graphembed
  version: {{ version }}

build:
  number: 2
  run_exports:
      - {{ pin_subpackage('graphembed', max_pin="x.x") }}
  skip: True  # [osx]
source:
  url: https://github.com/jianshu93/graphembed/archive/v{{ version }}.tar.gz
  sha256: 0e26dd04b757fa137e6ace4ca023870d4528d5d7878df2eb25ec3b35c7ff9485

requirements:
  build:
    - {{ compiler("cxx") }}
    - {{ compiler('c') }}
    - {{ compiler('rust') }}
    - make
    - cmake

test:
  commands:
    - graphembed -h

about:
  home: https://github.com/jianshu93/graphembed
  license: MIT
  summary: Efficient and Robust Graph Embedding via High-Order Proximity Preservation or Recursive Sketching, together with new link prediction metric centric AUC.
extra:
  maintainers:
   - Jianshu Zhao
   - Jean Pierre-Both
