package:
  name: gimmemotifs
  version: '0.8.9.1'

source:
  url: https://github.com/simonvh/gimmemotifs/archive/0.8.9.1.tar.gz 
  fn: gimmemotifs-0.8.9.1.tar.gz
  md5: 8cacf868a9fcadfccf77a9cd285dfcea 

build:
  number: 1
  skip: True # [not py27]

requirements:
  build:
    - python
    - numpy
    - scipy
    - matplotlib
    - kid
    - pyyaml
    - pybedtools

  run:
    - python
    - numpy
    - scipy
    - matplotlib
    - kid
    - pyyaml
    - pybedtools

test:
  imports:
    - gimmemotifs

about:
  home: https://github.com/simonvh/gimmemotifs
  license: MIT
  summary: Motif prediction pipeline and various motif-related tools
