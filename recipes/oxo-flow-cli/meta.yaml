{% set name = "oxo-flow-cli" %}
{% set version = "0.18.1" %}
{% set sha256 = "6ae1689435cd03d844043cf72affdf4f4ec2d7f376815d26c774d58b51777b4c" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/Traitome/oxo-flow/archive/refs/tags/v{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  skip: true  # [win]
  run_exports:
    - {{ pin_subpackage('oxo-flow-cli', max_pin="x.x") }}

requirements:
  build:
    - {{ compiler('c') }}
    - {{ stdlib('c') }}
    # Pin the raw rust compiler: oxo-flow 0.17.0+ requires rustc >= 1.98.0
    # (rust-version in Cargo.toml), but the rust_* metapackages that
    # {{ compiler('rust') }} resolves to top out at 1.97.1 in repodata
    # (the 1.98.x metapackage builds are missing on conda-forge).
    - rust >=1.98.0
    - cargo-bundle-licenses

test:
  commands:
    - oxo-flow --help

about:
  home: "https://github.com/Traitome/oxo-flow"
  license: Apache-2.0
  license_family: Apache
  license_file:
    - LICENSE
    - THIRDPARTY.yml
  summary: "CLI for the oxo-flow bioinformatics pipeline engine"
  description: |
    oxo-flow-cli is the command-line interface for oxo-flow, 
    a bioinformatics pipeline engine.
  dev_url: "https://github.com/Traitome/oxo-flow"

extra:
  additional-platforms:
    - linux-aarch64
    - osx-arm64
  recipe-maintainers:
    - ShixiangWang
