{% set version="beta_1.5" %}

package:
  name: minnow
  version: {{ version }}

source:
  url: https://github.com/COMBINE-lab/minnow/archive/{{ version  }}.tar.gz
  md5: ab586873c90f479575d2577e8b977500

build:
  number: 0

requirements:
  build:
    - autoconf
    - cmake
    - {{ compiler('cxx') }}
    - {{ compiler('c') }}
  host:
    - icu 
    - zlib
    - bzip2
    - unzip
  run:
    - zlib
    - bzip2

test:
  commands:
    - minnow --help

about:
  home: https://github.com/COMBINE-lab/minnow
  license: GPLv3
  summary: A principled framework for rapid simulation of dscRNA-seq data at the read level
