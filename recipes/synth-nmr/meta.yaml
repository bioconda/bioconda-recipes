{% set name = "synth-nmr" %}
{% set version = "0.7.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name | replace("-", "_") }}-{{ version }}.tar.gz
  sha256: 16816387cc6cf0f3a21027f1f52e0263455b370f9c3eff3549b5051601ab93dd

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . --no-deps -vv
  run_exports:
    - {{ pin_subpackage(name, max_pin="x") }}

requirements:
  host:
    - python >=3.8
    - pip
    - setuptools
  run:
    - python >=3.8
    - numpy >=1.20
    - biotite >=0.35.0

test:
  imports:
    - synth_nmr

about:
  home: https://github.com/elkins/synth-nmr
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: NMR spectroscopy calculations for protein structures
  doc_url: https://github.com/elkins/synth-nmr
  dev_url: https://github.com/elkins/synth-nmr

extra:
  recipe-maintainers:
    - elkins
