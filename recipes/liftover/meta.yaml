{% set name = "liftover" %}
{% set version = "1.5.0" %}
{% set sha256 = "cb1ca76f0275b7a498c2ec8b1a3a2d7b576e6d861d9f0ec340aa4861a3bf9dc2" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/liftover-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  script: {{ PYTHON }} -m pip install . --no-deps --no-build-isolation --no-cache-dir -vvv
  run_exports:
    - {{ pin_subpackage(name, max_pin="x") }}

requirements:
  build:
    - {{ compiler('cxx') }}
  host:
    - python
    - setuptools
    - pip
    - cython
    - zlib
  run:
    - python
    - urllib3

test:
  imports:
    - liftover

about:
  home: 'https://github.com/jeremymcrae/liftover'
  summary: 'A Python package for converting point coordinates between genome assemblies, inspired by pyliftover.'
  license: MIT
  license_family: MIT
  license_file: LICENSE.txt
  dev_url: 'https://github.com/jeremymcrae/liftover'

extra:
  additional-platforms:
    - linux-aarch64
    - osx-arm64
  recipe-maintainers:
    - mencian
  skip-lints:
    - compiler_needs_stdlib_c
