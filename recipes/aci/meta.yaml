{% set name = "aci" %}
{% set repo = "amplicon_coverage_inspector" %}
{% set version = "1.15.250702" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ repo[0] }}/{{ repo }}/{{ repo }}-{{ version }}.tar.gz
  sha256: f3dc71c89c6ed44707fdf6ce2f79f8ecbef8f273371ba5cbbaf3ff5224ec3450

build:
  number: 0
  noarch: python
  entry_points:
    - aci = aci.aci:main
  script: {{ PYTHON }} -m pip install . --no-deps --no-build-isolation --no-cache-dir -vvv
  run_exports:
    - {{ pin_subpackage('aci', max_pin="x") }}

requirements:
  host:
    - python >=3.8,<4.0
    - pip
    - setuptools
  run:
    - python >=3.8,<4.0
    - matplotlib-base >=3.8.2
    - numpy >=1.26.2
    - pandas >=2.1.4
    - pysam >=0.22.0

test:
  imports:
    - aci
  commands:
    - aci --help

about:
  home: "https://github.com/erinyoung/ACI"
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: "Visualizes coverage for amplicons."
  doc_url: "https://github.com/erinyoung/ACI/blob/{{ version }}/README.md"
  dev_url: "https://github.com/erinyoung/ACI"

extra:
  recipe-maintainers:
    - erinyoung
