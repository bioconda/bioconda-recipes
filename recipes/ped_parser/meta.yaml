build:
  number: 0
  skip: True # [osx]

package:
  name: ped_parser
  version: "1.6.5"

source:
  fn: ped_parser-1.6.5.tar.gz
  url: https://pypi.python.org/packages/source/p/ped_parser/ped_parser-1.6.5.tar.gz
  md5: 1f404e131c3d210d32e6253ac500b200
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ped_parser = ped_parser:main
    #
    # Would create an entry point called ped_parser that calls ped_parser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pytest
    - click

  run:
    - python
    - pytest
    - click

test:
  # Python imports
  imports:
    - ped_parser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/moonso/ped_parser
  license: BSD License
  summary: 'A ped file parser.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
