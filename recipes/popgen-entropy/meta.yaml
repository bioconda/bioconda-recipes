{% set version = "2.0" %}

package:
  name: popgen-entropy
  version: {{ version }}

source:
  url: https://bitbucket.org/buerklelab/mixedploidy-entropy/get/42d2ad1a054a.zip
  sha1: 469ae675c60df67145558a9f0159ae6fa0763ccb

build:
  number: 0

requirements:
  build:
    - {{ compiler('cxx') }}
    - autoconf
    - automake
  host:
    - hdf5
    - gsl
    - mkl
    - zlib
  run:
    - hdf5
    - gsl
    - mkl
    - zlib

test:
  commands:
    - entropy 
about:
  home: 'https://bitbucket.org/buerklelab/mixedploidy-entropy/src/master/'
  license: Public Domain
  summary: "This program is for inferring population structure from autopolyploid and mixed-ploidy individuals, similar to structure, but using genotype-likelihood data for low- to medium-coverage sequencing depth."

extra:
  identifiers:
      - biotools:popgen-entropy
