package:
  name: dscr
  version: 2014.12.17

source:
  fn: v2.0.2.tar.gz
  url: https://github.com/lrog/dsrc/archive/v2.0.2.tar.gz

build:
  skip: True # [osx]
  number: 0

requirements:
  build:
    - gcc  [not osx]
    - llvm  [osx]
    - zlib
    - boost
  run:
    - libgcc [not osx]
    - zlib
    - boost

# dsrc -h returns an error ... 
#tests:
#  commands:
#    - dscr -h

about:
  home: https://github.com/lrog/dsrc
  summary: high-performance compression of sequencing reads stored in FASTQ format
  license: GNU GPL 2
