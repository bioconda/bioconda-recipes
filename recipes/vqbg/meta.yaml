{% set name = "vqbg" %}
{% set version = "1.0.2" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: "https://github.com/qdu-bioinfo/VQBG/archive/refs/tags/v{{ version }}.tar.gz"
  sha256: "dffe7d4a6b41d349a7552ce33f007b15ab950931d983cf01fe2db97a0b07818d"

build:
  number: 0
  skip: True  # [osx]
  run_exports:
    - {{ pin_subpackage("vqbg", max_pin="x") }}

requirements:
  build:
    - make
    - glpk
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
  host:
    - glpk
    - zlib
    - boost-cpp

test:
  commands:
    - VQBG --help

about:
  home: "https://github.com/qdu-bioinfo/VQBG"
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: "De Novo Reconstruction of Viral Quasispecies from Bubble Graph"
  dev_url: "https://github.com/qdu-bioinfo/VQBG"

extra:
  additional-platforms:
    - linux-aarch64
