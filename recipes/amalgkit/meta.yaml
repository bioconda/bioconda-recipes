{% set name = "amalgkit" %}
{% set version = "0.12.15" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: https://github.com/kfuku52/amalgkit/archive/refs/tags/v{{ version }}.tar.gz
  sha256: 390a28b0159e8761da020f0edc1909e89293bc34f7db40a069bd06f522c860e6

build:
  noarch: python
  number: 0
  run_exports:
    - {{ pin_subpackage("amalgkit", max_pin="x.x") }}
  script: "{{ PYTHON }} -m pip install . -vv"

requirements:
  host:
    - python >=3.8
    - pip
  run:
    - python >=3.8
    - numpy
    - pandas
    - biopython
    - lxml

test:
  commands:
    - amalgkit --help
  imports:
    - amalgkit

about:
  home: "https://github.com/kfuku52/amalgkit"
  license: "BSD-3-Clause"
  summary: "Tools for transcriptome amalgamation"
  license_file: LICENSE

extra:
  recipe-maintainers:
    - kfuku52