package:
  name: genonets
  version: "1.1.6"

source:
  fn: genonets-1.1.6.tar.gz
  url: https://pypi.python.org/packages/c5/fa/7c26ef0b264b1217184dcbc20b5eed3abea107484a20c756197f97ffadf6/genonets-1.1.6.tar.gz
  md5: a3ad65614c18f8ec436325e9a08e3515
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch: python
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - genonets = genonets:main
    #
    # Would create an entry point called genonets that calls genonets.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-igraph >=0.6
    - numpy >=1.8.2

  run:
    - python
    - python-igraph >=0.6
    - numpy >=1.8.2

test:
  # Python imports
  imports:
    - genonets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fkhalid/genonets
  license: MIT License
  summary: 'Framework for creating and analyzing genotype networks from data.'
  license_family: MIT

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
