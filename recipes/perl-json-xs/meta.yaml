package:
  name: perl-json-xs
  version: "2.34"

source:
  url: "https://cpan.metacpan.org/authors/id/M/ML/MLEHMANN/JSON-XS-2.34.tar.gz"
  md5: dda3c03335665cdeff91c37a269b833b

build:
  number: 1

requirements:
  build:
    - perl
    - perl-common-sense

  run:
    - perl
    - perl-common-sense

test:
  # Perl 'use' tests
  imports:
    - JSON::XS

about:
  home: 'http://search.cpan.org/~mlehmann/JSON-XS/'
  license: perl_5
  summary: 'JSON serialising/deserialising, done correctly and fast'
