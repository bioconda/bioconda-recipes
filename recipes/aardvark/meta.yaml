{% set name = "aardvark" %}
{% set version = "1.0.0" %}
{% set sha256 = "876a43b545c27b61dfeba438adc1cc86f2946390fe347ddfbdb2956a9a6ee2b6" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://github.com/PacificBiosciences/{{ name }}/archive/v{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  run_exports:
    - {{ pin_subpackage(name, max_pin="x.x") }}

requirements:
  build:
    - {{ compiler('rust') }}
    - {{ stdlib('c') }}
    - cargo-bundle-licenses

test:
  commands:
    - aardvark --version

about:
  home: "https://github.com/PacificBiosciences/aardvark"
  license: MIT
  license_family: MIT
  license_file:
    - LICENSE.md      # the license file for your package
    - THIRDPARTY.yml  # this file is generated by cargo-bundle-licenses
  summary: "A tool for sniffing out the differences in vari-Ants."
  dev_url: "https://github.com/PacificBiosciences/aardvark"

extra:
  additional-platforms:
    - linux-aarch64
    - osx-arm64
  recipe-maintainers:
    - holtjma
    - zeeev
