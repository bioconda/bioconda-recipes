{% set name = "aardvark" %}
{% set version = "0.8.1" %}
{% set sha256 = "fd7a9e88410ccbec7021a865a9035e2bc5225aa11e47e80580bbe24614c15f00" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://github.com/PacificBiosciences/{{ name }}/archive/v{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 1
  run_exports:
    - {{ pin_subpackage(name, max_pin="x.x") }}

requirements:
  build:
    - {{ compiler('rust') }}
    - cargo-bundle-licenses

test:
  commands:
    - aardvark --version

about:
  home: "https://github.com/PacificBiosciences/aardvark"
  license: MIT
  license_family: MIT
  license_file:
    - LICENSE.md      # the license file for your package
    - THIRDPARTY.yml  # this file is generated by cargo-bundle-licenses
  summary: "A tool for sniffing out the differences in vari-Ants."
  dev_url: "https://github.com/PacificBiosciences/aardvark"

extra:
  additional-platforms:
    - linux-aarch64
    - osx-arm64
  recipe-maintainers:
    - holtjma
    - zeeev
