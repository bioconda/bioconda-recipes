{% set name = "cogent3" %}
{% set version = "2026.7.6a0" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: f29fa8850fbc5cbc611079c0ddc48e8d4c012c2a22e0233a5a2035518c3a3b40

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . --no-deps --no-build-isolation --no-cache-dir -vvv
  number: 0
  run_exports:
  - {{ pin_subpackage(name, max_pin=None) }}

requirements:
  host:
    - python >=3.11,<3.15
    - pip
    - flit-core >=3.2,<4
  run:
    - python >=3.11,<3.15
    - scipy
    - charset-normalizer
    - clangdev
    - llvmlite
    - citeable >=2026.3.11b1
    - loky !=3.5.0
    - numpy <2.4
    - numba >=0.55.0
    - numba >=0.57.0  # [ py == 311 ]
    - numba >=0.59.0  # [ py == 312 ]
    - numba >=0.61.0  # [ py == 313 ]
    - scitrack
    - scinexus >=2026.5.16
    - typing_extensions
    - stevedore

test:
  imports:
    - cogent3

about:
  home: "https://github.com/cogent3/cogent3"
  summary: "COmparative GENomics Toolkit 3: genomic sequence analysis within notebooks or on compute systems with 1000s of CPUs."
  license: "BSD-3-Clause"
  license_family: BSD
  license_file: LICENSE
  dev_url: "https://github.com/cogent3/cogent3"
  doc_url: "https://github.com/cogent3/cogent3/blob/{{ version }}/README.md"

extra:
  recipe-maintainers:
    - dpryan79
    - GavinHuttley
    - khiron
