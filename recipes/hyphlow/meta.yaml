{% set version = "1.0.8" %}

package:
  name: hyphlow
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/h/hyphlow/hyphlow-{{ version }}.tar.gz
  sha256: 6e3c49855738101a10ac074d577d4e78c0f78e2e08ec80dd9aff3df44c6483d2

build:
  number: 0
  noarch: python
  entry_points:
    - hyphlow = hyphlow.main:main
  script: {{ PYTHON }} -m pip install . -vvv --no-deps --no-build-isolation --no-cache-dir
  run_exports:
    - {{ pin_subpackage('hyphlow', max_pin="x") }}

requirements:
  host:
    - python >=3.8,<3.13
    - setuptools >=61.0
    - pip
  run:
    - python >=3.8,<3.13
    - hyphy
    - pyqt
    - qtawesome
    - pandas
    - ete3
    - rapidfuzz
    - taxopy
    - xlsxwriter
    - matplotlib-base
    - seaborn-base

test:
  imports:
    - hyphlow

about:
  home: "https://pypi.org/project/hyphlow"
  summary: "Bioinformatics selection analysis pipeline and GUI."
  license: MIT
  license_family: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - hellojung0810
