{% set name = "pedesigner" %}
{% set version = "0.2.0" %}
{% set sha256 = "a8de59429096384cedf5e8660eab7a5648a9b91f610d27a686c6a62d6b4c91ca" %}

package:
  name: {{ name }}
  version: {{ version }}

build:
  number: 0
  noarch: python

source:
  url: https://github.com/VeredKunik/{{ name }}/archive/refs/tags/v{{ version }}.tar.gz
  sha256: {{ sha256 }}

requirements:
  build:
    - python >=3.7

  run:
    - python >=3.7
    - cas-offinder

test:
  imports:
    - pedesigner
  commands:
    - pedesigner --help

about:
  home: https://github.com/VeredKunik/pedesigner
  license: GPL3
  license_family: GPL3
  summary: "A tool for prime-editing guideRNA (pegRNA) design"

extra:
  recipe-maintainers:
    - VeredKunik
