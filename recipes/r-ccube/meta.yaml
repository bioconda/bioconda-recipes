{% set name = 'ccube' %}
{% set version = 'v1.0_beta.1' %}
{% set sha256 = '6220b10b98257f7eaa8d69f52c0d83788cf392249bfa0b63431ecce18b21ce1d' %}

package:
  name: r-{{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/keyuan/{{ name }}/archive/v{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  script:  R CMD INSTALL --build . 
  rpaths:
    - lib/R/lib/
    - lib/

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
  host:
    - boost-cpp
    - r-base
    - r-dplyr
    - r-foreach
    - r-matrix
    - r-pracma
  run:
    - boost-cpp
    - r-base
    - r-dplyr
    - r-foreach
    - r-matrix
    - r-pracma

test:
  commands:
    - $R -e "library('ccube')"

about:
  home: https://github.com/keyuan/ccube
  summary: R package for clustering and estimating cancer cell fractions (CCF)
    of somatic variants (SNVs/SVs) from bulk whole genome/exome data.
  license: GPL-3
  license_family: GPL3
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-3'

extra:
  recipe-maintainers:
    - tdido
