--- hyphy-src/src/core/regex.cpp
+++ hyphy-src/src/core/regex.cpp
@@ -2999,3 +2999,3 @@
         regs->num_regs = 0;
-        regs->start = regs->end = (regoff_t) 0;
+        regs->start = regs->end = (regoff_t*) nullptr;
     }
