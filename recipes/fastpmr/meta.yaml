{% set name = "fastpmr" %}
{% set version = "0.1.1" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://github.com/ehuangc/{{ name }}/archive/v{{ version }}.tar.gz
  sha256: "912576d093f7b07b687428b896313506fb1e9a72730b337c988900f622ef1dc7"

build:
  number: 0
  run_exports:
    - {{ pin_subpackage(name, max_pin="x.x") }}

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - {{ compiler('rust') }}
    - cargo-bundle-licenses
    - pkg-config

test:
  commands:
    - fastpmr --help

about:
  home: "https://github.com/ehuangc/fastpmr"
  license: MIT
  license_family: MIT
  license_file:
    - LICENSE
    - THIRDPARTY.yml
  summary: "Calculate pairwise mismatch rates (PMRs) between ancient DNA sequences."
  dev_url: "https://github.com/ehuangc/fastpmr"

extra:
  additional-platforms:
    - linux-aarch64
    - osx-arm64
  recipe-maintainers:
    - ehuangc
