{% set name = "fastpmr" %}
{% set version = "0.2.1" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://github.com/ehuangc/{{ name }}/archive/v{{ version }}.tar.gz
  sha256: "f41041d41c56ca45cf11621722b47f32c4ae9da8254e26173f40166ac12d9c0f"

build:
  number: 0
  run_exports:
    - {{ pin_subpackage(name, max_pin="x.x") }}

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - {{ compiler('rust') }}
    - cargo-bundle-licenses
    - pkg-config

test:
  commands:
    - fastpmr --help

about:
  home: "https://github.com/ehuangc/fastpmr"
  license: MIT
  license_family: MIT
  license_file:
    - LICENSE
    - THIRDPARTY.yml
  summary: "Calculate pairwise mismatch rates (PMRs) between ancient DNA sequences."
  dev_url: "https://github.com/ehuangc/fastpmr"

extra:
  additional-platforms:
    - linux-aarch64
    - osx-arm64
  recipe-maintainers:
    - ehuangc
