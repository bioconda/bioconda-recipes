{% set name = "nucflag" %}
{% set version = "1.0.1" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/nucflag-{{ version }}.tar.gz
  sha256: 61de011797d00f11e321955343fcab56b1d5b9ffb1d3cdd18e790952578f78b8

build:
  number: 0
  noarch: python
  entry_points:
    - nucflag = nucflag.main:main
  script: {{ PYTHON }} -m pip install . --no-deps --no-build-isolation --no-cache-dir -vvv
  run_exports:
    - {{ pin_subpackage(name, max_pin="x") }}

requirements:
  host:
    - python >=3.11
    - setuptools
    - pip
  run:
    - python >=3.11
    - matplotlib-base >=3.8.3
    - numpy >=1.26.4
    - polars >=1.6.0
    - python-intervaltree >=3.2.1
    - py_nucflag >=0.1.9
    - pybigwig >=0.3.25
    - pyideogram >=0.1.0

test:
  imports:
    - nucflag
  commands:
    - nucflag --help

about:
  home: "https://github.com/logsdon-lab/NucFlag"
  summary: "NucFlag misassembly identifier."
  license: MIT
  license_family: MIT
  license_file: LICENSE
  dev_url: "https://github.com/logsdon-lab/NucFlag"
  doc_url: "https://github.com/logsdon-lab/NucFlag/wiki"

extra:
  recipe-maintainers:
    - koisland
