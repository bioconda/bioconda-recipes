{% set name = "salmon" %}
{% set version = "1.12.1" %}
# salmon 1.12.1 is the latest (and final-line) C++ release. This is a *version-pinned subrecipe*: it
# builds the same `salmon` package as recipes/salmon/meta.yaml (the Rust 2.0 line),
# but at the legacy 1.12.x C++ line. Users who need the C++ build install it
# explicitly with `conda install "salmon=1.12"`. v1.12.1 is an immutable tag; the
# C++ tree also lives on the `cpp` branch for future maintenance.
{% set sha256 = "cf025056ed0f6b5f8841ebd9b036076ab50c762d1c16125430cd5db3ee9600f7" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  - url: https://github.com/COMBINE-lab/salmon/archive/refs/tags/v{{ version }}.tar.gz
    sha256: {{ sha256 }}

build:
  # First build of the 1.12.1 C++ release (linux-64/osx-64/linux-aarch64/osx-arm64).
  number: 0
  run_exports:
    - {{ pin_subpackage("salmon", max_pin="x") }}

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - {{ stdlib('c') }}
    - cmake <4.0.0
    - ninja
    - make
    - pkg-config
  host:
    - cereal >=1.3.2
    - boost-cpp >=1.84
    - tbb >=2021.4
    - htslib >=1.23
    - mimalloc >=3.2.8
    - tbb-devel >=2022.3.0
    - libiconv
    - bzip2
    - xz
    - curl
  run:
    - {{ pin_compatible('htslib') }}
    - {{ pin_compatible('boost-cpp') }}
    - tbb
    - bzip2
    - xz
    - curl
    - boost-cpp
    - icu

test:
  commands:
    - salmon --help
    - salmon --version | grep -q 1.12.1

about:
  home: "https://github.com/COMBINE-lab/salmon"
  license: "GPL-3.0-or-later"
  license_family: GPL3
  license_file: LICENSE
  summary: "Transcript-level quantification from RNA-seq reads (final-line C++ salmon, 1.12.1)."
  description: |
    salmon 1.12.1 is the latest C++ release of salmon (selective-alignment
    transcript quantification). The default `salmon` package is the maintained
    Rust 2.0 build; install this legacy C++ line explicitly with
    `conda install "salmon=1.12"`, ideally in a dedicated environment.
  dev_url: "https://github.com/COMBINE-lab/salmon"
  doc_url: "https://salmon.readthedocs.io"

extra:
  # Version-pinned subrecipe: frozen w.r.t. the autobump bot (subrecipes are
  # excluded by default; this makes the intent explicit). Future C++ bugfixes are
  # bumped here by hand from a new tag cut off the `cpp` branch.
  autobump:
    enable: false
  additional-platforms:
    - linux-aarch64
    - osx-arm64
  recipe-maintainers:
    - rob-p
    - k3yavi
    - mjsteinbaugh
  identifiers:
    - doi:10.1038/nmeth.4197
  notes: |
    This subrecipe builds the `salmon` binary from the final-line C++ release (1.12.1).
    Because it is the same package name as the Rust 2.0 build, conda installs one
    or the other in a given environment — `conda install salmon` gets the latest
    (Rust 2.0); `conda install "salmon=1.12"` (e.g. in a dedicated env) gets this
    C++ line. The C++ build uses a new index format incompatible with Rust 2.0
    indices, so rebuild indices when switching. The pufferfish dependency is
    pinned to 1c78859 (the mapping/decoy-attribution and gcBias-crash fixes
    shipped in 1.12.1).
