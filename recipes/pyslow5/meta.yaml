{% set version = "0.2.0" %}

package:
  name: pyslow5
  version: {{ version }}

source:
  url: https://github.com/hasindu2008/slow5lib/archive/refs/tags/v0.2.0.tar.gz
  sha256: 22347a0675ca7633c09e8751cd8df54f278e2bd1ff91be89fe2712b760416154

build:
  number: 0

requirements:
  build:
    - {{ compiler('c') }}
  host:
    - python
    - cython
    - setuptools
    - zlib
  run:
    - python
    - zlib

test:
  imports:
    - pyslow5

about:
  home: https://github.com/hasindu2008/slow5lib
  license: MIT
  summary: 'slow5lib Python binding'
