{% set version = "0.4.5" %}


package:
  name: mimsi
  version: {{ version }}

source:
  url: https://github.com/mskcc/mimsi/archive/refs/tags/v{{ version }}.tar.gz
  sha256: 3cf3c8abff752dee4e7145fc8d7e8d0734b68a1d83958110340b15f3f47bc802

build:
  number: 0
  noarch: python
  script: "{{ PYTHON }} -m pip install . --no-deps --ignore-installed --no-cache-dir "

requirements:
  host:
    - python >=3.6,<3.9
    - pip
  run:
    - python >=3.6,<3.9
    - pytorch
    - numpy
    - pandas
    - pysam
    - scikit-learn
    - matplotlib-base
    - nose >=1.3.7
  
  
  

test:
  commands:
    - evaluate_sample --version
    - analyze --version
    - create_data --version
    - mi_msi_train_test --version
    - visualize_instance --version

about:
  home: https://github.com/mskcc/mimsi
  summary: 'A Deep Mulitple Instance Learning Classifier for Microsatellite Instability'
  license: GNU GPL v3.0
