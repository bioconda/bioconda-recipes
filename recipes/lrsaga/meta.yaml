{% set name = "lrsaga" %}
{% set version = "0.1" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://github.com/jamiemcg/{{ name }}/archive/refs/tags/{{ version }}.tar.gz
  sha256: 0445d89c53641d9d636baf785e4cf0d3ec5a38079007ea497431dede7a8ca823

build:
  number: 0
  noarch: python
  script: bash conda_build_package.sh

  run_exports:
    - {{ pin_subpackage('lrsaga', max_pin='x.x') }}

requirements:
  build:
    - python
  run:
    - python
    - gfapy
    - mbg
    - networkx

test:
  commands:
    - lrSAGA.py -h
    - gfa_stats.py -h
    - gfa_to_fasta.py -h
    - preprocess_mbg_gfa.py -h

about:
  home: https://github.com/jamiemcg/lrSAGA
  license: MIT
  license_family: MIT
  summary: 'lrSAGA (long-read Single Amplified Genome Assembly).'

extra:
  maintainers:
    - jamiemcg
