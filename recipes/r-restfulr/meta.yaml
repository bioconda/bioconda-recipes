{% set version = '0.0.17' %}

package:
  name: r-restfulr
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/restfulr_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/restfulr/restfulr_{{ version }}.tar.gz
  sha256: e50b9c4a9886f3756a9610f89f24d4de97262e7c5769dc3e4824ce888c304057

build:
  number: 0
  rpaths:
    - lib/R/lib/
    - lib/
  run_exports:
    - {{ pin_subpackage("r-restfulr", max_pin="x.x") }}

requirements:
  build:
    - {{ compiler('c') }}
    - {{ stdlib('c') }}
  host:
    - r-base
    - r-rcurl
    - bioconductor-s4vectors >=0.13.15
    - r-xml
    - r-rjson
    - r-yaml
  run:
    - r-base
    - r-rcurl
    - bioconductor-s4vectors >=0.13.15
    - r-xml
    - r-rjson
    - r-yaml

test:
  commands:
    - $R -e "library('restfulr')"

extra:
  additional-platforms:
    - linux-aarch64
    - osx-arm64
about:
  home: https://CRAN.R-project.org/package=restfulr
  license: Artistic-2.0
  summary: Models a RESTful service as if it were a nested R list.
  license_family: OTHER
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/Artistic-2.0'
