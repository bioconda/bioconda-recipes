{% set version = "1.1" %}

package:
  name: jupiterplot
  version: {{ version }}

source:
  url: https://github.com/JustinChu/JupiterPlot/releases/download/{{ version }}/jupiterplot-{{ version }}.tar.gz
  sha256: 8eeb95b60b1f773e8759a0a8da0e9d4b6dd2e95691fa1e623bdf4092b63f5d39

build:
  number: 0
  noarch: generic
  run_exports:
    - {{ pin_subpackage('jupiterplot', max_pin="x") }}
     
requirements:
  run:
    - circos
    - circos-tools
    - minimap2
    - samtools
    - make
    - perl-gd
  host:
    - perl

test:
  commands:
    - jupiter -h

about:
  home: https://github.com/JustinChu/JupiterPlot
  summary: "Circos Assembly Consistency (Jupiter) plot"
  license: GPL-3.0
  license_file: LICENSE

extra:
  identifiers:
    - doi:10.5281/zenodo.1241235