package : 
  name : repeatfinder
  version : "1.1.4"

source: 
  git_url: https://gitlab.fi.muni.cz/xkubin/bachelors-thesis.git
  git_rev: main
  git_depth: -1

requirements:
  build:
    - {{ compiler('c') }}
  host:
    - zlib
  run:
    - zlib