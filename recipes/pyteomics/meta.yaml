package:
  name: pyteomics
  version: "3.4"

source:
  fn: pyteomics-3.4.tar.gz
  url: https://pypi.python.org/packages/02/2d/0dfb9bd5b02db6228e3277020209ddb7217e12a76e39a1cb0d1c70756e30/pyteomics-3.4.tar.gz
  md5: 3c8d1bcc16c53100e531a10a2853f679
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyteomics = pyteomics:main
    #
    # Would create an entry point called pyteomics that calls pyteomics.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - pyteomics
    - pyteomics.mass
    - pyteomics.openms

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://hg.theorchromo.ru/pyteomics
  license: License :: OSI Approved :: Apache Software License
  summary: 'A framework for proteomics data analysis.'
  license_family: Proprietary

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
