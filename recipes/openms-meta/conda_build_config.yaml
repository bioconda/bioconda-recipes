# Override bioconda-utils qt6_main pin (6.7) to 6.8+
# qt6-main 6.8+ is built against libxml2-16 (libxml2 >=2.14), which is
# required by libarrow 23's Azure SDK transitive dependencies.
# qt6-main 6.7.x requires libxml2 <2.14, creating an unsolvable conflict.
qt6_main:
  - 6.9
