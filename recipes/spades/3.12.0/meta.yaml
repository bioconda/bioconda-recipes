package:
  name: spades
  version: '3.12.0'

source:
  fn: SPAdes-3.12.0-Linux.tar.gz                                              # [linux]
  url: http://cab.spbu.ru/files/release3.12.0/SPAdes-3.12.0-Linux.tar.gz  # [linux]
  fn: SPAdes-3.12.0-Darwin.tar.gz                                             # [osx]
  url: http://cab.spbu.ru/files/release3.12.0/SPAdes-3.12.0-Darwin.tar.gz # [osx]


build:
  number: 0
  skip: True  # [not py2k and not py35]

requirements:
  build:
  run:
    - python

test:
  commands:
    - spades.py --test

about:
  home: http://cab.spbu.ru/software/spades/
  license: GPLv2
  summary: SPAdes (St. Petersburg genome assembler) is intended for both standard isolates and single-cell MDA bacteria assemblies.
