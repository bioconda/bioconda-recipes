{% set name = "pilea" %}
{% set version = "1.3.4" %}

package:
  name: '{{ name|lower }}'
  version: '{{ version }}'

source:
  url: https://github.com/xinehc/pilea/archive/refs/tags/v{{ version }}.tar.gz
  sha256: d96a7c5779c8edf420bf3d7d23d17f563c03527e442603c9688281a92610d22f

build:
  number: 0
  script: '{{ PYTHON }} -m pip install . --no-deps --ignore-installed --no-cache-dir -vvv'
  run_exports:
    - {{ pin_subpackage(name, max_pin='x.x') }}

requirements:
  build:
    - {{ compiler('c') }}
  host:
    - python
    - setuptools
    - pip
    - wheel
    - cython
  run:
    - python
    - scikit-learn >=1.6.1
    - statsmodels >=0.14.4
    - needletail >=0.7.1
    - numpy
    - tqdm
    - threadpoolctl
    - rich-argparse
    - pigz
    - tar

test:
    imports:
      - pilea
    commands:
      - pilea -h

about:
  home: https://github.com/xinehc/pilea
  license: MIT
  license_family: MIT
  summary: 'Pilea: profiling bacterial growth dynamics from metagenomes with sketching'

extra:
  recipe-maintainers:
    - xinehc
  additional-platforms:
    - linux-aarch64
    - osx-arm64
