{% set name = "pilea" %}
{% set version = "1.3.7" %}

package:
  name: '{{ name|lower }}'
  version: '{{ version }}'

source:
  url: https://github.com/xinehc/pilea/archive/refs/tags/v{{ version }}.tar.gz
  sha256: 6157c371f0845310a8c2a7d637eee6c93049fc6cd7b0c947b1b685797cc87821

build:
  number: 0
  script: '{{ PYTHON }} -m pip install . --no-deps --ignore-installed --no-cache-dir -vvv'
  run_exports:
    - {{ pin_subpackage(name, max_pin='x.x') }}

requirements:
  build:
    - {{ compiler('c') }}
  host:
    - clang
    - python
    - setuptools
    - pip
    - wheel
    - cython
    - htslib
    - zlib
  run:
    - python
    - scikit-learn >=1.6.1
    - statsmodels >=0.14.4
    - numpy
    - tqdm
    - threadpoolctl
    - rich-argparse
    - wget
    - pigz
    - tar

test:
    imports:
      - pilea
    commands:
      - pilea -h

about:
  home: https://github.com/xinehc/pilea
  license: MIT
  license_family: MIT
  summary: 'Pilea: profiling bacterial growth dynamics from metagenomes with sketching'

extra:
  recipe-maintainers:
    - xinehc
  additional-platforms:
    - linux-aarch64
    - osx-arm64
