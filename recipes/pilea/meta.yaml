{% set name = "pilea" %}
{% set version = "1.3.1" %}

package:
  name: '{{ name|lower }}'
  version: '{{ version }}'

source:
  url: https://github.com/xinehc/pilea/archive/refs/tags/v{{ version }}.tar.gz
  sha256: f65bc91d6098c8486ce066c69248cbb9514701fb742eafd873f8703ee10f890f

build:
  number: 0
  script: '{{ PYTHON }} -m pip install . --no-deps --ignore-installed --no-cache-dir -vvv'
  run_exports:
    - {{ pin_subpackage(name, max_pin='x.x') }}

requirements:
  build:
    - {{ compiler('c') }}
  host:
    - python
    - setuptools
    - pip
    - wheel
    - cython
  run:
    - python
    - scikit-learn >=1.6.1
    - statsmodels >=0.14.4
    - needletail >=0.7.0
    - numpy
    - tqdm
    - threadpoolctl
    - rich-argparse
    - pigz
    - tar

test:
    imports:
      - pilea
    commands:
      - pilea -h

about:
  home: https://github.com/xinehc/pilea
  license: MIT
  license_family: MIT
  summary: 'Pilea: profiling bacterial growth dynamics from metagenomes with sketching'

extra:
  recipe-maintainers:
    - xinehc
  additional-platforms:
    - linux-aarch64
    - osx-arm64
