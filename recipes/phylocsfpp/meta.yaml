{% set version = "1.0.0_e8661ebc" %}
{% set commit_id = "e8661ebccb61be911ec822cce91186d9d6665a08" %}

package:
  name: "phylocsfpp"
  version: "{{ version }}"

source:
  git_url: https://github.com/cpockrandt/PhyloCSFpp.git
  git_rev: {{ commit_id }}

build:
  number: 0

requirements:
  build:
    - {{ compiler('cxx') }}
    - {{ compiler('c') }}
    - cmake >=3.2
    - make
    - zlib
  host:
    - openmp
    - gsl
    - zlib
  run:
    - openmp
    - gsl
    - zlib

test:
  commands:
      - phylocsf++ --help > /dev/null

about:
  home: https://github.com/cpockrandt/PhyloCSFpp
  license: AGPLv3
  license_file: LICENSE.md
  summary: 'A fast and user-friendly implementation of PhyloCSF with annotation tools.'

extra:
  skip-lints:
    - uses_vcs_url
    - missing_hash
