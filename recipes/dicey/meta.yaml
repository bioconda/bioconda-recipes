{% set name = "dicey" %}
{% set version = "0.5.1" %}
{% set sha256 = "8b314e7669337a56235b4b49bd870f39bd1eb04e0573ac19d0a1982a884b0bc5" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  - url: https://github.com/gear-genomics/dicey/archive/v{{ version }}.tar.gz
    sha256: {{ sha256 }}
  - folder: src/xxsds
    git_url: https://github.com/xxsds/sdsl-lite.git
    git_rev: a654b57

build:
  number: 0
  run_exports:
    - {{ pin_subpackage("dicey", max_pin="x.x") }}

requirements:
  build:
    - make
    - {{ compiler('cxx') }}
    - {{ stdlib('c') }}
    - cmake <4
    - autoconf
    - automake
    - libtool
    - pkg-config
  host:
    - zlib
    - bzip2
    - boost-cpp
    - xz
    - htslib
  run:
    - boost-cpp
    - htslib

test:
  commands:
    - dicey --help

about:
  home: "https://github.com/gear-genomics/dicey"
  license: "GPL-3.0-or-later"
  license_family: GPL3
  license_file: LICENSE
  summary: "In-silico PCR and variant primer design."
  dev_url: "https://github.com/gear-genomics/dicey"
  doc_url: "https://github.com/gear-genomics/dicey/blob/v{{ version }}/README.md"

extra:
  additional-platforms:
    - linux-aarch64
    - osx-arm64
  identifiers:
    - biotools:Gear-Genomics
    - doi:10.1186/s12864-020-6635-8
  skip-lints:
    - uses_vcs_url
    - missing_hash
