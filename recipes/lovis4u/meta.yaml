{% set name = "lovis4u" %}
{% set version = "0.1.8" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/lovis4u-{{ version }}.tar.gz
  sha256: 1b5d442da5fc5b778b1430f3250b0d736454069b983324e782bc1bf0fbde8015

build:
  number: 0
  noarch: python
  script: "{{ PYTHON }} -m pip install . --no-deps --no-build-isolation --no-cache-dir -vvv"
  run_exports:
    - {{ pin_subpackage('lovis4u', max_pin="x.x.x") }}

requirements:
  host:
    - python
    - pip
    - setuptools
  run:
    - python
    - biopython
    - configs
    - distinctipy
    - pandas
    - reportlab
    - bcbio-gff
    - matplotlib-base
    - seaborn
    - scipy
    - pyhmmer >=0.12.0
    - progress
    - requests

test:
  imports:
    - lovis4u

about:
  home: https://art-egorov.github.io/lovis4u/
  summary: Loci Visualisation Tool.
  license: BSD-3-Clause AND GPL-3.0 AND WTFPL
  license_file:
    - LICENSE
    - lovis4u/lovis4u_data/bin/mmseqs_mac/LICENSE.md
    - lovis4u/lovis4u_data/fonts/LICENSE

extra:
  recipe-maintainers:
    - art-egorov
