{% set name = "kalign3" %}
{% set version = "3.4.7" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

build:
  number: 0
  run_exports:
    - {{ pin_subpackage(name, max_pin='x') }}

source:
  url: https://github.com/TimoLassmann/kalign/archive/refs/tags/v{{ version }}.tar.gz
  sha256: 18d2b870a49cb58027f85eaa1973964f615d48968f4a06b8e238cfe6663f24b4

requirements:
  build:
  - {{ compiler('c') }}
  - {{ compiler('cxx') }}
  - make
  - cmake

test:
  commands:
    - kalign -v

about:
  home: https://github.com/TimoLassmann/kalign
  license: GPL-3.0-only
  license_file: COPYING
  summary: 'Kalign is a fast and accurate multiple sequence alignment algorithm.'

extra:
  additional-platforms:
    - linux-aarch64
    - osx-arm64
  identifiers:
    - doi:10.1093/bioinformatics/btz795
