package:
    name: "ucsc-hgloadnet"
    version: "324"
source:
    url: "http://hgdownload.cse.ucsc.edu/admin/exe/userApps.v324.src.tgz"          # [linux]
    fn: "userApps.src.tgz"                                                                     # [linux]
    md5: "c49c168584c5917081d71df854e3cd9d"                                                    # [linux]
    patches:                                                                                   # [linux]
        - "include.patch"                                                                      # [linux]

    url: "http://hgdownload.cse.ucsc.edu/admin/exe/macOSX.x86_64/hgLoadNet"                    # [osx]
    fn: "hgLoadNet"                                                                            # [osx]

build:                                                                                       # [osx]
    script: "mkdir -p $PREFIX/bin; cp hgLoadNet $PREFIX/bin; chmod +x $PREFIX/bin/hgLoadNet" # [osx]

requirements:
    build:
        - libpng                                                    # [linux]
        - mysql                                                     # [linux]
        - zlib                                                      # [linux]
        - openssl                                                   # [linux]

    run:                                                            # [linux]
        - libpng                                                    # [linux]
        - mysql                                                     # [linux]
        - zlib                                                      # [linux]
        - openssl                                                   # [linux]

test:
    commands:
        - hgLoadNet 2&>1 | grep hgLoadNet

about:
    home: "http://hgdownload.cse.ucsc.edu/admin/exe/"
    license: "varies; see http://genome.ucsc.edu/license"
    summary: "Load a generic net file into database"
