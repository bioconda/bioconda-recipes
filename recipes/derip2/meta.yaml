{% set name = "derip2" %}
{% set version = "0.3.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/derip2-{{ version }}.tar.gz
  sha256: 674807547e97e6201dfcaedd5a58877e2511d096a2a91403a8c5c007d3d28e17

build:
  run_exports:
    - {{ pin_subpackage('myrecipe', max_pin="x.x") }}
  entry_points:
    - derip2 = derip2.app:main
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.8
    - hatchling
    - hatch-vcs
    - pip
  run:
    - python >=3.8
    - biopython >1.80
    - click
    - matplotlib-base
    - numpy
    - pandas
    - tqdm

test:
  imports:
    - derip2
  commands:
    - pip check
    - derip2 --help
  requires:
    - pip

about:
  home: https://github.com/Adamtaranto/deRIP2
  summary: Predict ancestral sequence of fungal repeat elements by correcting for RIP-like mutations in multi-sequence DNA alignments.
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - Adamtaranto
