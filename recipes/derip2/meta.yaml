{% set name = "derip2" %}
{% set version = "0.5.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/derip2-{{ version }}.tar.gz
  sha256: 12fe00445adf2e5415a7c21cc76ba6bff2b87cb778ffcec78c0113fac4d841ec

build:
  run_exports:
    - {{ pin_subpackage('derip2', max_pin="x.x") }}
  entry_points:
    - derip2 = derip2.app:main
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.9
    - hatchling
    - hatch-vcs
    - pip
  run:
    - python >=3.9
    - biopython >1.80
    - click
    - ete4
    - iqtree
    - matplotlib-base
    - numpy
    - pandas
    - tqdm

test:
  imports:
    - derip2
  commands:
    - pip check
    - derip2 --help
    - derip2 --version
    - derip2-spectra --version
    - derip2-spectra --help
  requires:
    - pip

about:
  home: https://adamtaranto.github.io/deRIP2/ 
  dev_url: https://github.com/Adamtaranto/deRIP2
  summary: Predict ancestral sequence of fungal repeat elements by correcting for RIP-like mutations in multi-sequence DNA alignments.
  license: GPL-3.0-or-later
  license_file: LICENSE

extra:
  recipe-maintainers:
    - Adamtaranto
