{% set version = "0.2.2" %}

package:
  name: predictosaurus
  version: {{ version }}

build:
  number: 0
  run_exports:
    - {{ pin_subpackage('predictosaurus', max_pin="x") }}

source:
  url: https://github.com/fxwiegand/predictosaurus/archive/refs/tags/v{{ version }}.tar.gz
  sha256: 0f104ba3a6b0f98856d9140b3903d6721545b20d5f5a1e75acdc97071afb73d9

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - {{ compiler('rust') }}
    - clangdev
    - pkg-config
    - make
    - cmake
  host:
    - gsl
    - libcblas
    - libcurl
    - openssl

test:
  commands:
    - predictosaurus --help

about:
  home: https://github.com/fxwiegand/predictosaurus
  license: MIT
  license_family: MIT
  summary: |
    Predictosaurus is a command-line tool designed for uncertainty-aware haplotype-based genomic variant effect prediction.
  dev_url: https://github.com/fxwiegand/predictosaurus

extra:
  additional-platforms:
    - linux-aarch64
    - osx-arm64
  recipe-maintainers:
    - fxwiegand
    - felixmoelder
    - johanneskoester
