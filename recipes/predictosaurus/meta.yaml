{% set name = "predictosaurus" %}
{% set version = "0.11.2" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://github.com/fxwiegand/predictosaurus/archive/refs/tags/v{{ version }}.tar.gz
  sha256: ddbf4a9a74c69f04c4ce5e3f15f7d3a6f62cd8dfe61ff5f6ec35b72fde67174a

build:
  number: 0
  run_exports:
    - {{ pin_subpackage(name, max_pin="x.x") }}

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - {{ compiler('rust') }}
    - cargo-bundle-licenses
    - pkg-config
    - make
    - cmake
    - clangdev <22
  host:
    - gsl
    - libcblas
    - libcurl
    - openssl  # [not osx]

test:
  commands:
    - predictosaurus --help

about:
  home: "https://github.com/fxwiegand/predictosaurus"
  license: MIT
  license_family: MIT
  summary: "Predictosaurus is a command-line tool designed for uncertainty-aware haplotype-based genomic variant effect prediction."
  dev_url: "https://github.com/fxwiegand/predictosaurus"
  doc_url: "https://github.com/fxwiegand/predictosaurus/blob/v{{ version }}/README.md"

extra:
  additional-platforms:
    - linux-aarch64
    - osx-arm64
  recipe-maintainers:
    - fxwiegand
    - felixmoelder
    - johanneskoester
  skip-lints:
    - compiler_needs_stdlib_c
