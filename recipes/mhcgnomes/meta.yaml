{% set name = "mhcgnomes" %}
{% set version = "2.0.3" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: "https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz"
  sha256: fdc68b1943cbd9fa407182f93aeaa7ba182692c474460cb67679e80debbad11f

build:
  number: 0
  noarch: python
  script: "{{ PYTHON }} -m pip install . --no-deps --no-build-isolation -vv"
  run_exports:
    - {{ pin_subpackage(name, max_pin="x") }}

requirements:
  host:
    - pip
    - python
    - setuptools
  run:
    - numpy
    - pandas
    - python
    - pyyaml >=5.4
    - serializable

test:
  imports:
    - mhcgnomes

about:
  home: "https://github.com/til-unc/mhcgnomes"
  license: Apache-2.0
  license_family: APACHE
  summary: "Python library for parsing MHC nomenclature in the wild"

extra:
  recipe-maintainers:
    - jonasscheid
