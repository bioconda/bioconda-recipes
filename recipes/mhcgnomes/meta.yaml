{% set name = "mhcgnomes" %}
{% set version = "3.48.1" %}

package:
  name: "{{ name }}"
  version: "{{ version }}"

source:
  url: "https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz"
  sha256: 0bb4045874a4cdfbe72d615003da939b945ca8e1f8ad58d7d02be60f1277662d

build:
  number: 0
  noarch: python
  script: "{{ PYTHON }} -m pip install . --no-deps --no-build-isolation --no-cache-dir -vvv"
  entry_points:
    - mhcgnomes = mhcgnomes.cli:main
  run_exports:
    - {{ pin_subpackage(name, max_pin="x") }}

requirements:
  host:
    - pip
    - python >=3.9
    - setuptools
  run:
    - numpy
    - pandas
    - python >=3.9
    - pyyaml >=5.4
    - serializable

test:
  imports:
    - mhcgnomes
  commands:
    - mhcgnomes --help

about:
  home: "https://github.com/pirl-unc/mhcgnomes"
  license: "Apache-2.0"
  license_family: APACHE
  license_file: LICENSE
  summary: "Python library for parsing MHC nomenclature in the wild."
  dev_url: "https://github.com/pirl-unc/mhcgnomes"
  doc_url: "https://pirl-unc.github.io/mhcgnomes"

extra:
  recipe-maintainers:
    - jonasscheid
