{% set name = "mhcgnomes" %}
{% set version = "2.0.8" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: "https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz"
  sha256: f2482748ad1f101840dfdeba34b4b2be88c0cf85f54c2f5e49bb237141750543

build:
  number: 0
  noarch: python
  script: "{{ PYTHON }} -m pip install . --no-deps --no-build-isolation -vv"
  run_exports:
    - {{ pin_subpackage(name, max_pin="x") }}

requirements:
  host:
    - pip
    - python
    - setuptools
  run:
    - numpy
    - pandas
    - python
    - pyyaml >=5.4
    - serializable

test:
  imports:
    - mhcgnomes

about:
  home: "https://github.com/til-unc/mhcgnomes"
  license: Apache-2.0
  license_family: APACHE
  summary: "Python library for parsing MHC nomenclature in the wild"

extra:
  recipe-maintainers:
    - jonasscheid
