{% set name = "mhcgnomes" %}
{% set version = "3.37.0" %}

package:
  name: "{{ name }}"
  version: "{{ version }}"

source:
  url: "https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz"
  sha256: 2494ac2e9f86cd9f9806eb9cd4f4e21cb31a9df39bf2dcf35702ce7b64b639a5

build:
  number: 0
  noarch: python
  script: "{{ PYTHON }} -m pip install . --no-deps --no-build-isolation --no-cache-dir -vvv"
  entry_points:
    - mhcgnomes = mhcgnomes.cli:main
  run_exports:
    - {{ pin_subpackage(name, max_pin="x") }}

requirements:
  host:
    - pip
    - python >=3.9
    - setuptools
  run:
    - numpy
    - pandas
    - python >=3.9
    - pyyaml >=5.4
    - serializable

test:
  imports:
    - mhcgnomes
  commands:
    - mhcgnomes --help

about:
  home: "https://github.com/pirl-unc/mhcgnomes"
  license: "Apache-2.0"
  license_family: APACHE
  license_file: LICENSE
  summary: "Python library for parsing MHC nomenclature in the wild."
  dev_url: "https://github.com/pirl-unc/mhcgnomes"
  doc_url: "https://pirl-unc.github.io/mhcgnomes"

extra:
  recipe-maintainers:
    - jonasscheid
