{% set name = "mhcgnomes" %}
{% set version = "2.0.6" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: "https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz"
  sha256: fa0486505c3f4ce36d08693dabd0028d2f33559e1a097f6dcb44a4a955940edc

build:
  number: 0
  noarch: python
  script: "{{ PYTHON }} -m pip install . --no-deps --no-build-isolation -vv"
  run_exports:
    - {{ pin_subpackage(name, max_pin="x") }}

requirements:
  host:
    - pip
    - python
    - setuptools
  run:
    - numpy
    - pandas
    - python
    - pyyaml >=5.4
    - serializable

test:
  imports:
    - mhcgnomes

about:
  home: "https://github.com/til-unc/mhcgnomes"
  license: Apache-2.0
  license_family: APACHE
  summary: "Python library for parsing MHC nomenclature in the wild"

extra:
  recipe-maintainers:
    - jonasscheid
