package:
  name: viennarna
  version: 2.4.3.1

build:
  number: 0

source:
  fn: ViennaRNA-2.4.3.1.tar.gz
  url: ftp://biftp.informatik.uni-freiburg.de/pub/vienna-rna/ViennaRNA-2.4.3.1.tar.gz ##https://www.tbi.univie.ac.at/RNA/download/sourcecode/2_4_x/ViennaRNA-2.4.3.tar.gz
  sha256: 4a7cb1af711054c216a5a60f9504dd0790f5157ea982808d574c4d8e098798b3
  ##patches:
  ##  - patch.Fix-CPPFLAGS-for-Perl

requirements:
  build:
    - perl
    - python >3 # [linux]
    # - autoconf
    # - automake 
    # - libtool
    # - pkg-config
    # - check
  run:
    - perl

test:
  commands:
   - RNAalifold --version
   - RNAeval    --version
   - RNAfold    --version
   - RNAplfold  --version
   - RNAsubopt  --version
   - RNAlocmin  --version
   - perl -MRNA -e 'print "testimport"' | grep "testimport"
   - kinwalker  --help >& /dev/null

about:
  home: http://www.tbi.univie.ac.at/RNA/
  license: custom
  license_file: COPYING
  summary: Vienna RNA package -- RNA secondary structure prediction and comparison
