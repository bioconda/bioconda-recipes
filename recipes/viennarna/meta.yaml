{% set version = "2.4.18" %}
{% set sha256 = "7caa723f5e5cd61a60aba60b24a64db39817a76244293754b00fe83a1dab609d" %}

package:
  name: viennarna
  version: {{ version }}

build:
  number: 0
  run_exports:
    # Ronny said VRNA is stable in x.x releases
    - {{ pin_subpackage('viennarna', max_pin='x.x') }}

source:
  url: https://www.tbi.univie.ac.at/RNA/download/sourcecode/2_4_x/ViennaRNA-{{ version }}.tar.gz
  sha256: {{ sha256 }}

requirements:
  build:
    - make
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
  host:
    - pkgconfig ## needed for viennarna version checks in dependent packages
    - perl
    - python
    - zlib
    - llvm-openmp # [osx]
  run:
    - python
    - llvm-openmp # [osx]

test:
  commands:
    - RNAalifold --version
    - RNAeval    --version
    - RNAfold    --version
    - RNAplfold  --version
    - RNAsubopt  --version
    - RNAlocmin  --version
    - kinwalker  --help >& /dev/null

about:
  home: http://www.tbi.univie.ac.at/RNA/
  license: custom
  license_file: COPYING
  summary: Vienna RNA package -- RNA secondary structure prediction and comparison
