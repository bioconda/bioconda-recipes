about:
    home: 'ftp://ftp.ncbi.nlm.nih.gov/pub/agarwala/bmtagger/'
    summary: "BMTagger: Best Match Tagger for removing human reads from metagenomics datasets"
package: 
  name: bmtagger
  version: '3.101'
source:
  fn: bmtools.tar.gz
  md5: 719474a0e3b54449cbe06f6e442994dd
  url: ftp://ftp.ncbi.nlm.nih.gov/pub/agarwala/bmtagger/bmtools.tar.gz # [linux64]
requirements:
  build:
    - blast
  run:
    - blast
test:
    commands:
        #- "ls $PREFIX/bin && cd $SRC_DIR/bmtagger && ls ./ && echo '-----' && ls ./ && bmtagger.sh -V"# &> /dev/null"
        #- "ls $PREFIX/bin && which bmfilter && bmtagger.sh -V"# &> /dev/null"
        - "bmtagger.sh -V"# &> /dev/null"
