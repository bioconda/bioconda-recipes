{% set name = "barriers" %}
{% set version = "1.7.0" %}
{% set sha256 = "7061db4974b91d8dbace21256a7f3a9776f45358cc473e197d2f2dc7f236013a" %}

package:
  name: {{ name }}
  version: {{ version }}

about:
  home: https://www.tbi.univie.ac.at/RNA/Barriers/
  license: GPL
  license_file: COPYING
  summary: "Compute local minima and energy barriers of a landscape."

build:
  number: 1

source:
  url: https://github.com/ViennaRNA/Barriers/archive/v1.7.0.tar.gz
  sha256: {{ sha256 }}

requirements:
  build:
    - {{ compiler('c') }}
    - gengetopt
    - autoconf
    - automake
  host:
    - viennarna
  run:
    - viennarna

test:
  commands:
    - barriers --version
