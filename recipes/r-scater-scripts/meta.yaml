{% set version = '0.0.1' %}

package:
  name: r-scater-scripts
  version: {{ version }}

source:
  git_url: https://github.com/ebi-gene-expression-group/r-scater-scripts.git

requirements:
    build:
        - git
    run:
        - r-scater
        - r-optparse

test:
    commands:
