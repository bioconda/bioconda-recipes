package:
  name: renderproto
  version: 0.11.1

source:
  git_url: http://anongit.freedesktop.org/git/xorg/proto/renderproto.git

build:
  number: 1

requirements:
  build:
    - gcc
    - autoconf
    - automake
    - libtool
    - pkg-config
    - util-macros
    - m4
  run:

about:
  home: 'http://cgit.freedesktop.org/xorg/proto/renderproto/'
