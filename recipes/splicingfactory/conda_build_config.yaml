# Pin commonly used values to avoid conda-build warnings and ensure reproducible builds
numpy:
  - 1.26

# Use a recent R base used by bioconda R packages; adjust if bioconda defaults change
r_base:
  - 4.3
