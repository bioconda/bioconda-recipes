{% set name = "wizardeye" %}
{% set version = "0.1.5" %} 

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: https://github.com/TheLokj/WizardEye/archive/refs/tags/v{{ version }}.tar.gz
  sha256: c4c20ef43375fd7f693c03fef1fc94eae8705c703d80e5275078c59fb3be61a7 

build:
  number: 0
  noarch: python
  run_exports:
    - {{ pin_subpackage(name, max_pin="x.x.x") }}
  script: "{{ PYTHON }} -m pip install . -vv"
  entry_points:
    - wizardeye = wizardeye.cli:app

requirements:
  host:
    - pip
    - python >=3.8,<3.13
    - setuptools >=68
    - wheel
  run:
    - python >=3.8,<3.13
    - typer >=0.9
    - pyyaml >=6.0
    - pysam >=0.22
    - pybigwig >=0.3.25
    - numpy
    - bwa
    - samtools
    - bedtools
    - seqkit
    - ucsc-bedgraphtobigwig
    - ucsc-bigwigtobedgraph
    - parallel

test:
  source_files:
    - tests/  
  requires:
    - pytest >=7.0
  commands:
    - wizardeye --help

about:
  home: https://github.com/TheLokj/WizardEye
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: "A Python tool to create, manage, and filter by cross-mappability tracks."
