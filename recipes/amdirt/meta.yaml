{% set name = "amdirt" %}
{% set version = "1.4.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/AMDirT-{{ version }}.tar.gz
  sha256: 71768e9be5a19f2524d0c9b425433e9d5ed386e6e956bfe6e9736b7d02b64c81

build:
  entry_points:
    - AMDirT = AMDirT.cli:cli
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python >=3.9
  run:
    - click
    - colorlog
    - jsonschema
    - numpy
    - pandas
    - python >=3.9
    - requests
    - rich
    - streamlit >=1.21.0
    - streamlit-aggrid
    - tabulate

test:
  imports:
    - AMDirT
  commands:
    - pip check
    - AMDirT --help
  requires:
    - pip

about:
  home: https://github.com/SPAAM-community/AMDirT
  summary: 'AMDirT: AncientMetagenomeDir Toolkit'
  license: GPL-3.0-or-later
  license_file: LICENSE

extra:
  recipe-maintainers:
    - maxibor
