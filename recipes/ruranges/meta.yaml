{% set name = "ruranges" %}
{% set version = "0.1.8" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://files.pythonhosted.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: e944cd08305a8cb407b651bb2f652f754535c7f7954fd1ece2afab94046b13ee

build:
  number: 0
  skip: True  # [py < 312]
  script: {{ PYTHON }} -m pip install . --no-deps --no-build-isolation --no-cache-dir -vvv
  run_exports:
    - {{ pin_subpackage(name, max_pin="x.x") }}

requirements:
  build:
    - {{ compiler('rust') }}
  host:
    - python
    - pip
    - maturin
    - numpy
  run:
    - python
    - numpy

test:
  imports:
    - ruranges
  commands:
    - python -c "import ruranges; print(getattr(ruranges, '__version__', ''))"

about:
  home: "https://github.com/pyranges/ruranges"
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: "Rust-backed interval kernels exposed to Python/NumPy."
  dev_url: "https://github.com/pyranges/ruranges"

extra:
  additional-platforms:
    - linux-aarch64
    - osx-arm64
  skip-lints:
    - compiler_needs_stdlib_c
