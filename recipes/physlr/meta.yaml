{% set name = "physlr" %}
{% set version = "1.0.0.1" %}
 
package:
  name: {{ name|lower }}
  version: {{ version }}

build:
  number: 0

source:
  url: https://github.com/aafshinfard/{{ name|lower }}/releases/download/v{{ version }}/{{ name|lower }}-{{ version }}.tar.gz
  sha256: 114cb46439948761ed38eee1eba72e586f057ca284aeb6f3e7fc02d7260caf91

requirements:
  build:
    - {{ compiler('cxx') }}
    - llvm-openmp  # [osx]
    - libgomp      # [linux]
    - make
  host:
    - Python >=3.6.0
    - pip
    - boost-cpp
  run:
    - Python >=3.6.0
    - ntCard
    - ntHits
    - networkx
    - numpy
    - scipy
    - scikit-learn
    - tqdm
    - make
    - pigz
    
test:
  commands:
    - physlr version | grep -q "{{ version }}"
    # - physlr help | grep -q "physlr physical-map"
    # - physlr help | grep -q "physlr scaffold"
    # - physlr test

about:
  home: https://github.com/bcgsc/physlr
  license: GPL3
  license_file: LICENSE
  summary: Physlr; Next-generation physical maps using linked reads
  dev_url: https://github.com/bcgsc/physlr
  doc_url: https://github.com/bcgsc/physlr#readme
