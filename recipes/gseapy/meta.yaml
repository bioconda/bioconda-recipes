package:
  name: gseapy
  version: "0.7.8"

source:
  fn: gseapy-0.7.8.tar.gz
  url: https://pypi.python.org/packages/b5/84/0f92afd747d3f8fd268fbc1bcd4628cb858ea829edf4b8e0ceb790426217/gseapy-0.7.8.tar.gz
  md5: a36a227f4cf457fd57d9ced9f7426db5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # noarch_python: True
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gseapy = gseapy:main
    #
    # Would create an entry point called gseapy that calls gseapy.main()

    - gseapy = gseapy.__main__:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy >=1.8.0
    - pandas >=0.16
    - matplotlib >=1.4.3
    - beautifulsoup4 >=4.4.1
    - requests
    - lxml
    - html5lib

  run:
    - python
    - numpy >=1.8.0
    - pandas >=0.16
    - matplotlib >=1.4.3
    - beautifulsoup4 >=4.4.1
    - requests
    - lxml
    - html5lib

test:
  # Python imports
  imports:
    - gseapy

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - gseapy --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/BioNinja/gseapy
  license: MIT License
  summary: 'Gene Set Enrichment Analysis in Python'
  license_family: MIT

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
