{% set version = "0.32.0" %}
{% set sha256 = "7d485af48f7f2240ae36f251627b020ba7d020b15c284e392d241914f6bafb16" %}

package:
  name: mehari
  version: {{ version }}

source:
  url: https://github.com/varfish-org/mehari/archive/v{{ version }}.tar.gz
  sha256: {{ sha256 }}
  patches:
    - fix-release-profile.patch

build:
  skip: True  # [osx]
  number: 0
  run_exports:
    - {{ pin_subpackage("mehari", max_pin="x.x") }}

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - {{ compiler('rust') }}
    - cmake
    - make
    - pkg-config
    - libprotobuf
    - rocksdb =8.10.2
    - snappy
  host:
    - clangdev
    - openssl
    - protobuf
    - sqlite
    - zlib
    - rocksdb =8.10.2
    - snappy
  run:
    - sqlite
    - rocksdb =8.10.2
    - snappy

test:
  commands:
    - mehari -h

about:
  home: https://github.com/varfish-org/mehari
  license: MIT
  license_family: MIT
  summary: VEP-like tool for sequence ontology and HGVS annotation of VCF files written in Rust.
  dev_url: https://github.com/varfish-org/mehari

extra:
  additional-platforms:
    - linux-aarch64
