{% set version = "1.0.2" %}

package:
  name: pmmrcalculator
  version: {{ version }}

build:
  noarch: generic
  number: 0

source:
  url: https://github.com/TCLamnidis/pMMRCalculator/archive/{{ version }}.tar.gz
  sha256: c9a72eb784e8804bccf67065f8f4a5dce80123c0b5d9aaab3470c6e4f2dfa5fb

requirements:
  run:
    - python 3.7

test:
  commands:
    - pmmrcalculator -h

about:
  home: https://github.com/TCLamnidis/pMMRCalculator
  license: GPL-3.0
  summary: "A small python tool to calculate pairwise mismatch rate between all individuals in an EigenStrat dataset."
