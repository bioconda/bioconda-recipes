{% set name = "nanopolish" %}
{% set version = "0.12.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

build:
  number: 0
  skip: True  # [osx]

source:
  git_url: https://github.com/jts/nanopolish
  git_rev: 'v{{ version }}'
  sha256: b3c17366f0362ac9f5c1e6b1254c24bde0ec72ad5e6015c35dba352f40cc9595
  patches:
    - nanopolish.patch

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
  host:
    - eigen
    - htslib=1.8
    - hdf5
    - bzip2
    - openmp
    - libcurl
    - zlib
  run:
    - htslib=1.8
    - python
    - hdf5
    - biopython
    - libdeflate
    - openmp
    - libcurl
    - zlib

test:
  commands:
    - nanopolish --help
    - nanopolish --version

about:
  home: https://github.com/jts/nanopolish
  license: MIT
  license_file: LICENSE
  summary: 'Signal-level algorithms for MinION data.'

extra:
  skip-lints:
    - uses_vcs_url
    - missing_tests
