{% set name = "ggCaller" %}
{% set version = "1.3.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/samhorsfield96/{{ name }}/archive/v{{ version }}.tar.gz
  sha256: 896184addbcf1446f74a5844651f36c6dc43a9e59df6f998c32976cd35706f61

build:
  number: 0
  skip: True  # [py < 38]
  script: tbb_INCLUDE_DIR=$PREFIX/include {{ PYTHON }} setup.py install --single-version-externally-managed --record=record.txt
  entry_points:
    - ggcaller = ggCaller.__main__:main

requirements:
  build:
    - {{ compiler('c')}}
    - {{ compiler('cxx') }}
    - cmake >=3.10
    - make
  host:
    - python
    - pybind11
    - eigen 3.3
    - bifrost
    - zlib
    - pip
    - bzip2
    - pytorch
    - boost-cpp
    - setuptools
    - llvm-openmp # [osx]
    - libgomp # [linux]
    - mkl  # [linux]
    - lapack  # [osx]
    - openblas  # [osx]
    - tbb-devel
    - tbb
  run:
    - python
    - numpy
    - scipy
    - pandas
    - bifrost
    - biopython
    - bcbio-gff
    - diamond
    - hmmer
    - mafft
    - snp-sites
    - zlib
    - pytorch
    - matplotlib-base
    - xorg-libxi
    - xorg-libxcursor
    - xorg-libxdamage
    - xorg-libxcomposite
    - xorg-libxrandr
    - xorg-libxinerama
    - xorg-libxpm
    - xorg-libxaw
    - xorg-libxfixes
    - intbitset
    - networkx
    - uncertainties
    - eigen 3.3
    - tqdm
    - seaborn
    - rapidnj
    - python-edlib
    - joblib
    - gffutils
    - cd-hit
    - python-wget

test:
  imports:
    - ggCaller_cpp
    - ggCaller
    - panaroo_runner
  commands:
    - ggcaller -h | grep -q "Generates ORFs from a Bifrost graph."

about:
   home: https://github.com/samhorsfield96/ggCaller
   license: MIT
   license_file: LICENSE
   summary: 'A graph-based gene-caller and pangenome analysis tool'
