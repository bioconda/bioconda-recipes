{% set name = "robust-bias-aware" %}
{% set version = "0.0.1" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: "https://github.com/lspindler2509/robust_bias_aware_pip_package/archive/refs/tags/v{{ version }}-beta.tar.gz"
  sha256: 02928fda84c2b01192f2b06d35fb0f30ee70122c839b9de3e250488f0879effd

build:
  number: 0
  script: 
    - "{{ PYTHON }} -m pip install . -vvv --no-deps --no-build-isolation"
    - mkdir -p $PREFIX/site-packages
    - cp -r $SRC_DIR/robust_bias_aware/* $PREFIX/site-packages
  noarch: python
  run_exports:
    - {{ pin_subpackage('robust-bias-aware', max_pin="x.x") }}

requirements:
  host:
    - pip >=3.7
    - python

  run:
    - numpy >=1.26.4
    - matplotlib-base >=3.2.2
    - pandas >=1.3.5
    - networkx >=2.6.3
    - pcst-fast
    - python >=3.7


test:
  commands:
    - robust-bias-aware --help


about:
  home: https://github.com/bionetslab/robust_bias_aware_pip_package
  license: GPL-3.0-or-later
  license_family: GPL3
  license_file: LICENSE
  summary: "Robust bias aware."
