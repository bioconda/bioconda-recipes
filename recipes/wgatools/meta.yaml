{% set name = "wgatools" %}
{% set version = "1.1.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/wjwei-handsome/{{ name }}/archive/refs/tags/v{{ version }}.tar.gz
  sha256: 87696fca8d564750f7d4ee80118a1c8de5a27b2da62c8d73c5816981e8ef8ac3 
  patches:
    - rust-htslib-0.49.0.patch

build:
  number: 0
  run_exports:
      - {{ pin_subpackage(name, max_pin='x') }}

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('rust') }}
    - make
    - cmake
    - pkg-config
    - clangdev
  host:
    - openssl
  run:
    - perl

test:
  commands:
    - wgatools --help

about:
  home: https://github.com/wjwei-handsome/{{ name }}
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: "A Rust library and tools for whole genome alignment files."
  dev_url: https://github.com/wjwei-handsome/{{ name }}

extra:
  additional-platforms:
    - linux-aarch64
    - osx-arm64
  recipe-maintainers:
    - AndreaGuarracino
