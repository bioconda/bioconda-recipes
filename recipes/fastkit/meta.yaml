{% set version = "0.1.0" %}

package:
  name: fastkit
  version: {{ version }}

build:
  number: 0

source:
  url:
  sha256:

requirements:
  host:
    - python >=3.6
  run:
    - biopython
test:
  commands:
    - fastkit --help

about:
  home: https://github.com/UseGalaxyAU/tools-au/packages/fastkit
  license: MIT
  license_file: LICENSE
  summary: A toolkit for performing routine operations on biological data e.g. FASTA/FASTQ files
