{% set name = "megalodon" %}
{% set version = "2.2.10" %}
{% set hash = "7b9416c6454c4702984361e0c7b92eb04c30dccfa0a4fca03e7ca390ece0f8a6" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/nanoporetech/megalodon/archive/{{ version }}.tar.gz
  sha256: {{ hash }}

build:
  number: 0
  entry_points:
    - megalodon = megalodon.__main__:_main
    - megalodon_extras = megalodon_extras.__main__:_main
  skip: True  # [py2k]
  script: "{{ PYTHON }} -m pip install . -vv"

requirements:
  build:
    - {{ compiler('c') }}
  host:
    - cython
    - python
    - pip
    - numpy >=1.9.0
    - setuptools  # [osx]
  run:
    - python
    - setuptools
    - h5py >=2.2.1
    - numpy >=1.9.0
    - scipy >=1.1.0
    - mappy >=2.16
    - pysam >=0.15
    - ont-fast5-api >=1.1
    - tqdm
    - scikit-learn
    - seaborn

test:
  imports:
    - megalodon
  commands:
    - megalodon --help
    - megalodon_extras --help

about:
  home: https://github.com/nanoporetech/megalodon
  license: Mozilla Public License 2.0
  license_family: OTHER
  license_file: LICENCE.txt
  summary: Nanopore modified base and sequence variant detection.
  doc_url: https://nanoporetech.github.io/megalodon/index.html
  dev_url: https://github.com/nanoporetech/megalodon

extra:
  recipe-maintainers:
    - marcus1487
