{% set name = "megalodon" %}
{% set version = "1.0.0" %}
{% set hash = "44de743710c63267ac1c9e6235d0f57f0a07711b1a066e6f9874bf3944cbca92" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/nanoporetech/megalodon/archive/{{ version }}.tar.gz
  sha256: {{ hash }}

build:
  entry_points:
    - megalodon = megalodon.__main__:main
  number: 0
  script: python -m pip install --ignore-installed --no-deps -vv .

requirements:
  build:
    - {{ compiler('c') }}
  host:
    - python # [py3k]
    - pip
    - numpy >=1.9.0
    - cython >=0.25.2
  run:
    - python # [py3k]
    - h5py >=2.2.1
    - numpy >=1.9.0
    - scipy >=1.1.0
    - mappy >=2.16
    - pysam >=0.15
    - ont-fast5-api >=1.1
    - tqdm

test:
  imports:
    - megalodon
  commands:
    - megalodon --help

about:
  home: https://github.com/nanoporetech/megalodon
  license: Mozilla Public License 2.0
  license_family: OTHER
  license_file: LICENSE.md
  summary: Nanopore modified base and sequence variant detection.
  doc_url: https://nanoporetech.github.io/megalodon/index.html
  dev_url: https://github.com/nanoporetech/megalodon

extra:
  recipe-maintainers:
    - marcus1487
