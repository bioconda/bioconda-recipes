{% set name = "get_orfs" %}
{% set version = "0.2.0" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://github.com/linsalrob/{{ name }}/archive/v{{ version }}.tar.gz
  sha256: 6a9e66e4d6badeb5162e2af3ceb594c63f386720b2b698fe5f859f2e7fc2b981

build:
  skip: True  # [osx]
  number: 0
  run_exports:
    - {{ pin_subpackage('get_orfs', max_pin="x") }}

requirements:
  build:
    - make
    - {{ compiler('cxx') }}
    - cmake
  host:
    - zlib
    - pthread-stubs
  run:
    - zlib
    - pthread-stubs

test:
  commands:
    - get_orfs --help 2>&1 | grep translation

about:
  home: https://github.com/linsalrob/get_orfs
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: "Fast extraction of ORFs in all possible translation tables"
  dev_url: https://github.com/linsalrob/get_orfs

extra:
  recipe-maintainers:
    - linsalrob
