{% set version="2.1.0" %}

package:
  name: mylotools
  version: {{ version }}

source:
  url: https://github.com/bluenote-1577/mylotools/archive/v{{ version }}.tar.gz
  sha256: 7a119b5702fb0d399eb5922fd746cd7a4c181e6c2497096575602749e600ee20

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install --no-deps --no-build-isolation --no-cache-dir . -vvv
  run_exports:
    - {{ pin_subpackage('mylotools', max_pin="x") }}


requirements:
  host:
    - python >=3
    - pip
    - setuptools
  run:
    - python 
    - matplotlib-base
    - plotly >=6
    - biopython >=1

test:
  commands:
    - mylotools -h

about:
  home: https://github.com/bluenote-1577/mylotools
  license: MIT
  license_file:
    - LICENSE
  summary: Utilities for manipulation and quality control of the myloasm long-read assembler

extra:
  recipe-maintainers:
    - bluenote-1577
