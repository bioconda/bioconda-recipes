{% set version = "1.1.0" %}

package:
  name: svaba
  version: {{ version }}

build:
  number: 0

source:
  url: https://github.com/walaj/svaba/archive/{{ version }}.tar.gz
  sha256: d2381389a4e2420ad55d38c40bd57b91ad5619db08a0bdf0ec2321e9979bd12c

requirements:
  build:
    - {{ compiler('c') }}
  host:
    - zlib
  run:
    - zlib

about:
  home: https://github.com/walaj/svaba
  license: GPL-3
  summary: Structural variation and indel detection by local assembly

test:
  commands:
    - svaba --help
    - svaba run --help
