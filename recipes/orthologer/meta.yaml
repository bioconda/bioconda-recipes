{% set version = "3.2.2" %}
{% set sha256 = "c2ac2ba9c433de65cf7646a886e483a0ca464ad51ff6adf3f34ae7d53f472f7b" %}

package:
  name: orthologer
  version: {{ version }}

build:
  number: 0
  run_exports:
    - {{ pin_subpackage('orthologer', max_pin="x") }}

source:
  url: https://data.orthodb.org/v11/download/software/orthologer_{{ version }}.tgz
  sha256: {{ sha256 }}

requirements:
  build:
    - {{ compiler('cxx') }}
    - {{ compiler('c') }}
    - cmake
    - make
    - libstdcxx
    - llvm-openmp  # [osx]
    - libgomp      # [linux]
    
  host:
    - bash >=4.3.0
    - python >=3.4
    - wget
    - blast
    - mmseqs2
    - diamond >=2.1.0
    - biopython
    - numpy
  run:
    - bash >=4.3.0
    - python >=3.4
    - wget
    - blast
    - mmseqs2
    - diamond
    - biopython
    - numpy
    
test:
  commands:
    - orthologer -h
  #  source_files:
  #    # used in run_test.sh
  #    - test_data

about:
  home: https://orthologer.ezlab.org/
  license: MIT
  license_file: LICENSE
  summary: Assessment of assembly completeness using Universal Single Copy Orthologs
  description: compute orthology

extra:
  container:
    # Necessary to allow Orthologer to download files from the internet when used with Singularity
    # and default bind paths (see also https://github.com/bioconda/bioconda-recipes/issues/11583).
    # Should be removed again when the base container is fixed.
    extended-base: True
  identifiers:
    - biotools:orthologer
  recipe-maintainers:
    - ftegenfe
