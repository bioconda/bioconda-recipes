{% set version = "3.1.1" %}
{% set sha256 = "c4de8be107d6953c32be83f792314a9ac341f6c7c77b4716312034495ad76b10" %}

package:
  name: orthologer
  version: {{ version }}

build:
  number: 0
    
source:
  url: https://data.orthodb.org/v11/download/software/orthologer_{{ version }}.tgz
  sha256: {{ sha256 }}

requirements:
  build:
    - {{ compiler('cxx') }}
    - {{ compiler('c') }}
    - cmake
    - make
    - libstdcxx
    - llvm-openmp
    - libgomp      # [linux]
    
  host:
    - bash >=4.3.0
    - python >=3.4
    - wget
    - blast
    - mmseqs2
    - diamond >=2.1.0
    - biopython
    - numpy
  run:
    - bash >=4.3.0
    - python >=3.4
    - wget
    - blast
    - mmseqs2
    - diamond
    - biopython
    - numpy
    
test:
  commands:
    - orthologer -h
  #  source_files:
  #    # used in run_test.sh
  #    - test_data

about:
  home: https://orthologer.ezlab.org/
  license: MIT
  license_file: LICENSE
  summary: Assessment of assembly completeness using Universal Single Copy Orthologs
  description: compute orthology

extra:
  container:
    # Necessary to allow Orthologer to download files from the internet when used with Singularity
    # and default bind paths (see also https://github.com/bioconda/bioconda-recipes/issues/11583).
    # Should be removed again when the base container is fixed.
    extended-base: True
  identifiers:
    - biotools:orthologer
  recipe-maintainers:
    - ftegenfe
