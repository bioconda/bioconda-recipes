package:
  name: screamingbackpack
  version: "0.2.333"

source:
  fn: ScreamingBackpack-0.2.333.tar.gz
  url: https://pypi.python.org/packages/source/S/ScreamingBackpack/ScreamingBackpack-0.2.333.tar.gz
  md5: 22d776c6d49eb6e6b2f387af99cc471f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  skip: False
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - screamingbackpack = screamingbackpack:main
    #
    # Would create an entry point called screamingbackpack that calls screamingbackpack.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - screamingbackpack

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/ScreamingBackpack/
  license: GPLv3
  summary: 'ScreamingBackpack'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
