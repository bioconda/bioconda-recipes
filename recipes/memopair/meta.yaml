{% set version = "0.1.4" %}
{% set name = "memopair" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://github.com/SorenHeidelbach/{{ name }}/archive/v{{ version }}.tar.gz
  sha256: 412b439e1abce9b08363917b25f9bf218b06ba4c694d0a2576acebc5fb133f74

build:
  number: 0
  run_exports:
    - {{ pin_subpackage('memopair', max_pin="x.x") }}
  script:
    - cargo-bundle-licenses --format yaml --output THIRDPARTY.yml
    - cargo install -v --locked --no-track --root $PREFIX --path .

requirements:
  build:
    - {{ compiler('rust') }}
    - cargo-bundle-licenses

test:
  commands:
    - memopair --version

about:
  home: https://github.com/SorenHeidelbach/memopair
  license: MIT 
  license_file:
    - LICENSE
    - THIRDPARTY.yml  # this file is generated by cargo-bundle-licenses
  summary: A package for identifying methylated motif pairs

extra:
  additional-platforms:
    - linux-aarch64
    - osx-arm64
  recipe-maintainers:
    - SorenHeidelbach
