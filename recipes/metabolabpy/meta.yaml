{% set name = "metabolabpy" %}
{% set version = "0.9.27" %}
{% set sha256 = "1f0af04f3a9a9e8b25550fb5a67fc25ddae61ba87dd32fa468a99fc364d79df0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/metabolabpy-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . --no-deps -vvv
  entry_points:
    - metabolabpy = metabolabpy.__main__:main
  run_exports:
    - {{ pin_subpackage('metabolabpy', max_pin="x") }}

requirements:
  host:
    - python >=3.7
    - pip
  run:
    - python >=3.7
    - numpy
    - scipy
    - matplotlib-base
    - pandas
    - numba
    - scikit-learn
    - openpyxl
    - xlsxwriter
    - multiprocess
    - pybaselines
    - darkdetect
    - pygamma
    - pyside2
    - pyautogui
    - qtmodern

test:
  imports:
    - metabolabpy
  commands:
    - metabolabpy --help

about:
  home: https://github.com/ludwigc/metabolabpy
  summary: Python package for data processing of NMR 1D and 2D metabolomics and metabolism tracing data
  license: GPL-3.0-or-later
  license_file: LICENSE

extra:
  recipe-maintainers:
    - RJMW
    - ludwigc
