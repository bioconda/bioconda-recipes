{% set version = "0.1.1" %}

package:
  name: taxor
  version: {{ version }}

source:
  url: https://github.com/JensUweUlrich/Taxor/archive/refs/tags/{{ version }}.tar.gz # [linux]
  md5: 444d193afa23cae5f49912b9774ba5c2 # [linux]

build:
  number: 0
  run_exports:
    - {{ pin_subpackage('taxor', max_pin='x.x.x') }}
  skip: true  # [osx]

requirements:
  build:
    - make
    - {{ compiler('cxx') }}
    - cmake >=3.21
  host:
    - libgomp      # [linux]
    - bzip2
    - seqan3 ==3.3.0
    - cxxopts >=2.2.0
    - grep
    - coreutils
    - curl
    - diffutils
    - zlib

  run:
    - bzip2
    - grep
    - coreutils
    - curl
    - diffutils 
    - zlib
    - raptor ==3.0.1

test:
  commands:
    - taxor --help

about:
  home: https://github.com/JensUweUlrich/Taxor
  summary: Fast and space-efficient taxonomic classification of long reads
  license: BSD 3-Clause
