# current version does not build with newer compilers of osx - due to error from 
# implicit function declaration in xcode16.  This code likes to use c89 but with c99's 'inline' declaration :(
cxx_compiler_version:
  - 15  # [osx]
c_compiler_version:
  - 15  # [osx]
