{% set name = "emu-pca" %}
{% set version = "1.5.0" %}

package:
  name: "{{ name|lower }}"
  version: {{ version }}

source:
  url: 'https://github.com/Rosemeis/emu/archive/refs/tags/v{{ version }}.tar.gz'
  sha256: b1128d2c3fd964b594cbbe35a7a6d268b45e6bcfd6009cee185492b3c2e3b0d5

build:
  number: 0
  skip: True  # [py < 310]
  script: 
    - "{{ PYTHON }} -m pip install . -vvv --no-deps --no-build-isolation --no-cache-dir"
  entry_points:
    - emu=emu.main:main
  run_exports:
    {{ pin_subpackage('emu-pca', max_pin="x") }}

requirements:
  build:
    - {{ compiler('c') }}
  host:
    - pip
    - python
    - cython
    - numpy
  run:
    - python

test:
  commands:
    - emu --help

about:
  home: https://github.com/Rosemeis/emu
  license: GPL-3.0-only
  license_file: LICENSE
  summary: EM-PCA for Ultra-low Coverage Sequencing Data.
  dev_url: https://github.com/Rosemeis/emu

extra:
  additional-platforms:
    - linux-aarch64
    - osx-arm64
