{% set name = "Seqwin" %}
{% set version = "0.3.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/treangenlab/Seqwin/archive/refs/tags/v{{ version }}.tar.gz
  sha256: f8b7dd9761331937c63c79846bb902d911b148d5cfb8d673511edaf8b0cff85a

build:
  number: 0
  skip: true  # [py < 310]
  script: {{ PYTHON }} -m pip install . --no-deps --no-build-isolation --no-cache-dir -vvv
  run_exports:
    - {{ pin_subpackage(name|lower, max_pin="x.x") }}

requirements:
  build:
    - {{ compiler('cxx') }}
    - cmake >=3.18
    - ninja >=1.10
    - make
  host:
    - python
    - pip
    - scikit-build-core >=0.12
    - pybind11 >=2.12
    - zlib
  run:
    - python
    - numpy >=2
    - numba
    - pandas >=2
    - networkx
    - pydantic >=2
    - typer
    - mash
    - blast
    - ncbi-datasets-cli

test:
  imports:
    - seqwin
    - seqwin.btllib
  commands:
    - seqwin --help

about:
  home: https://github.com/treangenlab/{{ name }}
  license: GPL-3.0-only
  license_family: GPL3
  license_file: LICENSE
  summary: "Ultrafast identification of signature sequences in microbial genomes"
  dev_url: https://github.com/treangenlab/{{ name }}
  doc_url: https://github.com/treangenlab/{{ name }}/wiki

extra:
  additional-platforms:
    - linux-aarch64
    - osx-arm64
