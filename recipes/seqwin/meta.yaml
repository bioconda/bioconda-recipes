{% set name = "Seqwin" %}
{% set version = "0.3.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/treangenlab/Seqwin/archive/refs/tags/v{{ version }}.tar.gz
  sha256: 08c34a4f8177c6373bc5baf02bb6a541e31da54225268df182842bfb0e3a1f94

build:
  number: 0
  skip: true  # [py < 310]
  script: {{ PYTHON }} -m pip install . --no-deps --no-build-isolation --no-cache-dir -vvv
  run_exports:
    - {{ pin_subpackage(name|lower, max_pin="x.x") }}

requirements:
  build:
    - {{ compiler('cxx') }}
    - cmake >=3.18
    - ninja >=1.10
    - make
  host:
    - python
    - pip
    - scikit-build-core >=0.12
    - pybind11 >=2.12
    - zlib
  run:
    - python
    - numpy >=2
    - numba
    - pandas >=2
    - networkx
    - pydantic >=2
    - typer
    - mash
    - blast
    - ncbi-datasets-cli

test:
  imports:
    - seqwin
    - seqwin.btllib
  commands:
    - seqwin --help

about:
  home: https://github.com/treangenlab/{{ name }}
  license: GPL-3.0-only
  license_family: GPL3
  license_file: LICENSE
  summary: "Ultrafast identification of signature sequences in microbial genomes"
  dev_url: https://github.com/treangenlab/{{ name }}
  doc_url: https://github.com/treangenlab/{{ name }}/wiki

extra:
  additional-platforms:
    - linux-aarch64
    - osx-arm64
