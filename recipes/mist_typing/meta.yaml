{% set version = "0.0.3" %}

package:
  name: mist_typing
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/m/mist_typing/mist_typing-{{ version }}.tar.gz
  sha256: ef366ddd5f19f3bb2041b40167114b0c61241f8d114bc334337b7d73c841ea92

build:
  noarch: python
  number: 0
  entry_points:
    - mist=mist.scripts.cli:cli
  script: "{{ PYTHON }} -m pip install . --no-deps --no-build-isolation --no-cache-dir -vvv"
  run_exports:
    - {{ pin_subpackage('mist_typing', max_pin="x") }}

requirements:
  host:
    - python >=3.10
    - pip
    - setuptools >61
  run:
    - python >=3.10
    - biopython
    - bs4
    - cd-hit >=4.6.8
    - click
    - furl
    - minimap2 >=2.26
    - mummer4
    - pandas >2
    - pyyaml
    - rauth
    - requests

about:
  home: https://github.com/BioinformaticsPlatformWIV-ISP/MiST
  license: GPL-3.0-or-later
  license_family: GPL
  license_file: LICENSE
  summary: MiST is a rapid, accurate and flexible (core-genome) multi-locus sequence typing (MLST) allele caller.
  dev_url: https://github.com/BioinformaticsPlatformWIV-ISP/MiST

test:
  imports:
    - mist
  commands:
    - mist --version
    - mist --help

extra:
  recipe-maintainers:
    - BertBog
