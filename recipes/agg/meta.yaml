{% set version = "0.3.5" %}
{% set sha256 = "c2a8d30fbb30273a86d04bd1f967a9a01ccb761b362438667f52ae69407da465" %}

package:
  name: agg
  version: {{ version }}

build:
  number: 3

source:
  url: https://github.com/Illumina/agg/archive/v{{ version }}.tar.gz
  sha256: {{ sha256 }}

requirements:
  build:
    - {{ compiler('c') }}
  host:
    - zlib
  run:
    - zlib

test:
  commands:
    - agg  -h

about:
  home: https://github.com/Illumina/agg
  license: GPL3
  license_file: LICENSE
  summary: gvcf aggregation tool
