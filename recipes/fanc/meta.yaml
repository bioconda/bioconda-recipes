{% set name = "fanc" %}
{% set version = "0.9.0" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: "https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz"
  sha256: 2127ea65dbb9dae59024a9a1f17a958ceacf47deb6b6d66e1c680adebfc3eae4

build:
  number: 0
  script: "{{ PYTHON }} -m pip install . --no-deps --ignore-installed --no-cache-dir -vv"

requirements:
  build:
    - {{ compiler('c') }}
  host:
    - pip
    - python 3.6.11
    - cython
  run:
    - biopython
    - cooler >=0.8.0
    - future
    - genomic_regions >=0.0.7
    - gridmap >=0.14.0
    - h5py
    - intervaltree
    - matplotlib-base
    - msgpack-python
    - msgpack-numpy >=0.4.4.3
    - numpy >=1.16.0
    - pandas >=0.15.0
    - pillow
    - progressbar2
    - pybedtools
    - pybigwig
    - pysam >=0.9.1
    - pytest
    - python
    - pyyaml >=5.1
    - scikit-image >=0.15.0
    - scikit-learn
    - scipy
    - seaborn
    - pytables >=3.5.1

test:
  imports:
    - fanc
    - fanc.architecture
    - fanc.compatibility
    - fanc.plotting
  commands:
    - fanc -V
    - fancplot -V

about:
  home: https://github.com/vaquerizaslab/fanc
  license: MIT
  summary: "Framework for the ANalysis of C-data."
  doc_url: https://vaquerizaslab.github.io/fanc
  dev_url: https://github.com/vaquerizaslab/fanc

extra:
  recipe-maintainers:
    - kaukrise
