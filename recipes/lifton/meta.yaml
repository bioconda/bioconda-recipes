{% set name = "lifton" %}
{% set version = "1.0.13" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: f0bce4cee1b3c945736121171798f7f85ca827d857d8d41562af34f00a740c8d

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  entry_points:
    - lifton = lifton.lifton:main
    - gff3-validate = lifton.gff3_validator:_main
  run_exports:
    - {{ pin_subpackage(name, max_pin="x.x") }}

requirements:
  host:
    - python >=3.10
    - pip
    - setuptools >=61
    - wheel
  run:
    - python >=3.10
    - numpy >=1.22.0
    - biopython >=1.76
    - parasail-python >=1.2.4
    - intervaltree >=3.1.0
    - interlap >=0.2.6
    - networkx >=3.3
    - pyfaidx >=0.5.8
    - pysam >=0.19.1
    - gffutils >=0.10.1
    - ujson >=3.2.0
    - python-duckdb >=1.0,!=1.5.3,!=1.5.4
    - pyarrow >=14
    - mappy
    # External aligners LiftOn invokes as subprocesses:
    - minimap2
    - miniprot

test:
  imports:
    - lifton
    - lifton.gffbase
    - lifton.native_bindings
    - mappy
  commands:
    - lifton -V
    - lifton -h
    - gff3-validate -h
    - minimap2 --version
    - miniprot --version
    - python -c "from lifton.native_bindings import is_mappy_available; assert is_mappy_available()"

about:
  home: https://github.com/Kuanhao-Chao/LiftOn
  license: GPL-3.0-or-later
  license_family: GPL3
  license_file: LICENSE
  summary: "Accurate homology-based genome-annotation lift-over combining DNA and protein alignment"
  description: |
    LiftOn is a homology-based genome-annotation lift-over tool. Given a reference
    genome and its annotation plus a target genome, it produces an annotation for
    the target by combining DNA-level alignment (Liftoff / minimap2) with
    protein-level alignment (miniprot) via a protein-maximization CDS-chaining
    algorithm and an ORF-rescue pass. It also ships a standalone GFF3 validator
    (gff3-validate).
  doc_url: https://khchao.com/LiftOn/
  dev_url: https://github.com/Kuanhao-Chao/LiftOn

extra:
  recipe-maintainers:
    - Kuanhao-Chao
  identifiers:
    - doi:10.1101/gr.279620.124
