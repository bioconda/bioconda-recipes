{% set name = "pacini_typing" %}
{% set version = "3.1.1" %}

package:
  name: "{{ name }}"
  version: "{{ version }}"

source:
  url: https://github.com/RIVM-bioinformatics/Pacini-typing/archive/refs/tags/v{{ version }}.tar.gz
  sha256: ac2e2483ebe10aba4eeb9ffe32e88c94bd9f98b9bc0dc792b59e470da6ca231c

build:
  number: 0
  noarch: python
  script:
    - {{ PYTHON }} -m pip install . --no-deps --no-build-isolation -vv
  run_exports:
    - {{ pin_subpackage("pacini_typing", max_pin="x") }}

requirements:
  host:
    - python >=3.12
    - pip
    - setuptools
  run:
    - python >=3.12
    - pandas
    - pyyaml
    - blast
    - kma

test:
  commands:
    - pacini_typing --help
    - Pacini-typing --help

about:
  home: "https://github.com/RIVM-bioinformatics/Pacini-typing"
  license: AGPL-3.0
  license_file: LICENSE
  summary: "YAML-based bacterial genotyping application"
  dev_url: "https://github.com/RIVM-bioinformatics/Pacini-typing"

extra:
  recipe-maintainers:
    - MarkStreek
