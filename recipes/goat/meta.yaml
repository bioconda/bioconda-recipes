{% set version = "0.1.52" %}

package:
  name: goat
  version: {{ version }}

build:
  number: 0

source:
  url: https://github.com/genomehubs/goat-cli/archive/{{ version }}.tar.gz
  sha256: 9d72e81c50124e91eaa14a121f7ef74b816e9ed3c686349f1b3ab0640cdb71fa

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - cmake
    - make
    - rust >=1.52
    - pkg-config # [osx]
  host:
    - openssl
  run:
    

test:
  commands:
    - goat --help
    - goat --version

about:
  home: https://github.com/genomehubs/goat-cli
  license: MIT
  license_file: LICENSE
  summary: |
    Query metadata for any taxon across the tree of life.
  description: |
    A command line interface for the GoaT API, which is a collection of datasets to decorate the genomic tree of life.
