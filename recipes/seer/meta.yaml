{% set name = "seer" %}
{% set version = "1.1.4" %}

package:
  name: {{ name|lower}}
  version: {{ version }}

source:
  url: https://github.com/johnlees/{{ name|lower }}/releases/download/v{{ version }}/{{ name|lower }}_v{{ version }}_submodules.tar.gz
  sha256: 9871e85b42884c248f3e2969337cf0224836b9eee8b4f24a9aa5b00dc005152c

build:
  skip: True #  [osx]
  number: 0

requirements:
  build:
    - {{ compiler('cxx') }}
  host:
    - zlib
    - armadillo
    - hdf5
    - lapack
    - openblas
    - pthread-stubs
  run:
    - zlib
    - lapack
    - openblas
    - armadillo
    - hdf5
    - pthread-stubs

test:
  commands:
    - seer --version
    - kmds --version
    - combineKmers -h
    - filter_seer --version

about:
   home: https://github.com/johnlees/seer
   license: GPL-2
   license_family: GPL
   license_file: LICENSE
   summary: sequence element (kmer) enrichment analysis

extra:
   identifiers:
     - doi:10.1038/ncomms12797
