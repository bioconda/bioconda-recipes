{% set name = "biovalid" %}
{% set version = "0.5.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/biovalid-{{ version }}.tar.gz
  sha256: af6627605e4ad3e7d456600ece858140d7c3c18f7f17b1884b26858151c5815a

build:
  run_exports:
      - {{ pin_subpackage("biovalid", max_pin="x") }}
  entry_points:
    - biovalid = biovalid.__main__:run_cli
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.10
    - hatchling >=1.0
    - pip
  run:
    - python >=3.10

test:
  imports:
    - biovalid
  commands:
    - pip check
    - biovalid --help
  requires:
    - pip

about:
  home: "https://github.com/RIVM-bioinformatics/biovalid"
  summary: Quick validation of common bioinformatics files in pure Python
  license: AGPL-3.0-or-later
  license_file: LICENCE



extra:
  recipe-maintainers:
    - raaijmag
    - florianzwagemaker
    - ids-bioinformatics
