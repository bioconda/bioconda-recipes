package:
  name: cargo
  version: "0.8.0"

build:
  number: 0

source:
  fn: 0.8.0.tar.gz
  url: https://github.com/rust-lang/cargo/archive/0.8.0.tar.gz

requirements:
  build:
    - python 2.7.*
    - curl
    - cmake
    - rustc
  run:
    - rustc
   
test:
  commands:
    - cargo --help
 
about:
  home: https://crates.io
  license: MIT
  summary: The Rust package manager.
