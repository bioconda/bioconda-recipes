{% set name = "ntSynt" %}
{% set version = "1.0.4" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/bcgsc/{{ name|lower }}/releases/download/v{{ version }}/{{ name|lower }}-{{ version }}.tar.gz
  sha256: 8d94dcc3d87983d25a71ee263caff20d6c844dc681b550315b7522196868bdd4

build:
  number: 0
  run_exports: 
    - {{ pin_subpackage('ntsynt', max_pin="x") }}

requirements:
  build:
    - {{ compiler('cxx') }}
    - cmake
  host:
    - python
    - meson
    - ninja
    - llvm-openmp  # [osx]
    - libgomp      # [linux]
    - btllib >=1.7.1
  run:
    - python
    - llvm-openmp  # [osx]
    - libgomp      # [linux]
    - btllib >=1.7.1
    - intervaltree
    - pybedtools
    - ncls
    - python-igraph <1.0.0
    - snakemake-minimal
    - samtools
    - seqtk

test:
  commands:
    - ntSynt -h
  imports:
    - btllib

about:
  home: "https://github.com/bcgsc/ntsynt"
  license: "GPL-3.0-or-later"
  license_family: GPL3
  license_file: LICENSE
  summary: "Detecting multi-genome synteny blocks using minimizer graph mapping."
  dev_url: "https://github.com/bcgsc/ntsynt"

extra:
  additional-platforms:
    - linux-aarch64
    - osx-arm64
  skip-lints:
    - uses_vcs_url
