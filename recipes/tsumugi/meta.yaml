{% set name = "TSUMUGI" %}
{% set version = "1.0.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/tsumugi-{{ version }}.tar.gz
  sha256: 676126c893b05746fe1f2a792f8f102f9fc0b606b318e23dd3d43f35a17f81af

build:
  entry_points:
    - tsumugi = TSUMUGI.main:main
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0
  run_exports:
    - {{ pin_subpackage('tsumugi', max_pin="x.x") }}

requirements:
  host:
    - python >=3.10
    - poetry-core
    - pip
  run:
    - python >=3.10
    - numpy >=1.21.0
    - tqdm >=4.64.0
    - networkx >=3.3

test:
  imports:
    - TSUMUGI
  commands:
    - pip check
    - tsumugi --help
  requires:
    - pip

about:
  summary: 'TSUMUGI: Phenotype-driven gene network identifier'
  license: MIT
  license_file: LICENSE
  home: "https://github.com/akikuno/TSUMUGI-dev"
  dev_url: "https://github.com/akikuno/TSUMUGI-dev"
  doc_url: "https://github.com/akikuno/TSUMUGI-dev/blob/{{ version }}/README.md"


extra:
  recipe-maintainers:
    - akikuno
