{% set name = "dms" %}
{% set version = "v1.1" %}
{% set sha256 = "9b6fd14eee5b92c79c778f70943334ba01d248ef8f038d14f8605ee26483802a" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: https://github.com/qdu-zyf/dynamic-meta-storms/archive/master.zip
  sha256: {{ sha256 }}
    
build:
  number: 0

requirements:
  build:
    - {{ compiler('cxx') }}
    - openmp
    - llvm-openmp  # [osx]
    
  host:
    - openmp
    - clangdev  # [osx]
    - llvm-openmp  # [osx]

  run:
    - openmp
    - llvm-openmp  # [osx]

test:
  commands:
    - export DynamicMetaStorms=${PREFIX}
    - MS-comp-taxa-dynamic -h
    
about:
  home: https://github.com/qibebt-bioinfo/dynamic-meta-storms
  license: GPL3
  license_file: '{{ environ["RECIPE_DIR"] }}/LICENSE'  
  summary: "Comprehensive taxonomic and phylogenetic comparison of shotgun metagenomes at the species level"

extra:
  identifiers:
    - doi:10.1093/bioinformatics/btz910
