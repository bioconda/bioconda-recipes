{% set name = "dms" %}
{% set version = "v1.1" %}
{% set sha256 = "f9e6e5b0ac5fe7994d302fe0f63493512ebe11e65f0d37b7bb1aa405a2f031de" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: https://github.com/Chenyuzhu59/dynamic-meta-storms/archive/master.zip
  sha256: {{ sha256 }}
    
build:
  number: 0
  script_env:
    - DynamicMetaStorms

requirements:
  build:
    - {{ compiler('cxx') }}
    - llvm-openmp  # [osx]
    
  host:
    - clangdev  # [osx]
    - llvm-openmp  # [osx]

  run:
    - llvm-openmp  # [osx]

test:
  commands:
    - MS-comp-taxa-dynamic -h
    
about:
  home: https://github.com/qibebt-bioinfo/dynamic-meta-storms
  license: GPL3
  license_file: '{{ environ["RECIPE_DIR"] }}/LICENSE'  
  summary: "Comprehensive taxonomic and phylogenetic comparison of shotgun metagenomes at the species level"

extra:
  identifiers:
    - doi:10.1093/bioinformatics/btz910
