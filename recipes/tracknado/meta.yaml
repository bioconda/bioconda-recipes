{% set name = "tracknado" %}
{% set version = "0.3.1" %}
{% set sha256 = "f91a50fc6b7031b5a1ce954195c5b0f56442ce838712ac83f07a1fe27395c678" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/tracknado-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  noarch: python
  script_env:
    - SETUPTOOLS_SCM_PRETEND_VERSION={{ version }}
  script: {{ PYTHON }} -m pip install . --no-deps -vvv
  entry_points:
    - tracknado = tracknado.cli:cli
  run_exports:
    - {{ pin_subpackage('tracknado', max_pin='x') }}

requirements:
  host:
    - python >=3.7
    - pip
    - wheel
    - setuptools-scm >=6.2
  run:
    - python >=3.7
    - click
    - typer
    - pandas
    - cookiecutter
    - seaborn
    - trackhub
    - pillow
    - loguru
    - pydantic >=2.0
    - pandera >=0.18

test:
  imports:
    - tracknado
  commands:
    - tracknado --help

about:
  home: https://pypi.org/project/tracknado/
  summary: CLI library to generate UCSC trackhubs from sequencing data
  license: GPL-3.0-or-later
  license_family: GPL3
  license_file: LICENSE
