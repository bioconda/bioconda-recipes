{% set name = "grzctl" %}
{% set version = "2.0.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/grzctl-{{ version }}.tar.gz
  sha256: 1305fe3a169d8e99f995a168e156f4567d9513289c3012f7bc75d68cce9fae43

build:
  number: 0
  noarch: python
  entry_points:
    - grzctl = grzctl.cli:main
  script: {{ PYTHON }} -m pip install . -vvv --no-deps --no-build-isolation --no-cache-dir
  run_exports:
    - {{ pin_subpackage(name, max_pin="x.x") }}

requirements:
  host:
    - python >=3.12
    - hatchling
    - pip
  run:
    - python >=3.12
    - boto3 >=1.36,<2
    - click >=8.2,<9
    - python-crypt4gh >=1.7,<2
    - jsonschema >=4.23.0,<5
    - pyyaml >=6.0.2,<7
    - tqdm >=4.66.5,<5
    - psycopg >=3.2,<4
    - pydantic >=2.12,<3
    - pydantic-settings >=2.11,<3
    - platformdirs >=4.3.6,<5
    - rich ==13.*
    - requests >=2.32.3,<3
    - grz-db >=1.2,<2
    - grz-common >=1.5.0,<2
    - grz-cli >=1.4.0,<2
    - grz-pydantic-models >=2.5,<3
    - textual >=5.3,<6

test:
  imports:
    - grzctl
  commands:
    - pip check
    - grzctl --help
  requires:
    - pip

about:
  home: "https://github.com/BfArM-MVH/grz-tools"
  summary: "Control CLI for GRZ administrators."
  license: MIT
  license_family: MIT
  dev_url: "https://github.com/BfArM-MVH/grz-tools"
  doc_url: "https://github.com/BfArM-MVH/grz-tools/blob/grzctl-v{{ version }}/packages/grz-cli/README.md"

extra:
  recipe-maintainers:
    - twrightsman
    - tedil
    - hoeze
