{% set name = "deltapd" %}
{% set version = "0.1.3" %}
{% set sha256 = "a7ed0c851af3ae2fc791deae359881fec2a802ca180a55c32f1ff5dac7d3621d" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  script: {{ PYTHON }} -m pip install . --ignore-installed --no-deps -vvv
  entry_points:
    - deltapd = deltapd.__main__:main

requirements:
  build:
    - {{ compiler('cxx') }}
  host:
    - pip
    - python >=3.6
    - cython
    - numpy
  run:
    - python >=3.6
    - numpy
    - phylodm
    - tqdm
    - ete3
    - dendropy
    - matplotlib-base
    - jinja2
    - seaborn

test:
  imports:
    - deltapd
    - deltapd.file
  commands:
    - deltapd --version > /dev/null 2>&1

about:
  home: https://github.com/Ecogenomics/DeltaPD
  license: GNU Affero General Public License v3.0 only
  license_family: AGPL
  license_file: LICENSE
  summary: 'DeltaPD is a tool used to determine outliers in a gene tree when compared against a reference tree.'

extra:
  identifiers:
    - doi:10.5281/zenodo.4107213
