{% set name = "taxutils" %}
{% set version = "1.0.3" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: "https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz"
  sha256: a2dd31032c1b6e852898f21e97e076729fd0488669d6fbc29f740db586cd579c

build:
  noarch: python
  number: 0
  script: "{{ PYTHON }} -m pip install . --no-deps --no-build-isolation -vvv"
  run_exports:
    - {{ pin_subpackage(name, max_pin="x") }}

requirements:
  host:
    - python >=3.10
    - pip
    - setuptools >=68
    - wheel
  run:
    - python >=3.10
    - numpy
    - pandas

test:
  imports:
    - taxutils

about:
  home: https://github.com/SwabSeq/taxutils
  license: MIT
  license_file: LICENSE
  summary: "Utilities for working with NCBI taxonomic data."

extra:
  recipe-maintainers:
    - "wgobrien"
  authors:
    - "William O'Brien"
