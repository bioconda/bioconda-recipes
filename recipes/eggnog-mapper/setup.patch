--- setup.py
+++ setup.py
@@ -31,7 +31,7 @@ try:
         name = MOD_NAME,
         version = VERSION,
         packages = ['eggnogmapper'],
-
+        scripts = ['emapper.py', 'download_eggnog_data.py'],
         # Project uses reStructuredText, so ensure that the docutils get
         # installed or upgraded on the target machine
         install_requires = [
