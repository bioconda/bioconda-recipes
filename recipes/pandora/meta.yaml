{% set name = "pandora" %}
{% set version = "0.12.0_alpha.0" %}
{% set repo_URL = "https://github.com/rmcolq/pandora" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: {{ repo_URL }}/releases/download/0.12.0-alpha.0/{{ name }}-0.12.0-alpha.0.tar.gz
  sha256: 34ed20eecf98b884caf2fd9d70c8af3ab22fc2f523fe4f1117e97e7d2982b6dd

build:
  number: 0
  skip: True #  [osx]
  run_exports:
    - {{ pin_subpackage('pandora', max_pin="x") }}

requirements:
  build:
    - {{ compiler('cxx') }}
    - cmake ==3.16
    - boost-cpp
    - make
    - wget
    - mafft

test:
  commands:
    - pandora --help
    - pandora --version
    - pandora map --help
    - pandora index --help
    - pandora discover --help
    - pandora compare --help

about:
  home: {{ repo_URL }}
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: Pan-genome inference and genotyping with long noisy or short accurate reads

extra:
  recipe-maintainers:
    - mbhall88
    - leoisl
    - rmcolq
