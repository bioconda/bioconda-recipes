{% set version = "0.2.0" %}
{% set github = "https://github.com/acidgenomics/transformer" %}

package:
  name: r-transformer
  version: "{{ version }}"

source:
  url: "{{ github }}/archive/v{{ version }}.tar.gz"
  sha256: 5631b59013ae800baa854a9b5dc341821739ab1408a7c152096e3ed33cec736c

build:
  noarch: generic
  number: 0

requirements:
  host:
    - r-base
    - r-bioverbs >=0.1.9
    - r-goalie >=0.2.12
    - bioconductor-biocgenerics
    - bioconductor-genomicranges
    - bioconductor-s4vectors
    - bioconductor-summarizedexperiment
    - r-data.table >=1.12
    - r-matrix >=1.2
    - r-tibble >=2.1
  run:
    - r-base
    - r-bioverbs >=0.1.9
    - r-goalie >=0.2.12
    - bioconductor-biocgenerics
    - bioconductor-genomicranges
    - bioconductor-s4vectors
    - bioconductor-summarizedexperiment
    - r-data.table >=1.12
    - r-matrix >=1.2
    - r-tibble >=2.1

test:
  commands:
    - $R -e "library('transformer')"

about:
  home: https://transformer.acidgenomics.com/
  dev_url: "{{ github }}"
  license: MIT
  summary: Additional S3 and S4 coercion methods for easy interconversion
    between Bioconductor and tidyverse data classes.

extra:
  recipe-maintainers:
    - mjsteinbaugh
    - pinin4fjords
    - chapmanb
