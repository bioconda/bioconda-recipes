{% set version = "26.1.0" %}
{% set sha256 = "2cc2263458a58ef41de56858b20e8a9e18fbe339d438ffc517114f5fba962834" %}
{% set galaxy_version = version.split(".")[:2]|join(".") %}

package:
  name: galaxy-files-full
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/g/galaxy-files/galaxy_files-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . --no-deps --no-build-isolation --no-cache-dir -vvv
  run_exports:
    - {{ pin_subpackage('galaxy-files-full', max_pin="x") }}

requirements:
  host:
    - python {{ python_min }}
    - pip
    - setuptools
  run:
    - python >={{ python_min }}
    - galaxy-tool-util-models >={{ galaxy_version }}
    - galaxy-util >={{ galaxy_version }}
    - fs
    - pydantic >=2.7.4
    - typing-extensions
    - fsspec
    - fs.webdavfs
    - webdavclient3
    - fs.dropboxfs
    - fs.sshfs
    - fs.googledrivefs
    - gcsfs
    - fs.onedatarestfs
    - fs-azureblob
    - rspace-client
    
test:
  imports:
    - galaxy
    - galaxy.files

about:
  home: "https://galaxyproject.org"
  license: MIT
  license_family: MIT
  license_file: "LICENSE.txt"
  summary: "The Galaxy file sources framework and default plugins."
  doc_url: "https://docs.galaxyproject.org"
  dev_url: "https://github.com/galaxyproject/galaxy"

extra:
  identifiers:
    - biotools:galaxy
    - doi:10.1093/nar/gky379
  recipe-maintainers:
    - bernt-matthias
    - bgruening
