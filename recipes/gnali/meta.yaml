{% set name = "gNALI" %}
{% set version = "1.0.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/phac-nml/gnali/archive/v{{ version }}.tar.gz
  sha256: f209080e2989731942b0954754020d333067162530e5d24609d55d429eb32fcf

build:
  noarch: python
  number: 0
  entry_points:
    - gnali = gnali.gnali:main
    - gnali_setup = gnali.gnali_setup:main
  script: "$PYTHON setup.py install --single-version-externally-managed --record=record.txt"

requirements:
  host:
    - python >=3.6
    - pip
    - ensembl-vep ==101
    - samtools
    - tabix
    - libcurl
    - perl-bio-bigfile
    - py-bgzip
  run:
    - python >=3.6
    - pybiomart
    - pandas
    - numpy
    - pysam
    - filelock
    - pyyaml
    - py-bgzip

test:
  imports:
    - gnali
  commands:
    - gnali --help

about:
  home: https://github.com/phac-nml/gnali
  license: Apache-2.0
  license_file: LICENSE.txt
  license_family: APACHE
  summary: "gNALI (gene nonessentiality and loss-of-function identifier) is a tool for finding PLoF gene variants."
  dev_url: https://github.com/phac-nml/gnali
