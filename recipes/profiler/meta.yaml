{% set name = "profiler" %}
{% set version = "0.1.0" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  - url: https://github.com/xujialupaoli/profiler/releases/download/{{ version }}/{{ name }}.tar.gz
    sha256: 8777a46eefda2f666f28751af747f2e17cce395a0ea9798ebdd9e184a04bf6ca
build:
  skip: True  # [osx]
  number: 0            
  run_exports:
    - {{ pin_subpackage(name, max_pin="x") }} 

requirements:
  build:
    - {{ compiler('cxx') }}
    - {{ compiler('c') }}
    - cmake >=3.18
    - ninja
    - make
    - {{ compiler('rust') }}
    - cargo-bundle-licenses
  host:
    - python ==3.12
    - pip
    - setuptools >=68
    - seqan3 ==3.3.0
    - cxxopts >=2.2.0
    - catch2 ==2.*
    - zlib
    - bzip2
    - raptor ==3.*
    - pandas >=1.2.0
    - multitax >=1.3.1
    - genome_updater >=0.6.4
    - grep
    - coreutils
    - curl
    - diffutils
  run:
    - python ==3.12
    - pandas >=1.2.0
    - multitax >=1.3.1
    - genome_updater >=0.6.4
    - grep
    - coreutils
    - curl
    - diffutils
    - zlib
    - bzip2
    - raptor ==3.*
    - fastp


test:
  commands:
    - themis -h


about:
  home: https://github.com/xujialupaoli/profiler
  summary: "profiler: metagenomic profiler"
  license: GPL-3.0-or-later
  license_file: LICENSE
