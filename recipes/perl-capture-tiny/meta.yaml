package:
  name: perl-capture-tiny
  version: "0.36"

source:
  fn: Capture-Tiny-0.36.tar.gz
  url: https://cpan.metacpan.org/authors/id/D/DA/DAGOLDEN/Capture-Tiny-0.36.tar.gz
  md5: db6444111c30ac01a76a4c118241c7b6

build:
  number: 0

requirements:
  # NOTE: All other required build & run modules specified in the CPAN metadata
  #       are already installed as part of the default "perl" package.
  build:
    - perl-threaded

  run:
    - perl-threaded

test:
  imports:
    - Capture::Tiny

about:
  home: https://github.com/dagolden/Capture-Tiny
  license: apache_2_0
  summary: 'Capture STDOUT and STDERR from Perl, XS or external programs'
