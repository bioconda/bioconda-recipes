{% set version = '0.0.1' %}

package:
  name: bioconductor-scater-scripts
  version: {{ version }}

source:
  git_url: https://github.com/ebi-gene-expression-group/bioconductor-scater-scripts.git

requirements:
    build:
        - git
    run:
        - bioconductor-scater
        - r-optparse

test:
    commands:
        - scater-read-10x-results.R
        - scater-normalize.R
        - scater-calculate-qc-metrics.R
