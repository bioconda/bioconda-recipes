package:
  name: pygtftk
  version: "0.9.7"

source:
  url: https://github.com/dputhier/pygtftk/archive/0.9.7.tar.gz
  md5: 6ce8e5d2351cd039c11dee6814dc6b98
  sha1: 23862a12605a02de001d540c4e2fa5688294d662

build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  number: 1
  # TO DO try with true:
  #preserve_egg_dir: True

  #    # This is required to make R link correctly on Linux.
  #  rpaths:
  #    - lib/
  #    - lib64/

requirements:
  build:
    - {{ compiler('c') }}
  host:
    #- gitpython >=2.1.8
      #- gcc
      #- isl 0.12.2
      #- libgcc-ng # Trying to solve  https://github.com/ContinuumIO/anaconda-issues/issues/5191
    - python >=3.6
    - cython
    - argparse
      #- nose
      #- bats
    - bedtools ==2.27.1
    - cloudpickle >=0.4.0
    - ftputil >=3.3.1,<4.0.0
    - pandas >=0.23.3
    - numpy #Trying to solve RuntimeError: module compiled against API version 0xc but this version of numpy is 0xa
    - scipy >=1.1.0
    - pybedtools >=0.7.8
    - pybigwig >=0.3
#    - pysam >=0.9.1.4
    - pyyaml >=3.12
    - requests >=2.13.0
    - cffi >=1.10.0
    - pyparsing >=2.2.0
    - biopython >=1.69
    - zlib
      #- zlib >=1.2.11
    - matplotlib >=2.0.2
    - plotnine >=0.4.0
    - future
      #- make
      #    - libstdcxx-ng >=7.2.0
      #    - unzip
      #    - gawk
      #    - mizani >=0.4.5

  run:
    #- gitpython >=2.1.8
      #- gcc
      #- libgcc-ng # Trying to solve  https://github.com/ContinuumIO/anaconda-issues/issues/5191
    - python >=3.6
    - cython
    - argparse
      #- nose
      #- bats
    - bedtools ==2.27.1
    - cloudpickle >=0.4.0
    - ftputil >=3.3.1,<4.0.0
    - numpy #Trying to solve RuntimeError: module compiled against API version 0xc but this version of numpy is 0xa
    - pandas >=0.23.3
    - scipy >=1.1.0
    - pybedtools >=0.7.8
    - pybigwig >=0.3
#    - pysam >=0.9.1.4
    - pyyaml >=3.12
    - requests >=2.13.0
    - cffi >=1.10.0
    - pyparsing >=2.2.0
    - biopython >=1.69
      #- zlib >=1.2.11
    - zlib
    - matplotlib >=2.0.2
    - plotnine >=0.4.0
    - future

test:
  commands:
    - gtftk -h
  #    #- make test
  #    #- make nose
  imports:
    - pygtftk

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.
  # requires:
    # Put any additional test requirements here.

about:
  home: http://github.com/dputhier/pygtftk
  license: MIT
  summary: 'The gtftk suite.'
  #summary: 'The glorious Gtftk tool suite providing facilities for handling gtf files at blazing speed thanks to C code.'


