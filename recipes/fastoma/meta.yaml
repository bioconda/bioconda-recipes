{% set name = "fastoma" %}
{% set version = "0.5.1" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://github.com/DessimozLab/FastOMA/archive/refs/tags/v{{ version }}.tar.gz
  sha256: a8ba41cd7eea633837ee1b68419efd0b05999d2de93a2d2f394e796ece9c82d2

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation --no-cache-dir
  run_exports:
    - {{ pin_subpackage(name, max_pin="x.x") }}

requirements:
  host:
    - python >=3.8,<3.13
    - pip
    - hatchling
  run:
    - python >=3.8,<3.13
    - omamer
    - mafft
    - fasttree
    - nextflow
    - papermill
    - seaborn-base
    - matplotlib-base
    - pyparsing
    - networkx
    - jupyter
    - mmseqs2
    - dendropy
    - future

test:
  commands:
    - fastoma-check-input  -h
    - fastoma-batch-roothogs -h
    - fastoma-infer-subhogs -h 
    - fastoma-collect-subhogs -h
    - nextflow -h

about:
  home: "https://github.com/DessimozLab/FastOMA"
  summary: "FastOMA - a package to infer orthology information among proteomes."
  license: "MPL-2.0"
  license_file: license
  dev_url: "https://github.com/DessimozLab/FastOMA"

extra:
  recipe-maintainers:
    - sinamajidian
  identifiers:
    - doi:10.1038/s41592-024-02552-8
