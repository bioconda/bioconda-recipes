package:
  name: r-pamr
  # Note that conda versions cannot contain -, so any -'s in the version have
  # been replaced with _'s.
  version: "1.55"

source:
  fn: pamr_1.55.tar.gz
  url:
    - http://cran.r-project.org/src/contrib/pamr_1.55.tar.gz
    - http://cran.r-project.org/src/contrib/Archive/pamr/pamr_1.55.tar.gz


  # You can add a hash for the file here, like md5 or sha1
  # md5: 49448ba4863157652311cc5ea4fea3ea
  # sha1: 3bcfbee008276084cbb37a2b453963c61176a322
  # patches:
   # List any patch files here
   # - fix.patch

build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  number: 1

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/


requirements:
  build:
    - r
    - r-cluster # [not win]
    - r-survival # [not win]
    - gcc # [not win]

  run:
    - r
    - r-cluster # [not win]
    - r-survival # [not win]
    - libgcc # [not win]

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('pamr')" # [not win]
    - "\"%R%\" -e \"library('pamr')\"" # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  #home:
  license: GPL-2
  summary: Some functions for sample classification in microarrays

# The original CRAN metadata for this package was:

# Package: pamr
# Title: Pam: prediction analysis for microarrays
# Version: 1.55
# Author: T. Hastie, R. Tibshirani, Balasubramanian Narasimhan, Gil Chu
# Description: Some functions for sample classification in microarrays
# Maintainer: Rob Tibshirani  <tibs@stanford.edu>
# Depends: R (>= 2.10), cluster, survival
# License: GPL-2
# Packaged: 2014-08-27 02:07:43 UTC; tibs
# Repository: CRAN
# Date/Publication: 2014-08-27 07:27:29
# NeedsCompilation: yes

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
