build:
  number: 1
#  skip: True # [osx]

package:
  name: sprinkles
  version: "0.4.6"

source:
  fn: sprinkles-0.4.6.tar.gz
  url: https://pypi.python.org/packages/source/s/sprinkles/sprinkles-0.4.6.tar.gz
  md5: a653505d58e316b871cafe7df4c79048
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sprinkles = sprinkles:main
    #
    # Would create an entry point called sprinkles that calls sprinkles.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://an9.org/w/SprinklesPy
  license: MIT License
  summary: 'Plugins! Easy!'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
