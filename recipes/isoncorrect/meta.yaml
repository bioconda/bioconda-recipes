{% set name = "isONcorrect" %}
{% set version = "0.1.2" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: "https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz"
  sha256: 89102fdfaf690b31777e86f80519d7bffcc7d885e5d9bdb018ddab56b2ae7328

build:
  number: 0
  noarch: python
  script: "{{ PYTHON }} -m pip install . -vv --no-deps --ignore-installed"
  run_exports:
    - {{ pin_subpackage('isoncorrect', max_pin="x") }}

requirements:
  host:
    - pip
    - python >=3
    - hatchling
  run:
    - python >=3
    - python-edlib >=1.1.2
    - numpy >=1.16.2
    - parasail-python

test:
  commands:
    - isONcorrect --version
  imports:
    - modules

about:
  home: "https://github.com/ksahlin/isONcorrect"
  license: GPL-3.0-or-later
  license_family: GPL
  license_file: LICENSE.txt
  summary: "De novo error-correction of long-read transcriptome reads."

extra:
  recipe-maintainers:
    - mptrsen
