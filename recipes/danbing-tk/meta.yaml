{% set name = "danbing-tk" %}
{% set version = "1.3.2.4" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  path: ./
  url: "https://github.com/ChaissonLab/danbing-tk/archive/refs/tags/v1.3.2.4.tar.gz"
  sha256: 920d964db6e8c20a3d60f3aee61df0e7d809f0658d2d6b828058528d8ac34f6d

build:
  number: 0
  skip: True [osx]
  run_exports:
    - {{ pin_subpackage(name, max_pin="x") }}

requirements:
  build:
    - make
    - {{ compiler('cxx') }}
  host:
    - cereal
    - eigen
  run:
    - eigen

test:
  commands:
    - danbing-tk 2>&1 | grep "Usage"
    - danbing-tk-pred 2>&1 | grep "Usage"
    - genPanKmers 2>&1 | grep "Usage"
    - ktools 2>&1 | grep "Usage"
    - vntr2kmers_thread 2>&1 | grep "Usage"

about:
  home: "https://github.com/ChaissonLab/danbing-tk"
  summary: 'Toolkit for VNTR genotyping and repeat-pan genome graph construction'
  license: BSD-3-Clause

extra:
  additional-platforms:
    - linux-aarch64
  identifiers:
    - doi:10.1038/s41467-021-24378-0
