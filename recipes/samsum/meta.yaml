{% set name = "samsum" %}
{% set version = "0.1.3" %}

package:
  name: "{{ name }}"
  version: "{{ version }}"

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: f9d20462441dfc4bde30293098b38860f2f336d2f96aca59b595a36626d1d28c

build:
  number: 0
  skip: True  # [py2k]
  entry_points:
    - samsum = samsum.__main__:main
  script: "{{ PYTHON }} -m pip install . --no-deps --ignore-installed -vv "

requirements:
  build:
    - {{ compiler('cxx') }}
  host:
    - numpy
    - pip
    - pytest
    - python
  run:
    - {{ pin_compatible('numpy') }}
    - pytest
    - python
    - pyfastx

test:
  imports:
    - samsum
  commands:
    - {{ name }} --help
    - {{ name }} info

about:
  home: https://github.com/hallamlab/samsum
  license: GNU General Public v3 (GPLv3)
  license_family: GPL3
  license_file: LICENSE
  summary: A light-weight python package for summarizing sequence coverage from SAM and BAM files

extra:
  recipe-maintainers:
    - cmorganl
