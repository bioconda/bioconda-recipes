{% set name = "chronumental" %}
{% set version = "0.0.59" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/chronumental-{{ version }}.tar.gz
  sha256: 289ddf97e31269ad90f7d470a260bd65b2e27547f8a07f97b01c07e2eefae933

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python >=3.6
  run:
    - numpyro ==0.9.0
    - pandas
    - python >=3.6
    - scipy
    - alive-progress
    - treeswift
    - xopen

test:
  imports:
    - chronumental
  commands:
    - chronumental -h
  requires:
    - pip

about:
  home: https://github.com/theosanderson/chronumental
  summary: Make time trees from large phylogenetic divergence trees
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - tomkinsc
