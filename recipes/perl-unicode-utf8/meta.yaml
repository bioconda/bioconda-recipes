{% set name = "perl-unicode-utf8" %}
{% set version = "0.68" %}
{% set sha256 = "ca9dfbebf57cbe470dc68136ac792d6c89a38e7de5c7d2084b5c90e8d1010105" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://cpan.metacpan.org/authors/id/C/CH/CHANSEN/Unicode-UTF8-0.68.tar.gz
  sha256: {{ sha256 }}

build:
  run_exports:
    - {{ pin_subpackage(name, max_pin="x.x") }}
  number: 0

requirements:
  build:
    - make
    - {{ compiler('cxx') }}

  host:
    - perl
    - perl-test-fatal
    - perl-exporter
    - perl-encode
    - perl-extutils-makemaker
    - perl-carp
  run:
    - perl
    - perl-carp
    - perl-exporter

test:
  imports:
    - Unicode::UTF8

about:
  home: http://metacpan.org/pod/Unicode::UTF8
  license: perl_5
  summary: 'Encoding and decoding of UTF-8 encoding form'

extra:
  # osx-arm64 is erroring due to missing perl-encode in conda forge
  additional-platforms:
    - linux-aarch64
