package:
  name: genomepy
  version: "0.3.0"

source:
  fn: genomepy-0.3.0.tar.gz
  url: https://pypi.python.org/packages/8d/12/5a10995e86898b161b1e2e402adbe48e3d1b1e186dc7da520371fff8ee82/genomepy-0.3.0.tar.gz
  md5: 9e4e0429528e3e837092c01831076067
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - genomepy = genomepy:main
    #
    # Would create an entry point called genomepy that calls genomepy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - nose
    - click
    - pyfaidx
    - norns >0.0.1
    - xmltodict
    - bucketcache
    - msgpack-python

  run:
    - python
    - nose
    - click
    - pyfaidx
    - norns >0.0.1
    - xmltodict
    - bucketcache
    - msgpack-python

test:
  # Python imports
  imports:
    - genomepy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/simonvh/genomepy
  license: MIT License
  summary: 'Genomes in Python'
  license_family: MIT

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
