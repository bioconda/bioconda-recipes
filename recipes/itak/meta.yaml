{% set version = "2.0.2" %}
{% set name = "itak" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/kentnf/iTAK/archive/refs/tags/v{{ version }}.tar.gz
  sha256: e7209cfbb131f77fee2a5e998813f79a979016d1d7ba77a6cc078d220c9a576a

build:
  number: 0
  noarch: python
  run_exports:
    weak:
      - {{ pin_subpackage('itak', max_pin="x") }}

requirements:
  build:
    - python >=3.6
    - setuptools
    - biopython
    - requests
    - hmmer
  run:
    - python >=3.6
    - biopython
    - requests
    - hmmer

test:
  imports:
    - itakm
  commands:
    - iTAK.py -h 

about:
  home: https://github.com/kentnf/iTAK
  dev_url: https://github.com/kentnf/iTAK
  license: Apache-2.0
  summary: iTAK is a program to identify and classify plant TFs, TRs, and PKs. 

extra:
  recipe-maintainers:
    - kentnf
