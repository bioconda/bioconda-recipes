{% set name = "bwread" %}
{% set version = "0.0.3" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: "https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz"
  sha256: 4a8a552c215e3746a7cbabfe00f3da342f9372b53e448da1edb8fb2350038e12

build:
  number: 0
  script: "{{ PYTHON }} -m pip install . -vv"

requirements:
  host:
    - cython
    - natsort
    - numpy
    - pandas
    - pip
    - pybigwig
    - python
  run:
    - cython
    - natsort
    - numpy
    - pandas
    - pybigwig
    - python

test:
  imports:
    - bamread
    - bamread.src
    - bwread
    - bwread.src

about:
  home: "http://github.com/endrebak/bwread"
  license: MIT
  license_family: MIT
  license_file: 
  summary: "Read bigwig files quickly into PyRanges"
  doc_url: 
  dev_url: 

extra:
  recipe-maintainers:
    - endrebak
