package:
  name: glob2
  version: "0.4.1"

source:
  fn: glob2-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/g/glob2/glob2-0.4.1.tar.gz
  md5: 0a4f0030d202dc0a21d64946f6fd5f5d

build:
  number: 0
  skip: False

requirements:
  build:
    - python
  run:
    - python

test:
  # Python imports
  imports:
    - glob2

about:
  home: http://github.com/miracle2k/python-glob2/
  license: BSD License
  summary: 'Version of the glob module that can capture patterns and supports recursive wildcards'
