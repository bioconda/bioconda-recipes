{% set version = "0.6.4" %}

package:
  name: pyfastx
  version: {{ version }}

source:
  url: https://github.com/lmdu/pyfastx/archive/{{ version }}.tar.gz
  sha256: 892def8f33787247bfd615607cd3a5f2bfb17987898c4b91bc2aed8ed430987b


build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . -vv
  entry_points:
    - pyfastx = pyfastxcli:main

requirements:
  host:
    - python >=3.5
    - {{ compiler('c') }}
    - pip
  run:
    - python >=3.5
    - {{ compiler('c') }}

test:
  imports:
    - pyfastx
  commands:
    - pyfastx --help

about:
  home: https://github.com/lmdu/pyfastx
  license: MIT
  summary:  |
          pyfastx is a python module for fast random
          access to sequences from plain and gzipped
          FASTA/Q file

extra:
  identifiers:
