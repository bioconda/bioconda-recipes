{% set name = "methurator" %}
{% set version = "0.1.5" %}
package:
  name: {{ name|lower }}
  version: {{ version }}
source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/methurator-{{ version }}.tar.gz
  sha256: 604e5a98b2bd226835f8815e8ab3a9232ab68be6def1b7d2f0d6c9d943971b8a
build:
  run_exports:
    - {{ pin_subpackage('methurator', max_pin="x.x") }}
  entry_points:
    - methurator = methurator.cli:entry_point
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0
requirements:
  host:
    - python >=3.10,<3.14
    - setuptools >=61
    - pip
  run:
    - python >=3.10,<3.14
    - matplotlib-base
    - numpy
    - packaging >=24
    - pandas
    - pyfaidx
    - pysam
    - rich
    - rich-click
    - scipy
    - tqdm
    - plotly
    - samtools 1.21
    - htslib 1.21
    - methyldackel 0.6.1
test:
  imports:
    - methurator
  commands:
    - pip check
    - methurator --help
    - samtools --version
    - MethylDackel --version
  requires:
    - pip
about:
  home: https://github.com/VIBTOBIlab/methurator
  summary: Python package designed to estimate sequencing saturation for reduced-representation bisulfite sequencing (RRBS) data.
  license: MIT
  license_file: LICENSE
extra:
  recipe-maintainers:
    - https://github.com/edogiuili
