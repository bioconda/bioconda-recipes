{% set name = "methurator" %}
{% set version = "0.1.7" %}
package:
  name: {{ name|lower }}
  version: {{ version }}
source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/methurator-{{ version }}.tar.gz
  sha256: 6950bb4086a998200d3b4b1761e3073ba023b99e5bd77aeaebd5b2c56e4b534f
build:
  run_exports:
    - {{ pin_subpackage('methurator', max_pin="x.x") }}
  entry_points:
    - methurator = methurator.cli:entry_point
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0
requirements:
  host:
    - python >=3.10,<3.14
    - setuptools >=61
    - pip
  run:
    - python >=3.10,<3.14
    - matplotlib-base
    - numpy
    - packaging >=24
    - pandas
    - pyfaidx
    - pysam
    - pyyaml
    - rich
    - rich-click
    - scipy
    - tqdm
    - plotly
    - samtools >=1.21
    - htslib >=1.21,<1.23.0a0
    - methyldackel 0.6.1
test:
  imports:
    - methurator
  commands:
    - pip check
    - methurator --help
    - samtools --version
    - MethylDackel --version
  requires:
    - pip
about:
  home: https://github.com/VIBTOBIlab/methurator
  summary: Python package designed to estimate sequencing saturation for reduced-representation bisulfite sequencing (RRBS) data.
  license: MIT
  license_file: LICENSE
extra:
  recipe-maintainers:
    - edogiuili
  additional-platforms:
    - linux-aarch64
    - osx-arm64