{% set version = "1.3.0" %}
{% set sha256 = "eb193c27ce5a04ec83c55edcfa991a19d341e03d038d213e0846816d584e24d0" %}

package:
  name: irfinder
  version: '{{ version }}'

source:
  url: https://github.com/williamritchie/IRFinder/archive/v{{ version }}.tar.gz
  sha256: '{{ sha256 }}'

build:
  number: 0

requirements:
  build:
    - '{{ compiler("c") }}'
  run:
    - perl
    - r-base

test:
  commands:
    - IRFinder -v

about:
  home: https://github.com/williamritchie/IRFinder
  license: MIT
  license_family: MIT
  summary: Intron Retention Finder
