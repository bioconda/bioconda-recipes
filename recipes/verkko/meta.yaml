{% set name = "verkko" %}
{% set version = "1.3" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/marbl/{{ name }}/releases/download/v{{ version }}/{{ name }}-v{{ version }}.tar.gz
  md5: 0657abc847e3d554289d2a88a3fdf774

  patches:
    - version.patch

build:
  number: 0

requirements:
  build:
    - make
    - llvm-openmp  # [osx]
    - libgomp      # [linux]
    - {{ compiler('cxx') }}
    - rust
  host:
    - gsl
  run:
    - python >=3.7
    - snakemake-minimal >=7.8.0
    - graphaligner >=1.0.17
    - mbg >=1.0.14
    - findutils >=4.6.0
    - mashmap >=2.0

test:
  requires:
    - curl
  commands:
     - verkko --version

about:
  home: https://github.com/marbl/verkko
  license: CC0
  license_file: README.licenses
  summary: Assembler for hybrid combinations of long reads
