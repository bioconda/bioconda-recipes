package:
  name: perl-set-intspan
  version: "1.19"

source:
  fn: Set-IntSpan-1.19.tar.gz
  url: http://cpan.metacpan.org/authors/id/S/SW/SWMCD/Set-IntSpan-1.19.tar.gz
  md5: 7c21c2fedce5b7be7d8200e2cf2bdca1

# build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - perl-threaded
    - perl-extutils-makemaker

  run:
    - perl-threaded

test:
  imports:
    - Set::IntSpan

about:
  home: http://metacpan.org/pod/Set-IntSpan
  license: unknown
  summary: 'Manages sets of integers, newsrc style'
