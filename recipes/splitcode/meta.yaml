{% set name = "splitcode" %}
{% set version = "0.31.6" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://github.com/pachterlab/{{ name }}/archive/refs/tags/v{{ version }}.tar.gz
  sha256: 745f09f650c01c41ebde1a7ad809b6aae0a7e17081fe32904a8a8247ba51a98f

build:
  number: 0
  run_exports:
    - {{ pin_subpackage('splitcode', max_pin="x.x") }}

requirements:
  build:
    - make
    - cmake
    - {{ compiler('cxx') }}
  host:
    - zlib

test:
  commands:
    - splitcode --version

about:
  home: "https://github.com/pachterlab/splitcode"
  license: "BSD-2-Clause"
  license_family: BSD
  license_file: LICENSE
  summary: "Flexible parsing, interpretation, and editing of technical sequences."
  dev_url: "https://github.com/pachterlab/splitcode"
  doc_url: "https://splitcode.readthedocs.io"

extra:
  identifiers:
    - biotools:splitcode
    - doi:10.1101/2023.03.20.533521
  recipe-maintainers:
    - Yenaled
  additional-platforms:
    - linux-aarch64
    - osx-arm64
