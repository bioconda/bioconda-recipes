{% set version = "1.0.0" %}

package:
  name: gefast
  version: {{ version }}

build:
  number: 0

source:
  url: https://github.com/romueller/gefast/archive/v{{ version }}.tar.gz
  sha256: 66b14b6cc81aa99ba0c64bf817555f2d88e4601db60cf6b56d04aa0235cba378

requirements:
  build:
    - {{ compiler('cxx') }}
    - make

test:
  commands:
    - GeFaST --help | grep {{ version }}

about:
  home: https://github.com/romueller/gefast
  license: AGPL
  license_file: LICENCE
  summary: Clustering tool using Swarm's clustering strategy and Pass-Join's segment filter.
