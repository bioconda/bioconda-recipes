{% set version = '0.0.1' %}

package:
  name: r-workflowscriptscommon
  version: {{ version }}

source:
  git_url: https://github.com/ebi-gene-expression-group/workflow-scripts-common.git

requirements:
    build:
        - git
        - r-base
    run:
        - r-optparse

test:
    commands:
        - $R -e "library('workflowscriptscommon')"
