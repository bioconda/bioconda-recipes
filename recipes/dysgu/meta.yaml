{% set name = "dysgu" %}
{% set version = "1.3.15" %}
{% set sha256hash = "7b999150cbbd8a26d07596aea789e820dc6fa6d5fbcb893ff217b046c29e9c54" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/kcleal/dysgu/archive/v{{ version }}.tar.gz
  sha256: {{ sha256hash }}

build:
  number: 0
  binary_relocation: False
  entry_points:
    - dysgu = dysgu.main:cli
  run_exports:
      strong:
        - cython

requirements:
  build:
    - make
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
  host:
    - python >=3.7
    - setuptools
    - cython
    - htslib >=1.12
    - numpy >=1.18
    - scipy
    - pandas
    - networkx >=2.4
    - scikit-learn >=0.22
    - python-edlib
    - sortedcontainers
    - lightgbm
    - zlib
    - click >=8.0
    - pysam
    - libcurl
    - openssl
    - bzip2
    - xz
  run:
    - python >=3.7
    - numpy >=1.18
    - scipy
    - pandas
    - networkx >=2.4
    - scikit-learn >=0.22
    - python-edlib
    - sortedcontainers
    - lightgbm
    - zlib
    - click >=8.0
    - pysam
    - libcurl
    - openssl
    - bzip2
    - xz
    - htslib >=1.12

test:
  requires:
    - cython
  imports:
    - dysgu
  files:
    - dysgu/tests/*
  commands:
    - 'pip install cython; dysgu -v'


about:
  home: https://github.com/kcleal/dysgu
  summary: A collection of tools for calling structural variants using short or long reads
  license: MIT
  license_file: LICENSE.md

extra:
  container:
      extended-base: True
  recipe-maintainers:
    - kcleal
