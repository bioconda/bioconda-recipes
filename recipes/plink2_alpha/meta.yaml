# This file created by conda-build 4.16.4
# meta.yaml template originally from:
# ------------------------------------------------

package:
    name: plink
    version: 2_alpha 
source:
    path: ./plink2
test:
    commands:
        - plink2 --help | grep PLINK
about:
    summary: 'PLINK Whole genome data analysis toolset'
