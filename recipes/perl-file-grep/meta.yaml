package:
  name: perl-file-grep
  version: "0.02"

source:
  fn: File-Grep-0.02.tar.gz
  url: http://cpan.metacpan.org/authors/id/M/MN/MNEYLON/File-Grep-0.02.tar.gz
  md5: 6ea1d06a385229b1c015db23e1ef93fa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - perl-threaded

  run:
    - perl-threaded

test:
  # Perl 'use' tests
  imports:
    - File::Grep

  # You can also put a file called run_test.pl (or run_test.py) in the recipe
  # that will be run at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://metacpan.org/pod/File::Grep
  license: unknown
  summary: 'Find matches to a pattern in a series of files and related functions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
