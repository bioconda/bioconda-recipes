{% set version = "1.5" %}

package:
  name: ryuto
  version: {{ version }}

build:
  number: 0

source:
  url: http://silo.bioinf.uni-leipzig.de/thomas/RYUTO.tar.gz
  sha256: ea7c2b93cdc5a0a5b236f6a91821b77fa70877807152e2e11c5b9175032e7553

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - llvm-openmp  # [osx]
    - openmp  # [linux]
  host:
    - htslib
    - autoconf
    - automake
    - cmake 
    - zlib
    - boost-cpp
    - unzip
    - tar
    - llvm-openmp  # [osx]
    - openmp  # [linux]
  run:
    - htslib
    - boost-cpp 
    - zlib
    - llvm-openmp  # [osx]
    - openmp  # [linux]

about:
  home: https://github.com/studla/RYUTO/ 
  license: BSD
  license_file: LICENSE
  summary: Network-Flow based Transcriptome Reconstruction 

test:
  commands:
    - echo "Hello"

