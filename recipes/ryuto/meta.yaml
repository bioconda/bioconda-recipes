{% set version = "1.6.1" %}

package:
  name: ryuto
  version: {{ version }}

build:
  number: 0

source:
  url: http://silo.bioinf.uni-leipzig.de/thomas/1.6.tar.gz
  sha256: 4c0c85f360b5abe4fc6b76ce5f99007e87564783ae6e01342902518b403da25c

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - llvm-openmp  # [osx]
    - libgomp  # [linux]
    - autoconf
    - automake
    - cmake
    - unzip
    - tar
  host:
    - htslib
    - zlib
    - boost-cpp
  run:
    - htslib
    - boost-cpp 
    - zlib

test:
  commands:
    - ryuto --help

about:
  home: https://github.com/studla/RYUTO/ 
  license: BSD
  license_file: LICENSE
  summary: Network-Flow based Transcriptome Reconstruction 
