{% set version = "1.5" %}

package:
  name: ryuto
  version: {{ version }}

build:
  number: 0

source:
  url: http://silo.bioinf.uni-leipzig.de/thomas/RYUTO.tar.gz
  sha256: b6c157bb448d3dc184a9571a11fee7849d5ce6870b4dc01828a7a8da136c954a

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - llvm-openmp  # [osx]
    - openmp  # [linux]
  host:
    - htslib
    - autoconf
    - automake
    - cmake 
    - zlib
    - boost-cpp
    - unzip
    - tar
    - llvm-openmp  # [osx]
    - openmp  # [linux]
  run:
    - htslib
    - boost-cpp 
    - zlib
    - llvm-openmp  # [osx]
    - openmp  # [linux]

about:
  home: https://github.com/studla/RYUTO/ 
  license: BSD
  license_file: LICENSE
  summary: Network-Flow based Transcriptome Reconstruction 

test:
  commands:
    - echo "Hello"

