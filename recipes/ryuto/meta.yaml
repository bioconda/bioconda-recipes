{% set version = "1.5" %}

package:
  name: ryuto
  version: {{ version }}

build:
  number: 0

source:
  url: http://silo.bioinf.uni-leipzig.de/thomas/RYUTO.tar.gz
  sha256: 4b8b5da4d5a3d0a47ba69204a58177f4832fe8353aecb85c19de0f28b83b4138

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
  host:
    - htslib
    - autoconf
    - zlib
    - boost-cpp
    - unzip
    - tar
  run:
    - htslib
    - boost-cpp 
    - zlib

about:
  home: https://github.com/studla/RYUTO/ 
  license: BSD
  license_file: LICENSE
  summary: Network-Flow based Transcriptome Reconstruction 

test:
  commands:
    - ryuto --version

