{% set name = "babappalign" %}
{% set version = "1.0.2" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://github.com/sinhakrishnendu/BABAPPAlign/archive/refs/tags/v{{ version }}.tar.gz
  sha256: 3e8bb02b0fa9eaef7a22b69400b285b176ae02bfd3c573428c53fd8d8e829f15

build:
  number: 0
  script: "{{ PYTHON }} -m pip install . --no-deps -vv"
  run_exports:
    - {{ pin_subpackage('babappalign', max_pin='x') }}

requirements:
  host:
    - python >=3.9,<3.14
    - pip
    - setuptools
    - wheel

  run:
    - python >=3.9,<3.14
    - numpy
    - scipy
    - pandas
    - biopython
    - tqdm
    - pytorch >=1.12
    - transformers >=4.30
    - fair-esm

test:
  commands:
    # --- CRITICAL: redirect all caches away from /tmp ---
    - export HF_HOME="$PWD/hf_home"
    - export TRANSFORMERS_CACHE="$PWD/hf_home"
    - export XDG_CACHE_HOME="$PWD/hf_home"

    # sanity check only (no heavy downloads)
    - babappalign --help

about:
  home: https://github.com/sinhakrishnendu/BABAPPAlign
  license: MIT
  license_file: LICENSE
  summary: "Deep learning–based progressive multiple sequence alignment engine with learned residue scoring"

extra:
  skip-lints:
      - should_be_noarch_generic
  keywords:
    - multiple-sequence-alignment
    - protein-alignment
    - deep-learning
    - esm
    - transformers
    - bioinformatics
