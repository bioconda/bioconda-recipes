package:
  name: pita
  version: "1.73.3"

source:
  fn: pita-1.73.3.tar.gz
  url: https://pypi.python.org/packages/source/p/pita/pita-1.73.3.tar.gz
  md5: 24fa7746eb6fc6eb88eac846d8eb1d87
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pita = pita:main
    #
    # Would create an entry point called pita that calls pita.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - sqlalchemy
    - gimmemotifs >0.8.6
    - pysam >=0.9
    - pyyaml
    - htseq
    - biopython
    - networkx >=1.10
    - bcbio-gff

  run:
    - python
    - numpy
    - sqlalchemy
    - gimmemotifs >0.8.6
    - pysam >=0.9
    - pyyaml
    - htseq
    - biopython
    - networkx >=1.10
    - bcbio-gff

test:
  # Python imports
  imports:
    - pita

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  requires:
    - pytest
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/simonvh/pita
  license: MIT
  summary: '\npita - pita improves transcript annotation\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
