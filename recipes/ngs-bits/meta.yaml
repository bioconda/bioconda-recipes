package:
  name: ngs-bits
  version: 2018_03

build:
  number: 0

source:
  git_rev: cbc0399a15818d1e5a824b5d261d9a1bc3b243fa
  git_url: https://github.com/imgag/ngs-bits.git

requirements:
  build:
    - bzip2 {{ CONDA_BZIP2 }}*
    - xz {{ CONDA_XZ }}*
    - zlib {{ CONDA_ZLIB }}*
    - gcc   # [linux]
    - llvm  # [osx]
    - git
    - qt >=5.5.0
    - htslib
  run:
    - bzip2 {{ CONDA_BZIP2 }}*
    - xz {{ CONDA_XZ }}*
    - zlib {{ CONDA_ZLIB }}*
    - libgcc  # [linux]
    - python
    - matplotlib
    - qt >=5.5.0
    - htslib

about:
  home: https://github.com/imgag/ngs-bits
  license: MIT license
  summary: Short-read sequencing tools

extra:
  skip-lints:
    - uses_git_url
    - missing_hash