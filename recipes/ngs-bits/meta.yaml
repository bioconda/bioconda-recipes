package:
  name: ngs-bits
  version: 2018_03

build:
  number: 0

source:
  git_rev: e25323318a78472adb3b99c9a1b0c202b6d3ca7e
  git_url: https://github.com/imgag/ngs-bits.git

requirements:
  build:
    - bzip2 {{ CONDA_BZIP2 }}*
    - xz {{ CONDA_XZ }}*
    - zlib {{ CONDA_ZLIB }}*
    - gcc   # [linux]
    - llvm  # [osx]
    - git
    - qt >=5.5.0
    - htslib {{ CONDA_HTSLIB }}*
  run:
    - bzip2 {{ CONDA_BZIP2 }}*
    - xz {{ CONDA_XZ }}*
    - zlib {{ CONDA_ZLIB }}*
    - libgcc  # [linux]
    - python
    - matplotlib
    - qt >=5.5.0
    - htslib {{ CONDA_HTSLIB }}*

about:
  home: https://github.com/imgag/ngs-bits
  license: MIT license
  summary: Short-read sequencing tools

extra:
  skip-lints:
    - uses_git_url
    - missing_hash