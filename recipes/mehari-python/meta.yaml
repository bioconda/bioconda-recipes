{% set version = "0.45.1" %}
{% set sha256 = "19d2033a251302fa9f3034f61d776255383a3d46e4c04c6bee4107265ef5d171" %}
{% set name = "mehari-python" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://github.com/varfish-org/mehari/archive/{{ name }}-v{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  skip: True  # [osx or py<312]
  run_exports:
    - {{ pin_subpackage(name, max_pin="x.x") }}

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - {{ compiler('rust') }}
    - cmake
    - make
    - cargo-bundle-licenses
    - pkg-config
    - python                              # [build_platform != target_platform]
    - cross-python_{{ target_platform }}  # [build_platform != target_platform]
    - crossenv                            # [build_platform != target_platform]
    - maturin >=1.12,<2.0                 # [build_platform != target_platform]
    - libprotobuf
  host:
    - clangdev
    - llvmlite
    - openssl
    - zlib
    - sqlite
    - python
    - pip
    - maturin >=1.12,<2.0
  run:
    - sqlite
    - polars >=1.39.3
    - pyarrow >=23.0.1
    - python

test:
  imports:
    - mehari

about:
  home: "https://varfish-org.github.io/mehari"
  license: MIT
  license_family: MIT
  license_file: "LICENSE.txt"
  summary: "Python bindings for mehari."
  dev_url: "https://github.com/varfish-org/mehari"
  doc_url: "https://github.com/varfish-org/mehari/blob/v{{ version }}/{{ name }}/README.md"

extra:
  additional-platforms:
    - linux-aarch64
  identifiers:
    - doi:10.5281/zenodo.10603122
  skip-lints:
    - compiler_needs_stdlib_c
