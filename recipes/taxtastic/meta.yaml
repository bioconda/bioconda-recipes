{% set version = "1.0.0" %}
{% set sha256 = "564bb68da1e2f83e05a2429965af4ab81bf0fdb2c14ca0de6147b75810d388ce" %}

package:
  name: taxtastic
  version: '{{version}}'

source:
  url: https://pypi.python.org/packages/source/t/taxtastic/taxtastic-{{version}}.tar.gz
  sha256: '{{sha256}}'

build:
  noarch: python
  number: 0
  script: python setup.py install --single-version-externally-managed --record=record.txt
  run_exports:
    - {{ pin_subpackage('taxtastic', max_pin='x.x') }}

requirements:
  host:
    - python >=3
    - setuptools

  run:
    - python >=3
    - dendropy
    - pyyaml
    - decorator
    - fastalite
    - jinja2
    - psycopg2-binary
    - sqlalchemy >=2
    - sqlparse

test:
  imports:
    - taxtastic
    - taxtastic.scripts
    - taxtastic.subcommands

about:
  home: https://github.com/fhcrc/taxtastic
  license: GPL-3.0
  license_family: GPL
  license_file: LICENSE
  summary: Tools for taxonomic naming and annotation
