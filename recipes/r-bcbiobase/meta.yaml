{% set version = '0.6.19' %}
{% set github = "https://github.com/hbc/bcbioBase" %}

package:
  name: r-bcbiobase
  version: "{{ version }}"

source:
  url: "{{ github }}/archive/v{{ version }}.tar.gz"
  sha256: ba63e0d88665f368fb743561aaf30e3bd4b1a584bb2a7a0304873e8df33dcf29

build:
  noarch: generic
  number: 0
  rpaths:
    - lib/R/lib/
    - lib/

requirements:
  host:
      # Depends:
    - r-base
      # Imports:
    - bioconductor-s4vectors
    - r-basejump >=0.13.3
    - r-cli >=2.2
    - r-goalie >=0.4.11
    - r-plyr >=1.8
    - r-stringr >=1.4
      # Suggests:
    - r-rdrop2
  run:
      # Depends:
    - r-base
      # Imports:
    - bioconductor-s4vectors
    - r-basejump >=0.13.3
    - r-cli >=2.2
    - r-goalie >=0.4.11
    - r-plyr >=1.8
    - r-stringr >=1.4
      # Suggests:
    - r-rdrop2

test:
  commands:
    - $R -e "library('bcbioBase')"

about:
  home: http://bioinformatics.sph.harvard.edu/bcbioBase/
  dev_url: "{{ github }}"
  license: GPL-3.0
  license_file: LICENSE
  license_family: GPL
  summary: Base functions and generics for bcbio R packages.

extra:
  recipe-maintainers:
    - mjsteinbaugh
