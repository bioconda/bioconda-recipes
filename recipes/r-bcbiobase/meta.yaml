{% set version = '0.6.17' %}
{% set github = "https://github.com/hbc/bcbioBase" %}

package:
  name: r-bcbiobase
  version: "{{ version }}"

source:
  url: "{{ github }}/archive/v{{ version }}.tar.gz"
  sha256: a7c12df77a4ef57e38df2b2805e313681f606dec00a035606685673927824509

build:
  noarch: generic
  number: 0
  rpaths:
    - lib/R/lib/
    - lib/

requirements:
  host:
      # Depends:
    - r-base
      # Imports:
    - bioconductor-s4vectors
    - r-basejump >=0.13.3
    - r-cli >=2.2
    - r-goalie >=0.4.11
    - r-plyr >=1.8
    - r-stringr >=1.4
      # Suggests:
    - r-rdrop2
  run:
      # Depends:
    - r-base
      # Imports:
    - bioconductor-s4vectors
    - r-basejump >=0.13.3
    - r-cli >=2.2
    - r-goalie >=0.4.11
    - r-plyr >=1.8
    - r-stringr >=1.4
      # Suggests:
    - r-rdrop2

test:
  commands:
    - $R -e "library('bcbioBase')"

about:
  home: http://bioinformatics.sph.harvard.edu/bcbioBase/
  dev_url: "{{ github }}"
  license: GPL-3.0
  license_file: LICENSE
  license_family: GPL
  summary: Base functions and generics for bcbio R packages.

extra:
  recipe-maintainers:
    - mjsteinbaugh
