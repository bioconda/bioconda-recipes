{% set version = "2.19" %}

package:
  name: minimap2
  version: {{ version }}

source:
  url: https://github.com/lh3/minimap2/archive/v{{ version }}.tar.gz
  sha256: 440f8743ebb0bc82b6f17c3977fced6f6c9815833c5f719d6d308842fbb0eaf3

build:
  number: 0

requirements:
  build:
    - make
    - {{ compiler('c') }}
  host:
    - zlib
  run:
    - k8
    - zlib

test:
  commands:
    - minimap2 2>&1 | grep 'Usage'
    - paftools.js version | grep {{ version }}
    - sdust  2>&1 | grep 'Usage'

about:
  home: https://github.com/lh3/minimap2
  license: MIT
  license_file: LICENSE.txt
  summary: 'A versatile pairwise aligner for genomic and spliced nucleotide sequences.'
