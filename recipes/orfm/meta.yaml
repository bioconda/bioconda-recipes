{% set version = "2.1.0" %}
{% set name = "OrfM" %}
{% set sha256 = "98af8a51d494ca0b61cd386889297b3d566bfb663d6c2e6634fa4455268a07c8" %}

package:
  name: orfm
  version: {{ version }}

build:
  number: 0
  run_exports:
    - {{ pin_subpackage(name| lower, max_pin="x") }}

source:
  url: https://github.com/wwood/{{ name }}/archive/{{ version }}.tar.gz
  sha256: {{ sha256 }}

requirements:
  build:
    - {{ compiler('rust') }}
    - {{ compiler('c') }}
    - cargo-bundle-licenses
    - cmake
    - make

test:
  commands:
    - orfm -h

about:
  home: https://github.com/wwood/OrfM
  license: LGPL-3.0
  license_family: LGPL
  license_file:
    - LICENSE.txt
    - THIRDPARTY.yml
  summary: OrfM is a simple and not slow ORF caller

extra:
  additional-platforms:
    - linux-aarch64
    - osx-arm64
  identifiers:
    - biotools:{{ name| lower }}
    - doi:10.1093/bioinformatics/btw241
