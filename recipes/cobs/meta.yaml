{% set version = "0.2.0" %}
{% set name = "cobs" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/iqbal-lab-org/{{ name }}/releases/download/v{{ version }}/cobs-boost.tar.gz
  sha256: 60eab403e9fa41a976d67761ce95abb57822914dafd211136ddae33c9d35591a
  patches:
    - fs.patch

build:
  number: 0

requirements:
  build:
    - {{ compiler('cxx') }}
    - libgomp  # [linux]
    - llvm-openmp  # [osx]
    - boost=1.72.0
    - boost-cpp=1.72.0
    - cmake
    - make
  host:
    - boost=1.72.0
    - boost-cpp=1.72.0
    - python
    - zlib
  run:
    - boost=1.72.0
    - boost-cpp=1.72.0
    - python
    - zlib

about:
  home: https://panthema.net/cobs
  license: MIT
  summary: Compact Bit-Sliced Signature Index (for Genomic k-Mer Data or q-Grams)
  license_file: LICENSE

test:
  commands:
    - cobs

extra:
  recipe-maintainers:
    - luizirber
    - johnlees
    - leoisl
  skip-lints:
    - uses_vcs_url
