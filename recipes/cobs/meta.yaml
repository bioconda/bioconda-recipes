{% set version = "0.2.0" %}
{% set name = "cobs" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/iqbal-lab-org/{{ name }}/releases/download/v{{ version }}/cobs-boost.tar.gz
  sha256: 077078216619efd3021f83bc902848e46db30b390e690b384bfe9d3219c2a476
  patch: 
    - fs.patch

build:
  number: 0

requirements:
  build:
    - {{ compiler('cxx') }}
    - libgomp  # [linux]
    - llvm-openmp  # [osx]
    - boost=1.72.0
    - cmake
    - make
  host:
    - boost=1.72.0
    - python
    - zlib
  run:
    - boost=1.72.0
    - python
    - zlib

about:
  home: https://panthema.net/cobs
  license: MIT
  summary: Compact Bit-Sliced Signature Index (for Genomic k-Mer Data or q-Grams)
  license_file: LICENSE

test:
  commands:
    - cobs

extra:
  recipe-maintainers:
    - luizirber
    - johnlees
    - leoisl
  skip-lints:
    - uses_vcs_url

