{% set version = "1.5.1" %}

package:
  name: cgpbigwig
  version: {{ version }}

build:
  number: 0
  skip: True  # [osx]

source:
  url: https://github.com/cancerit/cgpBigWig/archive/{{ version }}.tar.gz
  sha256: 62ef90385d1350a802b532dabf5c48004e8ff09cd04bb9410b91ba29538b406c

requirements:
  build:
    - {{ compiler('c') }}
  host:
    - zlib
    - bzip2
    - xz
    - gnutls
    - gmp
    - libtasn1
    - nettle
    - p11-kit
    - htslib
    - libbigwig
  run:
    - zlib
    - bzip2
    - xz
    - gnutls
    - gmp
    - libtasn1
    - nettle
    - p11-kit
    - htslib
    - libbigwig

about:
  home: https://github.com/cancerit/cgpBigWig
  license: GPLv3
  license_file: LICENSE
  summary: BigWig manpulation tools using libBigWig and htslib

test:
  commands:
    - bam2bw -h
    - detectExtremeDepth -h
