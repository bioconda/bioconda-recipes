{% set name = "citeable" %}
  {% set version = "2026.9.24" %}

  package:
    name: {{ name|lower }}
    version: {{ version }}

  source:
    url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
    sha256: 3dc000c88c3fc0cf9a37860bbc17f94ea56eca38bae153bd03d5988390f9f0a8

  build:
    noarch: python
    number: 0
    script: {{ PYTHON }} -m pip install . --no-deps --no-build-isolation -vvv
    run_exports:
      - {{ pin_subpackage(name, max_pin=None) }}

  requirements:
    host:
      - python >=3.11
      - pip
      - hatchling
    run:
      - python >=3.11

  test:
    imports:
      - citeable

  about:
    home: https://github.com/cogent3/citeable
    license: BSD-3-Clause
    license_family: BSD
    license_file: LICENSE
    summary: Structured BibTeX citations for developers of scientific software packages

  extra:
    recipe-maintainers:
      - GavinHuttley
