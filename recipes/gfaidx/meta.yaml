{% set name = "gfaidx" %}
{% set version = "1.9.8" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://github.com/fawaz-dabbaghieh/gfaidx/archive/refs/tags/v{{ version }}.tar.gz
  sha256: fa2f5fddb93648c0e57db4b6876ab76b939435c99e0716a2408d4338ce92afbf

build:
  number: 0
  skip: True  # [win]
  run_exports:
    - {{ pin_subpackage(name, max_pin="x") }}

requirements:
  build:
    - {{ compiler("c") }}
    - {{ compiler("cxx") }}
    - {{ stdlib("c") }}
    - cmake >=4
    - make
  host:
    - zlib
  run:
    - zlib
    - coreutils

test:
  commands:
    - gfaidx --version
    - gfaidx --help

about:
  home: https://github.com/fawaz-dabbaghieh/gfaidx
  dev_url: https://github.com/fawaz-dabbaghieh/gfaidx
  license: MIT AND Custom
  license_file:
    - LICENSE
    - external/argparse/LICENSE
  summary: Community-based GFA graph indexing and subgraph extraction

extra:
  recipe-maintainers:
    - fawaz-dabbaghieh
  additional-platforms:
    - osx-arm64
