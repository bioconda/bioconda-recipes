{% set version = "1.5.2" %}

package:
  name: desalt
  version: {{ version }}

source:
  url: https://github.com/ydLiu-HIT/deSALT/archive/v{{ version }}.tar.gz
  sha256: 1110d4e8005235406def16eb054c8a4fe61fa10b9cd2b96f391c66fe23e1ee1e

build:
  number: 0

requirements:
  build:
    - {{ compiler('c') }}
    - llvm-openmp  # [osx]
  host:
    - zlib
    - llvm-openmp  # [osx]
  run:
    - zlib
    - llvm-openmp  # [osx]

test:
  commands:
    - deSALT aln

about:
    home: https://github.com/ydLiu-HIT/
    license: MIT
    license_file: LICENSE
    summary: De Bruijn graph-based Spliced Aligner for Long Transcriptome reads 
