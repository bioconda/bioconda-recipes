{% set version = "1.4" %}

package:
    name: desalt
    version: {{ version }}

source:
    sha256: 9f665ab34f03bc8ab5f9fbbccfcf8aa351f9c80343988464c663c3659a07d1b2
    url: https://github.com/ydLiu-HIT/deSALT/releases/download/v{{ version }}/deSALT_v{{ version }}.tar.gz

build:
    number: 0
    skip: True  # [osx]

requirements:
  build:
    - {{ compiler('c') }}
    - llvm-openmp  # [osx]
  host:
    - zlib
    - llvm-openmp  # [osx]
  run:
    - zlib
    - llvm-openmp  # [osx]

test:
  commands:
    - deSALT aln

about:
  home: https://github.com/ydLiu-HIT/
  license: MIT
  license_file: LICENSE
  summary: Third generation RNA sequence alignment
