{% set version = "1.4" %}

package:
    name: desalt
    version: {{ version }}

source:
    sha256: 8b0f20c9701229ff2edaf362dd28bc3587e7246ec12aa7a05d5f27266ad5485b
    url: https://github.com/ydLiu-HIT/deSALT/releases/download/v{{ version }}/deSALT_v{{ version }}.tar.gz

build:
    number: 0
    skip: True  # [osx]

requirements:
  build:
    - {{ compiler('c') }}
    - llvm-openmp  # [osx]
  host:
    - zlib
    - llvm-openmp  # [osx]
  run:
    - zlib
    - llvm-openmp  # [osx]

test:
  commands:
    - deSALT aln

about:
  home: https://github.com/ydLiu-HIT/
  license: MIT
  license_file: LICENSE
  summary: Third generation RNA sequence alignment
