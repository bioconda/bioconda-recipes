{% set version = "1.5.4" %}

package:
  name: desalt
  version: {{ version }}

source:
  url: https://github.com/ydLiu-HIT/deSALT/archive/v{{ version }}.tar.gz
  sha256: 654c5016b8e158fa3c49fa42b745f2a5c0289c9648f965867ffdb6fc4ff6be0e

build:
  number: 0

requirements:
  build:
    - {{ compiler('c') }}
    - llvm-openmp  # [osx]
  host:
    - zlib
    - llvm-openmp  # [osx]
  run:
    - zlib
    - llvm-openmp  # [osx]

test:
  commands:
    - deSALT aln

about:
    home: https://github.com/ydLiu-HIT/
    license: MIT
    license_file: LICENSE
    summary: De Bruijn graph-based Spliced Aligner for Long Transcriptome reads 
