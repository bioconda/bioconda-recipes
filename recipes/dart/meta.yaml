{% set version = "1.3.6" %}

package:
  name: dart
  version: {{ version }}

source:
  url: https://github.com/hsinnan75/Dart/archive/{{ version }}.tar.gz
  sha256: '1edbb888af90c0fafed468ddff23d20f3a97c2773ec0d9a8c6a465c0232eb5ac'

build:
  number: 0

requirements:
  build:
    - {{ compiler('cxx') }}
    - {{ compiler('c') }}
  host:
    - zlib
    - bzip2
    - xz
    - boost-cpp
  run:
    - zlib
    - bzip2
    - xz
    - boost-cpp

test:
  commands:
    - dart --version | tee /dev/stderr | grep -qF '{{ version }}'
    - bwt_index

about:
  home: https://github.com/hsinnan75/Dart
  license: MIT
  license_file: LICENSE
  summary: "Dart: a fast and accurate RNA-seq mapper"
  description: "An efficient short read mapper for RNA-Seq data."

extra:
  identifiers:
    - doi:10.1093/bioinformatics/btx558
