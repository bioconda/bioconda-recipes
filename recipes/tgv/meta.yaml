{% set name = "tgv" %}
{% set version = "0.2.0" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://github.com/zeqianli/{{ name }}/archive/refs/tags/v{{ version }}.tar.gz
  sha256: dab6a21dd334db5328653a04b5139c65835533dbe401ad68994123ae05efd0e8

build:
  number: 0
  run_exports:
    - {{ pin_subpackage(name, max_pin="x.x") }}

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - {{ compiler('rust') }}
    - cargo-bundle-licenses
    - make
    - pkg-config
    - clangdev
  host:
    - htslib
    - sqlite
    - openssl
    - curl
    - zlib

test:
  commands:
    - tgv --version

about:
  home: "https://github.com/zeqianli/tgv"
  license: MIT
  license_family: MIT
  summary: "Explore genomes in the terminal. Light, blazing fast 🚀, vim-motion."
  dev_url: "https://github.com/zeqianli/tgv"
  doc_url: "https://github.com/zeqianli/tgv/blob/v{{ version }}/README.md"

extra:
  additional-platforms:
    - linux-aarch64
    - osx-arm64
