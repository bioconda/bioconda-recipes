{% set name = "tgv" %}
{% set version = "0.3.0" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://github.com/zeqianli/{{ name }}/archive/refs/tags/v{{ version }}.tar.gz
  sha256: 1c198b0ab69ccd17e00ae5a296ea24b31beef41ddba3e66debe0eb056f235990

build:
  number: 0
  run_exports:
    - {{ pin_subpackage(name, max_pin="x.x") }}

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - {{ compiler('rust') }}
    - {{ stdlib("c") }}
    - cargo-bundle-licenses
    - make
    - pkg-config
    - clangdev
  host:
    - htslib
    - sqlite
    - openssl
    - curl
    - zlib

test:
  commands:
    - tgv --version

about:
  home: "https://github.com/zeqianli/tgv"
  license: MIT
  license_family: MIT
  summary: "Explore genomes in the terminal. Light, blazing fast 🚀, vim-motion."
  dev_url: "https://github.com/zeqianli/tgv"
  doc_url: "https://github.com/zeqianli/tgv/blob/v{{ version }}/README.md"

extra:
  additional-platforms:
    - linux-aarch64
    - osx-arm64
