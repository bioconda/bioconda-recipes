package:
  name: pcst_fast
  version: "1.0.0"

source:
  fn: pcst_fast-1.0.0.tar.gz
  url: https://pypi.python.org/packages/3b/01/58f24ba25de13fe02c5b92c0b441f6db216487cdc8eda1c51e546bde3cf9/pcst_fast-1.0.0.tar.gz
  md5: 13360d3e5ddc336844b89085b36ebc83
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pcst_fast = pcst_fast:main
    #
    # Would create an entry point called pcst_fast that calls pcst_fast.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pybind11 >=1.7

  run:
    - python
    - pybind11 >=1.7

test:
  # Python imports
  imports:
    - pcst_fast

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fraenkel-lab/pcst_fast
  license: MIT
  summary: 'UNKNOWN'
  license_family: MIT

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
