{% set version = "1" %}

package:
  name: minirmd
  version: {{ version }}

build:
  number: 0

source:
  url: https://github.com/yuansliu/minirmd/archive/v1.tar.gz
  sha256: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855

requirements:
  build:
    - {{ compiler('cxx') }}
  host:
    - zlib
  run:
    - zlib

test:
  commands:
    - minirmd -h

about:
  home: https://github.com/yuansliu/minirmd
  license:  GPL-3.0
  license_file: LICENSE
  summary: Remove duplicate and near-duplicate reads
