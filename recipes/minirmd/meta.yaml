{% set version = "1" %}

package:
  name: minirmd
  version: {{ version }}

build:
  number: 0

source:
  url: https://github.com/yuansliu/minirmd/archive/v1.tar.gz
  sha256: bb90edb36ffa89fdb42228a9cd8f5d4f327070c725e4c4c9c83c40782de7c15f

requirements:
  build:
    - {{ compiler('cxx') }}
  host:
    - zlib
  run:
    - zlib

test:
  commands:
    - minirmd -h

about:
  home: https://github.com/yuansliu/minirmd
  license:  GPL-3.0
  license_file: LICENSE
  summary: Remove duplicate and near-duplicate reads

