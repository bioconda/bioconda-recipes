package:
  name: colorlog
  version: "2.10.0"

source:
  fn: colorlog-2.10.0.tar.gz
  url: https://pypi.python.org/packages/33/3e/1e9affd7ee817370f04f33f675ed4b3e91b01ad569cbafa31b0dfda869c5/colorlog-2.10.0.tar.gz
  md5: fffcb1520917874cc520267368bf6fab
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - colorlog = colorlog:main
    #
    # Would create an entry point called colorlog that calls colorlog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - colorlog

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/borntyping/python-colorlog
  license: MIT License
  summary: 'Log formatting with colors!'
  license_family: MIT

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
