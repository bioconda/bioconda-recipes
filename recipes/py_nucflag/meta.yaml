{% set name = "py_nucflag" %}
{% set version = "0.1.9" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 00474eb1721c5242f494a142db27cac52e36df118e9b913397d35df4f22a6490

build:
  script:
    - {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
    - cargo-bundle-licenses --format yaml --output THIRDPARTY.yml
  number: 0
  skip: True  # [osx]
  run_exports:
    - {{ pin_subpackage(name, max_pin='x.x') }}

requirements:
  build:
    - {{ compiler("rust") }}
    - cargo-bundle-licenses
  host:
    - python
    - maturin >=1.8,<2.0
    - pip
  run:
    - python
    - polars

test:
  imports:
    - py_nucflag
  commands:
    - pip check
  requires:
    - pip

about:
  license: MIT
  license_file: py/LICENSE
  summary: Library to call misassemblies in genome assemblies from long-read alignments.
  home: https://github.com/logsdon-lab/rs-nucflag

extra:
  recipe-maintainers:
    - koisland
