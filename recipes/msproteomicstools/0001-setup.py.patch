--- setup.py
+++ setup.py
@@ -89,7 +89,7 @@
       install_requires=[
           "numpy",
           "scipy",
-          "cluster == 1.2.2", # note that 1.1.2 does not work with py3
+          "cluster", # note that 1.1.2 does not work with py3
           "pyteomics >= 2.4.0",
           "statsmodels >= 0.6.0",
           "xlsxwriter >= 0.5.3 ", # for xlsx
