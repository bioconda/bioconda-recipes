{% set name = "inquistr" %}
{% set version = "0.27.0" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://github.com/wdecoster/inquiSTR/archive/refs/tags/v{{ version }}.tar.gz
  sha256: 7f38d32d9cb92f8c1c163ffb67f1c3e1c94cf87ffa377803d31edf8602dc5fa8

build:
  number: 0
  run_exports:
    - {{ pin_subpackage('inquistr', max_pin="x.x") }}

requirements:
  build:
    - {{ compiler('cxx') }}
    - {{ compiler('rust') }}
    - clangdev
    - make
    - cmake
    - autoconf
    - automake
    - libtool
    - pkg-config
    - cargo-bundle-licenses
  host:
    - zlib

test:
  commands:
    - inquiSTR --help

about:
  home: "https://github.com/wdecoster/inquiSTR"
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: "Tandem repeat toolkit for long reads."
  dev_url: "https://github.com/wdecoster/inquiSTR"
  doc_url: "https://github.com/wdecoster/inquiSTR/blob/main/README.md"

extra:
  additional-platforms:
    - osx-arm64
    - linux-aarch64
  identifiers:
    - doi:10.1101/gr.279265.124 # to be updated
