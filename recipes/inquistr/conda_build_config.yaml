# inquiSTR uses edition 2024 (and several dependencies require it too),
# which needs cargo >= 1.85. The build environment defaults to 1.77.2,
# so pin a newer rust compiler matching the crate's rust-version.
rust_compiler_version:
  - 1.88.0
