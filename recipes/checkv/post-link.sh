#!/usr/bin/env bash

echo "
     CheckV v${PKG_VERSION} an external database which needs to be downloaded
     and unarchived. The link for the database can be found in CheckV's
     instructions at https://bitbucket.org/berkeleylab/checkv/

" > "${PREFIX}"/.messages.txt

