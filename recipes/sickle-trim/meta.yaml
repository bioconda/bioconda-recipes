package:
  name: sickle-trim
  version: '1.33'

source:
  fn: sickle-1.33.tar.gz
  url: https://github.com/najoshi/sickle/archive/v1.33.tar.gz

build:
  number: 0
  skip: True # [osx]

requirements:
  build:
  run:

test:
  commands:
    - sickle --version

about:
  home: https://github.com/najoshi/sickle
  license: MIT
  summary: Windowed Adaptive Trimming for fastq files using quality
