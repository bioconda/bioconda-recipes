{% set name = "pyensembl" %}
{% set version = "2.10.15" %}
{% set sha256 = "bd362af60343b15dfa7359edf9e77219e4a93ba25a67eda6374de01a7e80d644" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0]|lower }}/{{ name|lower }}/{{ name|lower }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . -vvv --no-deps --no-build-isolation --no-cache-dir
  entry_points:
    - pyensembl = pyensembl.shell:run
  run_exports:
    - {{ pin_subpackage(name, max_pin="x") }}

requirements:
  host:
    - python >=3.9
    - pip
    - setuptools
  run:
    - python >=3.9
    - typechecks >=0.0.2
    - numpy >=2.0.0
    - pandas >=0.15
    - datacache >=1.4.0,<2.0
    - memoized-property >=1.0.2
    - six >=1.9.0
    - gtfparse >=2.5.0
    - serializable
    - tinytimer
    - pylint >=1.4.4
    - python-dateutil >=2.5.0

test:
  imports:
    - pyensembl
  commands:
    - pyensembl --help

about:
  home: "https://github.com/openvax/pyensembl"
  license: "Apache-2.0"
  summary: "Python interface to ensembl reference genome metadata."
  license_family: APACHE
  license_file: LICENSE
  dev_url: "https://github.com/openvax/pyensembl"
