{% set name = "r-xmsannotator" %}
{% set version = "1.3.2+recetox0" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  git_url: https://github.com/RECETOX/xMSannotator.git
  git_rev: packaging
  sha256: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855

build:
  number: 0
  rpaths:
    - lib/
    - lib/R/lib/


requirements:
  build:
    - r-base >=4
    - r-dplyr
    - r-flashclust
    - r-wgcna
    - r-tidyr
    - r-purrr
    - r-tibble

  run:
    - r-base >=4
    - r-dplyr >=1
    - r-flashclust
    - r-wgcna
    - r-tidyr
    - r-purrr
    - r-tibble
    - r-data.table
    - bioconductor-rhdf5
    - r-rcpp
    - r-arrow

  test:
    commands:
      - $R -e "library('xmsannotator')"

about:
  home: https://github.com/RECETOX/xMSannotator
  summary: Annotate peak intensity table with compounds from the compounds database.

extra:
  maintainers:
   - maximskorik
   - xtracko
