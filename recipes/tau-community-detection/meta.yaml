{% set name = "tau-community-detection" %}
{% set version = "1.4.7" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/t/{{ name }}/{{ name.replace('-', '_') }}-{{ version }}.tar.gz
  sha256: f4089928b7303f8b1baf0003309531595ea45f6ed01bd00c92d444f238cfc4ac

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  run_exports:
    - {{ pin_subpackage(name, max_pin="x") }}

requirements:
  host:
    - python >=3.10
    - pip
    - setuptools
  run:
    - python >=3.10
    - networkx >=3.4
    - numpy >=2.0
    - python-igraph >=0.10
    - scikit-learn >=1.6

test:
  imports:
    - tau_community_detection
  commands:
    - tau --help
    - pip check
  requires:
    - pip

about:
  home: https://pypi.org/project/tau-community-detection/
  summary: Community detection via Louvain/Leiden + Genetic Algorithm
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - HillelCharbit
