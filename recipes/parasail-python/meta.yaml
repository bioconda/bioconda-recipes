{% set name = "parasail-python" %}
{% set version = "1.1.19" %}
{% set sha256 = "05911234db57e36118f83b4ba88f8d844290bba0ca13d5e44510d59cabc35885" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: https://github.com/jeffdaily/parasail-python/archive/v{{ version }}.tar.gz
  sha256: "{{ sha256 }}"
  patches:
    - darwin-libtool-name.patch

build:
  number: 0

requirements:
  host:
    - python
    - setuptools
    - zlib
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - m4
    - autoconf
    - automake
    - libtool
  run:
    - python
    - numpy

test:
  imports:
    - parasail

about:
  home: https://github.com/jeffdaily/parasail-python
  license: BSD
  license_family: BSD
  summary: pairwise sequence alignment library

