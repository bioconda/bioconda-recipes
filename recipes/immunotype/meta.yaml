{% set python_min = "3.10" %}
{% set version = "1.0.3" %}

package:
  name: immunotype
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/i/immunotype/immunotype-{{ version }}.tar.gz
  sha256: 983725bd391a4818013e04b1b560a1744a15cfbd96f5219ac9839c3ef7c142a0

build:
  entry_points:
    - immunotype = immunotype.cli:main
    - immunotype-app = immunotype.app:main
  noarch: python
  script: {{ PYTHON }} -m pip install . -vvv --no-deps --no-build-isolation --no-cache-dir
  number: 0
  run_exports:
    - {{ pin_subpackage('immunotype', max_pin="x") }}

requirements:
  host:
    - python {{ python_min }}.*
    - hatchling
    - pip
  run:
    - python >={{ python_min }}
    - pandas >=1.5.0,<3
    - numpy >=1.21.0
    - pytorch >=2.0.0
    - pytorch_geometric >=2.3.0
    - tqdm >=4.64.0
    - rich-click >=1.6.0
    - pandera >=0.26.0
    - gradio >=6.0.0
    - matplotlib-base >=3.5.0

test:
  imports:
    - immunotype
  commands:
    - pip check
    - immunotype --help
  requires:
    - pip
    - python {{ python_min }}.*

about:
  home: https://github.com/AG-Walz/immunotype
  summary: Peptide-based HLA typing from immunopeptidomics data
  license: MIT
  license_family: MIT
  license_file: LICENSE
  dev_url: https://github.com/AG-Walz/immunotype

extra:
  recipe-maintainers:
    - jonasscheid
