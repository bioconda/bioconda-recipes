{% set name = "metaquantome" %}
{% set version = "2.0.3" %}
{% set file_ext = "tar.gz" %}
{% set hash_type = "sha256" %}
{% set hash_value = "c2c27a89b31079efb21b854384ecd9b173f673fc1a9e581c33ed5af7a5dc66ed" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://github.com/galaxyproteomics/{{ name }}/archive/v{{ version }}.{{ file_ext }}
  '{{ hash_type }}': {{ hash_value }}

build:
  number: 0
  noarch: python
  entry_points:
    - metaquantome = metaquantome.cli:cli
  script: {{ PYTHON }} -m pip install . -vvv --no-deps --no-build-isolation --no-deps
  run_exports:
    - {{ pin_subpackage(name, max_pin="x") }}

requirements:
  host:
    - python >=3.5
    - pip
    - setuptools
  run:
    - python >=3.5
    - setuptools <82
    - ete3
    - goatools
    - statsmodels
    - biopython
    - pandas
    - numpy
    - r-gplots
    - r-ggplot2
    - r-base
    - r-jsonlite
    - r-scico
    - r-dplyr
    - r-tidyr
    - libiconv

test:
  import:
    - metaquantome
  commands:
    - metaquantome -h
    - metaquantome expand -h
    - metaquantome filter -h
    - metaquantome stat -h
    - metaquantome viz -h

about:
  home: "https://github.com/galaxyproteomics/metaquantome"
  license: "Apache-2.0"
  license_family: APACHE
  license_file: LICENSE
  summary: "Quantitative metaproteomics analysis of taxonomy and function."
  dev_url: "https://github.com/galaxyproteomics/metaquantome"
  doc_url: "https://galaxyproteomics.github.io/metaquantome_mcp_analysis/cli_tutorial/cli_tutorial.html"

extra:
  container:
    # extended-base generates en_US.UTF-8 locale and sets LC_ALL, LANG properly
    extended-base: True
