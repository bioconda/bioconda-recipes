{% set name = "haploview" %}
{% set version = "4.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://www.broadinstitute.org/ftp/pub/mpg/haploview/Haploview.jar

requirements:
  host:
  run:
    - openjdk >=6
    - python

test:
  command:
    - "java -jar Haploview.jar  -nogui -version"

about:
  license: MIT