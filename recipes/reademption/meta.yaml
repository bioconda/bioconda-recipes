package:
  name: reademption
  version: "0.4.3"

source:
  fn: READemption-0.4.3.tar.gz
  url: https://pypi.python.org/packages/50/c5/36e3b1654e65352ec95028b185abf54b0532251256f1f1c17663cc7c2ccb/READemption-0.4.3.tar.gz
  md5: 1ad830836d150d9e8f37f735523951d9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - reademption = reademption:main
    #
    # Would create an entry point called reademption that calls reademption.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - biopython >=1.67
    - matplotlib >=1.5.1
    - pandas >=0.18.1
    - pysam >=0.9.1.4

  run:
    - python
    - biopython >=1.67
    - matplotlib >=1.5.1
    - pandas >=0.18.1
    - pysam >=0.9.1.4

test:
  # Python imports
  imports:
    - reademptionlib
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: ISC License (ISCL)
  summary: 'A RNA-Seq Analysis Pipeline'
  license_family: OTHER

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
