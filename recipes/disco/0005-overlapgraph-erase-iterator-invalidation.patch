--- a/src/SimplifyGraph/src/OverlapGraph.cpp
+++ b/src/SimplifyGraph/src/OverlapGraph.cpp
@@ -347,7 +347,7 @@
 		if(it->second->empty())
 		{
 			delete it->second;
-			m_graph->erase(it);
+			it = m_graph->erase(it);
 		}
 		else
 			it++;