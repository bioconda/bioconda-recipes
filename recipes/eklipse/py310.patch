--- a/eKLIPse_circos.py
+++ b/eKLIPse_circos.py
@@ -1,7 +1,6 @@
 import sys
 import os
 import shutil
-from string import split
 from eKLIPse_fct import *
 
 
@@ -92,8 +91,8 @@ def circosConf(titleBam,dicoInit,lstError):
         CIRCOS2 = open(pathSCblastCircos2,'w')
         CIRCOS3 = open(pathSCblastCircos3,'w')        
         for key in list(dicoDel.keys()):
-            start = int(split(key,"-")[0])
-            end = int(split(key,"-")[1])
+            start = int(key.split("-")[0])
+            end = int(key.split("-")[1])
             meanFreq = (dicoDel[key]['freqF']+dicoDel[key]['freqR'])/2.0
             if meanFreq >= 10:
                 CIRCOS.write("hsM\t"+str(dicoDel[key]['scrR']['limit'])+"\t"+str(start)+"\thsM\t"+str(end)+"\t"+str(dicoDel[key]['scrF']['limit'])+"\n")
@@ -124,7 +123,7 @@ def circosConf(titleBam,dicoInit,lstError):
 
         #***** CREATE Main Circos Configuration File *****#
         IN = open(pathConfTemplate,'r')
-        lst_lines = split(IN.read(),"\n")
+        lst_lines = IN.read().split("\n")
         IN.close()
         OUT = open(pathConf,'w')
         for line in lst_lines:
--- a/eKLIPse_fct.py
+++ b/eKLIPse_fct.py
@@ -2,7 +2,6 @@ import sys
 import os
 import glob
 import json
-from string import split
 
 
 #=================================================================================================================================================#
@@ -43,9 +42,9 @@ def string_to_num(num):
 def load_json(path):
     # Read json file
     json_data=open(path)
-    data = json.load(json_data)
+    data = json.load(json_data, object_pairs_hook=lambda pairs: {int(k) if k.lstrip('-').isdigit() else k: v for k, v in pairs})
     json_data.close()
-    return byteify(data)
+    return data
 
 def byteify(input):
     if isinstance(input, dict):
@@ -110,7 +109,7 @@ def mean_coverage(dicoInit,lstError):
         # Write Mean coverage file
         MEAN = open(pathMeanCov,'w')
         for pos in range(dicoInit["dicoGbk"]['refLength']-1):
-            if str(pos) in dicoMean: ToWrite = "hsM\t"+str(pos)+"\t"+str(pos+1)+"\t"+str(float(dicoMean[str(pos)])/float(nb_sample))+"\n"
+            if pos in dicoMean: ToWrite = "hsM\t"+str(pos)+"\t"+str(pos+1)+"\t"+str(float(dicoMean[pos])/float(nb_sample))+"\n"
             else: ToWrite = "hsM\t"+str(pos)+"\t"+str(pos+1)+"\t0.0\n"
             MEAN.write(ToWrite)
         MEAN.close()
--- a/eKLIPse_init.py
+++ b/eKLIPse_init.py
@@ -68,12 +68,12 @@ def arg_manager(argv,pathRootDir,boolColor,boolQT,spinner):
             if not os.path.isfile(argv[i+1]): lstErrorDisplay.append("Input file \""+argv[i+1]+"\" not found")
             else:
                 dicoInit['pathInput'] = argv[i+1]
-                IN = open(argv[i+1],'r') ; lstLine = split(IN.read(),"\n") ; IN.close()
+                IN = open(argv[i+1],'r') ; lstLine = IN.read().split("\n") ; IN.close()
                 for j in range(len(lstLine)):
                     if lstLine[j]!="" and not lstLine[j][0]=="#":
                         try:
-                            pathBam = split(lstLine[j],"\t")[0]
-                            titleBam = split(lstLine[j],"\t")[1]
+                            pathBam = lstLine[j].split("\t")[0]
+                            titleBam = lstLine[j].split("\t")[1]
                             if not os.path.isfile(pathBam): lstErrorDisplay.append("Input file at line"+str(j+1)+": \""+pathBam+"\" not found")
                             if titleBam in dicoInit['lstTitleBam']: lstErrorDisplay.append("Input file at line "+str(j+1)+": \""+titleBam+"\" name already used")
                             dicoInit['lstTitleBam'].append(titleBam)
@@ -95,7 +95,7 @@ def arg_manager(argv,pathRootDir,boolColor,boolQT,spinner):
                             if feature.qualifiers['nomenclature'][0].lower().__contains__("trna"): geneType = "trna"
                             elif feature.qualifiers['nomenclature'][0].lower().__contains__("rna"): geneType = "rrna"
                             else: geneType = "protein"
-                            try: name = split(split(feature.qualifiers['nomenclature'][0],"|")[0],":")[1].replace(" ","")
+                            try: name = feature.qualifiers['nomenclature'][0].split("|")[0].split(":")[1].replace(" ","")
                             except: name = feature.qualifiers['gene'][0]
                             dicoInit["dicoGbk"]['lstGene'].append([name,int(feature.location.start)+1,int(feature.location.end),geneType])
             if len(dicoInit["dicoGbk"])==0: lstErrorDisplay.append("Cannot Read Reference GBK file \""+argv[i+1]+"\"")
@@ -115,7 +115,7 @@ def arg_manager(argv,pathRootDir,boolColor,boolQT,spinner):
                         if feature.qualifiers['nomenclature'][0].lower().__contains__("trna"): geneType = "trna"
                         elif feature.qualifiers['nomenclature'][0].lower().__contains__("rna"): geneType = "rrna"
                         else: geneType = "protein"
-                        try: name = split(split(feature.qualifiers['nomenclature'][0],"|")[0],":")[1].replace(" ","")
+                        try: name = feature.qualifiers['nomenclature'][0].split("|")[0].split(":")[1].replace(" ","")
                         except: name = feature.qualifiers['gene'][0]
                         dicoInit["dicoGbk"]['lstGene'].append([name,int(feature.location.start)+1,int(feature.location.end),geneType])
         # Results Directory
@@ -231,9 +231,9 @@ def arg_manager(argv,pathRootDir,boolColor,boolQT,spinner):
             if dicoInit['windows']==False: cmd_idxstats = dicoInit['pathSamtools']+" idxstats "+dicoInit['dicoBam'][titleBam]['path']+" 1>"+path_idxstats+" 2>/dev/null"
             else: cmd_idxstats = dicoInit['pathSamtools']+" idxstats "+dicoInit['dicoBam'][titleBam]['path']+" 1>"+path_idxstats+" 2> nul"
             os.system(cmd_idxstats)
-            IN = open(path_idxstats,'r') ; lst_lines = split(IN.read(),"\n") ; IN.close()
+            IN = open(path_idxstats,'r') ; lst_lines = IN.read().split("\n") ; IN.close()
             for line in lst_lines:
-                split_line = split(line,"\t")
+                split_line = line.split("\t")
                 if len(split_line)>1:
                     refName = split_line[0]
                     refSize = int(split_line[1])
--- a/eKLIPse_sc.py
+++ b/eKLIPse_sc.py
@@ -2,7 +2,6 @@ import json
 import os
 import sys
 import pybam
-from string import split
 from eKLIPse_fct import *
 
 
@@ -64,7 +63,7 @@ def Read_alignment(titleBam,dicoInit,lstError):
         # CLOSE files
         FASTA.close()
         # WRITE .json results
-        JSON = open(pathSCjson,'wb')
+        JSON = open(pathSCjson,'w')
         JSON.write(json.dumps(dicoBam))
         JSON.close()
     except:
@@ -91,13 +90,13 @@ def SC_blast(titleBam,dicoInit,lstError):
             os.system(cmd)
             #***** Read Blast output file *****#
             IN = open(pathSCDELBLAST,'r')
-            lst_lines = split(IN.read(),"\n")
+            lst_lines = IN.read().split("\n")
             IN.close()
             for line in lst_lines:
                 if line!="":
                     # Blast line parser
-                    splitLine = split(line,",")
-                    splitQread = split(splitLine[0],"_")
+                    splitLine = line.split(",")
+                    splitQread = splitLine[0].split("_")
                     SCposRead = int(splitQread[0])
                     SCmappedpart = int(splitQread[1])
                     readOrient = splitQread[2]
@@ -134,7 +133,7 @@ def SC_blast(titleBam,dicoInit,lstError):
                                 else: dicoDel[delName] = { 'scrF':{'nbBlast':0,'limit':0,'initial_SCposRead':[]}, 'scrR':{'nbBlast':1,'limit':sstart,'initial_SCposRead':[SCposRead]}, 'freqF':0.0, 'freqR':0.0 }
    
         #***** WRITE .json results file *****#
-        JSON = open(pathBLASTjson,'wb')
+        JSON = open(pathBLASTjson,'w')
         JSON.write(json.JSONEncoder().encode(dicoDel))
         JSON.close()
     except: 
@@ -159,14 +158,14 @@ def deletionPrediction(titleBam,dicoInit,lstError):
         # Block one position and apply shift
         dico_shift_del = {}
         for delName in list(dicoDel.keys()):
-            start = int(split(delName,"-")[0])
-            end = int(split(delName,"-")[1])
+            start = int(delName.split("-")[0])
+            end = int(delName.split("-")[1])
             dico_index = len(dico_shift_del)
             dico_shift_del[dico_index] = [delName]
             for delName2 in list(dicoDel.keys()):
                 if delName!=delName2:
-                    start2 = int(split(delName2,"-")[0])
-                    end2 = int(split(delName2,"-")[1])
+                    start2 = int(delName2.split("-")[0])
+                    end2 = int(delName2.split("-")[1])
                     # Same deletion start => shift end
                     if (start==start2 and (abs(end-end2)<=dicoInit["delShift"])) or (end==end2 and (abs(start-start2)<=dicoInit["delShift"])):
                         dico_shift_del[dico_index].append(delName2)
@@ -188,8 +187,8 @@ def deletionPrediction(titleBam,dicoInit,lstError):
                 initial_SCposRead_F = [] ; initial_SCposRead_R = []
                 # Browse deletion
                 for delName in dico_shift_del[key]:
-                    start = split(delName,"-")[0]
-                    end = split(delName,"-")[1]
+                    start = delName.split("-")[0]
+                    end = delName.split("-")[1]
                     try: delnbblastF = dicoDel[delName]['scrF']['nbBlast']
                     except: delnbblastF = 0
                     try: delnbblastR = dicoDel[delName]['scrR']['nbBlast']
@@ -214,8 +213,8 @@ def deletionPrediction(titleBam,dicoInit,lstError):
                     # Del entry
                     try : del(dicoDel[delName])
                     except: pass
-                if maxBlast_start=="": maxBlast_start = split(delName,"-")[0]
-                if maxBlast_end=="": maxBlast_end = split(delName,"-")[1]
+                if maxBlast_start=="": maxBlast_start = delName.split("-")[0]
+                if maxBlast_end=="": maxBlast_end = delName.split("-")[1]
                 dicoDel[maxBlast_start+"-"+maxBlast_end] = { 'scrF':{'nbBlast':nbBlast_F,'limit':limit_max_start,'initial_SCposRead':initial_SCposRead_F},\
                                                              'scrR':{'nbBlast':nbBlast_R,'limit':limit_max_end,'initial_SCposRead':initial_SCposRead_R},\
                                                              'freqF':0.0, 'freqR':0.0 }
@@ -227,8 +226,8 @@ def deletionPrediction(titleBam,dicoInit,lstError):
             elif dicoInit["bilateral"]==False and dicoDel[delName]['scrF']['nbBlast']<dicoInit["minblast"] and dicoDel[delName]['scrR']['nbBlast']<dicoInit["minblast"]: del(dicoDel[delName])
             # Compute frequency
             else:
-                start = int(split(delName,"-")[0])
-                end = int(split(delName,"-")[1])
+                start = int(delName.split("-")[0])
+                end = int(delName.split("-")[1])
                 nb_blast_F = float(dicoDel[delName]['scrF']['nbBlast'])
                 nb_blast_R = float(dicoDel[delName]['scrR']['nbBlast'])
                 nb_sc_fasta_F = 0 ; set_sc_fasta_pos_F = set()
@@ -239,18 +238,18 @@ def deletionPrediction(titleBam,dicoInit,lstError):
                 lst_nb_sc_reads_F = []
                 for init_pos in set(dicoDel[delName]['scrF']['initial_SCposRead']):
                     for i in range(max(1,init_pos-dicoInit["delShift"]),min(init_pos+dicoInit["delShift"],dicoInit["dicoGbk"]['refLength'])+1,1):
-                        lst_nb_sc_reads_F.append(dicoBam[str(i)]['nb_sc_reads_F'])
-                        if not i in set_sc_fasta_pos_F : nb_sc_fasta_F+=dicoBam[str(i)]['nb_sc_fasta_F'] ; set_sc_fasta_pos_F.add(i)
+                        lst_nb_sc_reads_F.append(dicoBam[i]['nb_sc_reads_F'])
+                        if not i in set_sc_fasta_pos_F : nb_sc_fasta_F+=dicoBam[i]['nb_sc_fasta_F'] ; set_sc_fasta_pos_F.add(i)
                 lst_nb_sc_reads_R = []
                 for init_pos in set(dicoDel[delName]['scrR']['initial_SCposRead']):
                     for i in range(max(1,init_pos-dicoInit["delShift"]),min(init_pos+dicoInit["delShift"],dicoInit["dicoGbk"]['refLength'])+1,1):
-                        lst_nb_sc_reads_R.append(dicoBam[str(i)]['nb_sc_reads_R'])
-                        if not i in set_sc_fasta_pos_R : nb_sc_fasta_R+=dicoBam[str(i)]['nb_sc_fasta_R'] ; set_sc_fasta_pos_R.add(i)
+                        lst_nb_sc_reads_R.append(dicoBam[i]['nb_sc_reads_R'])
+                        if not i in set_sc_fasta_pos_R : nb_sc_fasta_R+=dicoBam[i]['nb_sc_fasta_R'] ; set_sc_fasta_pos_R.add(i)
                 # Max occurence start/end
                 max_occ_start = max(dicoDel[delName]['scrF']['initial_SCposRead'],key=dicoDel[delName]['scrF']['initial_SCposRead'].count)
                 max_occ_end = max(dicoDel[delName]['scrR']['initial_SCposRead'],key=dicoDel[delName]['scrR']['initial_SCposRead'].count)
-                nb_reads_F = float(dicoBam[str(max_occ_start)]['nb_reads_F'])
-                nb_reads_R = float(dicoBam[str(max_occ_end)]['nb_reads_R'])
+                nb_reads_F = float(dicoBam[max_occ_start]['nb_reads_F'])
+                nb_reads_R = float(dicoBam[max_occ_end]['nb_reads_R'])
                 # Max number of sc reads
                 if len(lst_nb_sc_reads_F)==0: nb_sc_reads_F = 0
                 else: nb_sc_reads_F = float(max(lst_nb_sc_reads_F))
@@ -267,8 +266,8 @@ def deletionPrediction(titleBam,dicoInit,lstError):
         #***** CUMULATIVE Frequency *****#
         dicoCumulFreq = {}
         for delPos in list(dicoDel.keys()):
-            start = int(split(delPos,"-")[0])
-            end = int(split(delPos,"-")[1])
+            start = int(delPos.split("-")[0])
+            end = int(delPos.split("-")[1])
             if start<end: posRange = list(range(start,end+1,1))
             else: posRange = list(range(start,dicoInit["dicoGbk"]['refLength']+1,1)) ; posRange.extend(list(range(1,end+1,1)))
             for pos in posRange:
@@ -276,10 +275,10 @@ def deletionPrediction(titleBam,dicoInit,lstError):
                 else: dicoCumulFreq[pos] = (dicoDel[delPos]['freqF']+dicoDel[delPos]['freqR'])/200.0
 
         #***** WRITE .json results files *****#
-        JSON = open(pathBLASTjson,'wb')
+        JSON = open(pathBLASTjson,'w')
         JSON.write(json.JSONEncoder().encode(dicoDel))
         JSON.close()
-        JSON = open(pathCumuljson,'wb')
+        JSON = open(pathCumuljson,'w')
         JSON.write(json.JSONEncoder().encode(dicoCumulFreq))
         JSON.close()
 
@@ -315,8 +314,8 @@ def create_results_table(dicoInit,lstError):
         # Sort deletion by deletion start and stop
         dicoSortDel = {}
         for deletion in list(dicoDel.keys()):
-            start = int(split(deletion,"-")[0])
-            end = int(split(deletion,"-")[1])
+            start = int(deletion.split("-")[0])
+            end = int(deletion.split("-")[1])
             if start in dicoSortDel: dicoSortDel[start][end] = deletion
             else: dicoSortDel[start] = { end:deletion }
         lst_start = list(dicoSortDel.keys()) ; lst_start.sort()
--- a/pybam.py
+++ b/pybam.py
@@ -100,7 +100,7 @@ Github:     http://github.com/JohnLonginotto/pybam
 [ Force Internal bgzip Decompressor ]
   my_bam = pybam.read('/my/data.bam',decompressor='internal')
 [ Parse Words (hah) ]'''
-wat += '\n'+''.join([('\n===============================================================================================\n\n  ' if code is 'file_alignments_read' or code is 'sam' else '  ')+(code+' ').ljust(25,'-')+description+'\n' for code,description in sorted(parse_codes.items())]) + '\n'
+wat += '\n'+''.join([('\n===============================================================================================\n\n  ' if code == 'file_alignments_read' or code == 'sam' else '  ')+(code+' ').ljust(25,'-')+description+'\n' for code,description in sorted(parse_codes.items())]) + '\n'
 
 class read:
     '''
@@ -135,7 +135,7 @@ class read:
         self.file_chromosome_lengths = {}
 
         if fields is not False:
-            if type(fields) is not list or len(fields) is 0:
+            if type(fields) is not list or len(fields) == 0:
                 raise PybamError('\n\nFields for the static parser must be provided as a non-empty list. You gave a ' + str(type(fields)) + '\n')
             else:
                 for field in fields:
@@ -147,7 +147,7 @@ class read:
 
         if decompressor:
             if type(decompressor) is str:
-                 if decompressor is not 'internal' and '{}' not in decompressor: raise PybamError('\n\nWhen a custom decompressor is used and the input file is a string, the decompressor string must contain at least one occurence of "{}" to be substituted with a filepath by pybam.\n')
+                 if decompressor != 'internal' and '{}' not in decompressor: raise PybamError('\n\nWhen a custom decompressor is used and the input file is a string, the decompressor string must contain at least one occurence of "{}" to be substituted with a filepath by pybam.\n')
             else: raise PybamError('\n\nUser-supplied decompressor must be a string that when run on the command line decompresses a named file (or stdin), to stdout:\ne.g. "lzma --decompress --stdout {}" if pybam is provided a path as input file, where {} is substituted for that path.\nor just "lzma --decompress --stdout" if pybam is provided a file object instead of a file path, as data from that file object will be piped via stdin to the decompression program.\n')
 
         ## First we make a generator that will return chunks of uncompressed data, regardless of how we choose to decompress:
@@ -169,7 +169,7 @@ class read:
             self.file_name = os.path.basename(os.path.realpath(self._file.name))
             self.file_directory = os.path.dirname(os.path.realpath(self._file.name))
 
-            if magic == 'BAM\1':
+            if magic == b'BAM\1':
                 # The user has passed us already unzipped BAM data! Job done :)
                 data = 'BAM\1' + self._file.read(35536)
                 self.file_bytes_read += len(data)
@@ -180,11 +180,11 @@ class read:
                     self.file_bytes_read += len(data)
                 self._file.close()
                 DEVNULL.close()
-                raise StopIteration
+                return
 
-            elif magic == "\x1f\x8b\x08\x04":  # The user has passed us compressed gzip/bgzip data, which is typical for a BAM file
+            elif magic == b"\x1f\x8b\x08\x04":  # The user has passed us compressed gzip/bgzip data, which is typical for a BAM file
                 # use custom decompressor if provided:
-                if decompressor is not False and decompressor is not 'internal':
+                if decompressor is not False and decompressor != 'internal':
                     if type(f) is str: self._subprocess = subprocess.Popen(                                    decompressor.replace('{}',f),    shell=True, stdout=subprocess.PIPE, stderr=DEVNULL)
                     else:              self._subprocess = subprocess.Popen('{ printf "'+magic+'"; cat; } | ' + decompressor, stdin=self._file, shell=True, stdout=subprocess.PIPE, stderr=DEVNULL)
                     self.file_decompressor = decompressor
@@ -196,7 +196,7 @@ class read:
                         self.file_bytes_read += len(data)
                     self._file.close()
                     DEVNULL.close()
-                    raise StopIteration
+                    return
 
                 # else look for pigz or gzip:
                 else:
@@ -211,7 +211,7 @@ class read:
                             use = 'gzip'
                         except OSError: use = 'internal'
 
-                    if use is not 'internal' and decompressor is not 'internal':
+                    if use != 'internal' and decompressor != 'internal':
                         if type(f) is str: self._subprocess = subprocess.Popen([                                   use , '--decompress','--stdout',       f           ], stdout=subprocess.PIPE, stderr=DEVNULL)
                         else:              self._subprocess = subprocess.Popen('{ printf "'+magic+'"; cat; } | ' + use + ' --decompress  --stdout', stdin=f, shell=True, stdout=subprocess.PIPE, stderr=DEVNULL)
                         time.sleep(1)
@@ -225,7 +225,7 @@ class read:
                                 self.file_bytes_read += len(data)
                             self._file.close()
                             DEVNULL.close()
-                            raise StopIteration
+                            return
 
                     # Python's gzip module can't read from a stream that doesn't support seek(), and the zlib module cannot read the bgzip format without a lot of help:
                     self.file_decompressor = 'internal'
@@ -243,7 +243,7 @@ class read:
                             bs = 0
                         magic = raw_data[bs:bs+4]
                         if not magic: break # a child's heart
-                        if magic != "\x1f\x8b\x08\x04": raise PybamError('\n\nThe input file is not in a format I understand. First four bytes: ' + repr(magic) + '\n')
+                        if magic != b"\x1f\x8b\x08\x04": raise PybamError('\n\nThe input file is not in a format I understand. First four bytes: ' + repr(magic) + '\n')
                         try:
                             more_bs = bs + unpack("<H", raw_data[bs+16:bs+18])[0] +1
                             internal_cache.append(decompress(raw_data[bs+18:more_bs-8],-15))
@@ -278,15 +278,15 @@ class read:
                             '''
                         blocks_left_to_grab -= 1
                         if blocks_left_to_grab == 0:
-                            yield ''.join(internal_cache)
+                            yield b''.join(internal_cache)
                             internal_cache = []
                             blocks_left_to_grab = 50
                     self._file.close()
                     DEVNULL.close()
-                    if internal_cache != '': yield ''.join(internal_cache)
-                    raise StopIteration
+                    if internal_cache != '': yield b''.join(internal_cache)
+                    return
 
-            elif decompressor is not False and decompressor is not 'internal':
+            elif decompressor is not False and decompressor != 'internal':
                 # It wouldn't be safe to just print to the shell four random bytes from the beginning of a file, so instead it's
                 # written to a temp file and cat'd. The idea here being that we trust the decompressor string as it was written by 
                 # someone with access to python, so it has system access anyway. The file/data, however, should not be trusted.
@@ -303,7 +303,7 @@ class read:
                     self.file_bytes_read += len(data)
                 self._file.close()
                 DEVNULL.close()
-                raise StopIteration
+                return
             else:
                 raise PybamError('\n\nThe input file is not in a format I understand. First four bytes: ' + repr(magic) + '\n')
 
@@ -311,11 +311,11 @@ class read:
         self._generator = generator()
 
         ## So lets parse the BAM header:
-        header_cache = ''
+        header_cache = b''
         while len(header_cache) < 8: header_cache += next(self._generator)
 
         p_from = 0; p_to = 4
-        if header_cache[p_from:p_to] != 'BAM\1':
+        if header_cache[p_from:p_to] != b'BAM\1':
             raise PybamError('\n\nInput file ' + self.file_name + ' does not appear to be a BAM file.\n')
 
         ## Parse the BAM header:
@@ -334,18 +334,18 @@ class read:
             l_name = unpack('<l',header_cache[p_from:p_to])[0]
             p_from = p_to; p_to += l_name
             while len(header_cache) < p_to: header_cache += next(self._generator)
-            self.file_chromosomes.append(header_cache[p_from:p_to -1])
+            self.file_chromosomes.append(header_cache[p_from:p_to -1].decode("ascii", errors="replace"))
             p_from = p_to; p_to += 4
             while len(header_cache) < p_to: header_cache += next(self._generator)
             self.file_chromosome_lengths[self.file_chromosomes[-1]] = unpack('<l',header_cache[p_from:p_to])[0]
 
         self.file_bytes_read = p_to
-        self.file_binary_header = buffer(header_cache[:p_to])
+        self.file_binary_header = memoryview(bytes(header_cache[:p_to]))
         header_cache = header_cache[p_to:]
 
         # A quick check to make sure the header of this BAM file makes sense:
         chromosomes_from_header = []
-        for line in self.file_header.split('\n'):
+        for line in self.file_header.decode('ascii', errors='replace').split('\n'):
             if line.startswith('@SQ\tSN:'):
                 chromosomes_from_header.append(line.split('\t')[1][3:])
         if chromosomes_from_header != self.file_chromosomes:
@@ -512,8 +512,8 @@ class read:
             if 'bam_tags'        in dependencies: code += "\n        bam_tags         = self.bam[_end_of_qual  :                  ]"
 
             if 'sam_qname'       in dependencies:
-                if 'bam_qname'   in dependencies: code += "\n        sam_qname        = bam_qname[:-1]"
-                else:                             code += "\n        sam_qname        = self.bam[36            : _end_of_qname -1 ]"
+                if 'bam_qname'   in dependencies: code += "\n        sam_qname        = bam_qname[:-1].decode('ascii', errors='replace')"
+                else:                             code += "\n        sam_qname        = self.bam[36            : _end_of_qname -1 ].decode('ascii', errors='replace')"
 
             if 'sam_cigar_list'  in dependencies:
                 if 'bam_cigar'   in dependencies: code += "\n        sam_cigar_list = [( cig >> 4  , cigar_codes[cig & 0b1111]) for cig in array('I', bam_cigar) ]"
@@ -582,8 +582,10 @@ class read:
         else:
             def next_read(): return next(self._new_entry)
         self.next = next_read
+        self.__next__ = next_read
 
     def __iter__(self): return self
+    def __next__(self): return self.next()
     def __str__(self):  return self.sam
 
     ## Methods to pull out raw bam data from entry (so still in its binary encoding). This can be helpful in some scenarios.
@@ -645,7 +647,7 @@ class read:
     @property
     def sam_tlen(self):         return unpack( '<i', self.bam[ 32                     : 36                        ] )[0]
     @property
-    def sam_qname(self):        return               self.bam[ 36                     : self._end_of_qname -1     ] # -1 to remove trailing NUL byte
+    def sam_qname(self):        return               self.bam[ 36                     : self._end_of_qname -1     ].decode('ascii', errors='replace') # -1 to remove trailing NUL byte
     @property
     def sam_cigar_list(self):   return          [          (cig >> 4  , cigar_codes[cig & 0b1111] ) for cig     in array('I', self.bam[self._end_of_qname     : self._end_of_cigar ])]
     @property
