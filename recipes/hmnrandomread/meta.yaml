{% set name = "hmnrandomread" %}
{% set version = "0.11.0" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://github.com/guillaume-gricourt/HmnRandomRead/archive/{{ version }}.tar.gz
  sha256: cf7d8a01c118b3c8f5302917baa8fa4be75bdc3bc418aeb49fabe9ab7d82e39c

build:
  number: 0
  run_exports:
    - {{ pin_subpackage(name, max_pin="x.x") }}
  script:
    - export LIBCLANG_PATH=$BUILD_PREFIX/lib
    - cargo bundle-licenses --format yaml --output THIRDPARTY.yaml    
    - cargo install -v --locked --no-track --profile=release --root $PREFIX --path .

requirements:
  build:
    - {{ compiler('rust') }}
    - {{ compiler('c') }}
    - {{ stdlib('c') }}
    - clang
    - libclang
    - make
    - perl
    - pkg-config
    - cargo-bundle-licenses
  host:
    - zlib
    - curl
    - openssl
  run:
    - zlib
    - curl
    - openssl

test:
  commands:
    - HmnRandomRead version

about:
  home: https://github.com/guillaume-gricourt/HmnRandomRead
  summary: A sequence-read simulator program for NGS
  description: A sequence-read simulator program for NGS
  license: MIT
  license_file: 
    - LICENSE
    - THIRDPARTY.yaml

extra:
  additional-platforms:
    - linux-aarch64
    - osx-arm64
  recipe-maintainers:
    - guillaume-gricourt
