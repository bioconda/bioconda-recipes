{% set name = "nwkit" %}
{% set version = "0.42.0" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: https://github.com/kfuku52/{{ name }}/archive/refs/tags/v{{ version }}.tar.gz
  sha256: a9f595ce7f801eb40a084fad89959280c5d6f953ac895ee67a41bbf17fddacd9

build:
  number: 0
  noarch: python
  script: "{{ PYTHON }} -m pip install . --no-build-isolation --no-deps -vv"
  run_exports:
    - {{ pin_subpackage("nwkit", max_pin="x.x") }}

requirements:
  host:
    - python >=3.10
    - pip
    - setuptools >=77.0.3
    - wheel
  run:
    - python >=3.10
    - ete4 >=4.4.0
    - biopython
    - pandas
    - requests
    - numpy
    - scipy
    - matplotlib-base

test:
  commands:
    - nwkit --help
    - nwkit --version
    - python -m nwkit --version
    - printf '(A:1,B:1);' | nwkit validate --fail-on-issue yes > /dev/null
  imports:
    - nwkit

about:
  home: "https://github.com/kfuku52/nwkit"
  license: "MIT"
  license_file:
    - LICENSE
    - THIRD_PARTY_NOTICES
  summary: "Tools for processing newick trees"

extra:
  recipe-maintainers:
    - kfuku52
