{% set name = "feems" %}
{% set version = "2.0.1" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://github.com/NovembreLab/{{ name }}/archive/refs/tags/v{{ version }}.tar.gz
  sha256: 705a8cc081def2d095c013cebe173843bfb2017a70c644539426c01a5418551a

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . --no-deps --no-build-isolation --no-cache-dir --use-pep517 -vvv
  run_exports:
    - {{ pin_subpackage(name, max_pin="x") }}

requirements:
  host:
    - pip
    - python >=3.8
    - setuptools
  run:
    - python >=3.8
    - setuptools
    - suitesparse
    - scipy
    - scikit-learn
    - scikit-sparse
    - cartopy
    - pytest
    - pep8
    - flake8
    - click
    - statsmodels
    - pyyaml
    - xlrd
    - openpyxl
    # additional requirements used in the sources
    - fiona
    - matplotlib-base
    - pandas
    - pandas-plink
    - msprime
    - numpy <2
    - networkx
    - shapely
    - pyproj

test:
  imports:
    - feems
    - feems.utils

about:
  home: "https://github.com/NovembreLab/feems"
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: "Fast Estimation of Effective Migration Surfaces (FEEMS) + admixture (FEEMSmix)"
  description: "A python package implementing a statistical method for inferring and visualizing gene-flow in spatial population genetic data."
  dev_url: "https://github.com/NovembreLab/feems"
  doc_url: "https://github.com/NovembreLab/feems/blob/v{{ version }}/README.md"

extra:
  recipe-maintainers:
    - bernt-matthias
    - vivaswats
  identifiers:
    - doi:10.7554/eLife.61927
    - doi:10.1371/journal.pgen.1011612
