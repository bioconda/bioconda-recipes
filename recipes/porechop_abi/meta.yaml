{% set version = "0.5.0" %}

package:
  name: porechop_abi
  version: {{ version }}

source:
  url: https://github.com/bonsai-team/Porechop_ABI/archive/refs/heads/conda_test.zip
  sha256: 2d9d1a4051d29917a46b3e5fabc86e66320f8021c17ea73c3b954580d9d08378

build:
  number: 0
  skip: True  # [py < 37]
  script: {{ PYTHON }} -m pip install . --ignore-installed --no-deps -vv
  entry_points:
    - porechop_abi = porechop_abi.porechop_abi:main

requirements:
  build:
    - {{ compiler('cxx') }}
    - make
    - libgomp      # [linux]
    - llvm-openmp  # [osx]
  host:
    - python
    - setuptools
    - pip
    - networkx
    - zlib
    - libzlib
  run:
    - python
    - networkx
    - zlib

test:
  commands:
    - porechop_abi -h

about:
  home: https://github.com/bonsai-team/Porechop_ABI
  license: GPL3
  license_file: LICENSE
  summary: Adapter inferrence and removal of Oxford Nanopore reads
