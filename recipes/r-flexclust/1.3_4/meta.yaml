{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}
{% set package = 'flexclust' %}
{% set version = "1.3-4" %}

package:
  name: "r-{{ package }}"
  version: "{{ version | replace('-', '_') }}"

source:
  fn: "{{ package }}_{{ version | replace('-', '_') }}.tar.gz"
  url:
    - "https://cran.r-project.org/src/contrib/{{ package }}_{{ version }}.tar.gz"
    - "http://cran.r-project.org/src/contrib/Archive/{{ package }}/{{ package }}_{{ version }}.tar.gz"
  sha256: 7bbbb63bcf3aca81d8f6cafd674c9cc25dcea0a1b320c405b07cac99ccf32ff5

build:
  rpaths:
    - lib/R/lib/
    - lib/

requirements:
  build:
    - r-base
    - r-lattice
    - r-modeltools
    - posix               # [win]
    - {{native}}toolchain # [win]
    - gcc                 # [not win]

  run:
    - r-base
    - r-lattice
    - r-modeltools

test:
  commands:
    - $R -e "library('{{ package }}')" # [not win]
    - "\"%R%\" -e \"library('{{ package }}')\"" # [win]

about:
  home: "https://CRAN.R-project.org/package={{ package }}"
  license: GPL-2
  summary: The main function kcca implements a general framework for k-centroids cluster analysis supporting arbitrary distance measures and centroid computation. Further cluster methods include hard competitive learning, neural gas, and QT clustering. There are numerous visualization methods for cluster results (neighborhood graphs, convex cluster hulls, barcharts of centroids, ...), and bootstrap methods for the analysis of cluster stability.

# The original CRAN metadata for this package was:

# Version: 	1.3-4
# Depends: 	R (≥ 2.14.0), graphics, grid, lattice, modeltools
# Imports: 	methods, parallel, stats, stats4
# Suggests: 	ellipse, clue, cluster, seriation
# Published: 	2013-07-02
# Author: 	Friedrich Leisch [aut, cre], Evgenia Dimitriadou [ctb]
# Maintainer: 	Friedrich Leisch <Friedrich.Leisch at R-project.org>
# License: 	GPL-2
# NeedsCompilation: 	yes
# Citation: 	flexclust citation info
# Materials: 	NEWS
# In views: 	Cluster
# CRAN checks: 	flexclust results
# Downloads:
# Reference manual: 	flexclust.pdf
# Package source: 	flexclust_1.3-4.tar.gz
# Windows binaries: 	r-devel: flexclust_1.3-4.zip, r-release: flexclust_1.3-4.zip, r-oldrel: flexclust_1.3-4.zip
# OS X El Capitan binaries: 	r-release: flexclust_1.3-4.tgz
# OS X Mavericks binaries: 	r-oldrel: flexclust_1.3-4.tgz
# Old sources: 	flexclust archive
# Reverse dependencies:
# Reverse depends: 	funcy, ockc, RcmdrPlugin.BCA, RSKC
# Reverse imports: 	BCA, biclust, dtwclust, semiArtificial
# Reverse suggests: 	MVA
# Reverse enhances: 	clue
# Linking:

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
