{% set version = "0.13.2" %}

package:
  name: varfish-server-worker
  version: {{ version }}

build:
  number: 0
  skip: True # [osx]
  run_exports:
    - {{ pin_subpackage("varfish-server-worker", max_pin="x.x") }}

source:
  url: https://github.com/varfish-org/varfish-server-worker/archive/refs/tags/v{{ version }}.tar.gz
  sha256: 77c7ed41db052e3878abeb17cb065aa7fcee05e44dc24802019c91470648c6bc

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - {{ compiler('rust') }}
    - pkg-config
    - make
    - cmake
  host:
    - clangdev
    - openssl
    - sqlite
    - zlib
    - clangdev
    - protobuf
  run:
    - sqlite

test:
  commands:
    - varfish-server-worker --help

about:
  home: https://github.com/varfish-org/varfish-server-worker
  license: MIT
  license_family: MIT
  summary: |
    "Rust-based tool for the heavy lifting in varfish-server."
  dev_url: https://github.com/varfish-org/varfish-server-worker

extra:
  additional-platforms:
    - linux-aarch64
  recipe-maintainers:
    - holtgrewe
