{% set name = "mz_to_sqlite" %}
{% set version = "1.2.0" %}
# Do not forget to update the version string in the mz_to_sqlite.py file

about:
    home: https://github.com/jj-umn/mztosqlite
    license: GNU-GPL, Version 2.0
    summary: |
        mz_to_sqlite extracts data from proteomics mzIdentML and mass spec scan files: mzML, MGF, etc and 
        stores in a SQLite database.The intended purpose is to provide a 
        Galaxy dataset that can support an interactive Galaxy visualization plugin. 
package:
    name: mz_to_sqlite
    version: {{ version }}

build:
  number: 0

source:
    fn: 
    url: 

requirements:
  build:
  run:
    - java-jdk  >=6
    - python

test:
    commands:
      - 

extra:
  notes: |
    mz_to_sqlite extracts data from proteomics mzIdentML and mass spec scan files: mzML, MGF, etc and 
    stores in a SQLite database.The intended purpose is to provide a 
    Galaxy dataset that can support an interactive Galaxy visualization plugin.
