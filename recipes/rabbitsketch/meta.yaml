package:
  name: rabbitsketch
  version: "0.1.0"

source:
  url: https://github.com/RabbitBio/RabbitSketch/archive/refs/heads/bioconda.tar.gz
  sha256: f9a15e2d2d195129d0810661e235eebce3d960411caa516fe9f704a9773b5be9

build:
  number: 0
  script:
    - mkdir -p build
    - cd build
    - cmake -DCXXAPI=ON .. -DCMAKE_INSTALL_PREFIX=.
    - make
    - cd ..
    - "{{ PYTHON }} -m pip install . --no-deps --ignore-installed -vv"
  run_exports:
    - {{ pin_subpackage('rabbitsketch', max_pin="x.x") }}

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - cmake
    - make
    - llvm-openmp
    - setuptools
    - libxcrypt
    - pip
    - wheel
    - pybind11 >=2.6, <2.7
  host:
    - python =3.7
    - zlib
    - llvm-openmp
    - pybind11 >=2.6, <2.7
    - cython
    - sysroot_linux-64
    - libxcrypt
  run:
    - python =3.7
    - libxcrypt

test:
  requires:
    - python =3.7
  commands:
    - python -c "import sys; print(sys.path)"
    - python -c "import rabbitsketch; print(rabbitsketch.__file__)"
    - python -c "import rabbitsketch"
    - python -c "from rabbitsketch import Kssd"

about:
  home: https://github.com/RabbitBio/RabbitSketch
  license: https://github.com/RabbitBio/RabbitSketch/blob/lib/LICENSE.txt
  license_file: LICENSE.txt
  summary: "RabbitSketch is a highly optimized sketching library that exploits the power of modern multi-core CPUs."

