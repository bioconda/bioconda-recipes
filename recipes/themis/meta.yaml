{% set name = "themis" %}
{% set version = "0.1.0" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  - url: https://github.com/xujialupaoli/Themis/releases/download/v0.1.0/themis-0.1.0.tar.gz
    sha256: fac29b743b9667dff6d78fc45969cd36d4a6b64f8f1f2c346bc6dc7e509f6a04
build:
  skip: True  # [osx]
  number: 0            
  run_exports:
    - {{ pin_subpackage(name, max_pin="x") }} 

requirements:
  build:
    - {{ compiler('cxx') }}
    - {{ compiler('c') }}
    - cmake >=3.18
    - ninja
    - make
    - {{ compiler('rust') }}
    - cargo-bundle-licenses
  host:
    - python ==3.12
    - pip
    - setuptools >=68
    - seqan3 ==3.3.0
    - cxxopts >=2.2.0
    - catch2 ==2.*
    - zlib
    - bzip2
    - raptor ==3.*
    - pandas >=1.2.0
    - multitax >=1.3.1
    - genome_updater >=0.6.4
    - grep
    - coreutils
    - curl
    - diffutils
  run:
    - python ==3.12
    - pandas >=1.2.0
    - multitax >=1.3.1
    - genome_updater >=0.6.4
    - grep
    - coreutils
    - curl
    - diffutils
    - zlib
    - bzip2
    - raptor ==3.*
    - fastp


test:
  commands:
    - themis


about:
  home: https://github.com/xujialupaoli/Themis
  summary: "Themis: metagenomic profiler"
  license: GPL-3.0
  license_file: LICENSE



