{% set name = "ribotin" %}
{% set version = "1.4" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/user-attachments/files/18780237/{{ name }}.tar.gz
  sha256: 534c57fb044c4bcc562887e078e96fcd1442e452e7db1b0eb38ca51a1135fd17
  patches:
    - makefile.patch

build:
  number: 0
  skip: True # [osx]
  run_exports:
    - {{ pin_subpackage('ribotin', max_pin="x.x") }}

requirements:
  build:
    - make
    - {{ compiler('cxx') }}
  host:
    - zlib =1.2 # this does not need to be a specific version. however, conda environment solve did not work when the version was not specified. version pin can be removed as long as conda environment solve works.
    - pkg-config
    - bamtools
  run:
    - graphaligner
    - mbg
    - liftoff =1.6.3 # Use a specific version to make sure the annotation index files are compatible
    - winnowmap
    - samtools

test:
  commands:
    - (ribotin-ref -h 2>&1 || true) | grep ribotin

about:
  home: https://github.com/maickrau/ribotin
  license: MIT
  license_file: LICENSE.md
  summary: Ribosomal DNA assembly tool

extra:
  maintainers:
    - maickrau
