{% set name = "sos" %}
{% set version = "0.9.16.3" %}
{% set file_ext = "tar.gz" %}
{% set hash_type = "sha256" %}
{% set hash_value = "d460cf2e9822dffe5b6e9f11b87908b9e000149fd274ff39805473d555db2469" %}

package:
  name: '{{ name|lower }}'
  version: '{{ version }}'

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.{{ file_ext }}
  '{{ hash_type }}': '{{ hash_value }}'

build:
  number: 0
  skip: True  # [not py36]
  script: python -m pip install --no-deps --ignore-installed . --verbose

requirements:
  host:
    - python
    - pip

  run:
    - python
    - setuptools
    - psutil
    - tqdm
    - fasteners
    - pyyaml
    - pygments
    - networkx
    - pydot
    - pydotplus
    - pexpect
    - billiard
    - jinja2
    - nbformat
    - sos-pbs
    - sos-rq
    - sos-bioinfo
    - xxhash

test:
  imports:
    - sos
    - sos.docker
  commands:
    - sos --help
    - sos-runner --help

about:
  home: https://github.com/vatlab/SoS
  license: GNU General Public License (GPL)
  license_family: LGPL
  summary: 'Script of Scripts (SoS): an interactive, cross-platform, and cross-language workflow system for reproducible data analysis'
  doc_url: http://vatlab.github.io/SoS

extra:
  recipe-maintainers:
    - vibbits
