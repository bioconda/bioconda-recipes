{% set name = "pychopper" %}
{% set version = "2.0.2" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: https://github.com/nanoporetech/pychopper/archive/v{{ version }}.tar.gz
  sha256: ad5cb49d8d78d18a0dfcace2c39e2dbe40afc5def151c565c21c698e17546918

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . --no-deps --ignore-installed -vv

requirements:
  host:
    - python >=3
    - hmmer >=3
    - parasail-python >=1.1.17
    - edlib >=1.2.4
    - matplotlib
    - seaborn
    - six
    - tqdm
    - pandas
    - pytest
    - sphinx
    - sphinx_rtd_theme
  run:
    - python >=3
    - hmmer >=3
    - parasail-python >=1.1.17
    - edlib >=1.2.4
    - matplotlib
    - seaborn
    - six
    - tqdm
    - pandas
    - pytest
    - sphinx
    - sphinx_rtd_theme

test:
  commands:
    - cdna_classifier.py --help

about:
  home: https://github.com/nanoporetech/pychopper
  license: MPL2
  summary: A tool to identify, orient and rescue full length cDNA reads from nanopore data.
