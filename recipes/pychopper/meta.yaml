{% set name = "pychopper" %}
{% set version = "2.0.1" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: https://github.com/nanoporetech/pychopper/archive/v{{ version }}.tar.gz
  sha256: 9f74be776453eef545cbccf377cef12689655413b4da9090891ec6d30f151a38

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . --no-deps --ignore-installed -vv

requirements:
  host:
    - python >=3
    - hmmer >=3
    - parasail-python
    - edlib
    - matplotlib
    - six
    - tqdm
    - pandas
    - pytest
    - sphinx
    - sphinx_rtd_theme
  run:
    - python >=3
    - hmmer >=3
    - parasail-python
    - edlib
    - matplotlib
    - six
    - tqdm
    - pandas
    - pytest

test:
  commands:
    - cdna_classifier.py --help

about:
  home: https://github.com/nanoporetech/pychopper
  license: MPL2
  summary: A tool to identify, orient and rescue full length cDNA reads from nanopore data.
